You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- When `runHook` writes `activity_state: "standby"`, it also writes an `attention` object with `reason_kind: "input_requested"`, a fixed summary string `"Waiting for your input"`, `created_at: now`, and `expires_at: now + 2h`.
- When `runHook` writes `activity_state: "errored"`, it also writes `attention` with `reason_kind: "error_blocked"`, summary `"Something went wrong — agent stopped"`, and `expires_at: now + 30m`.
- For all other states, `attention` is absent from `state.json`.
- For every Bash or Shell tool_use event, `tool_command` is written to `state.json` (the raw command string). Absent for non-Bash/Shell events.

**Rationale notes:**
- `ClassifyResult.command` is only set in Bash/Shell branches where `command !== undefined`; the `command === undefined` early return intentionally omits it so `runHook` can distinguish "no command string" from "not a Bash/Shell event".
- `attention` key is entirely absent (not `null`) for non-standby/errored states via the spread-operator conditional — Zod `.optional()` round-trips cleanly.
- Fixed summary strings are v1 copy; richer summaries deferred to Phase 07.

### Files touched

Implementation:
packages/cli/src/hook-binary.ts

Tests:
packages/cli/src/hook-binary.test.ts

### Invariants to hold

1. `state.json` must contain `attention.reason_kind === "input_requested"` and `attention.expires_at === now + 7200000ms` exactly when `activity_state === "standby"`, and `attention` must be absent (no key, not null) for all other states.
2. `state.json` must contain `attention.reason_kind === "error_blocked"` and `attention.expires_at === now + 1800000ms` exactly when `activity_state === "errored"`.
3. `state.json` must contain `tool_command` exactly when the source event is a Bash or Shell `tool_use` with a defined command string, and `tool_command` must be absent for all other event types.

### Attack surfaces to probe

1. `buildAttention` TTL arithmetic — does `new Date(now.getTime() + TTL_MS).toISOString()` produce the correct ISO string for both 2h and 30m cases, and is `created_at` always set to `now.toISOString()` (not the expiry)?
2. `runHook` spread-conditional `...(attention !== undefined && { attention })` — does this pattern correctly omit the key (not write `null` or `{}`) when `buildAttention` returns `undefined`? Verify Zod `stateJsonV1Schema.parse()` round-trip in `writeStateAtomic` strips undefined fields correctly.
3. `isBashOrShell` check in `runHook` — does it use `classified.sourceEvent` (post-SoA-override) or `classified` raw fields? If a SoA gate event overrides `activityState` to e.g. `hyped` but the original `classified.sourceEvent` was Bash, does `tool_command` still get written? (Should it?)
4. `classifyEvent` Bash/Shell `command === undefined` early return — does it omit `command` from `ClassifyResult` correctly (no `command: undefined` vs no key), and does `runHook`'s `classified.command !== undefined` guard handle this correctly?
5. `tool_command` for Bash with Codex raw-stdin input (`tool_input.command` path) — does `normalize()` correctly extract `command` from `tool_input.command` and thread it to `ClassifyResult.command`?
6. Sticky-gate interaction — when a gate event fires (e.g. `ticket_started`) and the sticky-gate path overrides `activityState` to `hyped`, does `buildAttention(hyped, now)` correctly return `undefined` so no stale `attention` payload gets written?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — does the new `attention` and `tool_command` addition break existing consumers (Swift renderer, status.ts) that parse `state.json` without those fields? Check if existing schema-version fixtures (v1, v2) need updating.
2. **CLI flag/arg symmetry** — no new CLI flags introduced in this diff. [N/A — reason: no CLI changes]
3. **Error-class breadth in `catch` blocks** — `buildAttention` has no I/O and cannot throw; `writeStateAtomic` already validates through Zod. Probe whether Zod throws on an invalid `attention.reason_kind` value at the write site.
4. **Defensive layering at module boundaries** — `buildAttention` accepts raw `ActivityState`; does `writeStateAtomic`'s Zod validation catch any invalid `attention` shape that might slip through?
5. **Cross-file atomicity windows** — `attention` and `tool_command` are added to the same atomic write (`writeStateAtomic` via temp-file rename). No new multi-step writes introduced.
6. **Test-contract strength** — do new tests assert exact ISO string values (not just `toBeDefined()`) for `expires_at`, and do they cover the absence case for non-standby/errored states?
7. **Doc-vs-code drift in the ticket Rationale** — read `ticket-04-hook-standby-attention-payload-tool-command.md` Rationale and compare to the diff.

### Diff context

The diff adds four pieces to `packages/cli/src/hook-binary.ts`:

1. `command?: string` field added to `ClassifyResult` — set in all Bash/Shell return branches where `command !== undefined` (the `command === undefined` early return does not set it).

2. `buildAttention(state, now)` function: returns `AttentionPayload` for `standby` (`input_requested`, +2h) and `errored` (`error_blocked`, +30m), else `undefined`. Uses `NonNullable<StateJsonV1["attention"]>` for the type.

3. In `runHook`, after determining `activityState`:
   - `attention = buildAttention(activityState, opts.now)` — note uses final `activityState` after SoA override
   - `isBashOrShell` checks `classified.sourceEvent.kind === "tool_use"` and `name === "Bash" || "Shell"`
   - `toolCommand = isBashOrShell && classified.command !== undefined ? classified.command : undefined`

4. State payload spread:
   ```ts
   ...(attention !== undefined && { attention }),
   ...(toolCommand !== undefined && { tool_command: toolCommand }),
   ```

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
