You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `rawHookOrigin` correctly identifies Cursor events: camelCase `hook_event_name` values (e.g. `"afterFileEdit"`, `"beforeShellExecution"`) emit `source_origin: "cursor"` instead of `"claude_code"`.
- `afterFileEdit` hook events → `activity_state: "implementing"`.
- `beforeShellExecution` and `afterShellExecution` hook events → classified via the same Bash 3-bucket path (same as `Bash`/`Shell` tool_use).
- When `CURSOR_PROJECT_DIR` is absent, `workspace_roots[0]` from the Cursor hook payload is used for SoA root resolution.
- `source_event.origin` is `"cursor"` in `state.json` for all of the above.

**Rationale:**
- 4-branch origin detection: PascalCase (`"Stop"`) → `claude_code`; camelCase (`"afterFileEdit"`) → `cursor`; snake_case with underscores (`"session_end"`) → `codex`; simple lowercase without underscores (`"stop"`) → `cursor` (Codex never uses bare lowercase event names, only snake_case).
- `workspace_roots` fallback: Added `CURSOR_WORKSPACE_ROOT` to `SoaPathEnv` so `resolveSoaRoot` tries it between `CODEX_PROJECT_DIR` and `CWD`. The hook binary passes `input.workspace_roots?.[0]`. This keeps the engine layer agnostic to the Cursor payload shape.
- `beforeShellExecution`/`afterShellExecution`: Both events treated as valid classification signals; no deduplication needed.
- Deferred: `CURSOR_PROJECT_DIR` env var support — Phase 07.

### Files touched

Implementation:
- `packages/contracts/src/state-json.ts` — added `"cursor"` to `sourceEventOriginSchema`
- `packages/engine/src/sources/soa.ts` — added `CURSOR_WORKSPACE_ROOT` to `SoaPathEnv`, updated `resolveSoaRoot`
- `packages/cli/src/hook-binary.ts` — `workspace_roots` on `HookInput`, `rawHookOrigin` 4-branch, `rawHookKind` Cursor events, `normalize` name mapping, `runHook` passes `workspace_roots?.[0]`

Tests:
- `packages/cli/src/hook-binary.test.ts` — 5 new tests for Cursor origin fix, shell hooks, workspace_roots

### Invariants to hold

1. `rawHookOrigin` must return `"cursor"` for any camelCase `hook_event_name` (first char lowercase, contains an uppercase char) and must not return `"claude_code"` or `"codex"` for them.
2. `classifyEvent` with `hook_event_name: "afterFileEdit"` must produce `state: "implementing"` and `sourceEvent.origin: "cursor"`; `beforeShellExecution` with a reviewing-bucket command must produce `state: "reviewing"` and `origin: "cursor"`.
3. `resolveSoaRoot` must return `CURSOR_WORKSPACE_ROOT` when `CLAUDE_PROJECT_DIR` and `CODEX_PROJECT_DIR` are absent/empty, and fall through to `CWD` when `CURSOR_WORKSPACE_ROOT` is also absent/empty.

### Attack surfaces to probe

1. **`rawHookOrigin` — camelCase false-positive/negative edges**: Does the 4-branch logic correctly handle edge inputs — a single-character lowercase string like `"a"`, a string starting lowercase that becomes all-uppercase after index 0 like `"aBC"`, or a mixed-case string with underscores like `"afterFile_Edit"`? Probe whether these produce `"cursor"` or misfire into `"codex"` or `"claude_code"`.

2. **`rawHookKind` — Cursor event ordering vs `tool_name` check**: The Cursor event check (`afterFileEdit`, etc.) now precedes the `tool_name` check. If a payload has both `tool_name` and `hook_event_name: "afterFileEdit"`, the Cursor event name wins and returns `"tool_use"` from the first branch. Is this the right precedence given the `tool_name` check returning `"tool_use"` anyway? But does the same payload also get `rawName = "Edit"` from `normalize`'s Cursor override even when `input.name` or `input.tool_name` is set?

3. **`normalize` — Cursor name override precedence**: When `hook_event_name: "afterFileEdit"` is present alongside `input.name: "SomeOtherTool"` or `input.tool_name: "Bash"`, the current code unconditionally overrides `rawName` to `"Edit"`. Is this correct? Can a legitimate Cursor payload send a different tool name for the same event?

4. **`resolveSoaRoot` — empty-string `CURSOR_WORKSPACE_ROOT`**: The guard is `if (env.CURSOR_WORKSPACE_ROOT && env.CURSOR_WORKSPACE_ROOT.length > 0)`. Does passing an empty string from `input.workspace_roots?.[0]` (i.e., `workspace_roots: [""]`) correctly fall through to CWD?

5. **`sourceEventOriginSchema` — `"cursor"` added to enum**: Any code in the repo that does exhaustive matching over `SourceEventOrigin` values (switch statements, lookup maps by origin, profile XP tracking, loot source attribution) — does adding `"cursor"` as a valid origin silently drop Cursor-origin events from those paths or cause unexpected behavior?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `"cursor"` is a new enum value added to `sourceEventOriginSchema`. Does any persisted `state.json` consumer, renderer, or schema migration gate break when encountering a new origin value it previously never saw? Probe forward-compat policy and any schema-version guards.

2. **CLI flag/arg symmetry** — N/A for this ticket; no new CLI flags were added.

3. **Error-class breadth in `catch` blocks** — no new `catch` blocks added; existing silent-skip on write/IO failure in `runHookFromStdin` is unchanged.

4. **Defensive layering at module boundaries** — `CURSOR_WORKSPACE_ROOT` is passed from `hook-binary.ts` into `resolveSoaRoot` (engine boundary). The value is `input.workspace_roots?.[0]`, an untrusted hook payload string. Does `resolveSoaRoot` defend against path traversal or malformed paths, or does it pass the value directly to `join(rootDir, ".soa/events.ndjson")` and then `stat`/`open` without sanitization?

5. **Cross-file atomicity windows** — `runHook` reads `workspace_roots` from `HookInput` before acquiring the home lock. The `CURSOR_WORKSPACE_ROOT` value is read at the same point as `env` and `cwd`. No new atomicity window introduced; the resolution is synchronous before any file IO.

6. **Test-contract strength** — new tests assert `sourceEvent.origin === "cursor"` and `state === "implementing"/"reviewing"` directly. Do they also test that pre-existing Codex events (`"session_end"`, `"pre_tool_use"`) still resolve to `"codex"` after the 4-branch refactor (regression coverage)?

7. **Doc-vs-code drift in the ticket Rationale** — the Rationale mentions "simple lowercase without underscores (`'stop'`) → `cursor`" and "Codex never uses bare lowercase event names, only snake_case." Read the contracts and existing tests to verify this assumption holds, and check for drift between the Rationale's description of the 4-branch logic and the actual implementation.

### Diff context

Four files changed:

**`packages/contracts/src/state-json.ts`**: Added `"cursor"` to `sourceEventOriginSchema` z.enum — one line addition.

**`packages/engine/src/sources/soa.ts`**: Added `CURSOR_WORKSPACE_ROOT?: string` field to `SoaPathEnv`. Added a third priority check in `resolveSoaRoot` between `CODEX_PROJECT_DIR` and `CWD`, with the same non-empty guard pattern as the existing two checks.

**`packages/cli/src/hook-binary.ts`** (main changes):
- `HookInput`: added `workspace_roots?: string[]`
- `rawHookOrigin`: replaced two-branch logic (all-lowercase→codex, else→claude_code) with four-branch: if first char is lowercase → camelCase check (has uppercase) → cursor; underscore check → codex; else → cursor. If first char is uppercase → claude_code.
- `rawHookKind`: added early return for `afterFileEdit`/`beforeShellExecution`/`afterShellExecution` → `"tool_use"` before the `tool_name` check.
- `normalize`: added name override for `afterFileEdit` → `"Edit"` and `beforeShellExecution`/`afterShellExecution` → `"Shell"` before computing `rawKind`.
- `runHook`: passes `CURSOR_WORKSPACE_ROOT: input.workspace_roots?.[0]` to `resolveSoaRoot`.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
