I have enough information to write the review. The `name` field is `z.string().min(1)` — any non-empty string is valid, so `"Shell"` passes the schema. Let me now write the adversarial review.

---

**Invariant results**

**Invariant 1** — `classifyEvent` with `name === "Bash"` or `name === "Shell"` returns `reviewing` when trimmed command starts with a `REVIEWING_BASH_PREFIXES` prefix followed by end-of-string, space, or tab. **[held]** — `matchesReviewingCommand` (lines 150–156) mirrors the exact word-boundary pattern from `matchesTestRunner`: `trimStart()`, `startsWith(prefix)`, then checks the character at `prefix.length` is `""`, `" "`, or `"\t"`. Both `"Bash"` and `"Shell"` enter the same branch (line 200). Tests confirm `grep "foo" bar.ts` → `reviewing` for both tool names.

**Invariant 2** — `command === undefined` returns `{ state: "implementing" }`. **[held]** — Line 201–202 short-circuits before any `matchesReviewingCommand` call. The test at line 229 of the test file covers the `Bash` case. No test covers the `Shell` + `undefined` combination, but the code path is identical (same branch, same guard at the top of the branch). See Advisory Observations.

**Invariant 3** — `matchesReviewingCommand` must not false-positive on words sharing a prefix with a reviewing command but not word-boundary separated (e.g., `catfish`, `lsblk`). **[held]** — The `next === "" || next === " " || next === "\t"` guard (line 155) prevents `catfish` from matching `cat` (next char is `"f"`), `finds` from matching `find` (next `"s"`), `heading` from matching `head` (next `"i"`), and `lsblk` from matching `ls` (next `"b"`). No false-positive path exists. No test covers a false-positive probe explicitly (see Advisory Observations).

---

**Surface results**

**Surface 1 — `matchesReviewingCommand` word-boundary false positives** [probed] — Manually traced `catfish`, `finds`, `heading`, `lsblk` through the `startsWith` + next-char guard. All return false. The guard is identical to the battle-tested `matchesTestRunner` pattern. No false positive found.

**Surface 2 — Priority ordering in the Bash/Shell branch** [probed] — Lines 200–213: `undefined` check → `git push` → `matchesTestRunner` → `matchesReviewingCommand` → `implementing`. The `git push` and test-runner checks run before `matchesReviewingCommand`. A command that could theoretically match both (none exists in practice with current prefix lists) would correctly fall into the higher-priority bucket. `pushing` and `running-tests` cannot be shadowed by `reviewing`. Clean.

**Surface 3 — `undefined` command short-circuit for both `Bash` and `Shell`** [probed] — The `undefined` guard is inside the `if (name === "Bash" || name === "Shell")` branch (line 200) and fires first (lines 201–202), so `matchesReviewingCommand` is never reached with `undefined` for either tool name. No crash path exists. The test only covers `Bash` + `undefined` (line 229); `Shell` + `undefined` is untested but the code path is identical.

**Surface 4 — Cursor `Shell` normalization** [probed] — `sourceEventSchema.name` is `z.string().min(1)` (contracts/src/state-json.ts:37), so `"Shell"` passes schema validation. The branch condition `name === "Bash" || name === "Shell"` (line 200) uses the normalized `name` from `parsed.data`, which equals the raw `tool_name` or `name` field from input (line 120). `Shell` events reach the identical classification code. Tests confirm `Shell + grep` → `reviewing` and `Shell + npm install` → `implementing`.

**Surface 5 — `readRun` reset on reviewing** [probed] — Line 211 returns `{ state: "reviewing", sourceEvent, readRun: 0 }`. All four return sites in the Bash/Shell branch (lines 202, 205, 209, 211, 213) return `readRun: 0`. Tests at lines 112, 126, 140, 154, 168, 182, 196, 210, 224, 238, 252, 266 all assert against `{ readRun: 0 }` as the prior state, but none explicitly asserts `.readRun` on the output. See Advisory Observations.

**Surface 6 — Output stability across schema-version drift** [N/A] — The diff introduces no new persisted artifact format changes. `reviewing` was already a valid `ActivityState`. `StateJsonV1` and `SourceEvent` schemas are unchanged.

**Surface 7 — CLI flag/arg symmetry** [N/A] — No CLI flags or parsed arguments are added or changed by this diff.

**Surface 8 — Error-class breadth in `catch` blocks** [N/A] — No new `catch` blocks introduced. `trimStart()` on a guaranteed `string` is infallible.

**Surface 9 — Defensive layering at module boundaries** [probed] — `matchesReviewingCommand` receives `string` only after the `command === undefined` guard passes (line 201). TypeScript enforces this. No unsafe call site exists.

**Surface 10 — Cross-file atomicity windows** [N/A] — Pure in-memory classification logic; no multi-step writes.

**Surface 11 — Test-contract strength for `readRun` on reviewing bucket** [probed] — Tests pass `{ readRun: 0 }` as prior state and assert only `.state`. None of the 12 new tests assert `.readRun` on the returned object for the reviewing bucket. This means a regression where `readRun` is returned as something other than `0` from the reviewing path would not be caught. The ticket spec says `readRun: 0` for all Bash/Shell paths — this is an under-assertion gap, not a code bug, but worth noting.

**Surface 12 — Doc-vs-code drift in the ticket Rationale** [probed] — Rationale states: (a) `"Shell"` tool_name with capital S is confirmed — matches line 200's `name === "Shell"`; (b) `undefined` command returns `implementing` — matches lines 201–202; (c) Shell normalization is zero extra branches — confirmed, same `if` block. No drift found.

---

**Actionable findings**

None.

---

**Advisory Observations**

- **`Shell` + `undefined` command is untested.** The test suite covers `Bash` + `undefined` → `implementing` (line 229) but has no corresponding case for `Shell` + `undefined`. The code path is provably identical (same branch, same guard), so this is not a bug today, but a future refactor that accidentally splits the branch could introduce a regression with no failing test. Consider adding one test: `{ name: "Shell", command: undefined }` → `implementing`.

- **No false-positive word-boundary tests in the suite.** The ticket's Invariant 3 explicitly calls out `catfish`, `rg-something`, `lsblk` as cases that must not match. None of those probes appear in the test file. The implementation is correct, but the invariant has no test-level enforcement. If a future change loosens the word-boundary guard, no test will catch it.

- **`readRun` output not asserted for the reviewing bucket.** All 12 new tests assert only `.state`. The ticket contract requires `readRun: 0` for all Bash/Shell returns. Asserting `.readRun` on at least one reviewing-bucket test would lock in that contract against future regressions. This is an under-assertion gap, not a code defect.

---

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: Full review completed — all invariants probed, all surfaces covered, no actionable findings identified.
