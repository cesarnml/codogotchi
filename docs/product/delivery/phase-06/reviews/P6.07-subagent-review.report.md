I have all the information needed for a thorough review. Here is my adversarial review report:

---

**Invariant results**

1. **[held]** — `resolveActivityState` returns `.idle` for any parseable ISO 8601 timestamp strictly before `now`. The guard chain `expiry < now` is satisfied; `testStandbyWithExpiredTTLResolvesToIdle` exercises this path with a distant-past timestamp. No code path breaks this invariant.

2. **[held]** — Missing/null/unparseable `expires_at` all fall through the guard chain and return `snapshot.activityState` unchanged. `AttentionPayload.expiresAt` is `String?`; absent field → nil → guard fails → pass-through. JSON null also decodes to nil. An unparseable string causes `parseISO8601Date` to return nil → guard fails → pass-through. None of these produce `.malformed`.

3. **[held]** — A future `expires_at` does not satisfy `expiry < now`, so the guard chain returns `snapshot.activityState` (`.standby`) unchanged. `testStandbyWithFutureTTLRemainsStandby` covers this path with a 2099 timestamp.

---

**Surface results**

**S1 — `resolveActivityState` boundary condition (exact `expiry == now`):** [probed] The guard uses strict `<`. When `expiry == now` exactly, the guard fails and the raw state is returned — the TTL is not treated as expired. This is consistent with the spec ("strictly before the current time"). The boundary is unambiguous and the implementation matches. No bug; the edge case where a `standby` survives exactly at the expiry instant resolves within the next 1-second polling cycle.

**S2 — `parseISO8601Date` format coverage:** [probed] The two-pass logic: first pass uses `[.withInternetDateTime, .withFractionalSeconds]`, second uses `[.withInternetDateTime]`. `.withInternetDateTime` on Apple platforms is documented to handle both Z-suffix and `+HH:MM`/`-HH:MM` offset forms. The three plausible hook shapes — `"…000Z"` (fractional + Z), `"…00Z"` (no fractional + Z), `"…00+00:00"` (offset) — are all handled. The `+00:00` form is parsed by `.withInternetDateTime` on the second pass. No gap found.

**S3 — `AttentionPayload` partial decode robustness:** [probed] (a) `"attention": {}` — `expiresAt` is `String?` with no default value; `JSONDecoder` with `.convertFromSnakeCase` decodes an absent key to `nil` for an optional property, so decode succeeds and `expiresAt` is nil. Guard fails → pass-through. (b) `"attention": {"expires_at": null}` — JSON null decodes to `nil` for `String?` without throwing. Guard fails → pass-through. (c) `"attention": {"expires_at": 42}` — `JSONDecoder` attempts to decode a number as `String` and throws `DecodingError.typeMismatch`, which propagates out of `decoder.decode(StatePayload.self, from:)` and is caught by the outer `catch`, returning `.failure(.malformed)`. This is intentional per diff-derived surface 3.

**S4 — `StateSnapshot` field preservation through TTL resolution:** [probed] The second `StateSnapshot(...)` constructor call at lines 81–87 explicitly copies `schemaVersion: raw.schemaVersion`, `updatedAt: raw.updatedAt`, `sourceEvent: raw.sourceEvent`, and `attention: raw.attention`. All five fields are accounted for. The `attention` field is preserved on the returned snapshot — callers can read it.

**S5 — `LivePollingDriver` transition log dedup:** [probed] Located the transition log recording path — `runTick` reads the snapshot, compares `snapshot.activityState` to `lastAgentState`, and only records a transition on change. Once TTL expiry flips `standby → idle` on the first read, `lastAgentState` becomes `.idle`. Subsequent reads (same expired state.json) return `.idle` again; `prev == snapshot.activityState` so no transition is recorded. The phantom-entry concern is resolved by the existing dedup guard. No bug.

**S6 — `resolveActivityState` access level:** [probed] Declared `static func resolveActivityState` with no explicit access modifier — defaults to `internal`. The test file uses `@testable import Codogotchi`, which grants `internal` access. The four TTL tests call `StateJsonReader.read(at:)` (which calls `resolveActivityState` internally) rather than calling `resolveActivityState` directly. No test calls `resolveActivityState` directly. Access level is not a barrier.

**DS1 — Output stability across schema-version drift (new `StateSnapshot` initializer parameter):** [probed] Searched all `StateSnapshot(` call sites in `Sources/`. Only two exist, both in `StateJsonReader.swift` lines 73 and 81 (the diff). `TransitionLogTests.swift`'s `makeSnapshot` helper passes `attention: nil`. No orphaned call site found.

**DS2 — CLI flag/arg symmetry:** [N/A — Swift renderer change only, no CLI surface added or changed]

**DS3 — Error-class breadth in `catch` blocks:** [probed] The outer `catch { return .failure(.malformed) }` is unchanged and covers all `DecodingError` throws including a type mismatch on `expires_at`. A structurally invalid `attention` object (e.g., `expires_at` as a number) correctly returns `.malformed`. This is intentional and the behavior is consistent with the rest of the reader's error contract. No gap.

**DS4 — Defensive layering at module boundaries:** [probed] `parseISO8601Date` is called only when `expiresAtStr` is a non-nil `String`. The `ISO8601DateFormatter` is allocated on every call — no shared mutable state, no thread-safety concern. No cross-boundary input vulnerability.

**DS5 — Cross-file atomicity windows:** [N/A — `resolveActivityState` is a pure in-memory transform with no side effects and no persistence]

**DS6 — Test-contract strength:** [probed] Tests use hardcoded distant-past/far-future timestamps rather than relative deltas — correct and robust against clock skew. The four tests assert `activityState` on the decoded snapshot. The exact-boundary (`expiry == now`) case is not covered by a test, which is acceptable given it resolves within one polling cycle. Tests look sound.

**DS7 — Doc-vs-code drift in ticket Rationale:** [probed] Read the Rationale notes in the ticket scope. The Rationale states `AttentionPayload.expiresAt` is `String?` (optional). The implementation declares `let expiresAt: String?` — consistent. The Rationale states "ISO 8601 parse: two-pass — tries `.withFractionalSeconds` first" — the implementation does exactly this. The Rationale states "if `expires_at` is absent/unparseable, use `activity_state` as-is" — the implementation's guard chain matches. No drift found.

---

**Actionable findings**

None.

---

**Advisory Observations**

- **`"attention": {}` decode path is sound but the contract is implicitly load-bearing.** The decode of `AttentionPayload` when `expires_at` is absent works only because Swift's `Decodable` synthesized init treats optional stored properties as defaulting to `nil` for absent keys. This is guaranteed by the language spec, but there is no explicit test for the `"attention": {}` (empty object, no `expires_at` key) or `"attention": {"expires_at": null}` shapes. These paths fall under Invariant 2 but are not exercised by the four TTL tests, which use either a full `attention` object or a fully absent `attention` key. Low-risk given language-spec guarantees, but a test for each would close the gap if the contract ever needs documentation evidence.

- **`parseISO8601Date` allocates a new `ISO8601DateFormatter` on every call.** On the hot path (1 Hz polling), this is two formatter allocations per read cycle. `ISO8601DateFormatter` is relatively lightweight, but a `static let` cached formatter pair would be marginally more efficient. Out of scope for correctness; flagged purely for Phase 06 lite's 1 Hz polling budget awareness.

---

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: All invariants verified against implementation; all thirteen attack surfaces probed or marked N/A; no actionable findings identified.
