```
You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `codogotchi hooks install --platform cursor` writes `~/.cursor/hooks.json` calling `codogotchi-hook` for the relevant Cursor hook events (`afterFileEdit`, `beforeShellExecution`, `afterShellExecution`, `stop`, `sessionEnd`).
- `codogotchi hooks install` (no platform flag) installs Claude Code hooks only (existing behavior, unchanged).
- `codogotchi hooks status` reports the detected mode: `bridge` (Cursor with Third-party skills using `~/.claude/settings.json`) or `native` (Cursor with `~/.cursor/hooks.json`), in addition to the existing Claude Code status.
- `codogotchi hooks uninstall --platform cursor` removes the Cursor hook entries from `~/.cursor/hooks.json` without touching Claude Code hooks.
- README/runbook documents both paths and when to prefer each (bridge: simpler, no Cursor restart required; native: correct `source_origin`, shell classification, direct `afterFileEdit` signal).

**Rationale notes:**
- `~/.cursor/hooks.json` uses a flat event-name → hook-array map (no `"hooks"` wrapper key, unlike Codex).
- The command string follows the same `CODOGOTCHI_HOME='...' codogotchi-hook` pattern as Codex.
- No `--platform cursor` arg is passed to the binary — origin is auto-detected from `hook_event_name` camelCase heuristic (P6.05).
- Bridge detection is best-effort: `source_origin` will show `"cursor"` for native events but `"claude_code"` in bridge mode.
- `isCursorHookEntry` filter was updated to preserve unknown-type entries to avoid silent data loss if Cursor gains new hook entry types.

### Files touched

Implementation:
- `packages/cli/src/hooks.ts`
- `packages/cli/src/router.ts`
- `README.md`

Tests:
- `packages/cli/src/hooks.test.ts`

### Invariants to hold

1. `installCursorHooks` must write all 5 Cursor events (`afterFileEdit`, `beforeShellExecution`, `afterShellExecution`, `stop`, `sessionEnd`) to `~/.cursor/hooks.json`; running it twice must produce byte-identical output (idempotency guarantee).
2. `uninstallCursorHooks` must remove only Codogotchi-owned entries (those whose `command` includes `"codogotchi-hook"`), leaving all other entries intact — including entries with unknown `type` values. Running it when no `~/.cursor/hooks.json` exists must succeed without error.
3. `hooksStatus().cursor.source_origin` must be `"native"` when `~/.cursor/hooks.json` has Codogotchi entries present, `"bridge"` when Claude Code hooks are installed but no native cursor file exists, and `undefined` when neither condition holds. `installable_in_phase` must be `true`.

### Attack surfaces to probe

1. **`installCursorHooks` idempotency** — the command string is `CODOGOTCHI_HOME='/path' codogotchi-hook`. Does `isCodogotchiCommand(e.command)` (which uses `command.includes("codogotchi-hook")`) correctly match this? If the home path itself contained the substring `codogotchi-hook` (e.g., nested in a user's weird path), could it false-positive and strip a non-Codogotchi entry?
2. **`uninstallCursorHooks` when file is absent** — `readJsonOrEmpty` returns `{}` for ENOENT. `Object.entries({})` yields nothing, so the function writes `{}\n` to a newly created `~/.cursor/hooks.json`. This creates a file that did not exist before, which may surprise users who never ran install.
3. **`hooksStatus` bridge detection order** — `cursorBridgeInstalled = !cursorNativeInstalled && claudeCodeInstalled`. If `~/.cursor/hooks.json` exists but has NO Codogotchi entries (e.g., user has other Cursor hooks but never ran `hooks install --platform cursor`), `cursorNativeInstalled` is false and Claude Code hooks are installed → the status incorrectly reports `cursor: bridge` even though the user has a native hooks file with other content.
4. **`parseHooksPlatformFlag` in router** — what happens with `hooks install --platform` (flag present but value missing)? The loop increments `i` and reads `args[i + 1]` which may be `undefined`. Verify the error path.
5. **`router.ts` install path missing catch for `installCursorHooks` error** — `installHooks` throws when `~/.codogotchi/config.json` is absent, and the router has a pre-check. But `installCursorHooks` also throws the same error internally, and the router does NOT wrap the `installCursorHooks` call in a try/catch (unlike the `installHooks` path which has a separate config pre-check). An uncaught throw from `installCursorHooks` bubbles all the way to the process level.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `~/.cursor/hooks.json` has no schema version marker. Probe whether re-running `installCursorHooks` after a future format change could produce a partially upgraded file (mixing old and new entry shapes). Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
2. **CLI flag/arg symmetry** — `--platform` was added to `hooks install` and `hooks uninstall` in `router.ts`. Verify help text, USAGE string, and `hooks status` (which has no `--platform` flag since it reads all platforms). Probe that the help output for install/uninstall now mentions `--platform`. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
3. **Error-class breadth in `catch` blocks** — the router wraps `parseHooksPlatformFlag` in try/catch for both install and uninstall. The catch body uses `err instanceof Error ? err.message : String(err)`. Verify the catch block correctly propagates non-Error throws. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
4. **Defensive layering at module boundaries** — `installCursorHooks` calls `readJsonOrEmpty` on user-controlled `~/.cursor/hooks.json`. If the file exists but contains non-object JSON (e.g., a JSON array `[]`), `readJsonOrEmpty` returns `{}` (the empty-object default), discarding the existing array. Probe whether this would silently overwrite a valid-but-unexpected file. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
5. **Cross-file atomicity windows** — `installCursorHooks` uses `writeText` (non-atomic `writeFile`) not the `writeStateAtomic` (temp-rename) pattern used elsewhere. An interrupted write could leave a truncated `~/.cursor/hooks.json`. Probe whether this is consistent with how existing `installHooks` handles the same risk for `~/.claude/settings.json`. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
6. **Test-contract strength** — the new tests cover install, idempotency, uninstall (with pre-existing entries), and status native/bridge. Probe: is `uninstallCursorHooks` tested when the cursor hooks file does NOT exist? Is the case where `~/.cursor/hooks.json` has non-Codogotchi entries preserved across an install tested? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
7. **Doc-vs-code drift in the ticket Rationale** — README says `cursor: bridge` for bridge mode; `hooksStatus()` returns `source_origin: "bridge"`. Verify the `hooks status` text-format output in `router.ts` actually surfaces this value to the user (the current `rows` formatter outputs `${name}: ${installed}, ${installable}` — `source_origin` is omitted from text output). Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

### Diff context

Key changes in `packages/cli/src/hooks.ts`:
1. Added `CURSOR_HOOKS_REL = join(".cursor", "hooks.json")` and `CURSOR_CODOGOTCHI_EVENTS` array.
2. Added `CursorHookEntry`, `CursorHookSlot`, `CursorHooksJson` types; `isCursorHookEntry` type guard; `cursorHookCommand`; `cursorInstalled` (checks all 5 events).
3. `installCursorHooks(ctx)`: checks config exists → backups existing file → reads existing JSON → for each of 5 events, filters out Codogotchi entries (preserving unknown-type entries) → appends new entry → writes back. Command is `CODOGOTCHI_HOME='...' codogotchi-hook`.
4. `uninstallCursorHooks()`: reads existing JSON → for each event array, removes entries where `isCursorHookEntry(e) && isCodogotchiCommand(e.command)` → deletes empty slots → writes back.
5. `hooksStatus()`: now reads `~/.cursor/hooks.json`; computes `cursorNativeInstalled` (5 events all present), `cursorBridgeInstalled` (!native && claudeCodeInstalled); returns `installable_in_phase: true`, `source_origin: "native"|"bridge"|undefined`.

Key changes in `packages/cli/src/router.ts`:
1. Added `parseHooksPlatformFlag(args)` — parses `--platform claude_code|cursor` from args array.
2. `hooks install`: now accepts `--platform`; routes to `installCursorHooks` or `installHooks`.
3. `hooks uninstall`: now accepts `--platform`; routes to `uninstallCursorHooks` or `uninstallHooks`.
4. USAGE string updated with platform flag docs.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
```
