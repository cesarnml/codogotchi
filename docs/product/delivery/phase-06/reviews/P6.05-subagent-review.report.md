I have enough information to produce the review report.

---

**Invariant results**

**Invariant 1** — `rawHookOrigin` returns `"cursor"` for camelCase and never returns `"claude_code"` or `"codex"` for them. **Held.** Traced the four-branch logic: first-char-lowercase → camelCase check (`eventName !== eventName.toLowerCase()`) → `"cursor"`; underscore check → `"codex"`; else `"cursor"`. Only PascalCase (first char passes `.toUpperCase()` equality) reaches `"claude_code"`.

**Invariant 2** — `classifyEvent` with `"afterFileEdit"` produces `state: "implementing"` and `origin: "cursor"`; `"beforeShellExecution"` with a reviewing-bucket command produces `state: "reviewing"` and `origin: "cursor"`. **Held.** `normalize` overrides `rawName` to `"Edit"` / `"Shell"`, `rawHookOrigin` returns `"cursor"` for both camelCase names, and `classifyEvent` dispatches on name `"Edit"` → `"implementing"`, name `"Shell"` with reviewing command → `"reviewing"`. Both paths tested in the new test suite.

**Invariant 3** — `resolveSoaRoot` returns `CURSOR_WORKSPACE_ROOT` when `CLAUDE_PROJECT_DIR` and `CODEX_PROJECT_DIR` are absent/empty, falls through to `CWD` when it too is absent/empty. **Held.** `soa.ts` lines 43-45 guard with `env.CURSOR_WORKSPACE_ROOT && env.CURSOR_WORKSPACE_ROOT.length > 0`. An empty string passes the falsy guard and falls through. The integration test at `hook-binary.test.ts:868` passes `env: {}` and verifies SoA gate resolution via `workspace_roots[0]`.

---

**Surface results**

**S1 — `rawHookOrigin` camelCase false-positive/negative edges.** Probed. `"a"` (single lowercase char): first-char check triggers, `"a" !== "a".toLowerCase()` → false, no underscore → returns `"cursor"`. `"aBC"`: first-char check, `"aBC" !== "abc"` → true → `"cursor"`. `"afterFile_Edit"` (camelCase with underscore): first-char check, `"afterFile_Edit" !== "afterfile_edit"` → true → `"cursor"` — camelCase test fires before the underscore branch, so mixed forms land on `"cursor"` not `"codex"`. All three produce the expected result; no misfire.

**S2 — `rawHookKind` ordering vs `tool_name` check.** Probed. The diff-derived description says "Cursor event check precedes `tool_name` check," but the actual code at `hook-binary.ts:120` checks `input.tool_name` first, then checks the Cursor event names at lines 122-127. Because both branches return `"tool_use"`, the ordering is inconsequential for correctness. A payload with both `tool_name` and `hook_event_name: "afterFileEdit"` resolves kind `"tool_use"` regardless of branch order, and `normalize` still overrides `rawName` to `"Edit"` unconditionally when `hookName === "afterFileEdit"`. No correctness gap.

**S3 — `normalize` Cursor name override precedence.** Probed. When both `hook_event_name: "afterFileEdit"` and `input.name: "SomeTool"` or `input.tool_name: "Bash"` are present, the override at lines 139-146 unconditionally sets `rawName = "Edit"`. The Cursor hook name wins. This is the specified behavior; no legitimate Cursor payload would carry a different tool name for `afterFileEdit` since the event name already encodes the tool identity.

**S4 — empty-string `CURSOR_WORKSPACE_ROOT`.** Probed. `workspace_roots: [""]` passes `""` as `input.workspace_roots?.[0]`, which becomes `env.CURSOR_WORKSPACE_ROOT = ""`. The guard `env.CURSOR_WORKSPACE_ROOT && env.CURSOR_WORKSPACE_ROOT.length > 0` short-circuits on the falsy empty string and falls through to `CWD`. Correct.

**S5 — `"cursor"` added to enum; exhaustive matching downstream.** Probed. `LootSource` (`"claude_code" | "codex" | "github" | "wakatime"`) and `xp_by_source` are a separate type in `engine/src/loot.ts` and `contracts/src/sync-profile.ts`; they do not include `SourceEventOrigin` values and are not affected. The only per-origin branching on `SourceEventOrigin` in non-test code is `hooks.ts:473-481`, which explicitly checks `origin === "codex"` and `origin === "claude_code"`. A `"cursor"` origin falls into the else branch, which emits `firing_recently: false` / `last_event_at: null` / `source_origin: "phase-06-deferred"` — intentionally deferred per inline comment. No silent drop; a known-deferred path.

**S6 (diff-derived) — Output stability across schema-version drift.** Probed. `state-json.ts` uses `schemaVersionField = z.number().int().min(1).max(STATE_JSON_SCHEMA_VERSION)`. The `"cursor"` origin value is new but the schema version is not bumped by this ticket — it is already at 3. A persisted `state.json` with `origin: "cursor"` will parse fine on any reader that accepts schema_version ≤ 3 with the updated enum. Old readers (without `"cursor"` in the enum) would reject it on `safeParse`/`parse` — but this is expected enum-extension behavior and not a regression introduced here.

**S7 (diff-derived) — CLI flag/arg symmetry.** N/A — no new CLI flags were added by this ticket.

**S8 (diff-derived) — Error-class breadth in `catch` blocks.** Probed. No new `catch` blocks introduced. The silent-skip `catch {}` in `runHookFromStdin` (line 539) is unchanged. The `CURSOR_WORKSPACE_ROOT` injection path does not add new IO failure modes not already covered.

**S9 (diff-derived) — Defensive layering at module boundaries.** Probed. `CURSOR_WORKSPACE_ROOT = input.workspace_roots?.[0]` is an untrusted hook payload string. `resolveSoaRoot` returns it directly. It is then passed to `soaEventsPath(rootDir)` → `join(rootDir, ".soa/events.ndjson")` → `stat`/`open`. No sanitization against path traversal (e.g. `workspace_roots: ["../../etc"]`). The attack surface is real in that an adversarially crafted Cursor hook payload could direct `readSoaEventsSince` at an arbitrary path. However, the read is limited to `stat` and sequential byte reading of `.soa/events.ndjson` under that path — no write, no execution. The damage surface is information disclosure of a file whose path is attacker-controlled but whose name is fixed (`events.ndjson`). Given that the hook binary runs in the developer's own user context, this is low-severity. Flagging as advisory.

**S10 (diff-derived) — Cross-file atomicity windows.** Probed. `CURSOR_WORKSPACE_ROOT` is resolved synchronously before any file IO within `withHomeLock`. No new atomicity window introduced.

**S11 (diff-derived) — Test-contract strength.** Probed. The test suite has explicit Codex regression coverage: `"classifies Codex raw stdin as codex-origin tool-use"` (hook_event_name `"pre_tool_use"`) at line 424, and `"classifies Codex session_end raw stdin as session_end"` at line 435. Both assert `.sourceEvent.origin === "codex"` after the 4-branch refactor. Regression coverage is present.

**S12 (diff-derived) — Doc-vs-code drift in the ticket Rationale.** Probed. The Rationale's 4-branch description matches the code precisely. One drift item: the attack-surface description in this review prompt states "The Cursor event check (`afterFileEdit`, etc.) now precedes the `tool_name` check" — but the actual code at `hook-binary.ts:120-127` places the `input.tool_name` guard *before* the Cursor event-name guard. This is drift between the review brief and the code, not between the ticket Rationale and the code.

---

**Actionable findings**

None.

---

**Advisory Observations**

- **`hooks.ts` Cursor-origin `firing_recently` gap**: `hooks.ts:480-481` gates `firing_recently` and `last_event_at` on `origin === "claude_code"`. Cursor-origin events fall to the `else` branch and emit `firing_recently: false` with `source_origin: "phase-06-deferred"`. This is explicitly deferred per the inline comment, but it means the activity heartbeat signal is invisible to any consumer of that hook output when the agent is Cursor. No action needed this phase, but the deferred tag should track this.

- **Untrusted `workspace_roots[0]` passed unsanitized to `join`**: `resolveSoaRoot` accepts the raw Cursor payload string and `join` constructs the events path from it. A crafted payload like `workspace_roots: ["../../sensitive"]` would direct `readSoaEventsSince` to `../../sensitive/.soa/events.ndjson`. The blast radius is read-only (stat + sequential read of a fixed relative path), and the hook runs in the developer's own security context, so this is low-severity. It is worth noting that no path normalization or prefix-guard is applied.

- **`rawHookKind` attack-surface description vs code ordering**: The review prompt's attack surface S2 states "The Cursor event check now precedes the `tool_name` check." In the actual code, `tool_name` is still checked first (line 120). Both branches return `"tool_use"` so there is no correctness gap, but the mismatch between the review brief and the code could mislead future reviewers of this branch.

- **`"stop"` (Cursor lowercase) standby behavior untested in state output**: The test at line 511 asserts only `sourceEvent.origin === "cursor"` for `hook_event_name: "stop"`. The `classifyEvent` stop-detection branch at lines 210-216 uses `rawEventName === "stop"` (lowercased), so both Cursor `"stop"` and Claude Code `"Stop"` produce `state: "standby"`. This is correct but there is no test asserting `state === "standby"` for the Cursor `"stop"` variant — only origin is asserted. Not a correctness bug, but the test coverage is incomplete for the state dimension.

---

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: All invariants probed and held; all attack surfaces covered; no actionable findings; advisory observations recorded.
