You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**P6.08 Attention bubble UI — Outcome:**

- An attention bubble appears below the floating pet when `state.json` contains an `attention` object with an unexpired `expires_at`.
- Bubble shows `attention.summary` as primary text and `attention.reason_kind` as subtitle.
- ℹ️ icon in the top-right corner of the bubble is tappable — surfaces a popover or tooltip with `reason_kind` metadata.
- Hover state reveals: `×` dismiss button on the left, contextual action button on the right.
  - `input_requested` → `Focus` button (best-effort bring IDE/agent app to foreground via `source_origin`).
  - `error_blocked` → `Reply` button (focus the relevant session window).
  - `review_ready` → `Open` button (reserved — no handler in Phase 06, disabled/hidden).
- Dismiss (`×`) clears `attention` from the bubble view and causes the pet to return to `idle` pose. Does not write back to `state.json` — the renderer treats dismissed attention as locally suppressed for the session.
- Bubble does **not** appear when the pet is collapsed to menubar-only.
- Bubble does **not** appear when `attention.expires_at < now` (TTL already expired per P6.07 policy).
- Works in both lite and alive modes.

**Rationale notes from ticket:**
- Session-local dismiss avoids writing back to `state.json`. TTL from P6.07 handles the case where the user ignores the bubble — it decays naturally.
- `review_ready` action button is hidden or disabled in Phase 06 — do not leave a visible but non-functional button.
- Dismiss is session-local only; if user relaunches, bubble may reappear if `expires_at` hasn't passed. Acceptable Phase 06 behavior.
- Bubble positioning: ensure it doesn't clip off-screen when floating pet is near the bottom edge.
- `Focus` action for `source_origin: "cursor"`: silent failure if Cursor is not running.

### Files touched

Implementation:
- apps/menubar/Sources/ActivityState.swift (AttentionPayload expanded)
- apps/menubar/Sources/AttentionBubblePanel.swift (new)
- apps/menubar/Sources/FloatingPetController.swift (applyAttention added to protocol + impl)
- apps/menubar/Sources/FloatingPetPanel.swift (bubble management in controller)
- apps/menubar/Sources/LivePollingDriver.swift (applyAttention callback + Outcome fields)
- apps/menubar/Sources/MenubarApp.swift (wiring applyAttention in live driver)

Tests:
- None added (Red: skip — SwiftUI/AppKit visual component; manual test at exit condition)

### Invariants to hold

1. Bubble must NOT appear when `attention.expires_at` is parseable and strictly less than `now` — `AttentionBubblePanel` must not be shown when `payload.isExpired()` returns true.
2. Dismiss (`×`) must be session-local only: `dismissBubble()` calls `window?.orderOut(nil)` and does NOT write to `state.json` — verify no file I/O occurs in the dismiss path.
3. `review_ready` reason_kind must not produce a visible, tappable action button — the action button must be hidden or have `isHidden = true` for this case.

### Attack surfaces to probe

1. **`AttentionPayload.isExpired(now:)` — TTL parse correctness**: Does the two-pass ISO 8601 parser handle both fractional-seconds and whole-seconds forms correctly? Does it return `false` (not expired) when `expiresAt` is nil, preventing unintended bubble suppression?

2. **`FloatingPetPanelController.applyAttention` — expired payload gate**: When `payload.isExpired()` is true, does the bubble correctly hide and `attentionActive` correctly become false? If the bubble was showing and the payload then expires on the next poll tick, does it disappear?

3. **`AttentionBubbleView.configure` — `review_ready` action button visibility**: Verify the switch statement hides the action button for `review_ready` and unknown reason_kinds. Is `actionButton.isHidden = true` set and also `alphaValue` correctly handled so a previously-visible action button doesn't stay visible?

4. **`LivePollingDriver` attention cache — `(AttentionPayload?, String?)?` comparison**: The cache uses `last.0 != newAttention.0 || last.1 != newAttention.1`. Since `AttentionPayload` is `Equatable` this should work. But: when attention payload is nil on failure paths, does the cache correctly suppress repeated nil emissions after the first?

5. **`FloatingPetPanelController.show(frame:)` + bubble reposition timing**: When the main panel is dragged and `show(frame:)` is called continuously, the bubble is repositioned each call. Is `lastPanelFrame` updated correctly before `repositionAndShowBubble()` is called? Can `repositionAndShowBubble()` receive stale `lastPanelFrame` if `show(frame:)` is interleaved with `applyAttention`?

6. **`AttentionBubbleView.performAction` — bundle ID lookup silent failure**: If `sourceOrigin` is nil or not in the `bundleIdMap`, the action silently no-ops. Verify no crash path exists when `NSWorkspace.shared.runningApplications` returns an empty array.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `AttentionPayload` adds two new optional fields decoded from JSON. Verify: does adding `summary` and `reasonKind` as `String?` break decoding of older state.json payloads that omit these fields? The `JSONDecoder.keyDecodingStrategy = .convertFromSnakeCase` must map `reason_kind` → `reasonKind` correctly.

2. **CLI flag/arg symmetry** — N/A. No CLI flags changed in this diff. The delivery orchestrator is not modified.

3. **Error-class breadth in `catch` blocks** — The `isExpired(now:)` function uses `ISO8601DateFormatter` which returns nil (not throws) for unparseable strings. The two-pass parse returns `false` when both attempts fail — this means an unparseable `expiresAt` is treated as "not expired", showing the bubble indefinitely. Is that the intended behavior?

4. **Defensive layering at module boundaries** — `applyAttention(payload:sourceOrigin:)` is called from `LivePollingDriver` on the main thread. `FloatingPetPanelController` is `@MainActor`. Verify the `@MainActor` annotation propagates correctly through the call chain: `LivePollingDriver` → `FloatingPetController.applyAttention` → `FloatingPetPanelController.applyAttention`.

5. **Cross-file atomicity windows** — The attention bubble state (`attentionActive`, `isPanelShown`, `lastPanelFrame`) is maintained across multiple methods. Verify: if `hide()` is called and then `applyAttention` is called immediately after (race between timer tick and user action), does the bubble correctly stay hidden? Since everything is `@MainActor`, these are serialized — but verify the ordering is correct.

6. **Test-contract strength** — No tests added (Red: skip). The `FloatingPetPanelSpy` in `FloatingPetControllerTests` now uses the protocol extension no-op for `applyAttention`. Verify the existing 227 tests still pass after adding `applyAttention` to the protocol. Also verify `LivePollingTests` tests that create `LivePollingDriver` without `applyAttention` still compile and pass (it's an optional property, not a required init param).

7. **Doc-vs-code drift in the ticket Rationale** — The ticket says "Dismiss (`×`) clears `attention` from the bubble view and causes the pet to return to `idle` pose." The implementation does NOT cause the pet to return to idle pose — it only hides the bubble (`window?.orderOut(nil)`). The pet's ActivityState remains unchanged (still showing `standby` or `errored` animation). This may be drift between the spec and the implementation.

### Diff context

**ActivityState.swift**: Added `summary: String?` and `reasonKind: String?` to `AttentionPayload`. Added `isExpired(now:)` extension method with two-pass ISO 8601 parse (fractional-seconds first, then whole-seconds fallback).

**AttentionBubblePanel.swift** (new, 346 lines): 
- `BubbleLayout` enum: computes bubble frame centered below pet, clamped to visible screen frame. Width: max(200, min(280, petWidth+40)). Height: 58pt. Gap below pet: 6pt.
- `AttentionBubblePanel`: NSPanel (.borderless, .nonactivatingPanel, .floating level). Delegates to `AttentionBubbleView`. `update(payload:sourceOrigin:)` and `reposition(relativeTo:visibleFrame:)` are the two external entry points.
- `AttentionBubbleView` (private): NSView with `NSVisualEffectView` background (hudWindow, darkAqua). Shows summary label (12pt medium white) + subtitle label (10pt, 58% white). ℹ️ info button always visible. Dismiss (`×`) + action button fade in/out (`alphaValue 0→1`) on hover via NSTrackingArea. `configure(summary:reasonKind:sourceOrigin:)` sets content and resets hover state. Info button shows NSPopover with reasonKind text. Dismiss calls `window?.orderOut(nil)`. Action button looks up bundle ID by sourceOrigin and calls `NSRunningApplication.activate`.

**FloatingPetController.swift**: Added `applyAttention(payload:sourceOrigin:)` to `FloatingPetPanelManaging` protocol with default no-op extension. Added forwarding in `FloatingPetController`.

**FloatingPetPanel.swift**: `FloatingPetPanelController` gains `attentionBubble: AttentionBubblePanel?`, `lastPanelFrame`, `isPanelShown`, `attentionActive`. `show(frame:)` tracks frame and conditionally shows bubble. `hide()` hides bubble. `applyAttention(payload:sourceOrigin:)` gates on `isExpired()`, creates bubble lazily, updates content, shows if panel is shown.

**LivePollingDriver.swift**: Added `ApplyAttention` typealias and optional `applyAttention` property. Added `lastAttentionEmission: (AttentionPayload?, String?)?` cache. `Outcome` gains `attention: AttentionPayload?` and `sourceOrigin: String?`. `decide` populates from `snapshot.attention` and `snapshot.sourceEvent?.origin` on success; nil on failure. `emit` fires `applyAttention` when attention or sourceOrigin changes.

**MenubarApp.swift**: After creating `LivePollingDriver`, sets `driver.applyAttention` to weakly capture `floatingPetController` and forward calls.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
