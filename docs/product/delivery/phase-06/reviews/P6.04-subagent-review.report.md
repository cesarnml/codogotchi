---

**Invariant results**

**Invariant 1** (standby → `attention.reason_kind === "input_requested"`, `expires_at = now + 7200000ms`, absent for all other states): **held** — `buildAttention` returns the correct payload for `standby` with `STANDBY_TTL_MS = 2 * 60 * 60 * 1000`; for all other states it returns `undefined`; the spread `...(attention !== undefined && { attention })` omits the key entirely (spreading `false` is a no-op in ES2018+ object spread).

**Invariant 2** (errored → `attention.reason_kind === "error_blocked"`, `expires_at = now + 1800000ms`): **held** — `ERRORED_TTL_MS = 30 * 60 * 1000`, arithmetic is correct, `buildAttention("errored", now)` produces the correct ISO string.

**Invariant 3** (`tool_command` present exactly when source event is Bash/Shell `tool_use` with defined command): **held** — `isBashOrShell` checks `classified.sourceEvent` (pre-gate values), and `classified.command !== undefined` guards the assignment; the Bash/Shell early-return on `command === undefined` leaves `command` absent from `ClassifyResult`, so `toolCommand` is correctly `undefined` in that branch.

---

**Surface results**

**S1 — `buildAttention` TTL arithmetic:** [probed] `new Date(now.getTime() + STANDBY_TTL_MS).toISOString()` is correct; `STANDBY_TTL_MS = 7200000`, `ERRORED_TTL_MS = 1800000`. `created_at` is always `now.toISOString()` (not reused from the expiry date). No arithmetic error found.

**S2 — Spread-conditional omission:** [probed] `...(attention !== undefined && { attention })` evaluates to `...(false)` when `undefined`; `{ ...false }` is a spec-defined no-op in ES2018+ object spread — no `attention` key, not `null`, not `{}`. Zod `.optional()` round-trips cleanly: the `verified` object from `stateJsonV1Schema.parse()` will omit the key when it is not present. No issue.

**S3 — `isBashOrShell` uses `classified.sourceEvent`:** [probed] Lines 467–470 confirm `isBashOrShell` reads `classified.sourceEvent`, not the local `sourceEvent` variable that gets overwritten by the SoA tail read. When a SoA gate event overrides `activityState` to `hyped` via the tail path, a concurrent Bash `classified.sourceEvent` still sets `isBashOrShell = true` and `tool_command` is written. This behavior is consistent with the ticket outcome ("for every Bash or Shell tool_use event, tool_command is written") and is intentional per the diff description.

**S4 — `classifyEvent` Bash/Shell `command === undefined` early return:** [probed] Lines 202–204 return `{ state: "implementing", sourceEvent, readRun: 0 }` with no `command` field (TypeScript optional field, absent from the object literal). `runHook` guards with `classified.command !== undefined`, which is `false` for an absent field. No `command: undefined` phantom key is present. Correct.

**S5 — `tool_input.command` Codex raw-stdin path:** [probed] `normalize()` line 130: `const rawCommand = input.command ?? input.tool_input?.command` — correctly reads the Codex-shape `tool_input.command`. Line 131: `typeof rawCommand === "string" ? rawCommand : undefined` narrows correctly. The result propagates into the `NormalizedEvent.command` field and then into `ClassifyResult.command` for Bash/Shell branches. No gap found.

**S6 — Sticky-gate interaction with `buildAttention`:** [probed] When a gate fires (e.g. `ticket_started`), `GATE_STATES.has(activityState)` is true, `lastGate` is updated, and the final `activityState` at the `buildAttention` call site is `"hyped"`. `buildAttention("hyped", now)` → `undefined`. No stale `attention` payload is written. Correct.

**D1 — Output stability across schema-version drift:** [probed] `attention` and `tool_command` are both `.optional()` in `stateJsonV1Schema` (contracts/state-json.ts lines 48 and 56). Swift renderer and status.ts consumers parsing without those fields will deserialize them as absent/undefined — no break. No fixture files found that need updating in the contracts package.

**D2 — CLI flag/arg symmetry:** [N/A — no CLI flags or argument changes introduced in this diff]

**D3 — Error-class breadth in `catch` blocks:** [probed] `buildAttention` is pure with no I/O, so it cannot throw. `writeStateAtomic` validates through Zod before writing. `buildAttention` only emits `"input_requested"` and `"error_blocked"`, both present in the Zod enum `["input_requested", "error_blocked", "review_ready"]` (state-json.ts line 50). Zod will not throw on these values.

**D4 — Defensive layering at module boundaries:** [probed] `writeStateAtomic` calls `stateJsonV1Schema.parse(state)` unconditionally before any file write. Any invalid `attention` shape that somehow escaped `buildAttention`'s narrow return type would be caught here with a thrown Zod error, keeping the target path clean. Layering is sound.

**D5 — Cross-file atomicity windows:** [N/A — `attention` and `tool_command` are added to the same single `writeStateAtomic` temp-file rename; no multi-step write path introduced]

**D6 — Test-contract strength:** [probed] Tests at lines 676–680 and 694–698 assert exact ISO string values via `toBe(expectedExpiry)` for `expires_at`. The absence case is covered by `toBeUndefined()` for Edit tool-use (line 706). However, **neither attention test asserts `created_at`** — the field is written by `buildAttention` but the test suite never reads `state.attention?.created_at`. This leaves the `created_at === now.toISOString()` part of the spec unverified by the test suite. Additionally, Shell-tool and Codex raw-stdin (`tool_input.command`) paths for `tool_command` are not covered.

**D7 — Doc-vs-code drift in ticket Rationale:** [probed] The ticket Outcome section states "For every Bash or Shell tool_use event, `tool_command` is written to `state.json` (the raw command string)." The implementation omits `tool_command` when Bash/Shell fires with no command string (`command === undefined`). The Rationale section correctly documents this: "`ClassifyResult.command` is only set in Bash/Shell branches where `command !== undefined`; the `command === undefined` early return intentionally omits it." The Outcome prose is slightly looser than the Rationale. Invariant 3 in the review prompt already incorporates the "with a defined command string" qualifier, so the Rationale is authoritative and the implementation matches it. Minor phrasing imprecision in the Outcome paragraph only.

---

**Actionable findings**

None.

---

**Advisory Observations**

- **Missing `created_at` assertion in attention tests (D6):** Neither the standby nor the errored attention test (hook-binary.test.ts lines 666–698) asserts `state.attention?.created_at`. The spec requires `created_at: now.toISOString()`. If a future refactor accidentally swapped `created_at` with `expires_at` or set it to the expiry time, both tests would still pass. Adding `expect(state.attention?.created_at).toBe(FIXED_NOW.toISOString())` to both test cases would close the gap.

- **No test for Shell tool (D6):** `tool_command` persistence is tested only for `Bash` (line 709). A Shell event is the second explicitly named variant in the spec. One additional test covering `name: "Shell"` would complete the coverage.

- **No test for Codex raw-stdin `tool_input.command` path (D6):** The `normalize()` function supports `tool_input.command` as a fallback for Codex input shape. No test in the `runHook` block exercises `{ tool_input: { command: "..." }, hook_event_name: "pre_tool_use" }` and asserts `tool_command` is written. Low-risk because the path through `normalize()` is well-exercised for classification state, but the end-to-end `tool_command` thread is unverified for Codex input.

- **Loose Outcome prose vs. implementation (D7):** The ticket Outcome paragraph says "For every Bash or Shell tool_use event, `tool_command` is written" — but `tool_command` is omitted when the Bash/Shell event has no `command` string. The Rationale correctly explains this. Tightening the Outcome paragraph to say "for every Bash or Shell tool_use event with a defined command string" would eliminate the surface-level ambiguity for future readers.

---

**Runner termination**

`runnerStatus`: completed
`terminatedReason`: All attack surfaces probed to conclusion; no invariant breaks or actionable findings; advisory observations recorded above.
