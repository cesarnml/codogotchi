The `standby.json` fixture has two problems if parsed by the TypeScript schema: `hp_overlay: "stable"` (not in enum) and `source_event.kind: "demo"` (not in enum). However, this fixture is exclusively a Swift fixture — the TypeScript tests don't load it. The Swift reader ignores `hp_overlay` and decodes `source_event` as loose strings. No code path parses this fixture through the TS schema, so it's an advisory observation, not a blocking finding. Now I have everything I need.

---

**Invariant results**

**Invariant 1** — `parseStateJson` must reject `activity_state: "requesting_input"`.
[held] — `ACTIVITY_STATES` in `animation-state.ts:3-19` no longer contains `"requesting_input"`. The Zod enum `activityStateSchema` is derived directly from that tuple via `z.enum(ACTIVITY_STATES)` at line 21, so any payload carrying `"requesting_input"` will fail Zod validation and throw a ZodError. The test at `state-json.test.ts:38-45` asserts this explicitly.

**Invariant 2** — `parseStateJson` must accept `activity_state: "standby"` with `schema_version` 1, 2, or 3.
[held] — `"standby"` is present in `ACTIVITY_STATES`. `schemaVersionField` is `.min(1).max(3)`, so versions 1, 2, and 3 all pass the version gate. The test suite covers `schema_version: 3` + `standby` explicitly (`state-json.test.ts:28-35`) and backward-compat cases for schema_version 1 and 2 (`state-json.test.ts:152-164`). Notably, the tests do not assert `schema_version: 1` + `standby` or `schema_version: 2` + `standby` directly, but the schema logic permits them — no `if (version < 3) reject(standby)` guard exists.

**Invariant 3** — Swift `EXPECTED_STATE_SCHEMA_VERSION` must equal TypeScript `STATE_JSON_SCHEMA_VERSION` (both 3); v3 must parse; v4 must return `.schemaNewer(got: 4, expected: 3)`.
[held] — TypeScript constant is `3` (`state-json.ts:4`). Swift constant is `3` (`StateJsonReader.swift:8`). `StateJsonReaderTests.swift:90-91` asserts `EXPECTED_STATE_SCHEMA_VERSION == 3`. `testSchemaVersion4FailsWithSchemaNewer` asserts `.schemaNewer(got: 4, expected: 3)`. `testSchemaVersion3WithStandbyParsesSuccessfully` asserts v3 + standby parses to `.standby`.

---

**Surface results**

**Surface 1 — `schemaVersionField` max bound.**
[probed] `schemaVersionField` is `.number().int().min(1).max(STATE_JSON_SCHEMA_VERSION)` where `STATE_JSON_SCHEMA_VERSION = 3`. Module-evaluation order means the constant is read once when the module loads; no lazy reference issue. The TS test at line 168-172 explicitly rejects schema_version 4. Clean.

**Surface 2 — `activityStateSchema` propagation from `ACTIVITY_STATES` tuple.**
[probed] `activityStateSchema = z.enum(ACTIVITY_STATES)` at `animation-state.ts:21`. `stateJsonV1Schema.activity_state` references `activityStateSchema` imported directly. Removing `"requesting_input"` and adding `"standby"` from the tuple propagates automatically — no separate string literal in the Zod schema. The TypeScript type `ActivityState` is inferred from the schema, so there's no out-of-sync hand-maintained union. Clean.

**Surface 3 — Swift `ActivityState.init(from:)` fallback and raw-value match.**
[probed] `ActivityState.swift:21` declares `case standby = "standby"`. The hook emits `"standby"` (confirmed at `hook-binary.ts:164`). The Swift `init(from:)` at line 35-38 uses `ActivityState(rawValue: raw) ?? .idle` — for `"standby"` the rawValue lookup succeeds and does not fall back to `.idle`. The string is lowercase, no underscore variant (`"stand_by"`) or capitalization drift. Clean.

**Surface 4 — `attention` datetime validators reject naive datetimes.**
[probed] Both `created_at` and `expires_at` use `z.string().datetime({ offset: true })` (`state-json.ts:52-53`). The Zod `datetime({ offset: true })` validator requires an explicit UTC offset (`Z` or `±HH:MM`). A naive datetime like `"2026-05-29T00:00:00"` (no trailing `Z` or offset) will fail. The test payload at `state-json.test.ts:67-68` uses `"2026-05-29T00:00:00.000Z"` (UTC Z). Clean — though no test explicitly asserts rejection of a naive datetime string; this is a gap noted below in Advisory Observations.

**Surface 5 — `attention.reason_kind` enum breadth.**
[probed] `z.enum(["input_requested", "error_blocked", "review_ready"])` at `state-json.ts:50`. The test at `state-json.test.ts:91-104` passes `"unknown_kind"` and asserts it throws. Clean.

**Surface 6 — `work_mode` absent returns `undefined`, not `null` or `""`; no conflict with activity_state `"implementing"`.**
[probed] `z.enum([...]).optional()` — Zod `.optional()` returns `undefined` when the field is absent. The test at line 141-148 asserts `work_mode` is `undefined` when omitted. Regarding name conflict: `work_mode` and `activity_state` are separate fields in the schema object (`state-json.ts:43` vs `57`); they are parsed independently. A payload with `activity_state: "implementing"` and `work_mode: "implementing"` is valid and the values are distinct in the parsed output. Clean.

**Diff-derived surface 1 — Output stability across schema-version drift.**
[probed] Checked legacy shape via `state-json.test.ts:152-164` — schema_version 1 and 2 payloads parse cleanly. The `standby.json` fixture has `schema_version: 3`. The Swift fixtures directory contains `standby.json` with correct version. No adapter layer exists (direct Zod parse); no version-migration transform is needed. Clean.

**Diff-derived surface 2 — CLI flag/arg symmetry.**
[N/A — no new CLI flags introduced in this ticket. `hook-binary.ts` line 164 is a single string substitution in an existing return path.]

**Diff-derived surface 3 — Error-class breadth in `catch` blocks.**
[N/A — no new catch blocks added in this diff.]

**Diff-derived surface 4 — Defensive layering at TS/Swift module boundary.**
[probed] TS schema defines `activity_state` as Zod enum from `ACTIVITY_STATES`; Swift `ActivityState` enum is manually maintained. The two are now in sync: both have `standby`, neither has `requesting_input`. The Swift fixture `standby.json` has `schema_version: 3` matching the bumped constant. The Swift reader's pre-decode version check uses `EXPECTED_STATE_SCHEMA_VERSION = 3` matching TS `STATE_JSON_SCHEMA_VERSION = 3`. Clean.

**Diff-derived surface 5 — Cross-file atomicity windows.**
[N/A — no multi-step writes introduced; only schema and enum value changes.]

**Diff-derived surface 6 — Test-contract strength.**
[probed] TS tests cover: `STATE_JSON_SCHEMA_VERSION === 3`, accept `standby`, reject `requesting_input`, accept/reject `attention` fields, accept/reject `work_mode`, backward compat v1/v2, forward-compat rejection of v4. Swift tests cover: `EXPECTED_STATE_SCHEMA_VERSION === 3`, v3+standby parse success, v4 rejection with correct error, boolean and float schema_version rejection. One gap: no test asserts that a naive (no-offset) datetime in `attention.created_at` or `expires_at` throws. This is advisory, not blocking.

**Diff-derived surface 7 — Doc-vs-code drift in ticket Rationale.**
[probed — see Advisory Observations.]

---

**Actionable findings**

None.

---

**Advisory Observations**

- **`standby.json` fixture has an invalid `hp_overlay` value and a non-schema `source_event.kind`.** `apps/menubar/Fixtures/state-json/standby.json:4` uses `"hp_overlay": "stable"`, which is not in the `HP_OVERLAY_STATES` enum (`["thriving", "getting_sick", "near_death", "ghost"]`). The same fixture's `source_event.kind` is `"demo"`, which is not in `sourceEventKindSchema`. Neither issue causes a runtime failure today — the Swift `StatePayload` decoder does not decode `hp_overlay`, and `SourceEvent` fields are typed as `String?` (loose). However, if this fixture is ever loaded through `parseStateJson` (e.g., a future cross-language integration test), it will throw. The fixture was renamed from the old `requesting-input.json` and the `hp_overlay` value was not corrected to match the contract. Recommended correction: change `"stable"` → `"thriving"` and `"demo"` → `"cli"` (or `"manual"`) so the fixture is contract-valid.

- **No test probes naive datetime rejection for `attention` fields.** `z.string().datetime({ offset: true })` in `state-json.ts:52-53` correctly rejects naive ISO strings. However, neither `state-json.test.ts` nor any other test asserts that a payload with `attention.created_at: "2026-05-29T00:00:00"` (no `Z`) throws. The validator is correct, but the gap means a future refactor of that field (e.g., removing `{ offset: true }`) would not be caught by tests.

- **Doc-vs-code drift — forward-compat policy wording vs. schema enforcement direction.** `animation-state-vocabulary.md:58-60` states: "Adding a new optional field to a future schema version does not require a `schema_version` bump. Changing the meaning of an existing field, removing a field, or narrowing a field's domain … does require a bump." The v3 diff adds three optional fields (`attention`, `tool_command`, `work_mode`) and also renames `requesting_input` → `standby` (narrowing the enum domain), so the bump is correctly mandated. However, this same policy clause implies that a consumer who parses a v2 payload with a v3 schema (allowed by `max(3)`) will see the new optional fields as absent — this is correct behavior but the doc does not explicitly state that the parser is forward-schema-applied (v3 schema parses v2 wire payloads). Minor wording gap; no code impact.

- **`DemoCycleDriver.cycle` comment discrepancy.** The inline comment on `DemoCycleDriver.swift:17` says "The cycle is hardcoded: `.idle` → `.implementing` → `.runningTests` → `.celebrating` → loop." But the actual `cycle` array (lines 25-41) contains all 15 states. The comment is a stale copy from an earlier phase before the cycle was expanded. No behavioral impact, but a reader of the file gets an incorrect picture of the cycle length.

---

**Runner termination**

`runnerStatus`: completed
`terminatedReason`: All invariants held, all surfaces were probed successfully, and the review completed without rate limiting or sandbox denials.
