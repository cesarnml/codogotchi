**Invariant results**

1. [held] `readDirectoryImpl` returns `.fileNotFound` when `fileExists(..., isDirectory:)` is false or not a directory; existing empty-dir/stale/tmp-only paths fall through to `idleSnapshot()`.
2. [held] Multiple slices are reduced by parsed `updatedAt` with strict `>` replacement, so the latest parseable ISO date wins.
3. [held] Files with `mtime` older than `staleTTL` are skipped before decode, and names containing `.tmp-` are excluded by the filename guard.

**Surface results**

- `idleSnapshot()` defaults: [probed]. It emits schema version 7, `.idle`, level 1, level fraction 0, `MAX_HALF_HEARTS`, active minutes 0, and nil optional payloads. These match the legacy reader’s missing-field defaults.
- `parseISO8601Date` fallback to `Date.distantPast`: [probed]. A malformed `updatedAt` slice remains decodable but loses to any valid timestamp; if it is the only valid slice, it can still win. That matches the current best-effort decode posture, but see advisory doc drift around version/validator language.
- mtime filter ordering/race: [probed]. If a file disappears between directory listing and attribute/read, the optional `try?`/guard path skips it; no crash path found.
- `DemoCycleDriver` atomicity: [probed]. It creates `state.d/` before writing `demo:default.json`; a poll in that window reads an empty directory and returns idle. This is transient and consistent with the directory reader’s empty-dir contract.
- Gate precedence with directory reader: [probed]. `LivePollingDriver.decide` reads the gate after the reader result and calls `resolveActivityState(gate:hookState:)` with the aggregated `snapshot.activityState`, then applies revive override. Gate override still applies over the aggregate.
- Output stability across schema-version drift: [probed]. Legacy `read(at:)` accepts schema versions ≤7 and rejects 8+. Directory slices are decoded by `SlicePayload` with no schema field, matching the current TS `sliceEntrySchema`, while some ticket prose says otherwise.
- CLI flag/arg symmetry: [N/A — no CLI flags or args were added in this ticket].
- Error-class breadth in catch blocks: [probed]. `contentsOfDirectory` errors map to `.malformed`; per-file attribute/read/decode errors are silently skipped. That matches the stated best-effort slice reader contract.
- Defensive layering at module boundaries: [probed]. Empty string or a file path returns `.fileNotFound` via the directory existence guard; the public one-arg and test overloads both route through the same implementation.
- Cross-file atomicity windows: [probed]. Demo directory creation before slice write can produce one idle poll. Live writer uses tmp+rename per file; tmp names are filtered, so partial files are not decoded.
- Test-contract strength: [probed]. Tests cover missing directory, empty directory, stale slice exclusion, tmp-file exclusion, latest `updated_at` wins, and default reader wiring. They do not assert equal timestamp behavior or schema-field enforcement for slices.
- Doc-vs-code drift in ticket Rationale: [probed]. Drift found and recorded below: P12.03/P12.02 prose says slice files are `schema_version: 7` entries, while P12.01 and the actual TS/Swift slice models omit top-level `schema_version`.

**Actionable findings**

None.

**Advisory Observations**

- `docs/product/delivery/phase-12/ticket-03-swift-slice-reader.md:10` and `:27` claim the Swift reader decodes or requires a `schema_version: 7` slice entry, and `docs/product/delivery/phase-12/ticket-02-cli-slice-writer.md` similarly says slice files contain `schema_version: 7`. The actual contract in `docs/product/delivery/phase-12/ticket-01-keyed-slice-contract.md:10`, `packages/contracts/src/slice-entry.ts:17-37`, `packages/cli/src/hook-binary.ts:1219-1237`, and `apps/menubar/Sources/StateJsonReader.swift:292-308` omits top-level `schema_version` from slices and only adds schema version 7 after aggregation. This is doc-vs-code drift, not a blocking code finding, because the implemented writer and reader agree.

- Equal `updated_at` ordering is not pinned. TS documents strict `>` with first-in-array wins, and Swift also uses strict `>`, but both arrays come from filesystem enumeration (`readdir` / `contentsOfDirectory`) with no explicit sort. If two slices have identical timestamps, status and the pet could choose whichever file enumeration yields first. This is outside the stated invariant, which only requires most-recent wins.

- Some comments and tooltip copy still say `state.json` on paths now backed by `state.d/`, especially `LivePollingTooltips` and `DemoConfig` comments. The user-visible schema error copy is mostly legacy-reader/injected-test behavior after this change, so I did not treat it as a ticket-breaking behavior issue.

**Runner termination**

runnerStatus: completed
terminatedReason: review finished against the requested invariants, attack surfaces, implementation, tests, and ticket docs.
