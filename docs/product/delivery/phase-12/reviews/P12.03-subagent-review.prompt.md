You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `StateJsonReader` gains `readDirectory(at:)` which scans the `state.d/` directory, decodes each slice as a `schema_version: 7` slice entry (SlicePayload), and collapses via `globalAggregate` (most-recent `updated_at` wins).
- `EXPECTED_STATE_SCHEMA_VERSION` bumped to 7 (lockstep with P12.01 writer constant).
- The pet, menubar, and HUD render identically to v1.1.1 for equivalent input (behavior-invisibility gate).
- `GateJsonReader` / gate-override precedence reconciled: ambient gate overrides the global-aggregate resolved state.
- Slices older than mtime TTL (2 hours) are ignored; tmp files (`*.tmp-*`) are never read.

**Rationale notes:**
- `SlicePayload` omits top-level `origin`/`sessionId` — filename provides keying, fields unused by reducer.
- LivePollingTests error-visual tests inject custom `reader` closures instead of writing files, because the directory reader uses best-effort slice decoding (malformed slices silently skipped).
- `StateJsonReader.read(at:)` legacy path still exists for the existing `state.json` format tests.

### Files touched

Implementation:
- `apps/menubar/Sources/StateJsonReader.swift` — added `readDirectory(at:)` + `readDirectoryImpl` + `SlicePayload` struct + `idleSnapshot()` helper; bumped `EXPECTED_STATE_SCHEMA_VERSION` to 7
- `apps/menubar/Sources/DemoConfig.swift` — changed polling target from `state.json` to `state.d`
- `apps/menubar/Sources/DemoCycleDriver.swift` — changed write target from `state.json` to `state.d/demo:default.json`
- `apps/menubar/Sources/LivePollingDriver.swift` — changed default reader from `StateJsonReader.read(at:)` to `StateJsonReader.readDirectory(at:)`

Tests:
- `apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift` — added 15+ new `readDirectory` characterization tests; updated schema version assertions (v6→v7); renamed `testSchemaVersion7FailsWithSchemaNewer` → `testSchemaVersion8FailsWithSchemaNewer`
- `apps/menubar/Tests/MenubarTests/DemoModeTests.swift` — updated `makeSandboxPath()` to return `state.d/`, updated atomic-write test to read from `state.d/demo:default.json`, updated live-path assertion
- `apps/menubar/Tests/MenubarTests/LivePollingTests.swift` — updated `makeSandboxPath()` to return `state.d/`, added `writeSlice(_:to:)` helper, updated `copyFixture`/`writeV5StateJson` to write to directory, added `reader` param to `makeDriver`, error-visual tests now inject custom readers

### Invariants to hold

1. `StateJsonReader.readDirectory(at:)` must return `.failure(.fileNotFound)` when the path does not exist or is a file (not a directory), and must return `.success(idleSnapshot)` when the directory exists but contains no valid non-stale non-tmp slices.
2. `readDirectory` must select the slice with the latest `updatedAt` ISO 8601 date as winner when multiple slice files are present, matching the TS `globalAggregate` tiebreak in P12.01.
3. Slices whose filesystem mtime is more than 2 hours before `now` must be excluded from the reduction entirely, and files whose names contain `.tmp-` must never be decoded.

### Attack surfaces to probe

1. **`idleSnapshot()` in `readDirectoryImpl`** — does it return `EXPECTED_STATE_SCHEMA_VERSION` (7) and defaults matching the actual production idle state? A wrong `halfHearts` or schema version here would silently corrupt the pet display when the directory is empty.
2. **`parseISO8601Date` fallback to `Date.distantPast`** — if `updatedAt` is malformed or empty string, the slice gets the lowest possible date and is never selected as winner. Confirm this is correct vs a silent drop.
3. **`readDirectoryImpl` mtime filter ordering** — the filter reads mtime after opening the file. If the file is deleted between `contentsOfDirectory` and `attributesOfItem`, the `try?` silently produces nil → `continue`. This is a potential race but is it benign (skip) rather than a crash?
4. **`DemoCycleDriver` atomicity** — the new write path creates `state.d/` directory then writes `demo:default.json`. If `createDirectory` succeeds but `write` fails mid-cycle, `readDirectory` would see an empty directory and return idle. Is this the intended race-safe behavior?
5. **Gate precedence with directory reader** — `LivePollingDriver` now uses `readDirectory` as default reader. The gate override logic applies AFTER the read result. Does the gate still correctly override the aggregated snapshot, or does a code path exist where the gate check compares against the raw snapshot before overlay?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `EXPECTED_STATE_SCHEMA_VERSION` bumped from 6 to 7. `StateJsonReader.read(at:)` (legacy flat-file path) now accepts schema_version ≤ 7 and rejects 8+. The `readDirectory` path uses `SlicePayload` which has no schema_version field. Probe: does any persisted artifact format (state.json, slice files, test fixtures) assume the previous max version of 6?
2. **CLI flag/arg symmetry** — N/A for this ticket. No CLI flags were added.
3. **Error-class breadth in `catch` blocks** — `readDirectoryImpl` uses `try?` throughout (no explicit catch). Probe: is any error silently swallowed that should surface as `.failure(.malformed)`? In particular, `contentsOfDirectory` throws to a `do/catch` block that maps to `.failure(.malformed)` — verify this path is actually reached.
4. **Defensive layering at module boundaries** — `readDirectoryImpl` is called from the `readDirectory(at:)` public entry point and from the 2-parameter overload. Probe: is the `dirPath` validated as non-empty before filesystem calls? What happens with an empty string path?
5. **Cross-file atomicity windows** — `DemoCycleDriver` creates the directory then writes `demo:default.json`. The `LivePollingDriver` may poll between directory creation and file write, reading an empty directory → idle default. Probe: is this transient idle flash a behavioral regression vs v1.1.1 where the directory never existed until fully written?
6. **Test-contract strength** — new `readDirectory` tests use `makeTempSliceDir`/`sliceJSON` helpers. Probe: do the tests cover both directory-absent and directory-present-but-empty cases? Do they assert the multi-slice tiebreak against a concrete `updated_at` ordering?
7. **Doc-vs-code drift in the ticket Rationale** — ticket says "require `schema_version == 7` per the refuse-newer clause." `SlicePayload` has no `schema_version` field. Probe: does the Rationale accurately describe what the diff does, or does it overstate the versioning enforcement?

### Diff context

Key changes:
1. `StateJsonReader.swift`: Added `readDirectory(at:String) → Result<StateSnapshot, StateReadError>` (public entry), `readDirectory(at:String, now:Date, staleTTL:TimeInterval)` (testable overload), `readDirectoryImpl` (private implementation), `SlicePayload: Decodable` (no schema_version field, no origin/sessionId), `idleSnapshot()`. The implementation: checks directory existence, lists contents, filters `.tmp-` and stale-mtime files, decodes as `SlicePayload`, picks winner by `updatedAt`, calls `resolveActivityState` on the winner.
2. `DemoConfig.swift`: Changed `.appendingPathComponent("state.json")` → `.appendingPathComponent("state.d")` for both demo and live mode.
3. `DemoCycleDriver.swift`: Changed to `createDirectory(at: sandboxedPath, withIntermediateDirectories: true)` then write to `sandboxedPath.appendingPathComponent("demo:default.json")`. Changed `StateJsonReader.read(at:)` → `StateJsonReader.readDirectory(at:)` for transition log.
4. `LivePollingDriver.swift`: Changed default `reader` parameter from `StateJsonReader.read(at:)` to `StateJsonReader.readDirectory(at:)`.
5. Tests: `makeSandboxPath()` now returns `state.d/` directory URL; `copyFixture` and `writeV5StateJson` create the directory and write to `<dir>/default.json`; error tests use `{ _ in .failure(.X) }` custom readers.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
