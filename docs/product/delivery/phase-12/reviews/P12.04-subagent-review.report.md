**Invariant results**

1. [held] With `SYNC_SHARED_SECRET` set to a non-empty value, `convex/http.ts` reads `x-codogotchi-sync-secret` and returns `401` before JSON parsing or mutation when the header is absent or mismatched; tests cover absent, wrong, and correct header.
2. [held] With `SYNC_SHARED_SECRET` unset or empty, the truthy guard is skipped and the handler proceeds to normal payload validation/mutation; this matches the prompt’s explicit dev-permissive contract.
3. [held] `runSync` adds `x-codogotchi-sync-secret` only when `SyncDeps.syncSecret` is truthy/non-empty, and omits it for undefined or empty values; CLI tests cover configured and omitted cases, and the code path covers empty string via the same falsy branch.

**Surface results**

- `convex/http.ts` guard — empty-string env var: [probed]. `const requiredSecret = process.env.SYNC_SHARED_SECRET; if (requiredSecret)` treats `""` the same as unset, so requests proceed to payload validation. This is consistent with the prompt’s “unset/empty” dev-permissive invariant, though the ticket file’s Outcome text still contains older fail-closed wording; see Advisory Observations.
- `convex/http.ts` guard — header value coercion: [probed]. For a non-empty required secret, absent and empty headers both become `""`, and `"" !== requiredSecret` returns `401`; there is no empty-header bypass for a non-empty server secret.
- `packages/cli/src/sync.ts` — header when `syncSecret` is empty string: [probed]. `if (deps.syncSecret)` omits the header for `""`. That matches invariant 3 and aligns with the server treating an empty `SYNC_SHARED_SECRET` as not enforcing.
- `router.ts` env var read: [probed]. `router.ts` passes `process.env.CODOGOTCHI_SYNC_SECRET` directly. In JS/Bun, an unset env var is `undefined`, while an explicitly empty env var is `""`; both cause `runSync` to omit the header, which matches the requested undefined/empty behavior.
- `convex/http.ts` — 401 vs 403 status code: [probed]. The implementation returns `401` for both absent and wrong credentials. The prompt Outcome says `401`, while the checked ticket file allows `401/403`; this is within contract.
- Test isolation — `process.env` mutation: [probed]. `beforeEach` sets and `afterEach` deletes `SYNC_SHARED_SECRET`; Bun test hooks run after thrown test assertions, so ordinary failures should not leak the env var into later tests. A hard process abort would leak only to that process lifetime and is outside this ticket’s runtime behavior.
- Output stability across schema-version drift: [probed]. The new 401 body uses the existing `jsonError(status, body)` helper and `content-type: application/json`, consistent with the other HTTP errors in `convex/http.ts`. The CLI only inspects `res.ok`, so no existing `/sync` consumer in the touched code parses the 401 shape.
- CLI flag/arg symmetry: [probed]. The secret is env-only via `CODOGOTCHI_SYNC_SECRET`, not a CLI flag or config key. That matches the prompt and ticket Rationale, but the CLI usage text does not document the env var; see Advisory Observations.
- Error-class breadth in `catch` blocks: [probed]. `runSync` treats any non-ok response, including `401`, as `postSucceeded = false` with no auth-specific reason. This does not break the ticket invariants, but operators will see the same `sync failed` summary for auth failure, network failure, and server failure; see Advisory Observations.
- Defensive layering at module boundaries: [probed]. `router.ts` passes the raw env var value to `runSync`, which uses it as a header value. Newline-bearing values are not an HTTP header injection path through Fetch’s header validation; they would cause the fetch to reject and be reported as `postSucceeded = false`, not bypass the server guard.
- Cross-file atomicity windows: [probed]. The server guard runs before `request.json()`, schema validation, and `ctx.runMutation`, so unauthorized requests leave no sync mutation side effects. The CLI change is a single request-header addition with no multi-step write window.
- Test-contract strength: [probed]. HTTP tests assert status codes for absent, wrong, and correct secrets; CLI tests assert exact header name/value and absence when not configured. The omitted-header test uses the property omitted rather than explicit `undefined`/`""`, but the implementation’s falsy check covers both and the behavior is simple.
- Doc-vs-code drift in the ticket Rationale: [probed]. The Rationale and implementation agree on dev-permissive unset behavior and env-only CLI injection. The ticket Outcome still says unset should “fail closed in production posture,” which conflicts with both the Rationale and the implemented/prompted behavior; see Advisory Observations.

**Actionable findings**

None.

**Advisory Observations**

- `docs/product/delivery/phase-12/ticket-04-sync-shared-secret.md` has internal doc drift: the Outcome says unset `SYNC_SHARED_SECRET` should fail closed in production posture, while the Rationale says unset is dev-permissive/backward-compatible and the code implements dev-permissive. This is outside the three finding-discipline clauses because the prompt explicitly selected dev-permissive unset/empty behavior.

- `packages/cli/src/router.ts` usage text documents `CODOGOTCHI_HOME` and `CODOGOTCHI_USER_ROOT` but not `CODOGOTCHI_SYNC_SECRET`. The env-only design is correct per ticket scope, but operators enabling server enforcement must discover the CLI env var from delivery docs rather than `codogotchi help`.

- `packages/cli/src/sync.ts` collapses a 401 auth rejection into the same `sync failed`/`postSucceeded = false` path used for network and server failures. That preserves existing result shape and does not violate the ticket, but it gives no auth-specific feedback when the shared secret is missing or wrong.

**Runner termination**

runnerStatus: completed
terminatedReason: review finished against the provided invariants, attack surfaces, diff-related code, ticket rationale, and targeted tests.
