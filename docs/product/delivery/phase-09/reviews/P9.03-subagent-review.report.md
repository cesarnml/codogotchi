**Invariant results**
- `[held]` Invariant 1: I verified `CODOGOTCHI_ORIGIN=antigravity` `PostToolUse` returns `errored` for non-empty `error`, `thinking` for `error: ""`, and `thinking` when `error` is absent; the shipped tests pass, and the code does not persist any Pre/Post tool correlation state.
- `[held]` Invariant 2: I verified `CODOGOTCHI_ORIGIN=antigravity` `Stop` with `fullyIdle: false` returns `thinking`, while the same PascalCase `Stop` without the env override still takes the generic standby path; the Antigravity guard is ordered correctly ahead of generic stop handling.
- `[held]` Invariant 3: I installed Antigravity hooks into a temp user root, confirmed `hooksStatus().antigravity.installed === true`, confirmed a second install was byte-identical, and confirmed uninstall removed only `codogotchi` while preserving a pre-existing non-codogotchi named hook.

**Surface results**
- `[probed]` `rawHookKind` boundary: Antigravity `PostToolUse` without `toolCall.name` falls through to the neutral path as intended, but a malformed/future `PostToolUse` that includes `toolCall.name` is still promoted to `kind: "tool_use"` and gets a tool-derived `sourceEvent.name`. State stays `thinking`, but the metadata is no longer neutral; this is an actionable gap.
- `[probed]` `resolveAntigravityToolAlias`: `run_command` correctly feeds `toolCall.args.CommandLine` into the Shell classifier (`bun test ...` => `testing`, `grep ...` => `thinking`), and an unknown Antigravity tool falls through to the default `thinking` path.
- `[probed]` Antigravity Stop guard ordering: with `CODOGOTCHI_ORIGIN=antigravity`, `Stop` + `fullyIdle:false` returns `thinking`; without the override, PascalCase `Stop` is treated as Claude Code and returns generic `standby`. The origin gate is using the env override path correctly.
- `[probed]` `isAntigravityInstalled` detection vs `matcher: "*"`: I fed `hooksStatus()` a malformed file with `codogotchi: "bad-string"` and it returned `installed: false` / `partially_installed: false` without throwing. A non-`"*"` matcher would simply read as not installed, not crash.
- `[probed]` `uninstallAntigravityHooks`: I verified uninstall preserves unrelated named hooks and is a safe no-op when `~/.gemini/config/hooks.json` is absent.
- `[probed]` Output stability across schema-version drift: the repo ticket doc describes a plain named-hook map, and the implementation matches that shape. If extra top-level keys such as `version` already exist, install preserves them via object spread; I found no repo-local contract requiring a wrapper or version field on fresh writes.
- `[probed]` CLI flag/arg symmetry: `router.ts` updates the parser, install usage, uninstall usage, and dispatch branches for `antigravity`; I did not find a missing platform branch in the touched CLI paths.
- `[probed]` Error-class breadth in `catch` blocks: `readJsonOrEmpty()` only swallows `ENOENT`; JSON parse errors and other I/O failures rethrow. There is no silent corruption masking in the installer/detector read path.
- `[probed]` Defensive layering at module boundaries: malformed `codogotchi` values such as strings do not throw in `isAntigravityInstalled`; they resolve to `false` cleanly. Arrays would also degrade to `false` because the per-event slot checks fail.
- `[probed]` Cross-file atomicity windows: `installAntigravityHooks()` and `uninstallAntigravityHooks()` back up first, then overwrite `hooks.json` in place with `writeFile`. That leaves a real truncate-then-fail window where the live file can become empty/corrupt even though a backup exists.
- `[probed]` Test-contract strength: the added tests cover classifier happy paths, idempotency, status detection, and uninstall-removes-entries. They do not cover the stdout contract or uninstall preservation of unrelated named hooks.
- `[probed]` Doc-vs-code drift in the ticket Rationale: the implementation matches the rationale on aliasing, PostToolUse neutrality, Stop ordering, and the named-hook `codogotchi` map. The only drift I found is around hook stdout behavior, called out below.

**Actionable findings**
- [packages/cli/bin/codogotchi-hook.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/bin/codogotchi-hook.ts:14) and [packages/cli/src/hook-binary.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hook-binary.ts:828): the hook prints nothing to stdout. Running `bun packages/cli/bin/codogotchi-hook.ts < packages/engine/test/fixtures/hooks/antigravity/pre-tool-use-write.json` produced zero bytes of output, which breaks the ticket’s observational-output contract (`{}` only). This is a correctness gap under finding-discipline clause 2. Fix: emit `"{}\n"` unconditionally from the hook binary after processing, and add an integration test that captures stdout for an Antigravity fixture.
- [packages/cli/src/hook-binary.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hook-binary.ts:255) and [packages/cli/src/hook-binary.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hook-binary.ts:499): Antigravity `PostToolUse` is still tool-classified if a payload contains `toolCall.name`. With `CODOGOTCHI_ORIGIN=antigravity` and `{ hook_event_name: "PostToolUse", toolCall: { name: "write_to_file" } }`, `classifyEvent()` returns `state: "thinking"` but `sourceEvent.kind: "tool_use"` and `sourceEvent.name: "Edit"`, violating the ticket’s “PostToolUse is neutral/error-only, no tool-name correlation” behavior. This is a demonstrated correctness gap under clause 2. Fix: only use `toolCall.name` to promote Antigravity `PreToolUse`, or explicitly force Antigravity `PostToolUse` metadata to a neutral non-tool event before normalization.
- [packages/cli/src/hooks.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hooks.ts:332) and [packages/cli/src/hooks.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hooks.ts:418): install/uninstall overwrite `~/.gemini/config/hooks.json` in place after making a backup. If `writeFile()` truncates and then fails, the live hooks file can be left empty/corrupt and unrelated user hooks disappear until manual recovery from the backup. This is a spec-permits-real-bug under finding-discipline clause 3. Fix: write to a temp file in the same directory and `rename()` atomically, or restore the backup on write failure.

**Advisory Observations**
- [docs/product/delivery/phase-09/ticket-03-antigravity-hooks.md](/Users/cesar/code/codogotchi_p9_03/docs/product/delivery/phase-09/ticket-03-antigravity-hooks.md:17) says the hook may emit `{}` or `decision: "allow"` for `PreToolUse`, while this review brief requires `{}` only, and the current implementation emits nothing. The intended stdout contract should be reconciled in one place.
- [packages/cli/src/hooks.test.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hooks.test.ts:1038) covers install/idempotency/status/uninstall-removes-entries, but there is no Antigravity test asserting that uninstall preserves unrelated named hooks. The code currently does preserve them; the gap is test coverage, not observed behavior.
- [packages/cli/src/hook-binary.test.ts](/Users/cesar/code/codogotchi_p9_03/packages/cli/src/hook-binary.test.ts:727) locks the state outcomes well, but there is no test for malformed/future `PostToolUse` payloads that include `toolCall.name`, which is how the metadata-correlation bug above slips through.

**Runner termination**
runnerStatus: completed
terminatedReason: Finished the targeted adversarial review, including direct code inspection and focused execution probes of the Antigravity classifier and installer paths.
