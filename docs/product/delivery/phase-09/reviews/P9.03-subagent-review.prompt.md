You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P9.03 Antigravity native hooks end-to-end.

Outcome:
- `classifyEvent` correctly classifies Antigravity payloads to `source_origin: antigravity` using `CODOGOTCHI_ORIGIN=antigravity` env override. PascalCase events (`PreToolUse`, `PostToolUse`, `Stop`) with camelCase fields (`toolCall.name`, `toolCall.args.CommandLine`).
- Antigravity tool names map via a dedicated alias table: `run_command`→Shell semantics (test-runner/thinking/implementing split from CommandLine arg), `write_to_file`/`replace_file_content`/`multi_replace_file_content`→`implementing`, `view_file`/`read_url_content`→`reading`, `grep_search`/`find_by_name`/`list_dir`→`thinking`, `browser_.*`→`thinking`.
- `PreToolUse` does all tool classification. `PostToolUse` (which carries only `stepIdx` + `error`, no tool name) maps to `errored` when `error` is non-empty, otherwise neutral `thinking`. No stepIdx Pre/Post correlation.
- `Stop` maps to `standby` when `fullyIdle === true`; when `fullyIdle === false` (background tasks still running) it does NOT assert standby. `terminationReason: "error"` → `errored`.
- Hook output stays observational: emit `{}` only; never `deny`/`ask`, never `injectSteps`.
- `codogotchi hooks install --platform antigravity` writes idempotent config to `~/.gemini/config/hooks.json` (named-hook map with `matcher: "*"` per Antigravity schema) setting `CODOGOTCHI_HOME` + `CODOGOTCHI_ORIGIN=antigravity`; uninstall removes only codogotchi entries; re-install is byte-identical.
- `hooksStatus()` reports the `antigravity` platform with real detection.
- Doc-derived stdin samples committed under `packages/engine/test/fixtures/hooks/antigravity/`.

Rationale notes:
- Red first: `PreToolUse write_to_file classifies as implementing` failed first — `toolCall.name` not read by `rawHookKind`.
- Added `toolCall.name` to `isToolBoundaryHook` condition so Antigravity PreToolUse returns `tool_use` kind.
- Antigravity-specific Stop and PostToolUse handling inserted before the generic `rawEventName === "stop"` check.
- Named-hook map format written to `~/.gemini/config/hooks.json`; idempotent via full replacement of the `codogotchi` key.
- Alternative stepIdx Pre/Post correlation rejected; `PreToolUse` already classifies the tool.

### Files touched

Implementation:
packages/cli/src/hook-binary.ts
packages/cli/src/hooks.ts
packages/cli/src/router.ts

Tests:
packages/cli/src/hook-binary.test.ts
packages/cli/src/hooks.test.ts

Fixtures:
packages/engine/test/fixtures/hooks/antigravity/pre-tool-use-write.json
packages/engine/test/fixtures/hooks/antigravity/pre-tool-use-run-command.json
packages/engine/test/fixtures/hooks/antigravity/post-tool-use-error.json
packages/engine/test/fixtures/hooks/antigravity/stop-fully-idle.json
packages/engine/test/fixtures/hooks/antigravity/stop-background-running.json

### Invariants to hold

1. `classifyEvent` with `CODOGOTCHI_ORIGIN=antigravity` and `hook_event_name: "PostToolUse"` must return `errored` when `error` is a non-empty string, and `thinking` when `error` is empty-string or absent — no per-session tool-state persisted between Pre and Post.
2. `classifyEvent` with `CODOGOTCHI_ORIGIN=antigravity` and `hook_event_name: "Stop"` with `fullyIdle: false` must NOT return `standby`; it must return `thinking`. The Antigravity guard must intercept this before the generic `rawEventName === "stop"` → `standby` path fires.
3. `installAntigravityHooks` must write a `~/.gemini/config/hooks.json` that `isAntigravityInstalled` detects as fully installed for all three events (PreToolUse, PostToolUse, Stop); a second call with identical args must produce a byte-identical file; `uninstallAntigravityHooks` must remove all codogotchi entries and leave any non-codogotchi named hooks intact.

### Attack surfaces to probe

1. `rawHookKind` boundary — adding `input.toolCall?.name` to the `isToolBoundaryHook` condition: does Antigravity PostToolUse (no `toolCall.name`, only `stepIdx` + `error`) correctly fall through to `session_start` kind (neutral path), or does the new condition accidentally fire for PostToolUse payloads that include toolCall.name from a malformed payload?
2. `resolveAntigravityToolAlias` — does the `run_command` case correctly route `CommandLine` from `toolCall.args.CommandLine` into the Shell command classifier, and does an unknown Antigravity tool name (not in the switch, not `browser_*`) fall through to `thinking` via the final `return toolName` path reaching the classifyEvent default?
3. Antigravity Stop guard ordering — the guard `normalized.origin === "antigravity" && rawEventName === "stop"` runs before the generic `rawEventName === "stop"` path. Confirm: does `normalized.origin` use the env-override path (`rawHookOrigin`) so a Claude Code `Stop` payload without `CODOGOTCHI_ORIGIN=antigravity` is still handled by the generic guard, not the Antigravity guard?
4. `isAntigravityInstalled` detection vs `antigravityHookCommand` — the install uses `matcher: "*"` and the detector checks `m.matcher === "*"`. If the Antigravity runtime writes additional matchers (or the matcher field evolves), does the partial-install case fall through gracefully (reports `installed: false` / `partially_installed: false` rather than crashing)?
5. `uninstallAntigravityHooks` — removes the entire `codogotchi` key from the named-hook map. If the file contains other named hooks (user-added), are they preserved? If the file does not exist, is the early-return path safe?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — the `AntigravityHooksFile` format (`{ codogotchi: { PreToolUse: [...] } }`) is new and not versioned. If Antigravity's actual schema has a `version` field or wraps events differently, does the install write a valid file and does `isAntigravityInstalled` read it correctly?
2. **CLI flag/arg symmetry** — `HooksPlatform` type now includes `"antigravity"`, the parser validation allows it, the help strings are updated, and the install/uninstall dispatch branches handle it. Probe: is the uninstall usage string updated to include `antigravity`? Are there any other places in `router.ts` that iterate over `HooksPlatform` values (e.g. exhaustive switch, help text) that are missing `antigravity`?
3. **Error-class breadth in `catch` blocks** — `installAntigravityHooks` throws `Error` when `config.json` is missing (same pattern as the other installers). `readJsonOrEmpty` silently returns `{}` on `ENOENT`. Probe: does `readJsonOrEmpty` rethrow non-ENOENT I/O errors, or does it silently swallow them?
4. **Defensive layering at module boundaries** — `isAntigravityInstalled` reads `file.codogotchi` and checks `typeof codogotchi !== "object"`. Probe: if the on-disk file has `codogotchi` as an array or a string (malformed), does the guard correctly return `false` without throwing?
5. **Cross-file atomicity windows** — `installAntigravityHooks` calls `backupIfExists` (copies old file) then `writeText` (overwrites). Probe: if the write fails mid-way, is the backup the only recovery path? Is there a scenario where the backup is made but the new write fails, leaving the user with an empty file and a backup?
6. **Test-contract strength** — installer tests in `hooks.test.ts` use a temp `CODOGOTCHI_USER_ROOT`. Probe: do the tests cover the idempotency invariant (byte-identical second install), the uninstall-leaves-no-entries invariant, and the `hooksStatus().antigravity.installed` detection path? Do they also verify that non-codogotchi named hooks in the file are preserved on uninstall?
7. **Doc-vs-code drift in the ticket Rationale** — the Rationale says `isAntigravityInstalled` uses `matcher: "*"` and the install builds a named-hook map. Read the Rationale in `docs/product/delivery/phase-09/ticket-03-antigravity-hooks.md` and confirm the implemented behavior matches.

### Diff context

Key logic changes:
- `HookInput` gains `toolCall?: { name?, args?: { CommandLine? } }`, `fullyIdle?: boolean`, `terminationReason?: string`.
- `rawHookKind`: `isToolBoundaryHook(hookName) && (... ?? input.toolCall?.name)` — Antigravity PreToolUse now returns `tool_use` kind when `toolCall.name` is set.
- `resolveAntigravityToolAlias`: switch on `toolName.toLowerCase()` mapping Antigravity names to internal names (Edit/Shell/Read/Grep).
- `normalize`: `resolvedToolName` now includes `input.toolCall?.name`; when `rawOrigin === "antigravity"` calls `resolveAntigravityToolAlias`; command resolution adds `input.toolCall?.args?.CommandLine`.
- `classifyEvent`: Antigravity-specific Stop guard (before generic stop) and PostToolUse guard (after postToolUseFailure checks). Both use `normalized.origin === "antigravity"` to discriminate.
- `hooks.ts`: `GEMINI_HOOKS_REL`, `ANTIGRAVITY_CODOGOTCHI_EVENTS`, `AntigravityHooksFile` types, `installAntigravityHooks` / `uninstallAntigravityHooks`, updated `hooksStatus()`.
- `router.ts`: `HooksPlatform` gains `"antigravity"`, parser updated, install/uninstall dispatch updated.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
