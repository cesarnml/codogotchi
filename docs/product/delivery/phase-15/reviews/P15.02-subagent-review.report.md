I have now read all the relevant files. Here is the full adversarial review report.

---

**Invariant results**

1. **Behavior neutrality** — [held] Probed each listing-path branch against its self-scan counterpart. `readPerPlatformDirectoryImpl` with a supplied listing iterates `listing.entries` with the same `.json`-suffix / `.tmp-` / stale-TTL guards as the old per-file stat loop. `PerPlatformGateReader.readImpl` retains its `.tmp-` check and mtime guard. `newestFile` keeps the suffix filter and nil-mtime compactMap drop. No filter was widened or narrowed. The 712-test suite result is consistent with this.

2. **Missing-mtime semantics per consumer** — [held] `readPerPlatformDirectoryImpl`: `if let mtime = entry.mtime, now.timeIntervalSince(mtime) > staleTTL { continue }` — when `mtime` is `nil`, the `if let` binding fails, the `continue` is not reached, and the slice is still decoded. `PerPlatformGateReader.readImpl`: `guard let mtime = entry.mtime else { continue }` — nil mtime → entry skipped. `newestFile`: `guard let mtime = entry.mtime else { return nil }` inside `compactMap` — nil mtime → entry dropped before sort. All three match the stated per-consumer semantics.

3. **Directory-missing vs unreadable distinctions** — [held on the self-scan path] When `listing == nil`, `readPerPlatformDirectoryImpl` runs `fileExists(isDirectory:)` → `.fileNotFound`, then `scan` → `.malformed` if dir exists but is unreadable. `PerPlatformGateReader.readImpl` and `newestFile` self-scan paths return `[:]` / `nil` on either failure (unchanged). On the poll path (listing supplied) a race condition applies — see Advisory Observations.

**Surface results**

1. **`StateDirectoryListing.scan` — nil iff absent/unreadable, non-nil empty iff empty dir** — [probed] `try? fileManager.contentsOfDirectory(atPath:)` returns nil for both ENOENT and EACCES; the test `testScanMissingDirectoryReturnsNil` confirms the missing-dir branch. An existing empty directory: `contentsOfDirectory` succeeds with `[]`, `names.map` produces `[]`, and the function returns `StateDirectoryListing(entries: [])` — non-nil. `testScanEmptyDirectoryReturnsEmptyListing` exercises this. The `nil`-for-absent / non-nil-empty-for-existing distinction is correct.

2. **Poll path supplies listing only when scan succeeded** — [probed] `sharedListing = previewActive ? nil : StateDirectoryListing.scan(at: pollingTargetPath)`. If scan returns `nil` (directory absent at tick start), `readPerPlatformDirectory` receives `listing: nil` and self-scans, preserving the `fileExists` guard and `.fileNotFound` semantics. Race condition: if the directory exists at scan time (listing is non-nil) but is deleted before `readPerPlatformDirectory` runs, the consumer uses the listing (no `fileExists` re-check) and returns `.success([:])` instead of `.failure(.fileNotFound)`. Behavioral divergence in a race condition — see Advisory Observations.

3. **Stale-TTL guard rewrite** — [probed] Old `readDirectoryImpl`: `if let attrs = try? fm.attributesOfItem(atPath:), let mtime = ..., now - mtime > staleTTL { continue }` — nil mtime (failed stat) → outer/inner `if let` fails → NOT skipped. New `readPerPlatformDirectoryImpl`: `if let mtime = entry.mtime, now.timeIntervalSince(mtime) > staleTTL { continue }` — nil mtime → `if let` fails → NOT skipped. Semantics are identical.

4. **`PerPlatformGateReader.readImpl` — `.tmp-` exclusion, nil-mtime skip, last-writer-wins** — [probed] `.tmp-` check is `guard !name.contains(".tmp-") else { continue }`, executed for every entry before the suffix/mtime checks — same priority as pre-refactor. `guard let mtime = entry.mtime else { continue }` follows, skipping nil-mtime entries as before. Last-writer-wins: `if gates[origin] == nil || mtime > gates[origin]!.mtime` selects the entry with the latest filesystem mtime within each origin, identical to what the per-file stat produced. ✓

5. **`newestFile` — tie-break, suffix match, `.tmp-` safety** — [probed] The listing-path filters with `.filter { $0.name.hasSuffix(suffix) }`, drops nil-mtime entries via `compactMap`, then sorts descending by mtime and returns `.first?.path`. Tie-break is the same sort predicate as the old per-file approach. `.tmp-` intermediates follow the convention `<name>.tmp-<uuid>` (suffix appended), so a file like `origin:session.gate.json.tmp-uuid` does NOT `hasSuffix(".gate.json")` and is excluded by the suffix filter. A hypothetical `origin:session.tmp-x.gate.json` (tmp infix, not convention) would slip past the suffix filter, but this was also true of the old code — no regression. `newestFile` has no explicit `.tmp-` guard (unlike `PerPlatformGateReader`), so both old and new paths are equally exposed to non-standard naming. ✓ no new exposure.

6. **Preview-mode gating** — [probed] `sharedListing = previewActive ? nil : scan(...)` — listing is always nil in preview. `decide` calls `previewOutcome(...)` first; since `previewState != nil || previewGate != nil` is true, it returns early before reaching `newestFile`. The per-platform block is guarded by `if !previewActive` and is skipped. Zero added `state.d/` scans in preview mode. ✓

7. **Shared-listing staleness across the tick** — [probed] The listing is captured once at `runTick` line 220 and consumed by `decide` (two `newestFile` calls) and then by `readPerPlatformDirectory` and `PerPlatformGateReader.read` in the per-platform block. The old multi-scan code had per-site race windows (a new file written between `newestFile` and `readPerPlatformDirectory` would be visible to the second scan but not the first). The new single-capture is more consistent: all consumers see an identical snapshot. A file written after tick start is missed by all consumers uniformly, which is no worse than the old behavior and avoids inter-consumer skew.

8. **Output stability across schema-version drift** — [N/A — `StateDirectoryListing` is purely in-memory; no on-disk artifact or public return type changed shape.]

9. **CLI flag/arg symmetry** — [N/A — no CLI path is involved in this refactor.]

10. **Error-class breadth in `catch` blocks** — [probed] `scan` uses `try?` on `contentsOfDirectory`, swallowing both ENOENT and EACCES as `nil`. In `readPerPlatformDirectoryImpl`'s self-scan branch, `nil` from `scan` maps to `.failure(.malformed)` (after the `fileExists` pre-check passes), the same error class as the old `do { try fm.contentsOfDirectory(...) } catch { return .failure(.malformed) }`. No widening or narrowing.

11. **Defensive layering at module boundaries** — [probed] `StateDirectoryListing` is explicitly documented as an unfiltered raw enumeration. Each consumer independently applies its own suffix, `.tmp-`, mtime, and stale-TTL gates. Verified across all three consumers. No consumer trusts the listing to be pre-filtered.

12. **Cross-file atomicity windows** — [probed] The listing captures names+mtimes once; contents are read per-file via `try? Data(contentsOf:)`. A file present at enumeration but deleted before content read → `try?` returns nil → the entry's `guard let data = ...` or `try? decoder.decode` fails → entry is silently skipped. Same degradation as the old per-file `try? Data` reads. No new partial-read exposure.

13. **Test-contract strength** — [probed] `StateDirectoryListingTests` covers the three structurally critical branches of `scan`: missing directory → nil, empty directory → non-nil empty, present directory → entries with mtimes. Gap: no test injects a non-nil `listing:` into `readPerPlatformDirectory`, `PerPlatformGateReader.read`, or `newestFile`. The listing-branch code in each consumer is exercised only in production (poll path), not in the test suite. See Advisory Observations.

14. **Doc-vs-code drift in the ticket Rationale** — [probed] `SlicePruner.prune` is confirmed to be called only from `SlicePruneScheduler` (30-min interval timer); its `contentsOfDirectory` at `SlicePruner.swift:28` is not on the 1 Hz poll path. The injected primary `reader` seam (`StateJsonReader.readDirectory(at:)` → `readDirectoryImpl`) retains its own `contentsOfDirectory` at `StateJsonReader.swift:171` and is not threaded through the shared listing, consistent with the Rationale's stated intent. One minor imprecision found — see Advisory Observations.

**Actionable findings**

None.

**Advisory Observations**

- **Race-condition behavioral divergence (attack surface #2 / #7).** If `state.d/` exists at tick start (scan returns non-nil listing) but is deleted before `readPerPlatformDirectory` runs, the listing branch returns `.success([:])` rather than `.failure(.fileNotFound)`. The consequence is that `applyPerPlatform` IS called with an empty `PerPlatformSnapshot` (clearing the floating-window pool) instead of being suppressed (pool retains last state). This is a 1-tick flicker in an extreme edge case (directory deleted during a running tick), and the old multi-scan code had its own analogous race; neither path guarantees atomicity. No patch is warranted under the invariants as stated, but the behavior divergence is real and worth noting if teardown/reset scenarios are ever tested.

- **No test exercises the listing-injection branch of any consumer.** `StateDirectoryListingTests` proves `scan` is correct, and the existing suite covers the self-scan path. But if the listing-branch code in `readPerPlatformDirectoryImpl`, `PerPlatformGateReader.readImpl`, or `newestFile` had a filter inversion or wrong condition, the test suite would not catch it. The ticket's "Red: skip" stance accepts this; a follow-up test that constructs a mock listing and passes it to each consumer would close the gap for P15.03 (which extends the listing seam further).

- **Rationale doc minor imprecision.** The Rationale states "a listing-aware `readDirectory` overload already exists structurally but is intentionally not wired through the injected seam this ticket." No `readDirectory(at:listing:)` overload exists in `StateJsonReader.swift` — only `readPerPlatformDirectory` received listing overloads. The sentence likely intends "the two-overload pattern demonstrated by `readPerPlatformDirectory` could be applied," but as written it implies an extant overload. No code change needed; doc-only imprecision.

**Runner termination**
`runnerStatus`: completed
`terminatedReason`: All invariants held and all attack surfaces were probed to conclusion with no actionable findings; review completed normally.
