You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P15.02 Refactor: consolidate state.d directory scans (Swift menubar, `Red: skip` behavior-neutral refactor).

Outcome:
- The several per-tick scans of `state.d/` on the poll path are backed by a single shared directory listing produced once per tick.
- Behavior is unchanged: the per-origin state map, gate badges, and newest gate/context resolution are identical to before for the same on-disk contents.
- The consolidation leaves a single seam (`StateDirectoryListing`) that P15.03 extends to per-session granularity.

Key rationale / decisions:
- A new `StateDirectoryListing` value (`scan(at:)` → one `contentsOfDirectory` + per-child mtime) is produced once per non-preview tick in `LivePollingDriver.runTick` and threaded as an OPTIONAL parameter (default `nil` → self-scan, unchanged) into `StateJsonReader.readPerPlatformDirectory`, `PerPlatformGateReader.read`, and `LivePollingDriver.newestFile`.
- The injected primary `reader` seam (`StateJsonReader.readDirectory`, the global-winner status-item read) is intentionally NOT folded in — its `(String) -> Result` injected closure is relied on by tests, and it aggregates origin-less slices the per-origin read drops. Production goes from 5 enumerations/tick to 2.
- `SlicePruner` is NOT on the poll path (separate `SlicePruneScheduler`, 30-min timer, off-thread), so it is deliberately excluded — the ticket's "pruner/listing pass on the poll path" premise was inaccurate.
- Two new files required regenerating `Codogotchi.xcodeproj` via `xcodegen generate`.

### Files touched

Implementation:
apps/menubar/Sources/StateDirectoryListing.swift (new)
apps/menubar/Sources/StateJsonReader.swift
apps/menubar/Sources/GateJsonReader.swift
apps/menubar/Sources/LivePollingDriver.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj (regenerated)

Tests:
apps/menubar/Tests/MenubarTests/StateDirectoryListingTests.swift (new)

(Also: docs/product/delivery/phase-15/ticket-02-consolidate-state-scans.md Rationale — delivery doc, do not patch.)

### Invariants to hold

1. Behavior neutrality: for identical `state.d/` contents, `readPerPlatformDirectory`, `PerPlatformGateReader.read`, and `newestFile` return byte-identical results whether called with a shared `StateDirectoryListing` (poll path) or with `listing == nil` (self-scan / tests). The full pre-existing suite must pass unchanged.
2. Missing-mtime semantics are preserved PER CONSUMER: the stale-TTL reader (`readPerPlatformDirectory`) treats a `nil` mtime as "not stale" (still decodes the slice); the gate reader and `newestFile` SKIP an entry whose mtime is `nil`. The refactor must not flip any of these.
3. Directory-missing vs unreadable distinctions are preserved: `readPerPlatformDirectory` still returns `.failure(.fileNotFound)` for an absent directory and `.failure(.malformed)` for an unreadable one; `PerPlatformGateReader.read` and `newestFile` still yield empty/nil on failure.

### Attack surfaces to probe

1. `StateDirectoryListing.scan` — does it return `nil` ONLY for absent/unreadable directories and a non-nil empty listing for an existing empty directory? A `nil` for an empty dir would flip `readPerPlatformDirectory` from `.success([:])` to `.failure(.malformed)` on the self-scan path.
2. `readPerPlatformDirectoryImpl` listing branch — when a listing is supplied, the `fileExists`/`isDir` guard is skipped. Confirm the poll path only ever supplies a listing when the directory actually existed (i.e. `scan` succeeded), so `.fileNotFound` semantics are not silently lost when the dir vanishes mid-tick.
3. Stale-TTL guard rewrite — old code `if let attrs, let mtime, now - mtime > ttl { continue }` (nil mtime ⇒ NOT skipped) vs new `if let mtime = entry.mtime, now - mtime > ttl { continue }`. Confirm the nil-mtime slice is still decoded, not dropped.
4. `PerPlatformGateReader.readImpl` — old code skipped `.tmp-` names BEFORE stat and required mtime (`else { continue }`). New code stats all names via `scan` then filters. Confirm `.tmp-` files are still excluded and nil-mtime entries still skipped, and that a gate/context file with the same origin still resolves last-writer-wins by mtime.
5. `newestFile` — old code `contentsOfDirectory` nil ⇒ nil; filtered by suffix; dropped nil-mtime; sorted desc by mtime; first. Confirm the new listing-based path preserves the exact tie-break and the suffix match (`.gate.json` must not also match a hypothetical `x.gate.json.tmp-...`, which is excluded upstream only in some readers — check).
6. `runTick` preview-mode gating — the shared listing is `nil` when `previewActive`. Confirm preview mode still issues ZERO `state.d/` scans (no added scan) and that `decide`'s `newestFile` is never reached in preview (early return), so a nil listing there is harmless.
7. Shared-listing staleness across the tick — the listing is captured once at tick start and consumed by `decide` (gate/context) and the later per-platform block. Confirm nothing mutates `state.d/` between those two consumers within the same tick in a way the old multi-scan code would have observed differently (i.e. is single-capture actually equivalent, or could the old code have raced and seen a newer file?).

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — no persisted format changes; `StateDirectoryListing` is in-memory only. Confirm no on-disk artifact or public return type changed shape. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
2. **CLI flag/arg symmetry** — N/A (no CLI). Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
3. **Error-class breadth in `catch` blocks** — the readers use `try?`. Confirm the refactor did not widen or narrow which failures fall to the empty/failure branch (e.g. `scan` swallowing a permission error that old code surfaced as `.malformed`). Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
4. **Defensive layering at module boundaries** — `StateDirectoryListing` is a new cross-module input to three readers. Confirm each reader still revalidates suffix/tmp/mtime rather than trusting the listing to be pre-filtered (the listing is deliberately unfiltered raw enumeration). Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
5. **Cross-file atomicity windows** — the listing captures names+mtimes once, then contents are read per-file later in the tick. Confirm no new partial-read window: a file present at enumeration but deleted before content read must degrade the same way old code did (per-file `try?` ⇒ skip). Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
6. **Test-contract strength** — do `StateDirectoryListingTests` cover the three branches that matter (missing ⇒ nil, empty ⇒ non-nil empty, present ⇒ entries+mtime)? Is there a gap where a consumer's listing-vs-selfscan equivalence is unproven by any test (the existing suite only exercises the self-scan path, since no test injects a listing)? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
7. **Doc-vs-code drift in the ticket Rationale** — read the ticket Rationale + Outcome. It claims the pruner is off-path and the primary reader is intentionally left. Confirm the code matches those claims (e.g. `SlicePruner` really is only called from `SlicePruneScheduler`, and `runTick` really does still call the injected `reader` separately). Surface any drift in **Advisory Observations**. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

### Diff context

`StateDirectoryListing.scan(at:)` does one `contentsOfDirectory` then maps each name to `(name, mtime?)`. `LivePollingDriver.runTick` computes `sharedListing = previewActive ? nil : scan(...)` once and passes it to `decide(...)` (→ two `newestFile` calls) and to `readPerPlatformDirectory` + `PerPlatformGateReader.read`. Each reader gained an optional `listing:` param defaulting to `nil`; when nil they self-scan through the same `StateDirectoryListing.scan` (or their original guards) so direct callers/tests are unchanged. The stale-TTL, `.tmp-`, suffix, and last-writer-wins filters remain inside each consumer. The injected primary `reader` and the off-thread `SlicePruner` are untouched. Full menubar suite: 712 tests, 0 failures.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift, surface it under **Advisory Observations**
with the specific file, the conflicting claim, and what the diff actually does. The
primary agent decides whether to patch docs or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- Do not use `---` horizontal rules anywhere in the report.
- `Runner termination` must be the section heading, written as the bold span
  `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below.
- Inside `Advisory Observations`, write one observation per bullet or one
  observation per paragraph. Do NOT use a bold span on a line by itself before
  the observation body.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
