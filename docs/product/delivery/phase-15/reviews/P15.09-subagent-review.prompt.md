You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P15.09 Settings > Platform Settings UI (menubar app, macOS/AppKit, Swift)

## Outcome

- The Settings > Customization section titled "Platform Display Mode" is renamed **"Platform Settings"**.
- The section presents columns **Platform, Mode, Enable Session Pets, Session Cap**.
- The **Enable Session Pets** checkbox is interactive only when the platform's Mode ∈ {Own, Minimalist}; it is disabled (greyed) for Combined/Off.
- Checking Enable Session Pets exposes a **Session Cap** dropdown with options **`2`–`10` and `Unlimited`** (no `1`), defaulting to **`3`** on first enable. One cap per platform, shared across Own and Minimalist.
- `CustomizationTabViewModel` gains `setSessionPetsEnabled(_:for:)` and `setSessionCap(_:for:)`, persisting via the existing read-merge-write so unmanaged keys are never clobbered; `0` is written for Unlimited.

## Rationale (as recorded in the ticket doc)

Red first: `CustomizationTabViewModelTests` failed to compile — `setSessionPetsEnabled`, `setSessionCap`, and `effectiveSessionCap` did not exist on `CustomizationTabViewModel` yet.
Why this path: reused the existing `setMode`/`setTTL` read-merge-write pattern for the two new VM methods, and added `effectiveSessionCap(for:)` as a pure read-time resolver (no disk write) so "defaults to 3 on first enable" is a UI display concern, not a surprise persisted write. Renamed the "Platform Display Mode" card to "Platform Settings" and widened it to full width (stacking "Minimalist Panel Options" below it instead of beside it) because four columns did not fit in the previous half-window card width.
Alternative considered: auto-persisting `session_cap = 3` the moment the checkbox is first checked. Rejected — the ticket's Red test (3) says the default resolves "at the read point," and auto-writing on enable would silently create a `session_cap` entry the user never explicitly chose.
Deferred: no visual/screenshot verification was performed — this is a native AppKit UI with no browser-preview path available in this environment; verification relied on `bun run ci:quiet` (785 Swift tests, including the four new red→green VM tests) plus a manual diff self-audit.
Contract note: extended the `CustomizationSnapshot` sentinel (`defaultSessionCap = 3`, `unlimitedSessionCap = 0`) to `FloatingPetWindowPool`'s three previously-inline `?? 3` / `== 0` call sites per the ticket's Refactor note ("single source of truth for the sentinel") — same values, no behavior change.

### Files touched

Implementation:
apps/menubar/Sources/CustomizationJsonReader.swift
apps/menubar/Sources/CustomizationTabViewModel.swift
apps/menubar/Sources/FloatingPetWindowPool.swift
apps/menubar/Sources/SettingsWindowController.swift

Tests:
apps/menubar/Tests/MenubarTests/CustomizationTabViewModelTests.swift

Ticket doc (Rationale append only):
docs/product/delivery/phase-15/ticket-09-settings-platform-settings-ui.md

### Invariants to hold

1. `setSessionPetsEnabled(_:for:)` and `setSessionCap(_:for:)` must each persist only their own top-level JSON key (`session_pets_enabled` / `session_cap`) via `ConfigFileWriter.merge`, never clobbering `platform_modes`, `idle_dismiss_ttl_seconds`, `minimalist_badge_scale`, `menubar_icon_monochrome`, or the sibling session-pets key.
2. `effectiveSessionCap(for:)` must be a pure read: it returns the persisted `sessionCap[origin]` value, or `CustomizationSnapshot.defaultSessionCap` (3) when absent, and must never call `ConfigFileWriter.merge` or otherwise write to disk as a side effect of being called.
3. Toggling a platform's Mode to Combined or Off must disable (not clear or hide) that platform's Enable Session Pets checkbox and Session Cap dropdown, and must not erase a previously persisted `session_pets_enabled[origin]` or `session_cap[origin]` value — toggling back to Own/Minimalist must restore the same prior checkbox/dropdown state.

### Attack surfaces to probe

1. `CustomizationTabViewModel.setSessionPetsEnabled`/`setSessionCap` (CustomizationTabViewModel.swift): each builds `proposed` from the current in-memory map, sets one key, and merges only `["session_pets_enabled": proposed]` or `["session_cap": proposed]`. Confirm the two writers can never observe or persist each other's stale in-memory copy (e.g. calling setSessionPetsEnabled then immediately setSessionCap for the same origin) and that a `ConfigFileWriter.merge` failure (thrown error, caught and logged) leaves the in-memory `sessionPetsEnabled`/`sessionCap` properties unchanged (no partial update).
2. `SettingsWindowController.modePickerChanged` (SettingsWindowController.swift): after `viewModel.setMode(...)`, it reads `sessionPetsCheckboxes[origin]`/`sessionCapPickers[origin]` from local dictionaries and flips `isEnabled` only — verify this can never race with `sessionPetsCheckboxChanged`/`sessionCapPickerChanged` in a way that leaves the cap dropdown enabled while its checkbox is off, or enabled while mode is Combined/Off.
3. `sessionCapPickerChanged`/`sessionPetsCheckboxChanged` — `sender.identifier?.rawValue` origin lookup: since `NSUserInterfaceItemIdentifier` is set from `CustomizationTabViewModel.origins` at row-build time, confirm there is no path (e.g. AppKit control reuse, programmatic `selectItem` calls elsewhere) where a stale/wrong identifier could route a write to the wrong origin.
4. `SessionCapOption` (CustomizationTabViewModel.swift) — `enum SessionCapOption: Int, CaseIterable { case two = 2, ... case unlimited = 0 }`: confirm `CaseIterable`'s synthesized `allCases` produces the 9 numeric cases in ascending declaration order followed by `.unlimited`, matches the dropdown's intended `2–10, Unlimited` display order, and that no rawValue collides (particularly `.unlimited = 0` against the numeric range 2–10).
5. Initial dropdown selection fallback: `let currentCapOption = SessionCapOption.matching(effectiveCap) ?? .three` in `setupViews`. `CustomizationJsonReaderTests` (pre-existing) proves the reader accepts negative and otherwise out-of-enum `session_cap` values (e.g. `-1`) without clamping. If a platform has a persisted cap that doesn't match any `SessionCapOption` case, the dropdown silently displays "3" without correcting the persisted value or surfacing a mismatch — confirm this is safe (doesn't cause a later `setSessionCap` write to silently downgrade an out-of-range but intentionally-set value) and consider whether it should be flagged.
6. `FloatingPetWindowPool.isUnlimited(origin:)` refactor (FloatingPetWindowPool.swift lines ~309, ~608-626): confirms this is a behavior-preserving extraction of the previous inline `(currentCustomization.sessionCap[origin] ?? 3) == 0` expression into a helper using `CustomizationSnapshot.defaultSessionCap`/`unlimitedSessionCap`. Confirm no call site was missed and no semantics changed (same default-3, same zero-sentinel).

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [probe] Does the new `session_pets_enabled`/`session_cap` write shape stay compatible with `CustomizationJsonReader`'s existing snake_case decode (`session_pets_enabled`, `session_cap`) added in P15.01? Confirm the VM writer and reader use exactly matching key names and value shapes.
2. **CLI flag/arg symmetry** — N/A, no CLI flags touched in this ticket.
3. **Error-class breadth in `catch` blocks** — [probe] `setSessionPetsEnabled`/`setSessionCap` each catch `Error` generically and only `NSLog`. Confirm this matches the existing `setMode`/`setTTL`/`setCombinedMinimalistEnabled` pattern (no ticket-specific regression) and that a write failure doesn't leave the UI checkbox/dropdown showing a state that was never actually persisted.
4. **Defensive layering at module boundaries** — [probe] `SessionCapOption(rawValue:)` / `.matching(_:)` at the UI→VM boundary: `sessionCapPickerChanged` reads `sender.selectedItem?.representedObject as? SessionCapOption` — confirm this cast can never silently fail and skip a write when a user actually made a selection.
5. **Cross-file atomicity windows** — [probe] `setSessionPetsEnabled` and a later `setSessionCap` for the same origin are two separate `ConfigFileWriter.merge` calls (two separate file writes). If the app crashes between them, is the on-disk state still valid (each write is independently atomic per `ConfigFileWriter.merge`'s `.atomic` write), even though the two logical operations are not transactional together?
6. **Test-contract strength** — [probe] Review `CustomizationTabViewModelTests.swift` — do the four tests assert the persisted JSON shape (not just VM property state), and does `testTogglingModeToCombinedDoesNotEraseStoredCap` actually exercise invariant 3 end-to-end (mode change after cap is set)? Is there a gap, e.g. no test for disabling-then-re-enabling Enable Session Pets, or for the UI-level `isEnabled` wiring in `SettingsWindowController` (which has no unit test coverage — this ticket's Red section only covers the VM layer)?
7. **Doc-vs-code drift in the ticket Rationale** — [probe] Read `docs/product/delivery/phase-15/ticket-09-settings-platform-settings-ui.md` in full (Outcome, Red, Green, Refactor, Review Focus, Rationale) against the diff. Confirm the Rationale's claims match what the diff actually does, and check whether "Session Cap dropdown ... defaulting to 3 on first enable" is fully satisfied by a read-time-only default (no persisted write) or whether the ticket intended an actual persisted default-3 write on first enable that this implementation does not perform.

### Diff context

See the attached diff hunks (or read `git diff 38b2a7b0..HEAD -- apps/menubar/Sources apps/menubar/Tests/MenubarTests/CustomizationTabViewModelTests.swift` in the worktree at /Users/cesar/code/codogotchi_p15_09). Summary of the key logic changes:

1. `CustomizationJsonReader.swift`: added `PlatformMode.supportsSessionPets` (true for `.own`/`.minimalist`) and two `CustomizationSnapshot` static sentinels (`defaultSessionCap = 3`, `unlimitedSessionCap = 0`).
2. `CustomizationTabViewModel.swift`: added `SessionCapOption` enum (2–10 + Unlimited=0), two new stored properties (`sessionPetsEnabled`, `sessionCap`) initialized from the reader snapshot, and three new methods: `effectiveSessionCap(for:)` (pure read, defaults to 3), `setSessionPetsEnabled(_:for:)` and `setSessionCap(_:for:)` (both read-merge-write via `ConfigFileWriter.merge`, mirroring `setMode`).
3. `FloatingPetWindowPool.swift`: three previously-inline `?? 3` / `== 0` sentinel literals replaced with references to the new `CustomizationSnapshot` constants; the two `== 0` sites were further extracted into a new private `isUnlimited(origin:)` helper.
4. `SettingsWindowController.swift`: renamed "Platform Display Mode" → "Platform Settings", restructured the Customization tab from a two-column (Platform Settings | Minimalist Panel Options) layout to a full-width-stacked layout (Platform Settings full width, then Minimalist Panel Options full width, then Idle Dismiss) to fit two new per-row controls (Enable Session Pets checkbox, Session Cap dropdown) plus a new column-header row. Added `sessionPetsCheckboxChanged`/`sessionCapPickerChanged` action handlers and extended `modePickerChanged` to toggle the new controls' `isEnabled` state (never their persisted value) on mode change.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
