I have all the information needed. Here is the adversarial review report.

---

**Invariant results**

**Invariant 1** (session-pets off → byte-identical behavior): held. `resolveRenderKeys` collapses all sessions to the plain origin before the pool sees the snapshot; the pool's key-space is unchanged from pre-P15.03. Every pre-existing `FloatingPetWindowPoolTests` case covers this path and serves as the full regression net.

**Invariant 2** (pet identity and platform mode resolve from owning origin, not composite key): held. Step 7 (line 242) derives `let origin = Self.origin(forWindowKey: renderKey)` once and routes both `currentAssignments.resolve(origin:)` and `mode(for:)` through it. The minimalist platform chip uses `origin` directly; the own-mode chip uses `state.sourceEvent?.origin`, which carries the platform origin from the hook writer — not the session key. If `sourceEvent` is nil and mode is own, `applyPlatform` is silently skipped; this was pre-existing behavior and the new test covers it with a non-nil `sourceEvent`.

**Invariant 3** (`replacePet` fans out to all session windows of the origin, no spurious matches): held. The filter `key == foldedKey || Self.origin(forWindowKey: key) == origin` is sound. For a combined-mode origin, `foldedKey = "combined"` and `origin(forWindowKey: "combined") = "combined"` ≠ any platform name, so only the shared combined window matches. For session keys of a different origin ("cursor:s1"), `origin(forWindowKey: "cursor:s1") = "cursor"` ≠ the target origin. Full equality on the extracted prefix prevents prefix-collision false positives.

---

**Surface results**

**Surface 1 — `windowKey(for:)` double-folding / last-active identity loss:** probed. The literal "combined" short-circuits on the first clause without evaluating `mode(forWindowKey:)` — no double-fold. Unfolded combined-mode origins ("claude_code") reach the second clause and fold correctly. Session keys of a combined origin ("claude_code:s1") resolve through `mode(forWindowKey:)` → `mode(for: "claude_code")` → `.combined` → "combined". Last-active immunity: `lastActiveWindowKey = lastActiveRenderKey.map { windowKey(for: $0) }` maps any shape to the same window key, so immunity is consistent across both input shapes. Clean.

**Surface 2 — TTL isolation between session keys sharing one origin:** probed. `lastSeenAt` is keyed by render key, and `lastSeenForWindow(key:)` returns `lastSeenAt[key]` directly for non-combined keys — no origin-level aggregation leaks into sibling sessions. Test (3) (`testIdleSessionAgesOutWhileActiveSiblingSessionSurvives`) exercises the exact scenario. Clean.

**Surface 3 — Step 6b mode-transition teardown with multiple session keys:** probed. `directKeys = visibleEntries.keys.filter { windowKey(for: $0) != "combined" }`. The loop in Step 6b iterates all `directKeys`, so both "codex:s1" and "codex:s2" are checked when codex transitions own→minimalist. For each, `windowSpawnedModes[renderKey]` holds the old mode; `mode(forWindowKey: renderKey)` returns the new mode; the teardown fires for both. Edge case: a session key within TTL but absent from the current snapshot is not in `directKeys` and misses 6b on that tick. On the next tick it either times out (Step 5b) or reappears and 6b catches the stale `windowSpawnedModes` entry. This is a maximum one-tick delay, not a permanent skip. Test (5) covers the nominal fan-out path. Clean.

**Surface 4 — `replacePet` filter spurious match for combined-mode origin:** probed. Analyzed under Invariant 3 above. `origin(forWindowKey: "combined") = "combined"`, which equals no real platform name; the second predicate cannot spuriously match the combined window from an unrelated `replacePet(origin:)` call. Clean.

**Surface 5 — `resolveWindowOrigins` correctness and the deferred Force Idle targeting:** probed. `resolveWindowOrigins(windowKey: "claude_code:s1")` returns `["claude_code"]` — the owning origin. `StateJsonWriter.forceIdle(at:origins:["claude_code"])` resets the state for the owning origin's winner slice, which may be a sibling session's state. The ticket explicitly defers per-session-precise writer scoping to P15.07. This is safe in the sense that state.d slices are ephemeral and regenerated by the next hook event from the sibling — no persistent data loss. Flagged below in Advisory Observations under the spec-deferral clause.

**Surface 6 — `reloadActivePet()` race between fold and `MainActor.run` reinsert:** probed. The fold (`origins = Set(activeOrigins.map { origin(forWindowKey:) })`) and the `replacePet` calls both run on the `MainActor`, separated by a `Task.detached` phase. A session that ages out via TTL between the two `MainActor` hops causes `replacePet` to iterate `windows.keys` and find no matching entry — a no-op, not a crash or misroute. A new session spawned after the fold picks up the current assignment from the factory (which reads `currentAssignments` on each tick), so it renders the up-to-date pet independently. Neither race path is introduced by P15.04; both existed pre-phase. Clean.

**Diff-derived Surface 1 — Output stability across schema-version drift:** N/A — no persisted artifact, CLI output, or on-disk JSON format changes in this diff; `PerPlatformSnapshot.renderKeyIdentities` was added in P15.03 with a defaulted empty value.

**Diff-derived Surface 2 — CLI flag/arg symmetry:** N/A — this diff touches Swift menubar app code only, no CLI flags.

**Diff-derived Surface 3 — Error-class breadth in catch blocks:** N/A — no new or modified catch blocks in this diff; failure paths use `guard`/`NSLog` fallback, unchanged from prior phases.

**Diff-derived Surface 4 — Defensive layering at module boundaries (render key shape validation):** probed. `origin(forWindowKey:)` in both the pool and `splitSessionKey` in `RenderKeyResolver` split on the first colon — a two-colon key like "claude_code:s1:extra" yields "claude_code" correctly. An empty-string key or leading-colon key (":s1") yields an empty-string origin, which `mode(for:)` resolves to `.own` via the nil-coalescing default. This creates a window keyed by "" or ":s1" — degenerate but harmless. These shapes cannot arise from `resolveRenderKeys` (which only emits keys from reader-controlled per-session maps), so this is not a reachable production path. Clean.

**Diff-derived Surface 5 — Cross-file atomicity in `reloadActivePet()`:** probed. Covered by Surface 6 analysis above. Clean.

**Diff-derived Surface 6 — Test-contract strength for the six new P15.04 tests:** probed. All six assert structural key sets or call counts, not just booleans. Test (1) checks `Set(pool.activeOrigins)`, individual pet-id values per key, and platform chips. Test (3) asserts the surviving key by exact value. Test (5) checks both factory call-set equality and identity of the unaffected cursor stub (`===`). Test (6) checks `replacePetsCallCount` on each stub. The session-pets-on and session-pets-off branches for the same origin shape are covered by tests (1) and (2) respectively. One asymmetry: test (3) places the idle session with the earlier `updated_at` (making it non-last-active). The symmetric case — idle session with the later timestamp, which would be last-active and TTL-immune — is not explicitly tested for the per-session path. The invariant is indirectly covered by `testLastActiveWindowNeverDismissedRegardlessOfTTL` (single-window) since the code path is identical. This is a coverage gap, not a correctness gap.

**Diff-derived Surface 7 — Doc-vs-code drift in the ticket Rationale:** probed. Contract note: "gate-badge lookup on the combined path moved from the winning origin to the winning entry's render key." Code at Step 8: `snapshot.gateBadges[winnerEntry.key]` where `winnerEntry.key` is the winning combined entry's render key ("combined" for pre-folded input, the plain origin for unfolded test input). For unfolded input, `key == origin`, so `gateBadges[origin]` — byte-identical to pre-phase behavior as claimed. For pre-folded input, `key == "combined"`, matching the driver's badge keying scheme. Code matches the claim exactly. Deferred note: "winner-only writers still target the owning origin's freshest slice." Code in `resolveWindowOrigins` returns `[FloatingPetWindowPool.origin(forWindowKey: windowKey)]` for non-combined keys — confirmed above. No doc-vs-code drift found.

---

**Actionable findings**

None.

---

**Advisory Observations**

**A1 — Spec-permitted transient confusion: Force Idle from a session window resets sibling sessions' in-progress state.** When a user right-clicks a session window (e.g., "claude_code:s1") and selects Force Idle, `resolveWindowOrigins` returns `["claude_code"]`, and `StateJsonWriter.forceIdle` rewrites the owning origin's state.d slice. If a sibling session "claude_code:s2" is currently the winner (most-recent `updated_at`), its `.implementing` state is reset to idle for up to one polling period. The next hook event from s2 regenerates its state, so there is no persistent data loss. The ticket explicitly defers per-session-precise writer scoping to P15.07. This observation names the clause: the spec-deferral clause (Spec-permits-real-bug) permits the behavior, but the user experience is a visible one-tick animation reset on an unrelated active session. No patch needed before P15.07, but the P15.07 ticket scope should confirm it closes this gap.

**A2 — Test coverage asymmetry for per-session last-active immunity.** Test (3) (`testIdleSessionAgesOutWhileActiveSiblingSessionSurvives`) covers the case where the idle session has the earlier `updated_at` and is correctly dismissed. The symmetric case — idle session with the later `updated_at`, which would be elected `lastActiveRenderKey` and receive TTL immunity — is not covered by any new test. The pool code path for this is identical to the single-window `testLastActiveWindowNeverDismissedRegardlessOfTTL` case, so the invariant holds by shared code, but an explicit per-session test would make the guarantee visible and catch any future regression that adds a session-specific code branch to last-active election.

---

**Runner termination**
runnerStatus: completed
terminatedReason: All invariants and attack surfaces probed to completion with no actionable findings.
