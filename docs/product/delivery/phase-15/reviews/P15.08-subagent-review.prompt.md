You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

## Outcome

- When P15.07 emits the "all-remaining-active, newcomer blocked" signal for a platform, a **dismissable notification bubble** appears on that platform's **longest-lived active session's panel** (the only surface with a render presence).
- The bubble is **rate-limited to one per platform per hour** while the conflict persists — a re-blocked attempt within the window does not re-fire. Rate-limit state is in-memory (resets on relaunch; a post-relaunch re-fire is harmless).
- **Left-clicking** the bubble deep-links to **Settings > Customization** (the Platform Settings surface) so the user can raise the cap.
- Dismissing the bubble hides it without changing the cap; the blocked session remains pending and is promoted by P15.07 the instant a slot frees.

## Rationale (as committed)

Red first: rate-limiter tests (`ConflictBubbleRateLimiterTests`) and target-selector tests (`ConflictBubbleTargetSelectorTests`) failed to compile — `ConflictBubbleRateLimiter` / `ConflictBubbleTargetSelector` did not exist.
Why this path: two pure, independently-testable types (`ConflictBubbleRateLimiter.shouldShow(origin:now:)`/`recordShown`, `ConflictBubbleTargetSelector.longestLivedKey(firstSeenAt:)`) consumed by `FloatingPetWindowPool` in Step 6c, right where `blockedOrigins` is already computed (P15.07). The pool fires at most once per origin per rate-limit window and only clears the bubble (via a new `applyConflictBubble(_:)` protocol method, default no-op) when the origin leaves `blockedOrigins` — it never re-fires just because `blocked` is still true on a later tick. `firstSeenAt` is a new pool dictionary mirroring `lastSeenAt`'s lifecycle (set once, never explicitly cleared) so the target selector always has a stable "longest-lived" candidate.
Alternative considered: forking a brand-new bubble panel type for the conflict notice. Rejected — the ticket calls for reusing the attention-bubble primitive; a `reasonKind == "session_conflict"` branch plus a `configureConflict(origin:)` entry point and an `onOpenSettings` callback was the smallest addition. Each window controller owns a *second*, independent `AttentionBubblePanel` instance for the conflict notice so it never contends with a real per-session attention payload on the same window.
Deferred: retargeting the bubble mid-episode if the longest-lived session's window is torn down while the rate limiter is still within its lockout window — the bubble simply disappears with that window until the next allowed fire.

### Files touched

Implementation:
apps/menubar/Sources/ConflictBubbleRateLimiter.swift
apps/menubar/Sources/ConflictBubbleTargetSelector.swift
apps/menubar/Sources/ConflictBubblePayload.swift
apps/menubar/Sources/FloatingPetWindowPool.swift
apps/menubar/Sources/FloatingPetController.swift
apps/menubar/Sources/AttentionBubblePanel.swift
apps/menubar/Sources/FloatingPetPanel.swift
apps/menubar/Sources/MenubarApp.swift

Tests:
apps/menubar/Tests/MenubarTests/ConflictBubbleRateLimiterTests.swift
apps/menubar/Tests/MenubarTests/ConflictBubbleTargetSelectorTests.swift

### Invariants to hold

1. `ConflictBubbleRateLimiter.shouldShow(origin:now:)` must return `false` for `origin` whenever `now` is within one hour (strictly, `timeIntervalSince(last) <= 3600`) of the last `recordShown(origin:now:)` call for that same `origin`, and must be unaffected by `recordShown` calls for any other origin.
2. `FloatingPetWindowPool.update(snapshot:)` must call `applyConflictBubble(nil)` on a window that was previously showing a conflict bubble for an origin, in the same tick that origin is no longer present in the freshly computed `blockedOrigins` set — the bubble must never be left dangling on a resolved conflict.
3. `FloatingPetWindowPool.update(snapshot:)` must never call `applyConflictBubble` with a non-nil payload on a window whose origin is not currently in `blockedOrigins`, and must never fire two conflict-bubble presentations for the same `origin` inside `conflictBubbleRateLimiter`'s one-hour window even across many consecutive ticks.

### Attack surfaces to probe

1. `FloatingPetWindowPool.update` Step 6c fire/clear ordering: `activeConflictBubbleTargets` is mutated inside the same `for (origin, keys) in sessionKeyedByOrigin` loop that computes `computedBlockedOrigins`, and the clear-loop runs immediately after using the now-final `computedBlockedOrigins`. Verify the clear-loop cannot run against a stale/partial `computedBlockedOrigins` (e.g., iteration order of `sessionKeyedByOrigin`'s `Dictionary` view) and that an origin can't be both fired-on and cleared in the same tick.
2. `ConflictBubbleTargetSelector.longestLivedKey` candidate filtering: in the pool, `candidates = firstSeenAt.filter { currentlyRendered.contains($0.key) }` — check whether `firstSeenAt` can contain a stale entry for a window key that was torn down and later reused by an unrelated origin/session (composite `origin:session_id` keys should be unique enough, but confirm there is no key-reuse path where a stale `firstSeenAt` timestamp from a previous session with the same key silently wins "longest-lived" over a genuinely newer session).
3. Rate-limiter struct value semantics: `ConflictBubbleRateLimiter` is a `struct` stored as `private var conflictBubbleRateLimiter` on the pool (a `class`). Confirm `shouldShow`/`recordShown` calls inside `update(snapshot:)` correctly mutate the pool's stored copy (no accidental copy-on-call losing state between ticks) and that the `mutating func recordShown` call site compiles/behaves under `@MainActor` isolation without reentrancy hazards.
4. `AttentionBubblePanel`/`AttentionBubbleView` dual-purpose `reasonKind` branch in `performAction()`: confirm the new `"session_conflict"` branch is checked before the `AttentionFocusTarget.bundleId(for:)` path and can never fall through to attempt focusing an app for a conflict bubble (which has a synthetic `SourceEvent` with no real terminal/IDE identity) — and confirm `dismissBubble()` (the X button) does not also trigger `onOpenSettings`.
5. Field isolation between the real attention bubble and the new conflict bubble on the same window controller (`FloatingPetPanelController` and `MinimalistPanelController`): confirm `attentionBubble`/`bubblePanel` and `conflictBubble`/`conflictBubblePanel` never share layout/reposition state such that showing one clobbers or misplaces the other when both could theoretically be non-nil on the same panel at once (e.g., during a drag, `reanchorChrome` repositions both bubbles off the same `lastPanelFrame` — check for visual stacking/overlap, not just compile-time independence).
6. `MinimalistPanelController.hide()` / `FloatingPetPanelController.hide()` teardown: confirm the conflict bubble panel is always ordered out on `hide()` (window hidden by user or pool teardown) even though `currentConflictPayload`/`conflictActive` are not reset to `nil`/`false` in `hide()` — check whether a subsequent `show()` before a fresh `applyConflictBubble` call re-shows a stale conflict bubble because the stale payload/flag is still truthy.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — this ticket adds no persisted artifact, CLI stdout shape, or on-disk JSON format; `ConflictBubblePayload` is an in-memory struct only]. Confirm this is actually true (no code path serializes `ConflictBubblePayload` or the rate-limiter's `lastShownAt` map to disk).
2. **CLI flag/arg symmetry** — N/A, no CLI flags added or changed in this diff. Confirm no CLI surface exists in the touched files.
3. **Error-class breadth in `catch` blocks** — probe whether any new code path added a `catch` block; if none, confirm N/A. Otherwise verify recoverable vs. non-recoverable handling.
4. **Defensive layering at module boundaries** — probe the new `FloatingPetWindowControlling.applyConflictBubble(_:)` protocol method boundary: does the pool ever pass a payload with an empty-string `origin`, and does the receiving controller/bubble handle that degenerate case without crashing or rendering a broken platform chip?
5. **Cross-file atomicity windows** — probe whether `activeConflictBubbleTargets[origin] = target` and the `windows[target]?.applyConflictBubble(...)` call are effectively atomic within a single synchronous `update()` invocation (no `await` between them), so no other code path can observe an inconsistent tracking-vs-presentation state mid-tick.
6. **Test-contract strength** — probe whether `ConflictBubbleRateLimiterTests` and `ConflictBubbleTargetSelectorTests` assert the actual boolean/String contract (not just "no crash"), and whether both the "never fired" and "already fired" branches of `shouldShow` are covered, plus the empty-candidates and single-candidate branches of `longestLivedKey`.
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-15/ticket-08-conflict-bubble.md` in full (Outcome, Red, Green, Refactor, Review Focus, Rationale) against the diff. Confirm the "Refactor" note ("the rate limiter a standalone testable type; the presentation layer consumes its `shouldShow(origin:now:)` verdict") matches how the pool actually calls it, and confirm the "Review Focus" bullets (rate limit doesn't leak across platforms; target selection is stable; deep-link opens the correct tab and the bubble is genuinely dismissable) are each addressed by the implementation, not just asserted in prose.

### Diff context

Run `git diff agents/p15-07-selection-policy-cap-eviction-prune-session-keyed-ttl..HEAD` in this worktree for the full diff (12 files, ~379 insertions). Key logic:

- `FloatingPetWindowPool.swift`: adds `firstSeenAt` (set-once clock, mirrors `lastSeenAt`'s never-cleared lifecycle), `conflictBubbleRateLimiter`, and `activeConflictBubbleTargets`. In Step 6c's per-origin loop (`for (origin, keys) in sessionKeyedByOrigin`), after `selection.blocked` is true, it inserts into `computedBlockedOrigins`, checks `conflictBubbleRateLimiter.shouldShow`, filters `firstSeenAt` to `currentlyRendered`, picks a target via `ConflictBubbleTargetSelector.longestLivedKey`, records the fire, and calls `windows[target]?.applyConflictBubble(...)`. After the loop, a second pass clears the bubble for any origin no longer in `computedBlockedOrigins`.
- `ConflictBubbleRateLimiter.swift`: a `struct` with `private var lastShownAt: [String: Date]`, `shouldShow(origin:now:) -> Bool` (true if never shown or `now.timeIntervalSince(last) > 3600`), `mutating func recordShown(origin:now:)`.
- `ConflictBubbleTargetSelector.swift`: `enum` with one static pure function `longestLivedKey(firstSeenAt: [String: Date]) -> String?` returning the min-by-value key.
- `AttentionBubblePanel.swift`: adds `onOpenSettings` closure property (mirrors `onDismiss`), `updateConflict(origin:)` on the panel forwarding to `configureConflict(origin:)` on the view, which sets a synthetic `SourceEvent(origin:kind:nil,name:nil)`, `reasonKind = "session_conflict"`, static summary/subtitle text, and repurposes the existing "Focus" action button as "Settings". `performAction()` gets an early `if reasonKind == "session_conflict"` branch that calls `onOpenSettings?()` and orders the window out, bypassing the existing Focus/`AttentionFocusTarget` logic entirely.
- `FloatingPetController.swift`: adds `applyConflictBubble(_ payload: ConflictBubblePayload?)` to the `FloatingPetWindowControlling` protocol with a default no-op extension.
- `FloatingPetPanel.swift`: both `FloatingPetPanelController` and `MinimalistPanelController` get a second, independent `AttentionBubblePanel` instance (`conflictBubble`/`conflictBubblePanel`) plus `onOpenSettingsRequested` closures, wired into `show`/`hide`/drag-reposition alongside the existing real-attention bubble plumbing.
- `MenubarApp.swift`: wires `panel.onOpenSettingsRequested = { [weak self] in self?.settingsWindowController?.show(tab: .customization) }` in both the own-mode and minimalist window factories.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
