Now I have all the information I need to write the complete adversarial review.

---

**Invariant results**

**Invariant 1** [held] — Probed the sort comparator in `SessionSelectionPolicy.swift:70-78`. The comparator fires `rankA != rankB` first; the incumbency tie-break (`return incumbentB`) is only reached when both sessions share the same eviction rank. An incumbent rank-3 (in-flight) session can only be outranked by another rank-3 newcomer within the same-rank branch — where incumbency correctly flips: `return incumbentB` pushes the newcomer to `prefix` (pending) and keeps the incumbent in `suffix` (rendered). An idle incumbent (rank 0) competing against a rank-3 newcomer is resolved by rank ordering alone and correctly yields its slot.

**Invariant 2** [held] — `FloatingPetWindowPool.pruneSession` (`FloatingPetWindowPool.swift:492-493`) opens with `guard isSessionKeyed(windowKey), let identity = ...`. `isSessionKeyed` returns false for the literal `"combined"` and any key without a colon (plain origins). Both branches exit before any window teardown or store mutation, verified by `testPruneSessionIsNoOpForNonSessionKeyedWindow`.

**Invariant 3** [held] — Step 7's pending branch (`FloatingPetWindowPool.swift:321-329`) calls only `setFloatingPetVisible(false)`, `windows.removeValue`, `windowSpawnedModes.removeValue`, and `releaseSessionNumber` — all in-memory. No `SessionPruner` call, no `FileManager` operation, no `SessionLabelStore` write. Slice and label are left entirely untouched on cap-driven de-render.

---

**Surface results**

**Surface 1 — comparator incumbency vs rank ordering** [probed] — The comparator is a strict weak ordering: ranks partition first (`if rankA != rankB { return rankA < rankB }`), incumbency resolves within-rank ties only, and `a < b` provides a deterministic lexicographic tiebreak. An incumbent idle session (rank 0) is correctly out-prioritized by a newcomer active session (rank 3) via rank ordering, never protected by the incumbency branch. Verified against both the `testIncumbentActiveSessionIsNeverEvictedByANewcomerActiveSession` policy test and direct code trace. Clean.

**Surface 2 — `windowSessionIdentities ?? currentRenderKeyIdentities` fallback** [probed] — Window keys encode a unique `origin:session_id`; session IDs are unique across their origin's lifetime, making same-key identity aliasing structurally impossible. The fallback reaches `currentRenderKeyIdentities` only when the window was torn down before the prune arrives (e.g., pruning a window whose identity was released in a cap-hold tick). In that race the `isSessionKeyed` guard is still satisfied, and `currentRenderKeyIdentities[windowKey]` resolves the correct identity from the still-present snapshot entry. If the snapshot entry has also vanished, both lookups return nil and the guard exits — a correct no-op. Clean.

**Surface 3 — Step 6c grouping vs stale keys from Steps 5a/5b/6a/6b** [probed] — `sessionKeyedDirectKeys` is derived from `visibleEntries` (snapshot-filtered), not from `windows`. A key torn down in Step 5b (TTL) is still in `visibleEntries` if its slice lingers; its `currentlyRendered` membership is false (window nil). Step 6c may then place it in `pendingWindowKeys`. Step 7 evaluates `isTTLExpired` before `pendingWindowKeys.contains`, so the TTL-expired key exits via the TTL branch regardless. No collision risk: session keys are `origin:session_id`; distinct sessions cannot share a window key, so a stale pending entry for one key cannot suppress a different origin's key. Clean.

**Surface 4 — `SlicePruner.pruneOrphanLabels` sidecar exclusion** [probed] — `StateJsonReader.parseSliceFilename` (`StateJsonReader.swift:368-370`) returns nil for any file containing `.tmp-`, any file ending in `.gate.json`, and any file ending in `.context.json`, before attempting the colon-split parse. All known sidecar types are excluded before `liveKeys` is built. Confirmed by `testPruneOrphanLabelsRemovesKeyWhoseSliceIsGone` and direct review of the filter chain. Clean.

**Surface 5 — "Prune Session" gating in `FloatingPetPanel.swift`** [probed] — The prune item is appended inside the `if hasActiveSessionBadge` block (`FloatingPetPanel.swift:2993-3006`), the same gate as Rename. `hasActiveSessionBadge` is set only when `applySessionNumber` receives a non-nil number (`FloatingPetPanel.swift:779`); session numbers are assigned only to session-keyed windows in `assignSessionNumber`. `MinimalistPanelController` / `MinimalistBadgeView` have no `pruneHandler` property and no prune menu item. The `minimalistWindowFactory` closure in `MenubarApp.swift` does not wire `onPruneRequested`. Clean.

**Surface 6 — `cap == 0` sentinel** [probed] — `guard cap > 0, sessions.count > cap` short-circuits on `cap == 0`, returning all sessions as rendered. The pool reads `currentCustomization.sessionCap[origin] ?? 3` — default 3, never 0. The `assignSessionNumber` and `releaseSessionNumber` helpers both check `(sessionCap[origin] ?? 3) == 0` to set unlimited mode on the allocator. No site treats `0` as "render nothing." Clean.

**Diff-derived surface 1 — output stability across schema-version drift** [N/A — no persisted artifact schema changed; `session-labels.json` shape and `state.d` slice format are untouched.]

**Diff-derived surface 2 — CLI flag/arg symmetry** [N/A — no CLI flags added by this ticket.]

**Diff-derived surface 3 — `try?` swallow-all patterns** [probed] — `SessionPruner.pruneSession` uses `try? FileManager.default.removeItem(atPath:)`, which silences both "file absent" (correct no-op) and permissions failures (silently leaves the slice behind). In the permissions case the allocator and label store still execute their steps, leaving the slice as the only survivor. This is not permanent: `SlicePruner.prune` will age it out within 24 h, and `pruneOrphanLabels` on the next sweep would find no matching label (already removed). The ticket's "best-effort" clause explicitly permits this ordering. `pruneOrphanLabels`'s `try?` on the labels file correctly no-ops for a missing file. No blocking issue under the spec; silent failure vs absent-file distinction does not change correctness outcomes. Clean.

**Diff-derived surface 4 — defensive layering at module boundaries** [probed] — `MenubarApp.swift:267` wires `onPruneRequested` with `windowKey: origin` where `origin` is the factory's `(origin, petId)` parameter — the resolved render key, always well-formed. `FloatingPetWindowPool.pruneSession` validates `isSessionKeyed(windowKey)` before any destructive call; `SessionPruner.pruneSession` is never reached for a plain-origin or "combined" key. No externally-supplied unchecked input reaches `SessionPruner`. Clean.

**Diff-derived surface 5 — cross-file atomicity windows** [probed] — Three sequential steps in `SessionPruner.pruneSession`: slice delete → allocator release → label remove. If the process is killed between steps, `pruneOrphanLabels` (runs at launch and every 30 min) removes any label whose slice is gone; the allocator is rebuilt from scratch on the next launch, recovering leaked numbers. No orphan survives indefinitely. The ticket states "best-effort, not transactional." Clean.

**Diff-derived surface 6 — test-contract strength** [probed] — Pool tests assert on public stable API: `activeOrigins`, `isActive(for:)`, `sessionNumber(forWindowKey:)`, `StubWindowController.isFloatingPetVisible`. The "incumbent protected" branch is covered by `testAllActiveCapPressureNeverEvictsAnAlreadyRenderedSession`; the "newcomer blocked" branch is covered at the policy layer by `testCapTwoWithThreeActiveSessionsEmitsBlockedSignalWithoutEvicting`. The pool-level `blockedOrigins` wiring (Step 6c → `blockedOrigins = computedBlockedOrigins`) has no pool-level assertion — this is deferred per spec to P15.08. Slice-persistence invariant for cap-held sessions is tested indirectly: `testHeldIdleSessionIsPromotedWhenARenderedSessionIsPruned` reads slice files via `StateJsonReader` after the hold tick; the test would fail differently if the slice were deleted (readSnapshot would return 2 sessions, not 3). Both sibling-isolation tests (`testPruneSessionDoesNotAffectASiblingSessionsSliceOrLabel`, `testSessionKeyedTTLDismissesOneSessionWithoutRenumberingItsSibling`) directly verify that peer sessions' state is unmodified. Clean.

**Diff-derived surface 7 — doc-vs-code drift in the ticket Rationale** [probed] — Two specific claims checked: (1) "Prune Session is Own-mode only, matching Rename's existing scope" — the Minimalist strip (`MinimalistBadgeView`, `MinimalistPanelController`) has no prune affordance and `MenubarApp.minimalistWindowFactory` does not wire `onPruneRequested`. ✓ (2) "cap eviction never deletes a slice" — Step 7's pending branch (`FloatingPetWindowPool.swift:321-329`) has no file operations of any kind. ✓ No drift detected.

---

**Actionable findings**

None.

---

**Advisory Observations**

The pool-level `blockedOrigins` wiring (Step 6c accumulating `computedBlockedOrigins` into `blockedOrigins`) has no pool-level test asserting on that property. The policy-layer test covers `blocked` emission, and the ticket explicitly defers `blockedOrigins` consumption to P15.08. If P15.08 assumes `blockedOrigins` is correctly set per tick, the connecting contract (Step 6c → pool property) is only implicitly covered. A single pool test asserting `pool.blockedOrigins.contains("claude_code")` after the all-active scenario would close this seam. This is in-scope for P15.08's test setup.

The design releases a cap-held session's free-list number on de-render and lets a promoted session receive a new (potentially different) number. The Rationale documents this as intentional ("the ticket only requires the slice to survive a hold, not number stability"). A session the user has been watching as "Session 1" that is de-rendered and later promoted could reappear as "Session 3" if other sessions consumed numbers in between. No behavioral bug — Rename labels are session-keyed and survive the hold correctly — but the visible badge number can change across a hold/promote cycle, which may surprise users. Noting for UX review on the badge label spec.

`sessionNumber(forWindowKey:)` (`FloatingPetWindowPool.swift:529`) calls `sessionNumberAllocator.assign()` as a side effect inside a getter. The pool's own call sites are safe (gated behind the `windows[renderKey] != nil` check in Step 7, so pending sessions never reach it from within the pool). However, an external caller passing a pending session key would silently create an allocator entry for a non-rendered session, consuming a free-list slot until the next teardown. This is pre-existing behavior (not introduced by P15.07) and is low risk given the pool's tight wiring in `MenubarApp`.

---

**Runner termination**

runnerStatus: completed
terminatedReason: All invariants, spec-derived surfaces, and diff-derived surfaces were fully probed; no actionable findings were identified.
