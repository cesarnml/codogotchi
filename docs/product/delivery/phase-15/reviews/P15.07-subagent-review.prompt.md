You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P15.07 — Selection policy: cap, eviction, prune, session-keyed TTL

Outcome:
- When an Own/Minimalist platform (session-pets on) has more live session slices than its cap, the pool renders the top-N by priority and holds the rest as pending — a reversible de-render, not a delete. The held session's slice stays on disk, so no new persistence tracks it.
- Eviction/de-render priority (most- to least-evictable): idle/standby → errored → waiting_for_input → any in-flight active state (never de-rendered for a newcomer).
- Promotion: the instant a rendered slot frees (a rendered session ages out, is pruned, or drops to an evictable state), the highest-priority pending session is promoted to a real panel.
- Manual Prune: right-click → "Prune Session" destroys the panel, deletes the session's state.d slice, releases its free-list number (P15.05), and removes its session-labels.json key (P15.06) — the same end-state as automatic TTL expiry.
- Automatic aging: Phase 14's TTL/idle-hibernation mechanism keyed by session_id, running independently of cap pressure so dead panels don't linger.
- Orphan hygiene: the slice sweep drops session-labels.json keys whose origin:session_id slice no longer exists.
- When every remaining rendered panel is active and a new active session is blocked, the policy emits a blocked signal (consumed by a later ticket P15.08) — it does not evict an active session and does not itself render any UI.

Rationale (from ticket doc, appended during implementation):
- SessionSelectionPolicy.select(sessions:cap:currentlyRendered:) is a pure per-origin partition recomputed every pool tick — "promotion" needed no dedicated bookkeeping because a session dropping out of `pending` (rival pruned/TTL'd/idled) simply wins the next partition.
- The pool wires it into a new Step 6c ahead of the existing Step 7 spawn loop, gating spawn/de-render per key; manual Prune reuses the existing `windowSessionIdentities`/`releaseSessionNumber` machinery and delegates slice+number+label teardown to a new `SessionPruner.pruneSession`, mirroring the existing TTL-dismiss pattern used at every other window-teardown site in the file.
- Alternative rejected: keeping cap-held sessions' free-list numbers reserved across a de-render/promote cycle — the ticket only requires the slice to survive a hold, not number stability.
- Deferred: rendering the "blocked (all-active)" signal is P15.08's scope — this ticket only exposes `FloatingPetWindowPool.blockedOrigins`. "Prune Session" is Own-mode only, matching Rename's existing scope.

### Files touched

Implementation:
apps/menubar/Sources/SessionSelectionPolicy.swift
apps/menubar/Sources/SessionPruner.swift
apps/menubar/Sources/SlicePruner.swift
apps/menubar/Sources/FloatingPetWindowPool.swift
apps/menubar/Sources/FloatingPetPanel.swift
apps/menubar/Sources/MenubarApp.swift

Tests:
apps/menubar/Tests/MenubarTests/SessionSelectionPolicyTests.swift
apps/menubar/Tests/MenubarTests/SessionPrunerTests.swift
apps/menubar/Tests/MenubarTests/SlicePrunerTests.swift
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift

### Invariants to hold

1. `SessionSelectionPolicy.select` must never place a currently-rendered in-flight (`ActivityState.isInFlight == true`) session into `pending` when a same-rank newcomer is competing for its slot — only strictly more-evictable sessions (lower `evictionRank`) may bump an incumbent.
2. `FloatingPetWindowPool.pruneSession(windowKey:stateDirectory:)` must delete the `state.d` slice, release the session's free-list number, and remove its `session-labels.json` key together for a session-keyed window, and must be a complete no-op (no window teardown, no store mutation) for a plain-origin or `"combined"` window key.
3. Cap-driven de-render (a session moving from `rendered` to `pending` on a later tick, not via Prune or TTL) must never delete the session's `state.d` slice or its `session-labels.json` key — only `SessionPruner.pruneSession` and TTL-dismiss are allowed to delete state.

### Attack surfaces to probe

1. `SessionSelectionPolicy.select`'s sort comparator (`FloatingPetWindowPool.swift` Step 6c calling into `SessionSelectionPolicy.swift`): does the incumbency tie-break only activate within equal `evictionRank`, or could it override rank ordering and let an incumbent idle session block an evictable slot from a genuinely higher-priority newcomer?
2. `FloatingPetWindowPool.pruneSession`: the `windowSessionIdentities[windowKey] ?? currentRenderKeyIdentities[windowKey]` fallback — can this resolve a stale/wrong identity (e.g. after a session key is reused across a rapid prune+respawn within one tick), causing prune to release the wrong origin/sessionId's number or delete the wrong slice?
3. Step 6c's per-origin grouping (`Dictionary(grouping: sessionKeyedDirectKeys, by: Self.origin(forWindowKey:))`) combined with Step 7's `pendingWindowKeys.contains(renderKey)` gate: is there any render key that is session-keyed but excluded from `sessionKeyedDirectKeys` (e.g. because it was already torn down in Step 5a/5b/6a/6b before Step 6c runs), leaving a stale entry in `pendingWindowKeys` that could incorrectly suppress a *different* origin's key if window-key collisions existed?
4. `SlicePruner.pruneOrphanLabels`: does it correctly exclude `.gate.json`/`.context.json`/`.tmp-` sidecars via `parseSliceFilename` before building `liveKeys`, or could a sidecar filename be misparsed into a live key that shadows/protects a genuinely orphaned label?
5. `FloatingPetPanel.swift`'s new "Prune Session" menu item: is it gated by `hasActiveSessionBadge` the same way Rename is, so it can never appear on a plain-origin or "combined" window (where `pruneSession` would silently no-op if triggered)?
6. `SessionSelectionPolicy.select`'s Unlimited-cap early-return (`guard cap > 0, sessions.count > cap else { ... }`): does `cap == 0` ever get treated as "cap 0 renders nothing" instead of Unlimited anywhere in the pool wiring (e.g. `currentCustomization.sessionCap[origin] ?? 3` defaulting), given `0` is the sentinel and not a real cap value?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — reason]: no persisted artifact schema changed; `session-labels.json` shape (`[String: String]`) and `state.d` slice format are untouched by this ticket.
2. **CLI flag/arg symmetry** — [N/A — reason]: no CLI flags added; the CLI stays out of Phase 15 per the ticket's explicit deferral.
3. **Error-class breadth in `catch` blocks** — probe `SessionPruner.pruneSession`'s `try? FileManager.default.removeItem(atPath:)` and `SlicePruner.pruneOrphanLabels`'s `try? Data(contentsOf:)`/`try? JSONDecoder().decode` — do these `try?` swallow-all patterns silently mask a permissions error vs. "file genuinely absent," and does that distinction matter for this ticket's correctness contract?
4. **Defensive layering at module boundaries** — probe `FloatingPetWindowPool.pruneSession`'s trust in the caller-supplied `windowKey`/`stateDirectory` (from `MenubarApp`'s closure capturing `origin`/`stateDir`) — does the pool revalidate `isSessionKeyed(windowKey)` before doing anything destructive, or could a malformed `windowKey` reach `SessionPruner.pruneSession` unchecked?
5. **Cross-file atomicity windows** — probe `SessionPruner.pruneSession`'s three sequential steps (slice delete, allocator release, label remove) — if the process is killed between steps, does the resulting partial state (e.g. slice gone but label still present) get cleaned up by `SlicePruner.pruneOrphanLabels` on the next scheduled sweep, or does it silently persist?
6. **Test-contract strength** — probe whether `FloatingPetWindowPoolTests`' new cap/prune tests assert on the pool's public, stable surface (`activeOrigins`, `sessionNumber(forWindowKey:)`, `isActive(for:)`) rather than reaching into private state, and whether both the "incumbent protected" and "newcomer blocked" branches of the tie-break get distinct test coverage.
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-15/ticket-07-selection-policy-prune-ttl.md`'s Rationale section against the diff: does the claim "Prune Session is Own-mode only, matching Rename's existing scope" hold (i.e. is there truly no Prune wiring in the Minimalist panel path), and does "cap eviction never deletes a slice" hold everywhere in `FloatingPetWindowPool.swift`'s Step 7 pending-key branch?

### Diff context

`SessionSelectionPolicy.swift` (new): a stateless `enum` with `evictionRank(for:) -> Int` (idle/standby=0, errored=1, waitingForInput=2, everything else=3) and `select(sessions:cap:currentlyRendered:) -> Selection` (rendered/pending/blocked). `cap == 0` short-circuits to "render everything." Otherwise sessions are sorted ascending by `(evictionRank, isIncumbent ? 1 : 0, key)` and the top `cap` (by that order, i.e. least evictable) render; the rest are pending. `blocked` is true iff any pending session is itself in-flight (`ActivityState.isInFlight`).

`SessionPruner.swift` (new): `pruneSession(windowKey:origin:sessionId:stateDirectory:allocator:labelPath:)` — three independent best-effort steps: delete `<stateDirectory>/<windowKey>.json`, `allocator.release(origin:sessionId:)`, `SessionLabelStore.removeLabel(for:windowKey:at:labelPath)`.

`SlicePruner.swift` (modified): adds `pruneOrphanLabels(dir:labelPath:) -> Int` — scans `dir` for slice filenames via `StateJsonReader.parseSliceFilename`, builds a `liveKeys` set of `"origin:sessionId"`, reads the labels JSON, and removes any label key not in `liveKeys`. Wired into `SlicePruneScheduler.pruneAsync()` alongside the existing age-based `prune`.

`FloatingPetWindowPool.swift` (modified): new `blockedOrigins: Set<String>` property (public, recomputed every tick). New Step 6c inserted between the existing Step 6b (mode-transition teardown) and Step 7 (spawn loop): groups session-keyed `directKeys` by origin, computes `currentlyRendered` from `windows[$0] != nil` per key, calls `SessionSelectionPolicy.select`, and accumulates `pendingWindowKeys`/`blockedOrigins`. Step 7's loop gains a new branch: if `pendingWindowKeys.contains(renderKey)`, tear down any existing window for that key (same teardown calls as TTL-dismiss: `setFloatingPetVisible(false)`, remove from `windows`/`windowSpawnedModes`, `releaseSessionNumber`) and `continue` — never touching the slice or label. New public method `pruneSession(windowKey:stateDirectory:labelPath:)`: resolves identity from `windowSessionIdentities` (fallback `currentRenderKeyIdentities`), tears down the window, then calls `SessionPruner.pruneSession`.

`FloatingPetPanel.swift` (modified): new `FloatingPetHidePrompt.pruneTitle` constant, a new "Prune Session" item appended to the Own-mode right-click prompt (`presentHidePrompt(anchorInScreen:localPoint:)`) gated by the same `hasActiveSessionBadge` flag as Rename, routed through a new `presentPruneConfirmation()` (`NSAlert` with a destructive-styled "Prune" button) before firing `pruneHandler`. New `onPruneRequested`/`pruneHandler` closures parallel the existing `onRenameRequested`/`renameHandler` wiring.

`MenubarApp.swift` (modified): wires `panel.onPruneRequested` to call `floatingPetWindowPool?.pruneSession(windowKey: origin, stateDirectory: stateDir)`, alongside the existing `onRenameRequested` wiring in the same `windowFactory` closure.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
