You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

## Outcome

- `FloatingPetWindowPool` keys windows by the **resolved render key** from P15.03 uniformly. With session-pets on for an Own/Minimalist platform, each active `origin:session_id` gets its own window; with session-pets off, or for Combined/off, behavior is unchanged.
- All origin-keyed invariants operate on the resolved key: TTL clock (`lastSeenAt`), last-active election + immunity, user hide/show set, gate-badge join, and mode-transition teardown (own↔minimalist, own↔combined).
- Every session window on a platform renders that platform's assigned (or Default) pet via the existing `assignmentsReader.resolve(origin:)` (session_id does not affect pet identity this phase).
- Existing `FloatingPetWindowPoolTests` pass unchanged (they become the collapsed-case regression net); new tests cover fan-out.

## Rationale (as recorded on this ticket)

Red first: `testSessionPetsOnFansOutOneWindowPerActiveSession` — with session keys the pool resolved the pet from the raw key (`resolve(origin: "claude_code:s1")` misses the platform override) and the own-path platform chip was applied to `windows[sourceEvent.origin]`, which does not exist in the session-keyed window space. `testCombinedModeWithTwoSessionsFoldsToSingleCombinedWindow` also failed red: the driver's pre-folded `"combined"` key fell down the own-mode path (mode lookup on the literal key), losing the idle ⭐ Default badge.
Why this path: widened the existing key-typed dictionaries to resolved render keys (per the ticket's Refactor note) instead of adding a session map. `windowKey(for:)` is the sole branch site and accepts both input shapes: the driver's pre-folded `"combined"` and the unfolded per-origin maps the pre-existing tests feed, so the collapsed case stays byte-identical and the whole existing suite is the regression net. Pet identity, mode lookup, and the minimalist platform chip derive the owning origin from the key via one static helper (`origin(forWindowKey:)`), which MenubarApp reuses for winner-writer scoping and pet reload.
Alternative considered: a parallel `[origin: [sessionId: Window]]` map — rejected (two keyed spaces to keep consistent; every invariant — TTL, immunity, hide-set, teardown — would need dual bookkeeping; the ticket names this anti-pattern explicitly).
Deferred: per-session-precise Force Idle / attention dismiss — the winner-only writers still target the owning origin's freshest slice (a session window's right-click Force Idle may reset a fresher sibling session's slice); session-keyed writer scoping belongs with P15.07's session-keyed TTL/prune work. Session-window frame persistence keys off the raw render key, so saved positions are per opaque session id — disposable by design, like the in-memory auto-number (P15.05).
Contract note: gate-badge lookup on the combined path moved from the winning origin to the winning entry's render key — byte-identical for unfolded input (key == origin) and required for pre-folded input, where the driver keys the badge under `"combined"`.

### Files touched

Implementation:
apps/menubar/Sources/FloatingPetWindowPool.swift
apps/menubar/Sources/MenubarApp.swift

Tests:
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift

### Invariants to hold

1. With session-pets off (or origin absent from `sessionPetsEnabled`), the render key equals the plain origin, and `FloatingPetWindowPool` must produce byte-identical window/state/behavior to the pre-P15.03 per-origin pool — every pre-existing `FloatingPetWindowPoolTests` case must still pass unchanged.
2. For a session-keyed window (`origin:session_id`), pet identity and platform mode must resolve from the OWNING ORIGIN (via `Self.origin(forWindowKey:)`), never from the composite key itself or from `sourceEvent?.origin`, which is absent on many snapshots.
3. `replacePet(origin:)` must live-swap every currently-active window whose owning origin matches the given origin (all session windows of that origin, or its folded "combined" window), and must not touch windows of any other origin.

### Attack surfaces to probe

1. `windowKey(for:)` — the single branch site collapsing a render key to a window key: does it correctly treat the literal `"combined"` input (driver pre-fold) and a combined-mode origin's own key (unfolded/legacy-shaped test input) identically, without double-folding or losing last-active immunity for either shape?
2. `isTTLExpired` / `lastSeenForWindow` — with two session keys sharing one origin (`claude_code:s1`, `claude_code:s2`), does TTL dismissal correctly scope to each session's own `lastSeenAt`, or does one session's idle clock leak into the sibling's via the origin-level fallback path used for "combined"?
3. Step 6b mode-transition teardown (own↔minimalist) — with multiple session keys under one origin, does every session window get torn down and respawned through the correct factory, or can the loop skip a subset when `windowSpawnedModes` state is stale for only some of the origin's session keys?
4. `replacePet(origin:)` — filter predicate `key == foldedKey || Self.origin(forWindowKey: key) == origin`: for an origin currently in Combined mode, does this correctly target only the shared `"combined"` window (not spuriously matching some other origin's session key that happens to share a colon-split origin string)?
5. `resolveWindowOrigins(windowKey:)` in MenubarApp (winner-only writers `forceIdle`/`dismissAttention`): for a session-keyed window, does it correctly resolve to `{owning origin}` — and is the ticket's acknowledged deferral (writer targets the origin's freshest slice, not necessarily the clicked session's slice) something a user could interpret as data loss on the WRONG session's in-progress work?
6. `reloadActivePet()` in MenubarApp — origin-folding via `Set(...map { origin(forWindowKey:) })` before resolving pet ids: with two session windows on the same origin assigned different transient states, does the fold correctly reload exactly once per distinct pet id and still hit every active window via `replacePet`, or can a window be missed if it was mid-teardown during the fold?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — reason: no persisted artifact, CLI output, or on-disk JSON format changes in this diff; `PerPlatformSnapshot.renderKeyIdentities` was added in P15.03 with a defaulted empty value, not this ticket]
2. **CLI flag/arg symmetry** — [N/A — reason: no CLI flags touched; this is Swift menubar app code only]
3. **Error-class breadth in `catch` blocks** — [N/A — reason: no new or modified `catch` blocks in this diff; the pool's failure paths use `guard`/`NSLog` fallback (unchanged pattern), not exception handling]
4. **Defensive layering at module boundaries** — probe `windowFactory`/`minimalistWindowFactory` call sites in `FloatingPetWindowPool.update` and the `Self.origin(forWindowKey:)` helper: does the pool trust the render key shape from `PerPlatformSnapshot.perPlatform` without validating it (e.g. a key with two colons, or an empty origin before the colon)?
5. **Cross-file atomicity windows** — probe `reloadActivePet()`'s two-phase flow (fold origins → detached async load → `MainActor.run` reinsert + `replacePet` per origin): if the pool's active windows change (a session ages out via TTL) between the fold and the `MainActor.run` reinsert, does `replacePet` fail silently for a since-removed window, or misroute to a window that respawned with a different origin?
6. **Test-contract strength** — probe the six new P15.04 tests: do they assert structural window-key sets (`Set(pool.activeOrigins)`) and call counts (not just booleans), and do they cover both the session-pets-on and session-pets-off branches for the same origin shape?
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-15/ticket-04-pool-per-session-fanout.md`'s Rationale (Contract note: "gate-badge lookup on the combined path moved from the winning origin to the winning entry's render key") against the actual diff in `FloatingPetWindowPool.swift` Step 8 (`snapshot.gateBadges[winnerEntry.key]`) — does the code match the claim, and does the Deferred note about winner-only writers match `resolveWindowOrigins` in MenubarApp?

### Diff context

`FloatingPetWindowPool.swift`: every `[String: T]` dictionary previously keyed by "origin" (`windows`, `lastSeenAt`, `lastUpdatedAt`, `windowSpawnedModes`) is now keyed by "render key" (unchanged type, renamed semantics). A new `static func origin(forWindowKey:)` splits `origin:session_id` back to the origin (or returns the key unchanged for a plain origin / the literal "combined"). `windowKey(for:)` (private) is the sole fold-to-"combined" branch site: `renderKey == "combined" || mode(forWindowKey: renderKey) == .combined ? "combined" : renderKey`. Steps 2–9 of `update()` operate on the render-key space instead of origin space; Step 7 (spawn own-mode windows) now derives `origin = Self.origin(forWindowKey: renderKey)` once and uses it for `resolve(origin:)`, `mode(for: origin)`, and the minimalist/idle platform-chip branches, while the composite render key itself is what's passed to the window factory and used for gate-badge lookup. `replacePet(origin:)` now iterates `windows.keys` filtering on `key == foldedKey || origin(forWindowKey: key) == origin` instead of a single dictionary lookup, so it fans out to every session window of the origin. `MenubarApp.swift`: `resolveWindowOrigins(windowKey:)` now maps a non-"combined" window key through `FloatingPetWindowPool.origin(forWindowKey:)` instead of returning the key verbatim; `reloadActivePet()` folds `activeOrigins` to a `Set` of owning origins before resolving pet ids, relying on `replacePet(origin:)`'s new fan-out to reach every session window.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
