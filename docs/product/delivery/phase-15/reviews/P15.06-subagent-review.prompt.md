You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Outcome (from `docs/product/delivery/phase-15/ticket-06-rename-tooltip.md`):

- Right-clicking a session panel/badge exposes a **rename** affordance; the entered label is trimmed and capped at **24 characters** and replaces "Session N" on that panel.
- The label persists in a new Swift-owned sidecar `session-labels.json` — a flat map `{ "origin:session_id": "label" }` written via read-merge-write so concurrent key updates never clobber each other. The app is the sole writer; the CLI is untouched.
- On launch the pool seeds each session's displayed label from the sidecar when present, else falls back to the free-list "Session N".
- Hovering a session's animation badge shows, after a short delay, a truncated version of that thread's **last submitted user prompt**, sourced from the existing prompt-attention store keyed by `origin:session_id` (reuse `PromptAttentionReader`/the `by_session` store; no CLI or schema change).

Rationale (implementation notes appended during this ticket, see the ticket file's `## Rationale` section for full text): the rename UI reuses the existing right-click "Force Idle"/"Hide" pill-stack prompt plus a standard `NSAlert` + `NSTextField` modal (not a bespoke inline-editing pill), because `AnimationBadgePanel` is currently click-through (`ignoresMouseEvents = true`) and re-plumbing mouse routing on the badge itself was judged out of scope for this ticket's size. The tooltip uses native `NSView.toolTip` rather than a custom hover-delay timer. The 24-char cap counts `Character`s (grapheme clusters), not UTF-16 code units. Minimalist-mode session badge parity (rename + tooltip) is explicitly deferred — only Own-mode (`FloatingPetPanelController`/`AnimationBadgeView`) wires the new `applySessionLabel`/`applySessionTooltip` protocol methods end-to-end; `MinimalistWindowController` inherits the protocol's default no-op for both.

### Files touched

Implementation:
apps/menubar/Sources/SessionLabelStore.swift
apps/menubar/Sources/CodogotchiFolders.swift
apps/menubar/Sources/FloatingPetController.swift
apps/menubar/Sources/FloatingPetPanel.swift
apps/menubar/Sources/FloatingPetWindowPool.swift
apps/menubar/Sources/MenubarApp.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj

Tests:
apps/menubar/Tests/MenubarTests/SessionLabelStoreTests.swift
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift (additions only)

### Invariants to hold

1. `SessionLabelStore.setLabel(_:for:at:)` must never lose a previously-written key: writing key B after key A must leave key A's value readable, and the stored value for a freshly-written key must equal `SessionLabelStore.normalize(input)` (trimmed, then capped at exactly 24 `Character`s).
2. `FloatingPetWindowPool.sessionLabel(forWindowKey:)` and `sessionPromptSummary(forWindowKey:)` must return `nil` for any window key that is not session-keyed (plain origin or the literal `"combined"`), even when the injected reader would return a non-nil value for that exact key string.
3. `PromptAttentionReader.summary(forSessionKey:)` must return the summary for exactly the given `"origin:session_id"` key and must never return another session's summary under the same origin, even when that other session has a newer `updated_at`.

### Attack surfaces to probe

1. `SessionLabelStore.write(_:at:)` — the write path uses `Data.write(to:options:.atomic)` directly (no temp-file dance, no `FileManager.replaceItemAt`). Confirm this is actually atomic against a concurrent `read(at:)` from the same process on the main actor (no interleaving is possible since both are synchronous on the caller's thread) and that a write failure (`try?` swallows the error) does not leave a partially-written file readable by a subsequent `read(at:)`.
2. `SessionLabelStore.normalize(_:)` — verify grapheme-cluster counting behaves sanely for a label built from combining characters or multi-scalar emoji (e.g. a ZWJ sequence) right at the 24-character boundary; confirm `String(trimmed.prefix(maxLength))` cannot produce an invalid/dangling grapheme cluster that then fails to round-trip through `JSONEncoder`/`JSONDecoder`.
3. `FloatingPetWindowPool.sessionLabel(forWindowKey:)` / `sessionPromptSummary(forWindowKey:)` gating via `isSessionKeyed(_:)` — this reuses the *existing* `isSessionKeyed` check (colon-containing, not `"combined"`). Confirm a plain origin whose name itself happens to contain a colon (unlikely given current origin naming, but check for any validation gap) cannot be misclassified as session-keyed and leak a label.
4. `FloatingPetPanelController.applySessionLabel`/`applySessionTooltip` early-return guards (`guard currentSessionLabel != label else { return }`) — confirm these do not suppress a legitimate re-application needed after `(panel?.contentView as? FloatingPetInteractionView)?.currentSessionLabel = label` when the interaction view itself was just recreated (e.g. window respawn) with a stale/default value, i.e. check for a window-respawn path where `currentSessionNumber`/`currentSessionLabel` on the controller stay unchanged but the freshly-created `FloatingPetInteractionView` needs the value pushed to it.
5. `presentRenameAlert()` in `FloatingPetInteractionView` — `alert.runModal()` blocks the run loop; confirm this cannot be invoked twice concurrently (e.g. rapid double right-click) leaving two modal alerts or a dangling `hidePromptPanel` reference, and confirm the alert's accessory `NSTextField` value is read correctly on `.alertFirstButtonReturn` even if the user pressed Return inside the field instead of clicking the button.
6. `MenubarApp.swift` `panel.onRenameRequested = { newLabel in SessionLabelStore.setLabel(newLabel, for: origin) }` — confirm `origin` (the closure's captured render-key parameter) is correctly the *session* render key (`"origin:session_id"`) and not accidentally the bare platform origin in any mode (own vs. minimalist windowFactory) where this closure fires, since a wrong key would silently write to the wrong `SessionLabelStore` entry with no test coverage catching it (this callback itself is not covered by the new tests — it's wired only in `MenubarApp.swift`, which has no unit tests for this closure).

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `session-labels.json` is a brand-new file with no prior version to be compatible with. [N/A — new sidecar file, no legacy shape to preserve]. Still confirm `SessionLabelStore.read(at:)` degrades safely (empty dict, no throw) if a future version writes a different JSON shape (e.g. nested objects) to this same path.
2. **CLI flag/arg symmetry** — no CLI flags were added or changed in this ticket (the ticket explicitly states "the CLI is untouched"). [N/A — no CLI surface touched].
3. **Error-class breadth in `catch` blocks** — `SessionLabelStore.read`/`write` use `try?` (swallow-all), matching the existing `PromptAttentionReader`/`CustomizationJsonReader` pattern in this codebase. Confirm this swallow-all does not mask a permissions error or disk-full condition in a way that silently drops a user's rename with no feedback — is that acceptable given the existing sidecar-reader precedent, or does the rename UI need an error path back to the user?
4. **Defensive layering at module boundaries** — `FloatingPetWindowPool.sessionLabel(forWindowKey:)`/`sessionPromptSummary(forWindowKey:)` trust the window key format (`"origin:session_id"`) to double as the `SessionLabelStore`/`PromptAttentionReader` lookup key verbatim, with no re-validation. Confirm this key-format assumption is actually guaranteed upstream (by `RenderKeyResolver`/`resolveRenderKeys`) and cannot diverge — e.g. could a session_id itself contain a colon, breaking the "origin is everything before the first colon" assumption elsewhere in the pool (`Self.origin(forWindowKey:)`) in a way that also corrupts the SessionLabelStore key?
5. **Cross-file atomicity windows** — the rename flow spans `FloatingPetInteractionView.presentRenameAlert()` → `FloatingPetPanelController.applySessionLabel` (in-memory) → `MenubarApp`'s `onRenameRequested` closure → `SessionLabelStore.setLabel` (disk write) → the next pool tick's `sessionLabelReader` read-back. Confirm there's no window where the in-memory badge shows the new label but a crash before the disk write (or vice versa) leaves the two permanently diverged in a user-visible way, and confirm the very next `update()` tick's read-back doesn't visibly "flicker" the label back to the old value before the write lands.
6. **Test-contract strength** — do the new `FloatingPetWindowPoolTests` cases assert the pool-level `applySessionLabel`/`applySessionTooltip` calls (structural, via `StubWindowController.appliedSessionLabels`) rather than just prose? Confirm coverage of both the "reader has a value" and "reader returns nil" branches for session-keyed keys, and confirm the plain-origin-never-leaks case is asserted. Also confirm: is there any test coverage at all for the `FloatingPetPanelController`/`FloatingPetInteractionView` rename-UI wiring (the `presentRenameAlert`/`onRenameRequested`/`renameHandler` chain), or is that chain entirely untested by this ticket?
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-15/ticket-06-rename-tooltip.md` in full (including the newly appended `## Rationale` section) and confirm every claim there (grapheme-based cap, NSAlert modal choice, native toolTip choice, minimalist-mode deferral) actually matches what the diff does. Flag any mismatch.

### Diff context

Key changes:

- New `apps/menubar/Sources/SessionLabelStore.swift`: enum with static `label(for:at:)`, `setLabel(_:for:at:)`, `removeLabel(for:at:)`, `normalize(_:)` (trim + 24-char grapheme cap), private `read`/`write` doing whole-file read-merge-write over a `[String: String]` JSON dict at `~/.codogotchi/session-labels.json`.
- `CodogotchiFolders.swift`: added `PromptAttentionReader.summary(forSessionKey:at:)` — decodes the same `by_session` payload as the existing `latestSummary(origin:)` but looks up one exact key instead of prefix-filtering + picking the newest.
- `FloatingPetWindowPool.swift`: new `SessionLabelReader`/`SessionPromptSummaryReader` typealiases and injected closures (default to `SessionLabelStore.label(for:)` / `PromptAttentionReader.summary(forSessionKey:)`); new public `sessionLabel(forWindowKey:)`/`sessionPromptSummary(forWindowKey:)` methods gated on the existing private `isSessionKeyed(_:)`; Step 7 of `update(snapshot:)` now also calls `applySessionLabel`/`applySessionTooltip` on each spawned/updated window alongside the existing `applySessionNumber`.
- `FloatingPetController.swift`: `FloatingPetWindowControlling` and `FloatingPetPanelManaging` protocols gain `applySessionLabel(_:)`/`applySessionTooltip(_:)` with no-op default extensions; `FloatingPetController` forwards both to its wrapped `panel`.
- `FloatingPetPanel.swift` (largest diff): `PlatformSessionBadge.configure` gains `label`/`tooltip` params (label replaces "Session N" text when non-empty; tooltip sets `NSView.toolTip`); `AnimationBadgeView.configureSessionNumber` gains the same params and threads them to its internal `PlatformSessionBadge`; `AnimationBadgePanel.reposition` gains `sessionLabel`/`sessionTooltip` params; `FloatingPetPanelController` gains `currentSessionLabel`/`currentSessionTooltip` state, `applySessionLabel`/`applySessionTooltip` methods (with early-return-on-no-change guards), and `onRenameRequested: ((String) -> Void)?`; `FloatingPetHidePrompt` gains a `renameTitle` constant; `FloatingPetInteractionView` gains `hasActiveSessionBadge`/`currentSessionLabel`/`renameHandler` and a new `presentRenameAlert()` using `NSAlert` + accessory `NSTextField`, plus a new "Rename…" row conditionally appended in `presentHidePrompt(anchorInScreen:localPoint:)` when `hasActiveSessionBadge` is true.
- `MenubarApp.swift`: wires `panel.onRenameRequested = { newLabel in SessionLabelStore.setLabel(newLabel, for: origin) }` inside the own-mode `windowFactory` closure, where `origin` is the closure's render-key parameter (already `"origin:session_id"` for session-keyed windows). The minimalist `windowFactory` closure is NOT touched (matches the documented deferral).

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
