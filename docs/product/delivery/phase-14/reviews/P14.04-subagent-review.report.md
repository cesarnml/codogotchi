**Invariant results**

1. [held] Same-`petId` cache identity is implemented via `cache[petId]` before loading and `cache[petId] = pair` after successful load; repeated success for `"mali"` returns the same `CodexPet` instance.
2. [held] For non-`maew` `CodexPet` load failure, `resolve(petId:)` catches the error, logs, and calls `resolveMaewFallback()` instead of rethrowing; this holds when the Maew fallback itself is loadable, which the ticket rationale names as the only throwing case.
3. [held] `evict(petId:)` removes the keyed entry and `evictAll()` clears the dictionary, so the next resolve reaches the loader again.

**Surface results**

- `resolve(petId:)` cache identity: [probed]. `DEFAULT_PET_NAME` is the module-level `let DEFAULT_PET_NAME = "maew"` in `PetConfig.swift`, and resolver/tests reference that same symbol. Literal `"maew"` and the constant compare equal.
- `resolve(petId:)` fallback path for Maew itself: [probed]. If `petId == DEFAULT_PET_NAME`, the `catch` guard rethrows the original Maew load error and does not call `resolveMaewFallback()`. This matches the rationale note that only Maew fallback failure throws.
- `resolveMaewFallback()` double-fallback caching: [probed]. Fallback results are cached only under `DEFAULT_PET_NAME`; a later failed non-Maew resolve retries that non-Maew load, then reuses cached Maew. The failing `petId` is not incorrectly cached.
- `petsBaseURL(petId:)` path injection: [probed]. `appendingPathComponent("../evil")` produces a path under `pets/../evil`, which standardizes outside `~/.codogotchi/pets`. See Actionable findings.
- `defaultMaewFallbackURL()` bundle availability: [probed]. `project.yml` includes `Fixtures/maew` as a resource, and all new `PetAssetResolverTests` initializers pass `maewFallbackURL`, so tests do not call the default.
- Output stability across schema-version drift: [N/A — reason: this diff adds an in-memory resolver and no persisted artifact/schema writes].
- CLI flag/arg symmetry: [N/A — reason: this diff adds no CLI flags or args].
- Error-class breadth in `catch` blocks: [probed]. The broad `catch` only surrounds `codexLoader`; `codogotchiLoader` is invoked with `try?`, so Codogotchi load errors soft-degrade to `nil` without entering fallback. Broad `CodexPet` error fallback matches the ticket’s missing/corrupt asset behavior.
- Defensive layering at module boundaries: [probed]. Production defaults pass `url.path` into `CodexPet(petDirectory:)` / `CodogotchiPet(petDirectory:)`, which rebuild file URLs. No unusual URL scheme enters production, but unsanitized path components still matter for file-path traversal.
- Cross-file atomicity windows: [N/A — reason: this diff writes no files].
- Test-contract strength: [probed]. Identity assertions are meaningful because `CodexPet` is a `final class`. Fallback tests prove non-throwing Maew fallback, but only inspect `lastPathComponent`, not exact fallback URL; noted under Advisory Observations.
- Doc-vs-code drift in the ticket Rationale: [probed]. The rationale’s loader injection, Maew fallback cache, thread-safety deferral, and default real loaders match the implementation. No behavior drift found.

**Actionable findings**

- `apps/menubar/Sources/PetAssetResolver.swift:84` — `petsBaseURL(petId:)` appends unsanitized `petId` directly, so a value like `"../evil"` escapes the canonical pets root after filesystem path normalization and can make the resolver load `~/.codogotchi/evil` instead of `~/.codogotchi/pets/<petId>`. This is a correctness/security gap under finding-discipline clause 2 and the path-injection attack surface; `AssignmentsJsonReader` also accepts arbitrary non-empty pet IDs from disk. Fix: validate pet IDs before path construction, rejecting `/`, `\`, empty components, `"."`, `".."`, and percent-decoded separators, or construct the URL then require `standardizedFileURL.path` to remain under the standardized pets root before invoking loaders.

**Advisory Observations**

- `apps/menubar/Tests/MenubarTests/PetAssetResolverTests.swift:94` and `:112` distinguish fallback by `url.lastPathComponent == DEFAULT_PET_NAME`, so they would still pass if the resolver loaded `~/.codogotchi/pets/maew` instead of the injected `maewFallbackURL`. The implementation currently uses the injected URL correctly; this is only a test-contract blind spot.

**Runner termination**

runnerStatus: completed
terminatedReason: review finished against the requested invariants, attack surfaces, implementation, tests, and ticket rationale.
