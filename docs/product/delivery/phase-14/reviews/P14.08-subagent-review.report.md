**Invariant results**

1. [held] `makeCard(for:)` adds the import button only under `entry.state == .importable` and the assign button only under `entry.state == .installed`; no shared branch adds both.
2. [broken] Successful assign/unassign changes reach `onAssignmentsChanged`, but clicking the checked Default item calls `unassign(badge:"default")`, which is a model no-op and does not fire the callback.
3. [broken] Badge pills re-read `viewModel.badges(for:)`, but the blue Default border uses cached `entry.isDefault`; after Default reassignment, `rebuildGrid()` can redraw pills from fresh state while leaving the border on stale cached catalog entries.

**Surface results**

- `makeCard(for:)` entry-state guard: [probed]. Import uses a standalone `if entry.state == .importable`; assign uses `if entry.state == .installed` in the name row. With only two enum cases, a normal card cannot present both.
- `badgeMenuItemClicked(_:)` default unassign path: [probed]. A checked Default menu item is presented as toggleable, then routes to `PetTabViewModel.unassign`, which returns immediately for `"default"`. See Actionable findings.
- `objc_setAssociatedObject` key collisions: [probed]. `assignBtnKey`, `badgeMenuItemKey`, and `importBtnKey` are distinct static vars, and `actionKey` has no remaining references.
- `rebuildGrid()` after badge toggle: [probed]. No async race found, but `rebuildGrid()` does not refresh `currentEntries`, so Default-border state can be stale after a Default reassignment. See Actionable findings.
- `onAssignmentsChanged` wiring completeness: [probed]. The chain exists, but it has no changed-badge/origin context and reaches `reloadActivePet()`, which loops every active pool window. See Actionable findings.
- Output stability across schema-version drift: [N/A — pure AppKit/view wiring; no persisted artifact or schema output changed].
- CLI flag/arg symmetry: [N/A — no CLI flags or arguments changed].
- Error-class breadth in `catch` blocks: [probed]. `try? viewModel.assign` and `unassign` failures are silent. Normal UI guards make `invalidBadge`/`petNotAssignable` unreachable, but write failures still produce no user feedback.
- Defensive layering at module boundaries: [probed]. The ObjC bridge casts can fail silently, but the same code creates and consumes each association with unique keys; no reachable normal-path type mismatch found.
- Cross-file atomicity windows: [probed]. Persistence happens before UI rebuild and live-swap callback. No write/UI race found, but stale cached catalog state creates a UI consistency bug covered above.
- Test-contract strength: [probed]. `PetTabLayoutTests` checks top alignment and action-button shape, but does not seed/assert badge pills or the Default-border variant.
- Doc-vs-code drift in ticket Rationale: [probed]. The star pill implementation uses `"⭐ Default"` text, while `PlatformDefault.imageset/star.svg` exists for the shared window badge; this matches the documented shared star iconography well enough. No doc/code drift found.

**Actionable findings**

- `apps/menubar/Sources/SettingsWindowController.swift:819`, `apps/menubar/Sources/SettingsWindowController.swift:884`, `apps/menubar/Sources/SettingsWindowController.swift:973`, `apps/menubar/Sources/SettingsWindowController.swift:1152` — `rebuildGrid()` rebuilds cards from cached `currentEntries`, while the border uses `entry.isDefault`; after assigning Default to another pet, the Default pill can move but the 2pt accent border stays on the old holder. Breaks invariant 3. Fix: either refresh `currentEntries = viewModel.catalog()` before rebuilding after assignment changes, or derive the border from live `viewModel.badges(for: entry.id).contains("default")`; add a layout/model-backed test that reassigns Default and asserts the border moves.

- `apps/menubar/Sources/SettingsWindowController.swift:141`, `apps/menubar/Sources/MenubarApp.swift:287`, `apps/menubar/Sources/MenubarApp.swift:462` — assignment changes route through `onAssignmentsChanged -> onPetActivated -> reloadActivePet()`, and `reloadActivePet()` calls `replacePet` for every active origin. This violates the ticket’s “affected platform only / unrelated windows untouched” contract. Fix: pass assignment-change context through the callback and replace only the affected active window key; for Default changes, compute before/after resolved pet IDs and update only active origins whose resolved pet changed, plus `combined` when active.

- `apps/menubar/Sources/SettingsWindowController.swift:1130`, `apps/menubar/Sources/SettingsWindowController.swift:1147`, `apps/menubar/Sources/PetTabViewModel.swift:180` — the current Default holder shows Default as a checked menu item, but clicking it calls `unassign("default")`, which no-ops and does not fire `onAssignmentsChanged`. Breaks invariant 2 for the Default toggle path. Fix: do not present the current Default assignment as deselectable; disable that item or handle it as a no-op before routing to `unassign`, with a focused UI/view-model test.

**Advisory Observations**

- `apps/menubar/Tests/MenubarTests/PetTabLayoutTests.swift` constructs `PetTabViewModel` without an `assignmentsURL`, so the test can read the developer’s real `~/.codogotchi/assignments.json`. It also does not cover the badge-pill row or Default-border rendering path, which are central to this ticket’s UI contract.

- `badgeMenuItemClicked(_:)` silently discards `assign` errors via `try?`, and `unassign` silently returns on write failure. The normal UI makes validation errors unlikely, but a persistence failure leaves the user with no feedback beyond the menu/grid not changing.

**Runner termination**

runnerStatus: completed
terminatedReason: Review finished against the filled P14.08 prompt with static repo inspection and no file edits.
