**Invariant results**
1. `[held]` — `update(snapshot:)` refreshes `currentAssignments` each tick and passes `resolve(origin:)` into the factory for own-mode spawns and `resolve(origin: "combined")` for combined spawns.
2. `[held]` — `replacePet(origin:...)` computes one `windowKey(for:)` and calls `replacePets` only through `windows[key]?`.
3. `[held]` — Step 8 calls `applyPlatform(origin: "combined")` once in the idle branch and has no `"combined"` platform call in the non-idle branch.

**Surface results**
1. `[probed]` — Existing windows do not receive a new `petId` from `update`; only newly spawned windows do. That is acceptable only if assignment changes always call a scoped live-swap, but `MenubarApp.reloadActivePet()` currently loops every active window.
2. `[probed]` — `petAssetResolver == nil` is not reachable in the normal launched-pool path: if `CodexPet()` fails, `renderer` and pool are nil and `reloadActivePet()` returns at its guard. If the resolver is externally nulled while a renderer exists, the loop silently skips.
3. `[probed]` — `PlatformAttribution(origin: "combined")` maps to `.default`, and `FloatingPetPanel.applyPlatform` reaches the animation badge path. The rendering path then looks up `NSImage(named: "PlatformDefault")`, but that asset is missing.
4. `[probed]` — No trailing-slash bug was found in `DemoConfig.pollingTarget`; it builds `.../state.d` without a trailing slash. However, the migration derives `configURL` from `HOME/.codogotchi/state.d`, while `assignmentsURL` respects `CODOGOTCHI_HOME`.
5. `[probed]` — Absent/malformed assignments files return `.safeDefault`; schema-version drift is tolerated because the reader ignores `schema_version` and requires only a non-empty `default`.
6. `[N/A — no CLI changes]` — The ticket does not add or alter CLI flags/args.
7. `[probed]` — The factory’s `try? resolver.resolve` falls back to the already-loaded app pet if resolver loading throws. In `reloadActivePet`, a thrown resolve simply skips that window.
8. `[probed]` — Assignment-sourced `petId` still flows through `PetAssetResolver.resolve`, so the path traversal guard remains in force before non-default pet directories are loaded.
9. `[probed]` — `reloadActivePet()` and pool updates are `@MainActor` and contain no suspension points, so the `evictAll()` / resolve loop is serial with poll ticks.
10. `[probed]` — `testCombinedWindowAppliesDefaultBadgeWhenIdle` does induce the idle branch with idle snapshots, but it does not assert exact call count, non-idle absence, asset existence, or stale badge clearing.
11. `[probed]` — The idle branch fires only for idle winners. The code does not preserve active combined-window platform badging; a non-idle combined winner never applies `winner.sourceEvent?.origin`.

**Actionable findings**
1. `CODOGOTCHI_HOME` migration reads the wrong config: [MenubarApp.swift](/Users/cesar/code/codogotchi_p14_05/apps/menubar/Sources/MenubarApp.swift:159) seeds `$CODOGOTCHI_HOME/assignments.json` but derives `configURL` from `DemoConfig.pollingTarget`, which uses `HOME/.codogotchi/state.d`. A user with custom `CODOGOTCHI_HOME/config.json` and `pet: "custom"` gets `assignments.default = maew` instead of their existing pet. Use `PetConfig.configURL()` for the migration source.
2. The Default badge cannot render the required star: [PlatformAttribution.swift](/Users/cesar/code/codogotchi_p14_05/apps/menubar/Sources/PlatformAttribution.swift:19) maps `.default` to `PlatformDefault`, and [FloatingPetPanel.swift](/Users/cesar/code/codogotchi_p14_05/apps/menubar/Sources/FloatingPetPanel.swift:1172) loads that as an asset, but `Assets.xcassets` has no `PlatformDefault.imageset`. The combined idle badge will show an empty chip, not ⭐ Default.
3. Combined active-state platform badging regresses: [FloatingPetWindowPool.swift](/Users/cesar/code/codogotchi_p14_05/apps/menubar/Sources/FloatingPetWindowPool.swift:211) applies state/attention for the combined winner, but only calls `applyPlatform("combined")` in the idle branch. A non-idle combined winner with `sourceEvent.origin = "cursor"` gets no cursor badge, and after an idle tick can keep a stale Default chip.
4. App-level live-swap is not scoped: [MenubarApp.swift](/Users/cesar/code/codogotchi_p14_05/apps/menubar/Sources/MenubarApp.swift:443) loops `floatingPetWindowPool?.activeOrigins` and calls `replacePet` for all active windows. If this is used for a single platform assignment change, unrelated windows are reloaded despite the ticket’s “only that platform” contract.

**Advisory Observations**
`PlatformAttributionTests` still describes only five coding platforms and does not cover `"combined" -> .default`; adding that plus an asset-existence or rendering-path assertion would catch the missing-star issue.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: completed review per template.
