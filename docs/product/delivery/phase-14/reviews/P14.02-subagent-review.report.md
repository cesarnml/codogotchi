**Invariant results**

1. [held] `resolveConfigPath("pet")` returns `null`; `configSet` with `path: "pet"` rejects through the `resolved === null` branch with `ConfigCommandError`.
2. [held] `runSetup` and `runRpg` build config literals without `pet`; focused tests assert both returned config/on-disk setup output lacks `pet`, and direct source read found no reassignment.
3. [held] A raw on-disk `pet` key survives `configSet({ path: "handle" })` verbatim; direct probe rewrote `handle` and preserved `pet: "custom"`.

**Surface results**

- `writeConfig` merge strategy: [probed]. `rawBase ? { ...rawBase, ...config } : config` preserves top-level unknown `pet` while schema-owned `features` replaces raw nested content. Direct probe confirmed `pet` stayed and `features.extra` was dropped, matching the stated shallow-merge rationale.
- `readRawConfig` absent/corrupt behavior: [probed]. ENOENT returns `null`; corrupt JSON rethrows `SyntaxError`; missing-config `configSet` still exits via `ConfigCommandError` exit code 2 before writing.
- `resolveConfigPath("pet")` path: [probed]. `pet` is absent from `SETTABLE_TOP_LEVEL`, so `resolveConfigPath("pet")` returns `null` and `configSet` throws `Unknown or read-only config key: pet`.
- `configGet({ path: "pet" })`: [probed]. `getDottedValue` has no `pet` special case and `SETTABLE_TOP_LEVEL` does not include `pet`; direct probe returned `ConfigCommandError: Unknown config key: pet`.
- `runRpg` setup path: [probed]. Both `runSetup` and `runRpg` config literals omit `pet`; no other production `setup.ts` assignment reintroduces it.
- Output stability across schema-version drift: [probed]. No config schema-version marker changes. Legacy top-level `pet` parses because Zod strips unknown keys; `config list` succeeds but does not expose `pet`, and `config set` preserves it on disk.
- CLI flag/arg symmetry: [N/A — no CLI flag or argument shape was added or changed for this ticket].
- Error-class breadth in `catch` blocks: [probed]. The new raw-read catch only handles ENOENT and rethrows all other errors; no new broad swallow was introduced.
- Defensive layering at module boundaries: [probed]. `configSet` still validates through `readConfig` and `resolveConfigPath`; production `writeConfig` callers are limited to `configSet`, `runSetup`, and `runRpg`.
- Cross-file atomicity windows: [N/A — this ticket writes only `config.json`; the write path remains tmp-file plus rename, with no multi-file commit window].
- Test-contract strength: [probed]. Focused tests cover retired `pet`, setup omission, and raw top-level preservation; implementation message was verified directly though the test asserts the error class rather than exact prose.
- Doc-vs-code drift in the ticket Rationale: [probed]. `docs/product/delivery/phase-14/ticket-02-retire-config-pet.md`, the phase plan, and P14.03 migration ticket match the implementation: CLI stops owning `config.pet`, preserves leftover raw top-level `pet`, and defers migration to Swift.

**Actionable findings**

None.

**Advisory Observations**

None.

**Runner termination**

runnerStatus: completed
terminatedReason: review finished against all requested invariants and attack surfaces.
