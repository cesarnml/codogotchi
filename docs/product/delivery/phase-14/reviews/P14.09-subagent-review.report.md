**Invariant results**
[broken] README covers per-platform assignment and Minimalist mode, but it does not mention the retired `codogotchi config set pet <id>` command or the `~/.codogotchi/assignments.json` contract.

[held] START-HERE covers Phase 14 scope and CLI command ownership; the retrospective captures the Phase 14 deferrals and follow-up dependencies.

[held] Cross-agent transient pet-window behavior is documented in the retrospective as expected TTL-aging behavior, not a bug.

[held] Version mirrors are consistent: `Info.plist`, `project.yml`, and `web/src/pages/download.astro` all report `2.1.0`; `PlistBuddy` also reads `2.1.0`.

[held] The retrospective exists at `docs/product/retrospectives/phase-14-per-platform-pet-identity-retrospective.md` and follows the `soa-write-retrospective` section order.

**Surface results**
[probed] README P14.09 coverage: inspected `README.md` and searched for `config.pet`, `config set pet`, and `assignments.json`. The v2.1.0 paragraph covers assignment and Minimalist mode, but misses the breaking CLI note and on-disk assignment contract.

[probed] START-HERE/status coverage: inspected `START-HERE.md` and the retrospective. START-HERE describes `assignments.json`, v2.1.0, Settings → Pet, Minimalist mode, and removed `config set pet`; deferrals are captured in the retrospective follow-up.

[probed] Cross-agent visualization behavior: inspected the retrospective. It explicitly says a non-primary platform subagent can spawn that platform’s transient pet window and that the window ages out via TTL.

[probed] Version mirrors: used `PlistBuddy`, `rg`, and diff inspection across app and web version mirrors. `Info.plist`, `project.yml`, and `download.astro` are aligned at `2.1.0`; no generated Xcode project version mirror was found.

[probed] Retrospective structure/content: compared the file to `soa-write-retrospective` required sections. It includes scope delivered, went well, pain points, surprises, what we'd do differently, net assessment, follow-up, and the requested durable learnings.

[probed] Stale `config.pet` / single-global-pet docs sweep: searched current docs outside `docs/product/delivery/**`. `CONTRIBUTING.md` still says the active pet is resolved from `config.json` and still describes `config.json` as holding the active pet.

[probed] Diff hygiene: ran `git diff --name-only` and `git diff --check`. The branch touches only allowed doc/metadata/web file types, plus the added delivery review prompt; whitespace checks pass.

[probed] Output stability across schema-version drift: inspected `AssignmentsJsonReader`, `CustomizationJsonReader`, and `packages/contracts/src/customization.ts`. Runtime contracts accept `assignments.json` schema v1 and `minimalist` at customization schema v1, but the customization contract doc is stale.

[N/A — no CLI flags or args changed in this doc/version ticket] CLI flag/arg symmetry.

[N/A — no new or modified `catch` blocks in the changed files] Error-class breadth in `catch` blocks.

[probed] Defensive layering at module boundaries: checked the only directly relevant boundaries, version metadata mirrors and docs-to-contract claims. No runtime boundary changed in this diff; docs drift is covered under findings.

[probed] Cross-file atomicity windows: checked the multi-file version bump and changed-file set. All three version mirrors moved together in the diff; no write path changed.

[probed] Test-contract strength: Red is explicitly skipped for this doc-only ticket. I used static verification (`rg`, `PlistBuddy`, `git diff --check`) only; that matches the ticket gate but leaves human review as the final test.

[probed] Doc-vs-code drift in Rationale and contract docs: read the ticket Rationale, README, START-HERE, CONTRIBUTING, `docs/contracts/customization-json.md`, and related Swift/TS contract code. README and contract/developer docs do not fully match the delivered Phase 14 behavior.

**Actionable findings**
- `README.md:32`: The ticket requires README to document the retired `config.pet` CLI command and new `assignments.json` contract, but README only describes Settings-driven per-platform assignment and Minimalist mode. This breaks the README invariant. Fix by extending the v2.1.0 paragraph or FAQ with a breaking-change note: `codogotchi config set pet <id>` is removed, Settings → Pet owns assignment, and assignments persist to `~/.codogotchi/assignments.json` schema v1.

- `CONTRIBUTING.md:18`, `CONTRIBUTING.md:72`, `CONTRIBUTING.md:77`, `CONTRIBUTING.md:124`: The developer onboarding doc still describes pet selection as a single active pet in `~/.codogotchi/config.json`. That contradicts Phase 14’s `assignments.json` contract and the ticket’s stale-reference sweep. Fix by describing Settings → Pet as per-platform assignment, replacing the active-pet paragraph with `assignments.json` Default + platform overrides, and changing the data table so `config.json` no longer owns active pet identity.

- `docs/contracts/customization-json.md:35`, `docs/contracts/customization-json.md:61`: The contract doc omits `"minimalist"` even though TS and Swift accept it, and it still says Phase 14 might bump `customization.json` to schema v2. This is contract doc-vs-code drift. Fix by adding `"minimalist"` to the platform mode values, documenting the schema remains v1, and replacing the Phase 14 forward note with the current Phase 15/per-thread deferral.

**Advisory Observations**
None.

**Runner termination**
runnerStatus: completed

terminatedReason: Reviewed all stated invariants and ticket/diff-derived surfaces to completion with no file writes.
