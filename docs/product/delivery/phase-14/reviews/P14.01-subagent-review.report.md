**Invariant results**

1. [held] Probed `assignmentsJsonSchema.parse` with missing `default`, `default: ""`, and default-only payload; missing/empty default throws, default-only succeeds with absent platform keys omitted.
2. [held] Probed a fully populated payload and optional-key behavior; all five platform overrides round-trip as non-empty strings, absent keys parse as `undefined`, explicit `undefined` is omitted, and `null` rejects.
3. [held] Probed `customizationJsonSchema.parse` with `"minimalist"`, existing `"own"`/`"combined"`/`"off"`, absent `platform_modes`, and `"hidden"`; valid modes succeed, default remains `{}`, and `"hidden"` rejects.

**Surface results**

- Required/optional boundary: [probed]. `default` is required while platform keys are optional. Zod `.optional()` did not bypass `.min(1)`: omitted/`undefined` platform keys are accepted, but `claude_code: ""` rejects.
- Empty-string rejection via `z.string().min(1)`: [probed]. `default: ""` rejects, and the same min-length guard applies to platform overrides.
- `z.literal(1)` schema_version guard: [probed]. Missing `schema_version`, `schema_version: 2`, and `schema_version: "1"` all reject.
- `"minimalist"` enum extension backward compatibility: [probed]. Existing mode values still parse and round-trip, `"minimalist"` parses, `"hidden"` rejects, and absent `platform_modes` still defaults to `{}`.
- `index.ts` re-export conflict: [probed]. Export-name scan across `packages/contracts/src` found no existing `AssignmentsJson` or `assignmentsJsonSchema` export to shadow.
- Output stability across schema-version drift: [probed]. The contracts diff adds a new `assignments.json` schema at `schema_version: 1` and additively extends `customization.json` values without changing `customization` schema version; no persisted artifact writer or CLI stdout shape is changed in these files.
- CLI flag/arg symmetry: [N/A — reason: this diff adds no CLI flags, parser changes, validators, help text, or downstream CLI consumers].
- Error-class breadth in `catch` blocks: [N/A — reason: this diff adds no `catch` blocks or error-handling paths].
- Defensive layering at module boundaries: [probed]. The new module boundary is the exported Zod schema itself; it validates `schema_version`, required `default`, optional override types, and non-empty pet IDs before producing `AssignmentsJson`.
- Cross-file atomicity windows: [N/A — reason: this diff performs no writes and introduces no multi-step state/artifact update].
- Test-contract strength: [probed]. New tests cover the main outcome paths and existing invalid `"hidden"` coverage remains, but they do not directly assert platform empty-string rejection or schema-version rejection; executable probes confirmed the implementation currently handles those cases.
- Doc-vs-code drift in ticket Rationale: [probed]. The ticket Rationale is still placeholder text with no concrete behavior claim, so there is no Rationale-vs-code conflict; related public contract doc drift is listed under Advisory Observations.

**Actionable findings**

None.

**Advisory Observations**

- `docs/contracts/customization-json.md` still documents valid `platform_modes` values as only `"own"`, `"combined"`, and `"off"`, while `packages/contracts/src/customization.ts` now accepts `"minimalist"` as a fourth value. This is doc-vs-code drift for the public contract, but P14.09 appears to own the docs sweep.

- `docs/contracts/customization-json.md` says Phase 14 would bump `schema_version` to `2` if it requires new fields, while `docs/product/delivery/phase-14/implementation-plan.md` explicitly says Minimalist is additive and `customization.json` stays `schema_version: 1`. The code follows the phase plan; the public contract doc’s forward note is stale.

**Runner termination**

runnerStatus: completed  
terminatedReason: review finished against all requested invariants and attack surfaces with no worktree modifications.
