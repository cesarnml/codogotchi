You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `EXPECTED_STATE_SCHEMA_VERSION = 8` in `StateJsonReader.swift`
- `SlicePayload` drops all RPG fields (`level`, `levelFraction`, `halfHearts`, `activeMinutes`, `lastActivityAt`, `reviveUntil`) — `Decodable` silently ignores them if present in v7 slices
- New `RpgStateReader.swift` (enum namespace, mirrors `StateJsonReader` style): reads `~/.codogotchi/rpg-state.json`, returns `RpgSnapshot`; absent file or any read failure returns safe defaults (level=1, levelFraction=0.0, halfHearts=MAX_HALF_HEARTS, activeMinutes=0, lastActivityAt=nil, reviveUntil=nil)
- `LivePollingDriver` reads `rpg-state.json` on the same 1Hz tick via `RpgStateReader`; existing `applyRPGState` sink receives values from `RpgSnapshot` instead of from `StateSnapshot`
- `bun run mac:test` passes

**Rationale:**
- RPG fields moved from slice files to `rpg-state.json` in schema v8
- `makeDriver` in tests gained injectable `rpgStatePath` parameter; absent → safeDefault (no decay, no revive)
- `StateSnapshot` still carries RPG fields for `HalfHeartDecayEngine` path — they become unused placeholder zeros until ticket 04 removes them
- `reviveUntil` now comes from `rpgSnapshot`, not from the slice

### Files touched

Implementation:
- `apps/menubar/Sources/RpgStateReader.swift` (new)
- `apps/menubar/Sources/CodogotchiFolders.swift` (added `rpgStatePath()`)
- `apps/menubar/Sources/StateJsonReader.swift` (bumped version to 8, removed RPG fields from `SlicePayload`, fixed `readDirectoryImpl` to use safe defaults for RPG)
- `apps/menubar/Sources/LivePollingDriver.swift` (added `rpgStatePath` param, reads `RpgStateReader` per tick, routes `rpgSnapshot` values to decay engine and `applyRPGState`)

Tests:
- `apps/menubar/Tests/MenubarTests/RpgStateReaderTests.swift` (new — 4 cases)
- `apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift` (schema v8 version tests, v9 schemaNewer test)
- `apps/menubar/Tests/MenubarTests/LivePollingTests.swift` (added `rpgStatePath` injection, fixed decay + revive tests)

### Invariants to hold

1. `RpgStateReader.read(at:)` must never throw or propagate IO errors — any absent file, unreadable file, or malformed JSON must return `RpgSnapshot.safeDefault` (level=1, levelFraction=0.0, halfHearts=6, activeMinutes=0, lastActivityAt=nil, reviveUntil=nil).
2. `HalfHeartDecayEngine` and `resolveReviveState` must receive RPG values from `RpgSnapshot`, not from the slice-derived `StateSnapshot` — the `decide()` function must not read `snapshot.halfHearts`, `snapshot.lastActivityAt`, or `snapshot.reviveUntil` for any RPG computation after this ticket.
3. `EXPECTED_STATE_SCHEMA_VERSION = 8` means a `state.json` with `schema_version: 8` must parse successfully, and a payload with `schema_version: 9` must return `.schemaNewer(got: 9, expected: 8)`.

### Attack surfaces to probe

1. **`RpgStateReader.read(at:)` error suppression** — does every error path (file not found, unreadable data, malformed JSON, valid JSON with wrong structure) truly return `safeDefault`? Probe the `Data(contentsOf:)` failure path and the `try? decoder.decode` failure path separately. Is there any path that returns partial data (e.g., only some fields from a partially-valid payload)?
2. **`decide()` RPG routing** — confirm that after this ticket, `snapshot.halfHearts`, `snapshot.lastActivityAt`, and `snapshot.reviveUntil` are not read inside `decide()`. If any reference remains, the decay engine silently reads stale values (always zero/nil from the slice-derived snapshot in v8).
3. **`rpgStatePath` nil vs. absent-file behavior** — `rpgStatePath: nil` causes `runTick` to use `.safeDefault` directly (the `map` returns `nil`, `?? .safeDefault` fires). An non-nil path to a non-existent file causes `RpgStateReader.read(at:)` to return `.safeDefault` via the `Data(contentsOf:)` guard. Both paths produce the same result. Probe whether any test or caller accidentally passes `nil` thinking "no file" when the default wires the production path (`CodogotchiFolders.rpgStatePath()`).
4. **`SlicePayload` RPG field removal and backward compat** — v7 slices still have RPG fields in the JSON. After removing them from `SlicePayload`, `Decodable` silently ignores those keys. Probe whether any slice test fixture still expects RPG values from a slice read and would silently regress.
5. **`readDirectoryImpl` snapshot construction** — the winner-slice snapshot now hardcodes RPG safe-defaults (level=1, halfHearts=6, etc.) rather than reading from the slice. Confirm no existing test asserts that `readDirectory` returns a non-default `halfHearts` or `level` from a slice payload — that would be a silent regression.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — v7 slices with RPG fields are silently tolerated by the new `SlicePayload`; `readDirectoryImpl` returns hardcoded safe defaults rather than the slice's RPG values. Any consumer reading RPG from `readDirectory` output will silently get wrong values. Probe whether `LivePollingDriver` is the only such consumer, and whether any direct `readDirectory` call in app code (not tests) reads RPG fields from the returned snapshot.
2. **CLI flag/arg symmetry** — `LivePollingDriver.init` gained an `rpgStatePath` parameter with a default. Probe that `MenubarApp` (the production wiring site) does not need an explicit argument — the default `CodogotchiFolders.rpgStatePath()` must resolve correctly in the production environment, respecting `CODOGOTCHI_HOME`.
3. **Error-class breadth in `catch` blocks** — `RpgStateReader.read` uses `try?` for both `Data(contentsOf:)` and `decoder.decode`, silently swallowing all errors. Probe whether this is intentional (ticket says "any read failure returns safe defaults") or whether some error class (e.g., a permissions error vs. a missing file) should be logged for debugging.
4. **Defensive layering at module boundaries** — `LivePollingDriver` passes `rpgStatePath` as a raw `String?` to `RpgStateReader.read(at:)`, which then constructs a `URL`. No path normalization or existence pre-check occurs. Probe whether a path like `""` (empty string from a misconfigured default) or a path with trailing whitespace causes unexpected behavior vs. the guard returning safeDefault.
5. **Cross-file atomicity windows** — `runTick()` reads `reader(pollingTargetPath)` and `RpgStateReader.read(at: rpgStatePath)` in two separate calls. If the CLI writes `state.d/<slice>` and `rpg-state.json` non-atomically, the driver could read a new slice with a stale RPG file (or vice versa) in the same tick. Probe whether this is an acceptable window given 1Hz polling and the ticket's explicit "1Hz poll latency is imperceptible" rationale.
6. **Test-contract strength** — the new `RpgStateReaderTests` cover absent, valid, partial, and malformed paths. Probe whether the "missing individual fields" test covers each field independently, or only covers all-absent. Is there a test that asserts a partially-decoded payload (e.g., only `level` present) returns the correct per-field default for the absent fields?
7. **Doc-vs-code drift in the ticket Rationale** — the Rationale says "`StateSnapshot` still carries RPG fields for the `HalfHeartDecayEngine` path" and "they become unused placeholder zeros". Confirm that `StateSnapshot` was not modified in this diff and that `HalfHeartDecayEngine` still reads from its parameters (not directly from `StateSnapshot`) — if the decay engine signature changed, the Rationale is stale.

### Diff context

Key changes:
- `RpgStateReader.swift` (new): enum namespace with `read(at:) -> RpgSnapshot`. Uses `try? Data(contentsOf:)` then `try? decoder.decode(RpgPayload.self, from: data)`. All optional fields on `RpgPayload` default via `?? safeDefault-value`. `RpgSnapshot.safeDefault` is a static constant.
- `StateJsonReader.swift`: `EXPECTED_STATE_SCHEMA_VERSION` bumped 7→8. `SlicePayload` loses 6 RPG fields (`level`, `levelFraction`, `halfHearts`, `activeMinutes`, `lastActivityAt`, `reviveUntil`). `readDirectoryImpl` snapshot construction now hardcodes `level: 1, levelFraction: 0.0, halfHearts: MAX_HALF_HEARTS, activeMinutes: 0, lastActivityAt: nil, reviveUntil: nil`.
- `LivePollingDriver.swift`: `rpgStatePath: String?` added to stored properties and `init`. `runTick()` reads `rpgSnapshot = rpgStatePath.map { RpgStateReader.read(at: $0) } ?? .safeDefault`. `decide()` gains `rpgSnapshot: RpgSnapshot` parameter; `resolveReviveState` uses `rpgSnapshot.reviveUntil`; `HalfHeartDecayEngine.displayed` uses `rpgSnapshot.halfHearts` and `rpgSnapshot.lastActivityAt`; `rpgState` tuple in `Outcome` uses all four `rpgSnapshot` fields.
- `CodogotchiFolders.swift`: adds `rpgStatePath() -> String` static helper using `dataFolderURL().appendingPathComponent("rpg-state.json").path`.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
