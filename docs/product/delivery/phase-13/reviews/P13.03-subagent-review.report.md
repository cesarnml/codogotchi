I now have enough information to write the complete review. Let me compile my findings.

---

**Invariant results**

**Invariant 1 — `RpgStateReader.read(at:)` never throws:** *held.* All error paths are covered by `try?` guards. `guard let data = try? Data(contentsOf: url) else { return .safeDefault }` catches file-not-found, permissions errors, and unreadable-data. `guard let payload = try? decoder.decode(RpgPayload.self, from: data) else { return .safeDefault }` catches malformed JSON and structurally invalid payloads. All six fields on `RpgPayload` are `Optional`; partial payloads decode successfully and fall back per-field via `?? safeDefault-value` in the `RpgSnapshot` constructor. The `String??` nil-coalescing for `lastActivityAt` and `reviveUntil` correctly maps both absent key (outer `.none`) and explicit JSON null (`.some(.none)`) to `nil`, matching the same pattern used in `StatePayload`.

**Invariant 2 — `decide()` reads RPG from `rpgSnapshot`, not from `StateSnapshot`:** *held.* In the `.success(let snapshot)` arm of `decide()`: `resolveReviveState` receives `rpgSnapshot.reviveUntil` (line 273); `HalfHeartDecayEngine.displayed` receives `rpgSnapshot.halfHearts` and `rpgSnapshot.lastActivityAt` (lines 286–289); the `rpgState` tuple in `Outcome` is built from `rpgSnapshot.levelFraction`, `.level`, `.activeMinutes`, and `displayedHalfHearts` (lines 298–301). There are zero references to `snapshot.halfHearts`, `snapshot.lastActivityAt`, or `snapshot.reviveUntil` in the `decide()` body.

**Invariant 3 — Schema version gate:** *held.* `EXPECTED_STATE_SCHEMA_VERSION = 8` confirmed in `StateJsonReader.swift:8`. `testSchemaVersion8ParsesSuccessfullyAfterV8Bump` constructs a `schema_version: 8` payload and asserts `.success`. `testSchemaVersion9FailsWithSchemaNewer` constructs `schema_version: 9` and asserts `.schemaNewer(got: 9, expected: 8)`. The fixture-based `testNewerSchemaVersionFailsWithSchemaNewer` asserts `got == 99, expected == 8`. All three pass through `EXPECTED_STATE_SCHEMA_VERSION = 8`.

---

**Surface results**

**S1 — `RpgStateReader.read(at:)` error suppression:** *probed — clean.* Verified the four failure paths separately. File not found: `guard let data = try?` returns `.safeDefault`. Permissions/unreadable: same guard, same result. Malformed JSON (e.g., `"{ not valid json"`): second `guard let payload = try?` returns `.safeDefault`. Valid JSON with wrong structure (e.g., `{"level": "not-an-int"}`): `Int?` decoding fails, falls to `.safeDefault`. Empty data: `Data(contentsOf:)` succeeds but `decoder.decode` fails, `.safeDefault`. No path returns partial data — the snapshot is either fully constructed from the payload or fully replaced by `.safeDefault`.

**S2 — `decide()` RPG routing:** *probed — clean.* Confirmed under Invariant 2 above. Searched `LivePollingDriver.swift` for all references to `snapshot.halfHearts`, `snapshot.lastActivityAt`, and `snapshot.reviveUntil`; grep returns zero hits in `decide()`.

**S3 — `rpgStatePath` nil vs absent-file behavior:** *probed — clean.* `rpgStatePath = nil` → `rpgStatePath.map { ... } ?? .safeDefault` short-circuits immediately. Non-nil path to non-existent file → `RpgStateReader.read(at:)` → `Data(contentsOf:)` fails → `.safeDefault`. Both produce identical `RpgSnapshot.safeDefault`. `makeDriver` in tests defaults `rpgStatePath: URL? = nil`, so every test that omits the argument exercises the direct `.safeDefault` path; the production `LivePollingDriver.init` default `CodogotchiFolders.rpgStatePath()` exercises the file-read path. The distinction is correct, but see Finding 1 below for how this interacts with two legacy tests.

**S4 — `SlicePayload` RPG field removal and backward compat:** *probed — clean.* `SlicePayload` has no RPG fields. v7-format slices with embedded RPG keys (e.g., `level`, `half_hearts`, `revive_until`) are decoded silently without error because Swift's `Decodable` ignores unknown keys. The `SliceDirReaderTests` fixtures in `sliceJSON()` carry `hp` and `hp_overlay` keys (also not in `SlicePayload`) and decode cleanly. No test in `SliceDirReaderTests` asserts RPG values from the returned snapshot — all assertions are on `activityState` only.

**S5 — `readDirectoryImpl` snapshot construction:** *probed — clean.* The hardcoded safe defaults (`level: 1, levelFraction: 0.0, halfHearts: MAX_HALF_HEARTS, activeMinutes: 0, lastActivityAt: nil, reviveUntil: nil`) in `readDirectoryImpl` (lines 214–219 of `StateJsonReader.swift`) are passed into `StateSnapshot`. No test in `SliceDirReaderTests` or `StateJsonReaderTests` asserts a non-default `halfHearts` or `level` value from `readDirectory(at:)`. The v5/v6 RPG-field assertions in `StateJsonReaderTests` (lines 462–502) all go through `StateJsonReader.read(at:)` (single-file path), which still decodes RPG fields via `StatePayload` for backward compat. No silent regression there.

**SD1 — Output stability across schema-version drift:** *probed — clean.* `LivePollingDriver` is the only production consumer of `readDirectory` output that then handles RPG. It reads RPG from `rpgSnapshot` (not the snapshot returned by `readDirectory`). `MenubarApp.swift` confirms the wiring: `driver.applyRPGState` closure routes to `floatingPetController.applyRPGState`, which receives values from `emit()` in `LivePollingDriver`. No other call site in `Sources/` invokes `readDirectory` and then reads RPG fields from the result.

**SD2 — CLI flag/arg symmetry:** *probed — clean.* `MenubarApp.swift` line 288 calls `LivePollingDriver(pollingTargetPath: config.pollingTarget.path, gatePath: gateJsonPath, deliveryContextPath: deliveryContextPath, apply: ..., setTooltip: ..., ...)` without passing `rpgStatePath:`. The default `CodogotchiFolders.rpgStatePath()` resolves via `dataFolderURL().appendingPathComponent("rpg-state.json").path`, and `dataFolderURL()` checks `CODOGOTCHI_HOME` first (line 10–13 of `CodogotchiFolders.swift`). The production wiring is correct and respects the env override.

**SD3 — Error-class breadth in `catch` blocks:** *probed — spec-permits.* `try?` swallows all error classes uniformly, including permissions errors that are operationally distinct from missing files. The ticket spec says "any read failure returns safe defaults," so this behavior is explicitly permitted. There is no logging on failure paths in `RpgStateReader.read(at:)`. See Advisory Observations.

**SD4 — Defensive layering at module boundaries:** *probed — clean.* An empty-string path `""` creates a `URL(fileURLWithPath: "")` that resolves to the process working directory; `Data(contentsOf:)` on a directory URL raises an error → `guard let data = try?` → `.safeDefault`. A path with trailing whitespace (`" /bad"`) fails file I/O similarly. Both edge cases are handled by the same guard.

**SD5 — Cross-file atomicity windows:** *probed — within spec.* `runTick()` reads the slice directory and `rpg-state.json` in two separate `Foundation` calls with no atomic guarantee between them. A CLI write that updates `state.d/<slice>` and `rpg-state.json` non-atomically could produce a 1-tick window where a new slice is paired with a stale RPG file (or vice versa). The worst observable effect is a one-tick delay in RPG value update at 1Hz. The ticket Rationale explicitly calls this an acceptable window. Not a finding.

**SD6 — Test-contract strength (missing-field coverage):** *probed — adequate at unit level.* `testMissingOptionalFieldsFallBackToDefaults` (line 52) writes `{"level": 7}` and asserts that all five absent fields (`levelFraction`, `halfHearts`, `activeMinutes`, `lastActivityAt`, `reviveUntil`) return their respective defaults. Since each field in `RpgPayload` is independently optional and defaults are applied per-field in the `RpgSnapshot` constructor, this combinatorial test is sufficient to establish that any subset of absent fields will default correctly by structural induction. No independent per-field isolation test is strictly required.

**SD7 — Doc-vs-code drift in the ticket Rationale:** *probed — no drift.* Rationale claim: "`StateSnapshot` still carries RPG fields for `HalfHeartDecayEngine` path." Confirmed: `StateSnapshot` definition is unchanged; it still has `level`, `levelFraction`, `halfHearts`, `activeMinutes`, `lastActivityAt`, `reviveUntil`. Rationale claim: "They become unused placeholder zeros." Confirmed: `readDirectoryImpl` hardcodes those fields to safe defaults; `decide()` ignores them in favor of `rpgSnapshot` for all RPG computations. Rationale claim: "`reviveUntil` now comes from `rpgSnapshot`, not from the slice." Confirmed: `resolveReviveState` in `decide()` receives `rpgSnapshot.reviveUntil`. Rationale claim: "`makeDriver` in tests gained injectable `rpgStatePath` parameter; absent → safeDefault." Confirmed: `makeDriver` in `LivePollingTests.swift` line 72 adds `rpgStatePath: URL? = nil`; the `LivePollingDriver` init receives `rpgStatePath?.path` which is `nil` when no URL is provided, triggering the `?? .safeDefault` branch.

---

**Actionable findings**

**Finding 1 — `testExpiredReviveUntilRendersBaseHookState` no longer exercises the expired-reviveUntil code path in `resolveReviveState`.**

File: `apps/menubar/Tests/MenubarTests/LivePollingTests.swift:247`

The test writes a v7-style slice embedding `"revive_until": "2020-01-01T00:00:00.000Z"` and passes no `rpgStatePath` to `makeDriver`. In v8 the slice's `revive_until` is ignored by `SlicePayload`, so `rpgSnapshot` is `.safeDefault` with `reviveUntil = nil`. The `guard let reviveUntil` in `resolveReviveState` fails on `nil` and returns `base` — never reaching the `parseGateISO8601Date` or `expiry > now` comparisons. The test asserts `[.implementing]` (correct), but the branch it was designed to cover (past-timestamp reviveUntil → base state) is dead in this test. A regression where `resolveReviveState` incorrectly activated `.revive` for an expired-but-present `reviveUntil` (e.g., the `>` flipped to `<`) would not be detected.

Finding-discipline clause: 2 (demonstrable correctness gap).

Fix: mirror `testFutureReviveUntilRendersReviveRow` — write an `rpg-state.json` with a past `reviveUntil` via `writeRpgStateJson(rpgTarget, reviveUntil: "2020-01-01T00:00:00.000Z")` and pass `rpgStatePath: rpgTarget` to `makeDriver`. The assertion stays the same (`[.implementing]`), but the expired-date guard branch in `resolveReviveState` is now exercised.

---

**Advisory Observations**

Three stale test function names will cause confusion in future diffs: `testExpectedSchemaVersionIs7` (line 110), `testExpectedSchemaVersionIsV4` (line 294), and `testExpectedSchemaVersionIsV5` (line 355) all assert `EXPECTED_STATE_SCHEMA_VERSION == 8`. The assertions are correct; the names are wrong. Low churn to fix (`rename` only).

`testNullLastActivityAtDoesNotDecay` (line 597) writes a v5-style slice with `lastActivityAt: nil` but omits `rpgStatePath`. After v8 the slice field is ignored and the driver runs with `.safeDefault` (halfHearts=6, lastActivityAt=nil). The test passes for a trivial reason — it exercises the safeDefault bypass path, not the "rpg-state.json with null last_activity_at → no decay" path. The unit-level coverage in `RpgStateReaderTests.testMissingOptionalFieldsFallBackToDefaults` ensures null `lastActivityAt` decodes correctly from the file, but the driver-level integration of that path is not tested. Not flagged as a clause-2 finding because the unit tests sufficiently isolate the behavior, but the test comment is now misleading.

`RpgStateReader.read(at:)` swallows all IO errors silently (intentional per spec). A permissions error on `~/.codogotchi/rpg-state.json` is indistinguishable from a missing file from the caller's perspective. In production the HUD will silently revert to safe defaults with no observable signal. Consider an `NSLog` on the file-read guard path in a future ticket for operational debuggability.

`writeV5StateJson` in `LivePollingTests` (line 116) still writes v5-style slices with embedded `level`, `level_fraction`, `half_hearts`, and `last_activity_at` fields. In v8 these are silently ignored by `SlicePayload`. The helper's parameter signature and its in-test comment ("Minimal slice payload for decay tests: fields the decay engine reads") are now describing fields that the slice reader ignores. The actual decay-test inputs come from `writeRpgStateJson`. No behavioral defect, but the helper name and docstring imply the slice fields are load-bearing.

**Runner termination**

runnerStatus: completed
terminatedReason: All invariants and attack surfaces were probed and resolved; one actionable finding reported with a concrete fix shape.
