**Invariant results**
1. [held] Static menubar icon: `MenubarApp` still wires `LivePollingDriver.apply` to `renderer.update`, but `MenubarRenderer.setStaticMode()` makes `paintCurrent()` return before calling the sink. The new test covers `update` and `replacePets`.
2. [broken] Monochrome toggle persistence/apply contract: the happy path is read-merge-write, but `GeneralTabViewModel.setMonochromeMenubarIcon` mutates in-memory state before persistence and `SettingsWindowController` applies the icon even if the write aborts or fails.
3. [held] Version bump: `Info.plist`, `project.yml`, and `web/src/pages/download.astro` all read `2.0.0`; no remaining `1.1.2` in those release-touch files.

**Surface results**
- [probed] `MenubarRenderer.update` static sink suppression: `paintCurrent()` gates on `isStaticMode`, so state updates can still occur internally while the status-item sink is suppressed.
- [probed] `MenubarApp.applicationDidFinishLaunching` static icon setup: startup applies the app icon and then calls `renderer.setStaticMode()`. Color mode passes `isTemplate = false`; monochrome passes `true`.
- [probed] `LivePollingDriver` render path: live polling still calls `apply(outcome.state, outcome.mode)` on render changes, and `MenubarApp` still maps that to `renderer.update`.
- [probed] Floating panel animation path: `LivePollingDriver.applyPerPlatform` still feeds `FloatingPetWindowPool.update`, so floating windows remain the animation surface.
- [probed] Monochrome setting write path: valid existing JSON is merged, preserving unmanaged keys; invalid/unwritable JSON aborts, but UI/icon state is still updated by the caller.
- [probed] Version bump locations: checked the three effective repo files and confirmed `2.0.0`.

- [probed] Output stability across schema-version drift: `customization.json` adds optional `menubar_icon_monochrome`; `CustomizationJsonReader` defaults missing key to `false`, so older files remain readable.
- [N/A — no CLI flags or args changed] CLI flag/arg symmetry.
- [probed] Error-class breadth in `catch` blocks: no new `catch`; new write code uses `try?`, which silently loses write failures and contributes to the actionable finding.
- [probed] Defensive layering at module boundaries: `CustomizationJsonReader` defends missing/malformed reads with defaults; the settings write boundary does not report success/failure to the UI caller.
- [probed] Cross-file atomicity windows: version files are independent release metadata; `customization.json` write uses `.atomic`, but write failure is ignored after local state has changed.
- [probed] Test-contract strength: static renderer sink suppression is tested; monochrome read decoding is tested through `CustomizationJsonReaderTests`; the new General-tab write path lacks failure-path coverage.
- [probed] Doc-vs-code drift in the ticket Rationale: the prompt references `apps/site/src/pages/download.astro`, but the repo change is `web/src/pages/download.astro`; the requested template path exists under `.son-of-anton/docs/...`, not `docs/template/...`.

**Actionable findings**
- `apps/menubar/Sources/GeneralTabViewModel.swift:101` and `apps/menubar/Sources/SettingsWindowController.swift:137`: `setMonochromeMenubarIcon` sets `menubarIconMonochrome = value` before it knows persistence succeeded, and the controller immediately calls `onMonochromeChanged?(isMonochrome)` regardless of whether the file was written. If `customization.json` exists but is invalid/unreadable, or if atomic write fails, the ticket’s “toggling it writes `menubar_icon_monochrome`” contract is false while the menu bar icon still flips to the unpersisted value. Fix by making the setter return a persisted Bool/result, only mutating view-model state after successful write, and only applying the status icon from the persisted value. Add a test with invalid existing JSON proving no clobber, no local state drift, and no icon callback on failed persistence.

**Advisory Observations**
- `bun run mac:test` passed: 592 tests, 0 failures. The run emitted AppKit unsatisfiable-constraint logs in `SettingsWindowOpenTests`, apparently from `GeneralTabView` being laid out at an autoresized width of 20 before the full window layout settles. It did not fail the ticket behavior, but it is a UI-quality signal worth tracking separately.
- The ticket’s version checklist names `apps/site/src/pages/download.astro`; this repo’s actual changed download page is `web/src/pages/download.astro`, and it is correctly bumped to `2.0.0`.

**Runner termination**
runnerStatus: completed
terminatedReason: Review completed against the diff, ticket invariants, and required surfaces; `bun run mac:test` passed.
