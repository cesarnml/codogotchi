Now I have enough information to write the review report.

---

**Invariant results**

**Invariant 1** — held. `customizationJsonSchema.parse({schema_version: 1, platform_modes: {}, idle_dismiss_ttl_seconds: 300, menubar_icon_monochrome: false})` must succeed. The schema accepts all four required fields at their stated valid values. `platform_modes: {}` satisfies `z.record(z.string(), z.enum([...]))` with no entries. The test at line 22–29 of `customization.test.ts` confirms absent `platform_modes` defaults to `{}`, which subsumes the non-empty case.

**Invariant 2** — held. `{...platform_modes: {claude_code: "hidden"}, ...}` must throw. `z.enum(["own", "combined", "off"])` is the value schema for `z.record`; `"hidden"` is not in the enum and will produce a ZodError. Confirmed by test at line 31–40.

**Invariant 3** — held. `{...platform_modes: {jetbrains: "own"}, idle_dismiss_ttl_seconds: 0, ...}` must succeed. `z.string()` as the key schema allows any string including unknown origins. `"own"` is valid. `z.number().int().min(0)` accepts 0. Confirmed by tests at lines 42–50 and 52–60.

---

**Surface results**

**Surface 1 — `schema_version` literal constraint.** Probed. `z.literal(1)` in Zod v4 is strict: it matches exactly the number `1`. Passing `schema_version: 2` would fail (value mismatch). Passing `schema_version: "1"` would fail (string vs number — Zod v4 does not coerce by default). No bypass path found.

**Surface 2 — `platform_modes` value schema enum validation.** Probed. The value validator is `z.enum(["own", "combined", "off"])`, applied to every value regardless of key. `"hidden"` → rejected (test confirms). `null` → rejected (not a string). `undefined` → rejected (not a string). Numeric `1` → rejected (not a string). Enum enforcement is not relaxed by the `z.string()` key schema.

**Surface 3 — `idle_dismiss_ttl_seconds` non-negative integer constraint.** Probed. `z.number()` rejects strings (no coercion). `.int()` rejects floats like `300.5`. `.min(0)` rejects `-1`. TTL `0` is accepted (`.min(0)` is inclusive). No gap found.

**Surface 4 — `index.ts` re-export name collision.** Probed. Scanned all existing barrel exports: `animation-state.ts`, `config.ts`, `decay-constants.ts`, `slice-entry.ts`, `soa-events.ts`, `state-json.ts`, `sync-profile.ts`, `username.ts`. Neither `customizationJsonSchema` nor `CustomizationJson` appear in any of those files. No collision.

**Surface 5 — Default-value propagation.** Probed. In Zod v4, `.default(x)` on an object field applies when the key is absent (the value arriving as `undefined`). The three defaulted fields (`platform_modes`, `idle_dismiss_ttl_seconds`, `menubar_icon_monochrome`) all carry `.default(...)`. The test at lines 22–29 verifies the absent-key → default path for `platform_modes`. The pattern holds for the other two fields by the same mechanism.

**Diff surface 1 — Output stability across schema-version drift.** Probed. `customization.ts` hardcodes `z.literal(1)` with no reference to `STATE_JSON_SCHEMA_VERSION` (currently `8` in `state-json.ts`). These are independent literals in separate files with distinct names. No confusion path between the two version spaces.

**Diff surface 2 — CLI flag/arg symmetry.** N/A — this ticket adds a Zod schema and TypeScript type only; no CLI argument parsers are touched.

**Diff surface 3 — Error-class breadth in `catch` blocks.** N/A — no try/catch blocks in the implementation.

**Diff surface 4 — Defensive layering at module boundaries.** Probed. Searched all CLI and Convex consumers of `@codogotchi/contracts` for calls to `customizationJsonSchema`. No existing callsite was found — `customization.ts` is imported only by `customization.test.ts` at this point. No unguarded parse sites created by this diff.

**Diff surface 5 — Cross-file atomicity windows.** N/A — schema definition is a pure module addition with no multi-step file writes.

**Diff surface 6 — Test-contract strength.** Probed. Tests assert structural behavior (parse succeeds, throws on invalid input), not implementation internals. Both the valid-input and invalid-input branches are covered. The `idle_dismiss_ttl_seconds: 0` edge case is explicitly tested (line 52–60). Gaps: no test asserts that `schema_version: 2`, `schema_version: "1"`, absent `schema_version`, float TTL `300.5`, negative TTL `-1`, or `null`/numeric values in `platform_modes` each throw. These are absence-of-coverage rather than known failures.

**Diff surface 7 — Doc-vs-code drift in the ticket Rationale.** Probed. Two separate drift issues found; see Advisory Observations below.

---

**Actionable findings**

None.

---

**Advisory Observations**

**A1 — Ticket `## Review Focus` contradicts `## Outcome`.** The Review Focus in `docs/product/delivery/phase-13/ticket-02-customization-contract.md` (line 39) says: "`platform_modes` should use `z.record(sourceEventOriginSchema, z.enum([...]))` not a plain `z.record` — ensure origin keys are validated." But the Outcome (line 13) explicitly requires: "Unknown `platform_modes` origin keys are tolerated (passthrough via `z.record`)." In Zod v4, `z.record(enumSchema, valueSchema)` requires all enum keys to be present, which also makes unknown keys impossible — both requirements contradict using `sourceEventOriginSchema` as the key validator. The implementation correctly satisfied the Outcome by using `z.string()`. The Review Focus note is misleading and should be updated to record the Zod v4 constraint and the deliberate choice of `z.string()`.

**A2 — Rationale placeholders never filled.** The `## Rationale` section of `ticket-02-customization-contract.md` (lines 47–48) still contains `Red first: [what test failed first]` and `Why this path: [why this implementation was the smallest acceptable]`. The non-obvious design decision — using `z.string()` as the key schema because Zod v4 changed `z.record(enumSchema)` semantics — has no permanent record in the ticket. This is the kind of decision that will confuse a future maintainer who sees the Review Focus note and wonders why the implementation diverges from it.

**A3 — Negative-path test coverage gaps.** No test asserts that any of the following throw: `schema_version: 2`, `schema_version: "1"`, absent `schema_version`, TTL `300.5`, TTL `-1`, or a numeric/null value in `platform_modes`. All of these should currently fail per the schema, but the absence of explicit tests means a schema weakening (e.g., accidentally adding `.optional()` to `schema_version`) would go undetected. These are low-effort tests and would close the coverage gap on the rejection-path invariants named in the review prompt.

**A4 — `schema_version` is silently required.** The Outcome spec says "Absent file or absent individual keys resolve to defaults," but `schema_version` carries no `.default()`. A customization.json without `schema_version` will hard-fail `.parse()`. The spec's defaults list (`platform_modes: {}`, TTL `300`, monochrome `false`) does not include a `schema_version` default, so this appears intentional. The downstream ticket-05 (Swift reader) and any JS/TS consumer reading the file should use `.safeParse()` and handle a missing/malformed file as a signal to apply all defaults from scratch rather than passing the raw parsed object through `.parse()`.

---

**Runner termination**

`runnerStatus`: completed
`terminatedReason`: All invariants and attack surfaces probed; no actionable findings; report delivered in full per the required format.
