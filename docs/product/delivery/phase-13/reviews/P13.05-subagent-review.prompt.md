You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- New `CustomizationJsonReader.swift` (enum namespace): reads `~/.codogotchi/customization.json`, returns `CustomizationSnapshot` with `platformModes: [String: PlatformMode]`, `idleDismissTtlSeconds: Int`, `menubarIconMonochrome: Bool`; absent file or any read failure returns all-"own", TTL 300, monochrome false
- `PlatformMode` enum: `own | combined | off`; unknown mode strings degrade to `own`
- `FloatingPetWindowPool` reads `CustomizationSnapshot` on each 1Hz tick; updates mode routing and TTL live without restart
- Setting an origin to "combined" on the next tick collapses its window into the shared window
- Setting an origin to "off" on the next tick removes it from the render pipeline
- `bun run mac:test` passes

**Review Focus from ticket:**
- TTL `0` must map to "never dismiss" in the pool — confirm the pool treats `idleDismissTtlSeconds == 0` as an infinite TTL, not an immediate dismiss
- Mode change on tick N must take effect before the pool dispatches state on tick N — read customization before routing, not after
- Unknown origin keys: Swift `Decodable` with a custom decode or a dictionary decode — confirm the implementation tolerates keys not in `SourceEventOrigin` without throwing
- `CustomizationJsonReader` must not cache between ticks — reads fresh from disk each time so Settings writes are reflected within one tick

**Rationale notes from ticket doc:**
- Alternative considered: observing `customization.json` via `DispatchSource` / FSEvents — rejected; 1Hz re-read is sufficient latency for a user toggling a settings picker, and avoids a new async delivery path.
- Deferred: `menubar_icon_monochrome` field is read here but not consumed until ticket 07.

### Files touched

Implementation:
- apps/menubar/Sources/CustomizationJsonReader.swift (new)
- apps/menubar/Sources/CodogotchiFolders.swift (added customizationPath())
- apps/menubar/Sources/FloatingPetWindowPool.swift (refactored to use customizationReader closure)
- apps/menubar/Sources/MenubarApp.swift (removed hardcoded ttlSeconds/platformModes)

Tests:
- apps/menubar/Tests/MenubarTests/CustomizationJsonReaderTests.swift (new — 5 tests)
- apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift (updated to customizationReader API)
- apps/menubar/Tests/MenubarTests/MenuItemsTests.swift (updated to customizationReader API)

### Invariants to hold

1. `CustomizationJsonReader.read(at:)` must return `CustomizationSnapshot.safeDefault` (empty `platformModes`, `idleDismissTtlSeconds: 300`, `menubarIconMonochrome: false`) when the file is absent OR when the file contents are malformed JSON.
2. `FloatingPetWindowPool.update(snapshot:)` must read `CustomizationSnapshot` as the first operation, before any call to `mode(for:)` — so mode routing and TTL are always coherent with the current tick's customization state, not the previous tick's.
3. When `CustomizationSnapshot.idleDismissTtlSeconds == 0`, the pool must never dismiss any window via TTL — the effective TTL must be `TimeInterval.infinity`, not zero.

### Attack surfaces to probe

1. `CustomizationJsonReader.read(at:)` — unknown/invalid mode string handling: does `PlatformMode(rawValue: $0) ?? .own` correctly degrade for ALL invalid inputs including empty string `""`, whitespace-only `" "`, and multi-word strings like `"not_a_valid_mode"`? The `.convertFromSnakeCase` decoder does NOT apply to the map values (only keys), so the raw string is what hits `PlatformMode.init(rawValue:)`.

2. `CustomizationJsonReader.read(at:)` — JSON null vs absent key for `platform_modes`: if `customization.json` contains `{"platform_modes": null, ...}`, does `CustomizationPayload.platformModes` decode as `nil` (Optional wrapping) and correctly fall through to `(payload.platformModes ?? [:])`, or does the decoder throw a type mismatch?

3. `FloatingPetWindowPool.update(snapshot:)` — `currentCustomization` mutation ordering: the code sets `currentCustomization = customizationReader()` before computing `ttlSeconds` and before filtering `visibleEntries`. Verify that `mode(for:)` and `lastSeenForWindow(key:)` both read from `currentCustomization` (the tick-start snapshot), not from a separate call to the reader that could return different values mid-tick.

4. `FloatingPetWindowPool.update(snapshot:)` — TTL=0 special case correctness: `let ttlSeconds: TimeInterval = currentCustomization.idleDismissTtlSeconds == 0 ? .infinity : TimeInterval(currentCustomization.idleDismissTtlSeconds)`. Verify that `TimeInterval.infinity` causes `timeIntervalSince($0) > ttlSeconds` to always evaluate to false (never dismiss), and that the lastActiveOrigin eligibility check `<= ttlSeconds` also handles infinity correctly.

5. `CodogotchiFolders.customizationPath()` — `CODOGOTCHI_HOME` env var: `dataFolderURL()` respects `CODOGOTCHI_HOME`. Verify that `customizationPath()` also points to the env-var-specified directory and not a hardcoded `~/.codogotchi` path.

6. `FloatingPetWindowPool` — `lastSeenForWindow(key:)` uses `currentCustomization.platformModes` to find combined-mode origins. This means if `platformModes` changes tick-to-tick (e.g., an origin flips from combined to own), `lastSeenForWindow("combined")` may find zero contributing origins after the flip, returning `nil` — potentially causing the combined window to be dismissed incorrectly even if it's last-active. Probe whether this creates a window-lifecycle correctness gap.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `CustomizationSnapshot` struct is a new in-process value type; it is not persisted to disk. `customization.json` is read and decoded per-tick. No version field is present in the format. Probe whether the absence of a schema version marker creates a silent compatibility hazard when the file is written by a future CLI version with new fields.

2. **CLI flag/arg symmetry** — No new CLI flags or arg parsers are added in this diff. The reader uses `JSONDecoder.keyDecodingStrategy = .convertFromSnakeCase`, not a manual arg parser. `[N/A — no CLI flags changed]`

3. **Error-class breadth in `catch` blocks** — Both `Data(contentsOf:)` and `decoder.decode` are guarded with `try?`, silently converting all errors to `nil`. Probe whether this swallows meaningful failure signals (e.g., permission denied vs file-not-found vs malformed JSON) that would be useful for debugging.

4. **Defensive layering at module boundaries** — `customizationReader` is injected as a closure into `FloatingPetWindowPool`. The pool trusts the closure to return a valid `CustomizationSnapshot` without revalidating the values it receives (e.g., negative `idleDismissTtlSeconds`). Probe whether negative TTL values passed via the test injection closure create unexpected pool behavior.

5. **Cross-file atomicity windows** — The pool calls `customizationReader()` (which reads the file) at the start of `update(snapshot:)`. The `customization.json` file is written by Settings in a future ticket. No multi-step write is present in this diff. `[N/A — no multi-step write in this diff]`

6. **Test-contract strength** — Five new `CustomizationJsonReaderTests` tests cover: absent file, valid parse, unknown origin key, invalid mode string, and TTL=0. Pool tests were updated to use `customizationReader` closures. Probe whether any coverage gap exists: negative TTL, null JSON field for `platform_modes`, the default production reader closure in `FloatingPetWindowPool.init`.

7. **Doc-vs-code drift in the ticket Rationale** — The ticket Rationale says `menubar_icon_monochrome` is deferred until ticket 07. The diff reads the field into `CustomizationSnapshot.menubarIconMonochrome` but the pool never consumes it. Read the Rationale note in the ticket doc and surface any drift between the declared deferral and what the diff actually does.

### Diff context

Key logic changes:

1. `CustomizationJsonReader.swift` (new): `PlatformMode` enum with `.own`, `.combined`, `.off` raw values. `CustomizationSnapshot` struct with `safeDefault`. `CustomizationPayload` private struct decodes `platform_modes` as `[String: String]?` (not directly as `[String: PlatformMode]?`) to allow per-value degradation: `mapValues { PlatformMode(rawValue: $0) ?? .own }`.

2. `FloatingPetWindowPool.swift`: `ttlSeconds: TimeInterval` and `platformModes: [String: String]` stored properties removed. `customizationReader: () -> CustomizationSnapshot` closure added to init (default = real disk read). `currentCustomization: CustomizationSnapshot` mutable stored property initialized to `.safeDefault`. First two lines of `update(snapshot:)` set `currentCustomization = customizationReader()` and compute local `ttlSeconds` with `== 0 ? .infinity : ...`. All `mode(for:)` calls updated from string comparisons to `PlatformMode` enum comparisons. `lastSeenForWindow(key:)` updated to filter `currentCustomization.platformModes`.

3. `MenubarApp.swift`: Pool init now uses the default `customizationReader` closure; explicit `ttlSeconds: 300, platformModes: [:]` removed.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
