**Invariant results**

1. [held] Compared the pre-merge protocol requirements with both concrete conformers. `FloatingPetPanelController` still directly implements every formerly required Floating member; none falls through to a merged default.
2. [held] `MinimalistPanelController` directly implements all 15 listed members, including `applyActivity`, `applyPromptSummary`, and `applyBadgeScale`; protocol-typed dispatch uses these witnesses rather than defaults.
3. [held] Inspected both wrapper controllers and repository-wide merged-protocol references. Each wrapper calls only its own shape’s specialized members, and no concrete-controller downcasts or cross-shape calls were found.

**Surface results**

1. [probed] Inspected every `PanelManaging` default against both concrete conformers. Because all methods are protocol requirements, direct conformer implementations populate the witness table; protocol-typed calls cannot accidentally select the default where a conformer supplies an implementation.
2. [probed] Compared `FloatingPetPanelController` with the former `FloatingPetPanelManaging` requirements. All formerly required methods remain directly implemented with unchanged bodies.
3. [probed] Compared `MinimalistPanelController` with the former `MinimalistPanelManaging` requirements. All formerly required methods remain directly implemented, including the three newly defaultable Minimalist-only members.
4. [probed] Read `FloatingPetController` and `MinimalistWindowController` storage, initialization, and forwarding paths. Floating routes sprite/RPG/interaction/idle-escalation operations; Minimalist routes activity/prompt-summary/badge-scale operations. No inappropriate cross-shape forwarding was found.
5. [probed] Compared `FloatingPetPanelSpy` and `StubMinimalistPanel` before and after the merge. Each still explicitly implements every formerly required shape-specific member that became defaultable, so this change did not silently weaken those call-recording seams. Existing shared defaults were already optional before the merge.
6. [probed] Reconciled the merged comments for `applySessionLabel`, `applySessionTooltip`, and `applyConflictBubble` with both concrete implementations and their shared badge behavior. No retained factual claim is exclusive to only one conformer.
7. [N/A — output stability across schema-version drift: the implementation diff changes no persisted schema, JSON format, or externally emitted output.]
8. [N/A — CLI flag/arg symmetry: the diff adds or changes no command, flag, or argument parsing.]
9. [N/A — error-class breadth: the full implementation diff adds no `catch` block or error-handling branch.]
10. [probed] At the controller-to-renderer boundary, the merged protocol broadens which operations are callable but does not loosen any parameter or return signature. Current wrapper call paths preserve the old shape routing; the new risk is limited to future misuse being silently swallowed by defaults.
11. [N/A — cross-file atomicity windows: the implementation is a protocol/conformance refactor with no multi-step state or artifact write.]
12. [probed] Verified test-contract strength around all five newly defaulted formerly-required members. `FloatingPetPanelSpy` records `apply(state:visualMode:)` and `setInteraction`; `StubMinimalistPanel` explicitly supplies `applyActivity`, `applyPromptSummary`, and `applyBadgeScale`, with behavior assertions covering activity and prompt forwarding. No merge-induced false-negative seam was found.
13. [probed] The ticket Rationale’s Floating-only and Minimalist-only member lists exactly match the merged extension. The capability matrix’s behavioral distinctions remain implemented, but its literal protocol names and “absent from protocol” wording no longer match the merged declaration; this is recorded below as advisory doc drift.

**Actionable findings**

None.

**Advisory Observations**

- `docs/contracts/window-capability-matrix.md` §2 is structurally stale after the merge. The shape definitions, source note, and R2.1 still name `FloatingPetPanelManaging` and `MinimalistPanelManaging`; R2.2–R2.5 say Floating-only members are “absent from protocol” for Minimalist, while `PanelManaging` now declares those members and supplies Minimalist no-op defaults. Runtime capability behavior remains consistent with the matrix, but the contract’s description of how that distinction is represented is no longer factual.

**Runner termination**

runnerStatus: completed  
terminatedReason: Completed the requested read-only adversarial review across every mandated invariant and attack surface.
