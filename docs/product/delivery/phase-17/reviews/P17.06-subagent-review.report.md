**Invariant results**

1. `[held]` Both controllers conform to `PanelActionHandling` and expose the same nine shared slots. All nine production assignments occur only in `MenubarApp.wirePanelActions` at `MenubarApp.swift:675-707`. Minimalist panel-size wiring remains concrete at `MenubarApp.swift:315-320`, outside the protocol.

2. `[held]` Direct comparison with the deleted factory closures found equivalent behavior:

   - Attention dismissal and Force Idle still delegate to `WindowActionRouter`.
   - Force Idle retains router-owned prompt-timer reset before the state rewrite, followed by gate/conflict clearing.
   - Rename, sync-label, prune, hide-all-other, settings navigation, and hide-current preserve their keys and side effects.
   - Mode switching retains origin/session-key folding through `modeSwitchOrigin`, with the combined-key special case.
   - Hide-current retains controller-identity lookup and menu-title refresh.

3. `[broken]` DMG evidence is currently verifiable: the local artifact exists with the documented SHA-256, `/Applications/Codogotchi.app` reports version 2.7.0 build 12, and that installed binary is running. The Phase 18 three-shape soak is correctly marked incomplete. However, Exit Condition 3 makes an unsupported source claim: `exit-audit.md:32-36` says anchoring/fronting live in the coordinator and that no parallel controller-owned anchoring/fronting algorithms remain. Both controllers still directly reposition/front chrome, including `FloatingPetPanelController.swift:771-838` and `MinimalistPanelController.swift:370-411`.

**Surface results**

1. `[probed]` Capture lifetimes: shared closures capture `MenubarApp`, panels, and concrete controllers weakly where cycles are possible. Value-only closures capture `WindowKey`/state-directory data. No new retain cycle or premature controller release was found. A nil app intentionally makes app-owned actions no-op, consistent with teardown.

2. `[probed]` Factory parameters: Own and Minimalist preserve their inverse mode targets, combined-mode flags, session-to-origin folding, controller-identity hide lookup, visibility update, and menu refresh.

3. `[probed]` Protocol conformance and renamed slots: repository-wide assignment and invocation searches found no stale `onHideFloatingPet`, `onHidePanel`, `onSwitchToMinimalist`, or `onSwitchToPetMode` production path. Both views invoke the renamed shared slots.

4. `[probed]` Force Idle ordering: each view resets its local prompt presentation state before invoking the shared handler; the router then resets the pool tracker before asynchronous state rewriting. Gate and conflict clearing still follows router invocation.

5. `[probed]` Exit evidence and rationales: P17.02’s retained view-level prompt entrypoints are represented honestly; P17.05’s incomplete factory collapse and the developer-approved P17.06 correction are documented. DMG/install/process facts match local state. The chrome-coordinator PASS claim does not match source.

6. `[N/A — no persisted JSON/schema output changed]` The protocol and closure refactor introduces no serialization, schema, or persisted-data changes.

7. `[N/A — no CLI surface changed]` No flags, arguments, or command routing changed.

8. `[N/A — no catch block changed]` The diff adds no error-handling changes.

9. `[probed]` Protocol/helper boundary: the class-bound protocol permits weak panel capture; the helper receives only the two genuine skin-specific operations. No unsafe cast or shape assumption was found.

10. `[probed]` Cross-file atomicity: runtime code and audit claims are committed separately, and the DMG is intentionally ignored/local-only. The artifact/install facts remain locally verifiable, but Exit Condition 3 can currently be committed as PASS despite contradictory source.

11. `[probed]` Test-contract strength: existing tests cover prompt-item dispatch, router targeting across plain/session/combined keys, and `modeSwitchOrigin`. They do not instantiate or directly exercise private `wirePanelActions`, either controller’s renamed forwarding slots, hide-controller lookup, settings routing, or the complete nine-handler factory wiring.

12. `[probed]` Doc-versus-code rationale: matrix R2.1 matches the new protocol shape, and P17.06 accurately describes the factory correction. The stronger chrome-coordinator exit claim conflicts with both the P17.03 rationale—which explicitly retained controller-side live re-anchor call sites—and current source.

**Actionable findings**

- `docs/product/delivery/phase-17/exit-audit.md:30-36` incorrectly marks the chrome-coordinator condition PASS. Current controllers still contain skin-specific anchoring/fronting paths and directly operate coordinator-exposed panels. This also conflicts with the phase plan’s exit condition that anchoring and fronting be implemented once. Either complete that collapse or revise the audit/approved rationale so it does not claim those paths are absent.

**Advisory Observations**

- The refactor’s most important new contract—both skins reaching all nine handlers exclusively through `wirePanelActions`—has no behavioral test. Existing prompt, router, and pool tests would not detect a helper slot accidentally omitted, swapped, or left unwired in one factory.
- The behavior-bar section records pass claims but does not link durable logs or artifacts. Its wording about the final publication gate being rerun is weaker evidence than the otherwise exact test-count claim.

**Runner termination**

runnerStatus: completed  
terminatedReason: The requested read-only adversarial review completed successfully.
