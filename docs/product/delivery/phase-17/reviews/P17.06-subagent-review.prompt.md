You are conducting an adversarial review of a code change. Assume the implementation has holes and find them. You may add ticket-relevant attack surfaces. Report off-scope but real issues only under Advisory Observations. Do not write any files.

### Ticket scope

P17.06 records an honest six-condition exit audit, passes the full behavior bar, packages and installs a local-only dogfood DMG, writes the Phase 17 retrospective, and restates the three-shape Phase 18 soak gate. During the audit, the developer explicitly approved closing P17.05's incomplete factory collapse: both renderer skins must share one action protocol and one nine-handler wiring path with zero user-visible behavior change.

### Files touched

Implementation:
apps/menubar/Sources/App/MenubarApp.swift
apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift
docs/contracts/window-capability-matrix.md
docs/product/delivery/phase-17/exit-audit.md
docs/product/delivery/phase-17/ticket-06-closeout-dogfood-retro.md
docs/product/retrospectives/phase-17-surface-convergence-retrospective.md

Tests:
No test files changed. Existing full suite and focused prompt/router/pool tests must protect behavior.

### Invariants to hold

1. Both panel skins expose the same nine shared handler slots, and every slot is assigned only by `MenubarApp.wirePanelActions`; Minimalist-only panel-size behavior remains outside that protocol.
2. The helper preserves the pre-collapse semantics for attention dismissal, Force Idle cleanup, rename, sync label, prune, hide-all-other, mode switch, settings navigation, and hide-current across plain, session-keyed, and combined keys.
3. The exit audit and retrospective make only evidence-supported claims: local DMG/install/launch is complete, while the longer three-shape Phase 18 transition soak remains explicitly incomplete.

### Attack surfaces to probe

1. `MenubarApp.wirePanelActions` capture lifetimes: look for retain cycles, panels/controllers deallocated before actions, or no-op handlers when `self` is nil.
2. Own and Minimalist factory parameters: compare mode-switch combined/origin behavior and hide-controller lookup against the deleted closures for semantic drift.
3. `PanelActionHandling` conformance and renamed slots: search every invocation and assignment for a missed old name or handler no longer firing from its view.
4. Force Idle ordering and cleanup: verify router call plus gate/conflict clearing remain equivalent for both skins and do not lose prompt-timer ordering owned by the router.
5. Exit-audit evidence versus source, tests, DMG artifact/install facts, and the approved P17.02/P17.05 rationales.

#### Diff-derived attack surfaces

1. Output stability across schema-version drift: N/A expected; no persisted JSON/schema output changed. Confirm no hidden contract change.
2. CLI flag/arg symmetry: N/A expected; no CLI surface changed.
3. Error-class breadth in catch blocks: N/A expected; no catch block changed.
4. Defensive layering at module boundaries: probe the new protocol/helper boundary and closure parameters for unsafe assumptions.
5. Cross-file atomicity windows: probe whether docs claim install/audit completion that could be committed independently of code or artifact verification; no runtime multi-file write changed.
6. Test-contract strength: no new tests were added; determine whether existing tests genuinely cover renamed action wiring or whether a demonstrable regression can escape.
7. Doc-vs-code drift in ticket Rationale: compare P17.06 rationale, exit audit, matrix R2.1, and actual diff. Put drift only in Advisory Observations unless it breaks an invariant.

### Diff context

The diff adds class-bound `PanelActionHandling: PanelManaging`, makes both panel controllers conform, and renames skin-specific hide/mode-switch properties to shared slots. `MenubarApp` replaces two parallel handler blocks with `wirePanelActions`, parameterizing only mode-switch behavior and controller-specific hide lookup; Minimalist panel-size wiring stays concrete. The ticket rationale documents the developer-approved closeout correction. The matrix, exit audit, and retrospective are updated, while the DMG remains ignored/local-only.

### Directives and required output

Read the diff against `origin/agents/p17-05-seam-2-router-factory-collapse` and directly related code. Advisory-only: do not create, modify, or delete files. Probe every listed surface and use exactly these top-level headings, in order, with no horizontal rules:

**Invariant results**
For each invariant: `[held | broken | untested]` and evidence.

**Surface results**
For every ticket and diff-derived surface: `[probed | N/A — reason | blocked — missing-input]` and evidence.

**Actionable findings**
Concrete correctness findings only, or `None.`

**Advisory Observations**
One observation per bullet, or `None.`

**Runner termination**
runnerStatus: completed | rate_limit | sandbox_denied | runner_unavailable
terminatedReason: one short sentence.
