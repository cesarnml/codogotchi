**Invariant results**

1. [held] Compared every migrated reposition call against `agents/p17-02-shared-prompt-dismissal-component`: the same panel overloads receive the same arguments in the same order, and the promoted attention layout math is unchanged.
2. [held] Own’s live `reanchorChrome` uses only `existing*Panel?.reposition(...)` for animation badge, attention bubble, conflict bubble, gate badge, HUD, tombstone, and regen meter. Minimalist’s attention/conflict live moves likewise use existing accessors without fronting or creation; its gate badge retains the explicit invariant-3 exception.
3. [held] Minimalist’s gate badge still calls the fronting `repositionGateBadgeMinimalist` path on content changes and badge moves, while attention and conflict bubbles front only from their content-change presentation paths.

**Surface results**

- Placeholder `ChromeRouting`: [probed] Both `@MainActor` controller initializers configure routing synchronously before returning or invoking any coordinator panel factory. Because panel callbacks snapshot routing only when a panel is later created, no reachable host path can create an interactive panel with the placeholder closures.
- Live re-anchor paths: [probed] `FloatingPetPanelController.reanchorChrome` uses existing-instance accessors without fronting or lazy creation for all seven relevant panels. Minimalist attention/conflict moves do the same; its gate badge intentionally uses the pre-existing fronting/lazy helper behavior required by invariant 3.
- Animation badge visibility guard: [probed] The `if !badge.isVisible { badge.orderFrontRegardless() }` logic and its position after `reposition(...)` match the baseline implementation. Lazy creation does not alter the read: a new panel remains non-visible until the guard fronts it.
- Hidden Own gate-badge lifecycle: [probed] `applyGateBadge` still creates and updates the hidden panel exactly as before. A subsequent `show()` positions the animation badge first and then calls `repositionGateBadgeOwn`, which recalculates content size and frame using the current pet and chip geometry before fronting.
- Coordinator instance isolation: [probed] All routing and panel references are ordinary instance properties. No static, shared, singleton, or global panel state exists in `ChromeFlockCoordinator`.
- `AttentionBubbleLayout` promotion: [probed] Repository-wide symbol search found one top-level declaration and no collision. The frame function plus hPad, vPad, cornerRadius, closeButtonSize, actionButtonWidth, and actionButtonHeight references were all consistently renamed; no executable `BubbleLayout` reference remains.
- Output stability across schema-version drift: [N/A — `ChromeFlockCoordinator` performs no persistence, serialization, or CLI output, and the diff changes no persisted schema].
- CLI flag/arg symmetry: [N/A — no CLI command, flag, or argument surface is touched].
- Error-class breadth in `catch` blocks: [N/A — the diff introduces no `catch`, throwing operation, or error-swallowing path].
- Defensive layering at module boundaries: [probed] The coordinator passes caller-provided frames, scalar anchors, metrics, and optional sprite anchors directly to the same panel methods used before the refactor. It removes no pre-existing validation and adds no behavior-changing validation.
- Cross-file atomicity windows: [N/A — no persisted multi-step write or other cross-file transaction is introduced].
- Test-contract strength: [probed] The new tests call `AttentionBubbleLayout.frame` directly and independently pin the caller-controlled leading X, bottom anchor, pet-width-derived width, fixed height, and visible-frame clamping. Since the function contains no shape branch, those inputs cover both Own and Minimalist call-site combinations without leaving a distinct per-shape geometry path unexercised.
- Doc-vs-code drift in the ticket Rationale: [probed] Final controller files contain no declarations of the deleted per-shape chrome panel fields or remaining direct lifecycle bypasses. Combined still routes through the two migrated factories, and `GateBadgeLayout.frame(relativeTo:badgeSize:visibleFrame:)` plus its tests have no diff from the baseline, confirming the centered overload was pre-existing and untouched.

**Actionable findings**

None.

**Advisory Observations**

None.

**Runner termination**

runnerStatus: completed  
terminatedReason: Completed the adversarial diff and directly related code-path review without modifying the repository.
