You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Outcome (from `docs/product/delivery/phase-17/ticket-03-chrome-flock-coordinator.md`):

- One coordinator component owns "these chrome panels fly in formation with this host window" — anchoring math, drag routing from chrome into the host, and z-order/fronting rules — for the five chrome panel types (AnimationBadge, GateBadge, AttentionBubble, SpeechBubble, RPGHUD).
- All three window shapes (Own, Minimalist, Combined) consume the coordinator; the per-shape hand-sewn anchoring/drag/fronting implementations are deleted in this PR.
- Per-shape behavior is reproduced verbatim: where the shapes' current behaviors genuinely differ, the coordinator expresses the difference as a named capability from `docs/contracts/window-capability-matrix.md` — it never converges them.
- Anchoring math is extracted as pure functions with unit tests covering each shape's current anchor geometry.
- Full existing suite green; re-anchor cadence, screen-edge behavior, and Chapter-14 tuning are untouched.

Rationale (append-only section of the same ticket doc — read the full section in the ticket file for complete context): archaeology found four of the five chrome panel types' anchor math (`AnimationBadgeLayout`, `GateBadgeLayout`, `SpeechBubbleLayout`, `RPGHUDLayout`) already pure and unit-tested; only `AttentionBubble`'s anchor math was a file-private, untested `BubbleLayout` enum, promoted to `AttentionBubbleLayout`. `ChromeFlockCoordinator` owns panel-instance lifecycle, the mechanical reposition/front/hide act, and drag/right-click routing — not new anchor math. Two verbatim distinctions were identified and preserved: (1) a fronting "content-changed" reposition path vs a non-fronting "live re-anchor" path during drag/resize; (2) Minimalist's gate badge fronts unconditionally on both paths, unlike its attention/conflict bubbles. Combined shape needed no separate migration (routes through the Own/Minimalist factories, not a third renderer). No separate "dead code deletion" commit was needed — each migration commit removed its own per-shape bypass code as part of that same commit.

### Files touched

Implementation:
apps/menubar/Sources/Windows/ChromeFlockCoordinator.swift (new)
apps/menubar/Sources/Windows/AttentionBubblePanel.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj (xcodegen-generated, new file registration only)

Tests:
apps/menubar/Tests/MenubarTests/AttentionBubbleLayoutTests.swift (new)

### Invariants to hold

1. Every chrome panel's on-screen frame must be byte-identical (within floating-point rounding) to the pre-refactor frame for the same inputs — `ChromeFlockCoordinator`'s reposition methods must delegate to the exact same panel `reposition(...)` overloads and `*Layout` pure functions the deleted inline code called, with no parameter substitution or reordering.
2. The "live re-anchor" hot path (`FloatingPetPanelController.reanchorChrome`, `MinimalistPanelController.applyBadge`'s badge-move repositioning of the bubble/conflict-bubble/gate-badge panels) must never call `orderFrontRegardless()` and must never lazily create a panel — it may only reposition an already-existing panel via the coordinator's `existing*Panel` accessors, exactly mirroring the pre-refactor code's direct-field `?.reposition(...)` calls with no fronting.
3. Minimalist's gate badge must front unconditionally on both its content-change path and its badge-move path, while Minimalist's attention bubble and conflict bubble must front only on their content-change path — this pre-existing asymmetry must be preserved exactly, not converged into one uniform behavior.

### Attack surfaces to probe

1. `ChromeFlockCoordinator.init()`'s placeholder `ChromeRouting` (no-op closures set before `configureRouting(_:)` runs) — both controllers call `configureRouting` synchronously in their own `init`, immediately after constructing the coordinator. Is there any path where a panel could be lazily created and receive a drag/right-click event before `configureRouting` has run, silently no-op-ing the gesture?
2. `FloatingPetPanelController.reanchorChrome` and `MinimalistPanelController.applyBadge`'s bubble/conflict/gate-badge repositioning calls — verify each one genuinely calls the coordinator's non-fronting `existing*Panel.reposition(...)` accessor and not a fronting `repositionX(...)` wrapper, for every panel type touched in that hot path (attention bubble, conflict bubble, gate badge, animation badge, HUD, tombstone, regen meter).
3. `ChromeFlockCoordinator.repositionAnimationBadge`'s `if !badge.isVisible { badge.orderFrontRegardless() }` guard — was this copied byte-for-byte from the pre-refactor `repositionAndShowAnimationBadge`, and does the new lazy-creation indirection (`animationBadgePanelInstance()`) change `AnimationBadgePanel.isVisible`'s semantics at the moment this guard reads it?
4. `ChromeFlockCoordinator.updateGateBadge` (Own) vs `repositionGateBadgeOwn` — `FloatingPetPanelController.applyGateBadge` calls `updateGateBadge` unconditionally then `repositionGateBadgeOwn` only `if isPanelShown`. Does skipping the reposition call while hidden ever leave the badge panel mis-sized/mis-positioned when the pet later becomes visible and `show()` calls `repositionAndShowGateBadge()`?
5. Each host constructs its own `ChromeFlockCoordinator()` instance in `init`. Confirm there is no static/shared/singleton state anywhere in `ChromeFlockCoordinator` that could leak panel state between two independent windows' coordinators (e.g., two Own-mode pet windows open simultaneously, or an Own + Minimalist window pair) — every stored property must be a plain instance member.
6. The `AttentionBubbleLayout` rename (private `BubbleLayout` → internal `AttentionBubbleLayout` in `AttentionBubblePanel.swift`) — confirm no other file declares a colliding top-level `AttentionBubbleLayout` symbol, and that every reference inside `AttentionBubbleView` (hPad, vPad, cornerRadius, closeButtonSize, actionButtonWidth, actionButtonHeight) and `AttentionBubblePanel.reposition` was updated consistently with no stray reference to the old name.

#### Diff-derived attack surfaces

In addition to the ticket-spec-derived surfaces above, probe each of the seven
diff-derived classes below. These are the finding classes external code review
(e.g. CodeRabbit) catches consistently. Report coverage for every class using the form
`[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

1. **Output stability across schema-version drift** — no persisted artifact, CLI stdout, or on-disk JSON format is touched by this diff (pure in-process refactor of AppKit panel controllers). Expected: N/A, but confirm nothing in `ChromeFlockCoordinator` persists state to disk.
2. **CLI flag/arg symmetry** — no CLI surface is touched by this diff. Expected: N/A.
3. **Error-class breadth in `catch` blocks** — this diff introduces no new `catch`/error-handling code (all reposition/hide calls are non-throwing). Confirm this is actually true — no swallowed errors were introduced.
4. **Defensive layering at module boundaries** — `ChromeFlockCoordinator` is a new cross-module boundary (both `FloatingPetPanelController` and `MinimalistPanelController` now call into it). Does it revalidate anything the host isn't already guaranteeing (e.g. frame validity, nil panel states), or silently trust caller-supplied `CGRect`/`CGFloat` values the same way the pre-refactor inline code did (a behavior-preserving "no new validation" is correct here — flag only if the coordinator now does *less* validation than the code it replaced, not more)?
5. **Cross-file atomicity windows** — no multi-step persisted write (state + artifact, commit + push, file-A + file-B) is introduced. Expected: N/A.
6. **Test-contract strength** — `AttentionBubbleLayoutTests.swift` pins geometry via relationship-based assertions (leading-alignment, gap-below-anchor, height constant, width-grows-with-pet-width, visible-frame clamping), mirroring the sibling `SpeechBubbleLayoutTests.swift` convention. Probe whether these tests actually exercise the promoted `AttentionBubbleLayout.frame` the same way the two real call sites (Own's `chipLeadingX`/`chromeBottomY`, Minimalist's `badgeFrame.minX + hPad`/`badgeFrame.minY`) invoke it, or whether a real per-shape input combination could produce geometry the tests don't cover.
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-17/ticket-03-chrome-flock-coordinator.md`'s Rationale section (pasted above) against the actual diff. Does the Rationale's claim that "each migration commit removed its own per-shape bypass code" hold — i.e., grep the final state of `FloatingPetPanelController.swift` and `MinimalistPanelController.swift` for any remaining reference to the deleted private fields (`attentionBubble`, `conflictBubble`, `gateBadgePanel`, `animationBadgePanel`, `rpgHUDPanel`, `tombstonePanel`, `regenMeterPanel`, `bubblePanel`)? Also verify the Rationale's dead-code claim about `GateBadgeLayout.frame(relativeTo:badgeSize:visibleFrame:)` (the centered overload) being pre-existing and untouched by this diff, not newly orphaned by it.

### Diff context

This is a mechanical "Extract Class" refactor with no new business logic. `ChromeFlockCoordinator` (new, ~365 lines) owns: (1) lazy panel-instance creation for `AnimationBadgePanel` (Own only), `GateBadgePanel`, `AttentionBubblePanel`, `SpeechBubblePanel` (conflict), and the RPG HUD family (`RPGHUDPanel`/`TombstonePanel`/`RegenMeterPanel`, Own only); (2) drag/right-click routing via a host-supplied `ChromeRouting` closure bundle (`presentHidePrompt`/`beginDrag`/`continueDrag`/`endDrag`); (3) `reposition*` methods that call each panel's existing (unchanged) `reposition(...)` overload then front it, plus `existing*Panel` accessors for callers that must reposition an already-created panel without fronting or creating one.

`FloatingPetPanelController.swift` (Own, -151/+74 lines net) and `MinimalistPanelController.swift` (Minimalist, -67/+45 lines net) each construct their own `ChromeFlockCoordinator`, wire routing + dismiss/action callbacks in `init`, and replace every direct panel-field access with a coordinator call — `show`/`hide`/`applyGateBadge`/`applyAttention`/`applyConflictBubble` call the fronting `repositionX`/`updateX` methods; `reanchorChrome` (Own) and `applyBadge` (Minimalist) call the non-fronting `existing*Panel.reposition(...)` accessors directly, preserving the original no-refront-during-drag behavior.

`AttentionBubblePanel.swift` (+27/-9 lines) renames its file-private `BubbleLayout` enum to internal `AttentionBubbleLayout` (pure rename, all internal references updated) so it can be unit-tested and consumed by the coordinator's callers. `AttentionBubbleLayoutTests.swift` (new) pins its current geometry.

Read the full diff with `git diff agents/p17-02-shared-prompt-dismissal-component..HEAD` for exact hunks.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
