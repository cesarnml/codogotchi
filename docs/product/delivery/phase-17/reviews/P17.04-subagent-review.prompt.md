You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome (from `docs/product/delivery/phase-17/ticket-04-renderer-protocol-merge.md`):**

- `FloatingPetPanelManaging` and `MinimalistPanelManaging` no longer exist as separate protocols; one renderer protocol ("one renderer interface, two skins") is declared once, with `FloatingPetPanelController` and `MinimalistPanelController` as its two conformers.
- Per-shape differences the old protocols encoded structurally (e.g. `applyBadgeScale` on Minimalist only) are expressed as capabilities consistent with `docs/contracts/window-capability-matrix.md`, not as a second protocol.
- `FloatingPetController` and all other consumers hold the merged protocol type; no call site downcasts to a concrete panel controller to reach a method the merge dropped.
- Existing tests pass unmodified — the neutrality proof; zero behavior change.
- `MenubarApp` is touched only where type names force it — factory structure and wiring are untouched (that is P17.05's disruption).

**Rationale (appended during implementation):**

Merged `FloatingPetPanelManaging` and `MinimalistPanelManaging` into a single `PanelManaging` protocol declared once in `FloatingPetController.swift`, with one default-impl extension holding every shape-specific member as a no-op (Floating-only: `apply(state:visualMode:)`, `replacePets`, `applyRPGState`, `setRPGHUDEnabled`, `setHUDDemoActive`, `setHUDPinned`, `setInteraction`, `updateIdleEscalationConfig`; Minimalist-only: `applyActivity`, `applyPromptSummary`, `applyBadgeScale`). Alternative considered and rejected: a shared base protocol with two shape-specific extension protocols — rejected because that still leaves two protocol types at the consumer/storage boundary, which doesn't satisfy "one renderer protocol."

### Files touched

Implementation:
apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift

Tests:
apps/menubar/Tests/MenubarTests/FloatingPetControllerTests.swift
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift

### Invariants to hold

1. `FloatingPetPanelController` must not gain a default no-op behavior for any member it previously implemented directly (i.e. no method body silently became a no-op via the merged extension shadowing an intended override).
2. `MinimalistPanelController` must not gain a default no-op behavior for any member it previously implemented directly, and must continue to receive real behavior (not a silent no-op) for every member `MinimalistPanelController.swift` implements today (`show`, `hide`, `applyConflictBubble`, `applyPlatform`, `applyActivity`, `applyPromptTimerStatus`, `applyAttention`, `applyPromptSummary`, `applyBadgeScale`, `applySessionNumber`, `applySessionLabel`, `applySessionTooltip`, `applyGateBadge`, `setFrameChangeHandler`).
3. No consumer of the merged `PanelManaging` type (`FloatingPetController`, `MinimalistWindowController`, or any other call site) may call a shape-specific member (e.g. `applyBadgeScale`, `setInteraction`, `applyRPGState`) on the *other* shape's conformer and rely on the no-op default silently swallowing behavior that should have been routed somewhere — i.e. the merge must not have hidden a call site that assumed shape-specific dispatch.

### Attack surfaces to probe

1. `extension PanelManaging { ... }` in `FloatingPetController.swift` (lines ~112–141) — does any default no-op accidentally cover a member that `FloatingPetPanelController` or `MinimalistPanelController` DOES implement, such that Swift's static/witness-table dispatch could resolve to the wrong implementation under some call pattern (e.g. protocol-typed variable vs. concrete-typed variable calling the same method)?
2. `FloatingPetPanelController: PanelManaging` conformance (`FloatingPetPanelController.swift` line 4) — does `FloatingPetPanelController` still implement every member the old `FloatingPetPanelManaging` protocol required it to implement (i.e. no member silently dropped from its concrete implementation, now masked by the new default)?
3. `MinimalistPanelController: PanelManaging` conformance (`MinimalistPanelController.swift` line 20) — same check: does it still implement every member the old `MinimalistPanelManaging` protocol required (particularly `applyActivity`, `applyPromptSummary`, `applyBadgeScale`, which are NOT in the merged extension's Minimalist-only no-op block's method signatures being accidentally identical to something `MinimalistPanelController` doesn't actually override)?
4. `FloatingPetController.panel: PanelManaging` and `MinimalistWindowController.panel: PanelManaging` storage/init-param sites (`FloatingPetController.swift`) — do these two classes still call only the members appropriate to their own shape (Own calls `apply(state:visualMode:)`/`setInteraction`/etc., Minimalist calls `applyActivity`/`applyBadgeScale`/etc.), or did the merge make it possible for one wrapper class to accidentally call a shape-inappropriate member on its `panel` property that now silently no-ops instead of failing to compile?
5. Test doubles `FloatingPetPanelSpy` (`FloatingPetControllerTests.swift`) and `StubMinimalistPanel` (`FloatingPetWindowPoolTests.swift`) — do their conformances to `PanelManaging` still implement (override) every member the corresponding real conformer implements, or did the merge's shared protocol surface let a previously-required override become optional in the test double, weakening test coverage without anyone noticing (a spy that silently uses a no-op default instead of recording a call is a false-negative test)?
6. Doc-comment reconciliation on `applySessionLabel`/`applySessionTooltip`/`applyConflictBubble` — do the merged protocol's doc comments still accurately describe behavior for BOTH conformers (the merge picked the more detailed of two near-identical comments; verify no factual claim in the kept comment is Minimalist-specific-only or Floating-specific-only in a way that's now misleading for the other conformer).

#### Diff-derived attack surfaces

In addition to the ticket-spec-derived surfaces above, probe each of the seven
diff-derived classes below. These are the finding classes external code review
(e.g. CodeRabbit) catches consistently and the prior template did not name. The
primary agent must enumerate concrete surfaces drawn from this diff against each
class. The subagent must report coverage for every class using the form
`[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

1. **Output stability across schema-version drift** — N/A candidate: this diff touches no persisted artifact, CLI output, or on-disk JSON format. Confirm no such surface exists in the diff before marking N/A.
2. **CLI flag/arg symmetry** — N/A candidate: this diff touches no CLI flags. Confirm before marking N/A.
3. **Error-class breadth in `catch` blocks** — N/A candidate: this diff adds no new `catch` blocks (pure protocol/conformance/type-annotation changes). Confirm by reading the full diff.
4. **Defensive layering at module boundaries** — the `panel: PanelManaging` boundary in `FloatingPetController`/`MinimalistWindowController` is a real module boundary crossing (controller → renderer). Probe whether either wrapper class now passes/receives any input across this boundary that the merged protocol's signature no longer constrains as tightly as the original two protocols did.
5. **Cross-file atomicity windows** — N/A candidate: no multi-step write (state+artifact, commit+push) in this diff — it is a single mechanical Swift refactor commit. Confirm before marking N/A.
6. **Test-contract strength** — probe whether `FloatingPetPanelSpy` and `StubMinimalistPanel` still assert real call-recording behavior for every member each real conformer implements (see surface 5 above) — this is the sharpest version of this diff-derived class for a protocol-merge refactor.
7. **Doc-vs-code drift in the ticket Rationale** — read the ticket's `## Rationale` section (appended this ticket) and `docs/contracts/window-capability-matrix.md` §2 (Panel-managing protocols, R2.1–R2.5) against the actual merged protocol in `FloatingPetController.swift`. Confirm the Rationale's claimed member-by-member disposition (Floating-only members listed, Minimalist-only members listed) exactly matches what's declared/defaulted in the diff, and that the capability matrix's row-by-row "absent from protocol" claims still hold true post-merge (they should — the merge didn't change which members are no-op'd for which shape, only reduced protocol count from 2 to 1).

### Diff context

Full diff for this ticket (`git diff agents/p17-03-chrome-flock-coordinator...HEAD` — the immediate parent branch through this ticket's tip):

The two protocols `FloatingPetPanelManaging` (`FloatingPetController.swift:72-98` + extension `100-115`, pre-merge line numbers) and `MinimalistPanelManaging` (pre-merge `303-326` + extension `328-332`) were replaced by a single `protocol PanelManaging: AnyObject { ... }` (post-merge lines 77-110) plus one `extension PanelManaging { ... }` (post-merge lines 112-141) containing every previously-defaulted member from both original extensions, PLUS newly-added default no-ops for every member that was previously NOT defaulted because each protocol required its own conformer to implement it directly: `apply(state:visualMode:)`, `setInteraction(_:)` (both were required-only in `FloatingPetPanelManaging`, now defaulted so `MinimalistPanelController` isn't forced to implement them) and `applyActivity(_:)`, `applyPromptSummary(_:)`, `applyBadgeScale(_:)` (required-only in `MinimalistPanelManaging`, now defaulted so `FloatingPetPanelController` isn't forced to implement them).

`FloatingPetPanelController: FloatingPetPanelManaging` → `FloatingPetPanelController: PanelManaging` (single-token change, method bodies untouched).
`MinimalistPanelController: MinimalistPanelManaging` → `MinimalistPanelController: PanelManaging` (single-token change, method bodies untouched).
`FloatingPetController.panel: FloatingPetPanelManaging` → `PanelManaging` (stored property + init param).
`MinimalistWindowController.panel: MinimalistPanelManaging` → `PanelManaging` (stored property + init param).
Test doubles `FloatingPetPanelSpy` and `StubMinimalistPanel` conformance clauses updated identically; no assertion logic changed.

No conformer method body was modified. No new `as?` downcasts introduced. `MenubarApp.swift`, `FloatingPetWindowControlling`, and `FloatingPetWindowPool.swift` were not touched.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
