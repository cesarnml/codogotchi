**Invariant results**

1. [held] `handleAttentionDismissed(for:)` invokes `resetPromptTimer(windowKey)` synchronously before selecting or issuing every writer call for `.origin`, `.session`, and `.combined`.
2. [held] Session-keyed Force Idle calls the exact-slice `StateJsonWriter.forceIdle(at:origin:sessionId:)` overload; no winner scan or sibling-session path is reachable.
3. [held] Both combined handlers call `resolveWindowOrigins` per invocation, which calls the injected `combinedModeOrigins()` closure live and passes the resulting set directly; an empty set completes without falling back to all slices.

**Surface results**

- Literal origin `"combined"` collision: [probed]. `resolveWindowOrigins` compares typed enum values, so `.origin("combined") != .combined` and correctly resolves to `["combined"]`. The documented non-injectivity exists only at the `rawValue` boundary; there is no router collision, although no router test locks this down.
- Own-window Force Idle cleanup ordering: [probed]. Badge and conflict-bubble cleanup remains synchronous immediately after a fire-and-forget state write. The pre-refactor closure had exactly the same ordering, so this ticket introduced no change.
- Router construction before pool assignment: [probed]. `FloatingPetWindowPool.init` stores the two factories but never invokes them; factories first run from later `update` spawn paths, after `self.floatingPetWindowPool = pool`. No construction-time no-op path was found.
- Optional completion parameter: [probed]. The only four production handler calls are in the two `MenubarApp` factories and all omit completion. Supplying or omitting completion changes only whether the writer schedules a main-queue callback after settling; targeting and write control flow are unchanged.
- Shared router across own/minimalist factories: [probed]. Exactly one router is constructed and assigned to `self.windowActionRouter`; both factories resolve that same stored instance.
- Duplicate `MenubarApp.resolveWindowOrigins`: [probed]. The remaining method serves `refreshTtlForShow`, which was not one of the two duplicated targeting handlers the implemented rationale limits this ticket to. It is residual targeting duplication relative to the literal Outcome wording, but not an undisclosed implementation defect; see Advisory Observations.
- Output stability across schema-version drift: [probed]. The router delegates to the same `StateJsonWriter` overloads previously called by the closures. It introduces no persisted fields or serialization changes and therefore does not alter `state.d/` JSON shape.
- CLI flag/arg symmetry: [N/A — reason: the ticket changes no CLI, flags, arguments, or command dispatch].
- Error-class breadth in catch blocks: [N/A — reason: the router adds no catch blocks and preserves the writer’s existing best-effort silent-failure contract].
- Defensive layering at module boundaries: [probed]. The router accepts an unchecked state-directory closure, but the sole production construction supplies the already-resolved `config.pollingTarget.path`; no plausible ticket-relevant path to an empty or malformed value was found.
- Cross-file atomicity windows: [probed]. Session attention dismissal still starts its asynchronous disk rewrite and then clears bubbles synchronously. This exactly matches the pre-refactor closure ordering and creates no new race in this ticket.
- Test-contract strength: [probed]. Tests read and assert actual temporary `state.d/` JSON contents for all six handler/shape combinations and use supplied completions deterministically. They do not exercise omitted-completion production calls, directly prove reset-before-write ordering, mutate the combined-origin provider between calls, or cover `.origin("combined")`; see Advisory Observations.
- Doc-vs-code drift in the ticket Rationale: [probed]. The Rationale accurately describes the implemented extraction, retained factories, retained non-targeting handlers, shared router, and test-only completion hook. It also explicitly discloses the deviation from the Outcome’s literal single-factory requirement.
- One-disruption history check: [probed]. The P17.05 implementation diff rewires only the duplicated attention/Force Idle bodies plus router construction; it does not redo the P17.02–P17.04 panel/protocol wiring work.

**Actionable findings**

None.

**Advisory Observations**

- The literal Outcome in `docs/product/delivery/phase-17/ticket-05-router-factory-collapse.md` requires two factories to collapse into one parameterized factory and says the router owns the other `onX` bodies. The implementation retains two factories, leaves non-targeting handlers inline, and leaves Show targeting in `MenubarApp.resolveWindowOrigins`. The Rationale accurately and explicitly records this scope reduction, so it is disclosed spec deviation rather than a demonstrated correctness bug.

- `WindowActionRouterTests.swift` asserts final writes and reset calls, but its comments overstate direct ordering coverage: no injected reset closure checks that the target file is still unchanged when reset runs. The suite also only supplies completion, uses an immutable combined-origin fixture, and omits the documented `.origin("combined")` edge. These are test-strength gaps, not evidence that the implementation violates an invariant.

**Runner termination**

runnerStatus: completed  
terminatedReason: Completed the targeted review of every listed invariant and attack surface without modifying repository files.
