You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Outcome (from `docs/product/delivery/phase-17/ticket-05-router-factory-collapse.md`):

- A Seam-2 router type owns "which slices does this window's action touch": the session / combined / plain-origin targeting policy currently inline in `MenubarApp`'s factory closures (attention dismissal fan-out, Force Idle slice selection, prompt-timer resets, and the other `onX` handler bodies).
- The router has unit tests covering targeting per `WindowKey` shape: session-keyed targets exactly its own slice, combined resets exactly its combined origin set, plain-origin targets its origin's winner slice — the semantics documented in today's closure comments become executable.
- `MenubarApp`'s two factory closures collapse to one parameterized factory of dumb wiring: `panel.onX = { router.handle(.x, for: key) }`; no targeting policy remains in the factory.
- No parallel comment blocks survive in `MenubarApp` — the per-shape targeting prose moves to the router (once) or dies.
- Full existing suite green; this is the phase's single `MenubarApp` disruption.

Rationale (as recorded in the ticket doc after implementation):

Why this path: `WindowActionRouter` owns exactly the two duplicated handlers whose bodies branch on `WindowKey` shape — `onAttentionDismissed` and `onForceIdle` — ported verbatim (including the session-fan-out / exact-slice / combined-set / never-all-slices asymmetries and the prompt-timer-before-rewrite ordering) from the pre-existing closures. Both the own-window and minimalist-window factories construct one shared router instance and delegate to it.

Alternative considered / Deferred: collapsing the own-window and minimalist-window factories into one literal single parameterized factory (per the ticket's outcome wording) was explicitly NOT done — `FloatingPetPanelController` and `MinimalistPanelController` are distinct concrete types with no shared action protocol. The two factories still exist as two functions; only the `onAttentionDismissed`/`onForceIdle` targeting bodies (the highest-risk, `WindowKey`-shape-branching lines per Review Focus) were extracted into the router and deduplicated. The other duplicated `onX` handlers (rename, sync label, prune, hide-all-other, mode-switch, open-settings, panel-size) were intentionally left inline in both factories — they have no `WindowKey`-shape branching to centralize.

Review Focus (from the ticket doc):
- Targeting fidelity: the router must preserve every asymmetry the current comments document — sibling-session fan-out on dismiss, exact-slice Force Idle, combined-set resets, the "never all slices" rule protecting aged-out pets.
- Ordering guarantees preserved: pool prompt-timer reset happens before the on-disk rewrite.
- The factory is genuinely dumb: any residual conditional on window shape inside the factory is targeting policy that escaped the router.
- One disruption: confirm `MenubarApp` needed no wiring rework in P17.02–P17.04 that this ticket then redid.

### Files touched

Implementation:
- apps/menubar/Sources/Pool/WindowActionRouter.swift (new)
- apps/menubar/Sources/App/MenubarApp.swift (modified — both factory closures rewired)

Tests:
- apps/menubar/Tests/MenubarTests/WindowActionRouterTests.swift (new)

Docs:
- docs/product/delivery/phase-17/ticket-05-router-factory-collapse.md (Rationale appended)

### Invariants to hold

1. `WindowActionRouter.handleAttentionDismissed(for:)` must call `resetPromptTimer` before issuing the `StateJsonWriter` write for every `WindowKey` shape (`.origin`, `.session`, `.combined`).
2. For a `.session(origin:id:)` key, `handleForceIdle` must rewrite exactly `origin:id`'s slice and must never touch a sibling session slice for the same origin, even if that sibling is fresher.
3. For `.combined`, both handlers must resolve origins via the injected `combinedModeOrigins()` closure at call time (not a cached/stale set) and must never fall back to "all slices."

### Attack surfaces to probe

1. `WindowActionRouter.resolveWindowOrigins` — does `.origin("combined")` (a real platform origin literally named "combined", however unlikely) collide with `.combined` and get treated as the shared combined window instead of a plain origin? (Compare against `WindowKey`'s own documented non-injectivity caveat.)
2. `MenubarApp`'s own-window factory `panel.onForceIdle` closure — after delegating to `router.handleForceIdle(for: origin)`, it synchronously calls `panel?.applyGateBadge(content: nil)` / `panel?.applyConflictBubble(nil)` without waiting for the router's (async, fire-and-forget) write to complete. Does this ordering change from the pre-refactor code, or was it already fire-and-forget before this ticket?
3. Router construction in `MenubarApp.applicationDidFinishLaunching` — `windowActionRouter` is built and assigned to `self.windowActionRouter` *before* `FloatingPetWindowPool` is constructed, and the router's injected closures read `self?.floatingPetWindowPool` (weakly, lazily) rather than capturing the pool directly. Is there any code path where a factory closure could fire before `self.floatingPetWindowPool` is assigned (i.e., during pool construction itself), causing `resetPromptTimer`/`combinedModeOrigins`/`clearAttentionBubbles` to silently no-op?
4. `WindowActionRouter`'s two handlers now take an optional `completion` parameter (added post-initial-implementation to fix an async test race). Confirm every production call site in `MenubarApp` passes `nil` (the default) and that this addition changed no production control flow — only test-observability.
5. Minimalist-window factory's `onForceIdle`/`onAttentionDismissed` — confirm both now delegate to the *same* router instance as the own-window factory (not a second router instance), so pool-tracker state (e.g. prompt timers) isn't split across two disconnected trackers.
6. `resolveWindowOrigins` still exists as a *separate*, near-identical private method directly on `MenubarApp` (used by `refreshTtlForShow` for the Show action) alongside the new `WindowActionRouter.resolveWindowOrigins`. Is this acceptable scope (Show was never one of the two duplicated targeting handlers this ticket names) or is it undetected residual duplication the ticket should have collapsed too?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — N/A expected (no persisted artifact/JSON shape changes), but confirm `WindowActionRouter` doesn't alter the `state.d/` JSON shape written by `StateJsonWriter` (it should only change which files get written, not their contents).
2. **CLI flag/arg symmetry** — N/A expected, no CLI surface in this diff; confirm.
3. **Error-class breadth in `catch` blocks** — N/A expected, no new `catch` blocks in this diff; confirm `WindowActionRouter` has none and doesn't need one (it delegates to `StateJsonWriter`, which already fails silently by design).
4. **Defensive layering at module boundaries** — `WindowActionRouter` takes closures at init with no runtime validation (e.g., no guard against an empty `stateDir()` string). Is that acceptable given `MenubarApp` is the sole caller and controls those inputs, or is there a plausible path where `stateDir()` returns something invalid?
5. **Cross-file atomicity windows** — `handleAttentionDismissed`'s session branch issues an async `StateJsonWriter.dismissAllSessionsAttention` write and then *synchronously* (not inside the completion) calls `clearAttentionBubbles(windowKey)`. Confirm this ordering (bubble-clear before the on-disk write is confirmed complete) matches the pre-refactor closure exactly, and that it's not a new race.
6. **Test-contract strength** — do `WindowActionRouterTests` assert on the actual `state.d/` file contents (structural) rather than only on recorded closure calls? Confirm coverage includes both the omitted-`completion` (production) path and the supplied-`completion` (test) path.
7. **Doc-vs-code drift in the ticket Rationale** — the ticket's Outcome section literally says "MenubarApp's two factory closures collapse to one parameterized factory" and "no targeting policy remains in the factory" — the implementation keeps two factories and only migrates the two targeting-branching handlers into the router. The Rationale section documents this deviation explicitly under "Alternative considered" / "Deferred." Confirm the Rationale's description of what was and wasn't done accurately reflects the diff (i.e., no further drift beyond what's already disclosed).

### Diff context

`WindowActionRouter` (new file, `Sources/Pool/WindowActionRouter.swift`) is a `@MainActor final class` taking four injected closures (`stateDir`, `resetPromptTimer`, `combinedModeOrigins`, `clearAttentionBubbles`) and exposing `resolveWindowOrigins(_:)`, `handleAttentionDismissed(for:completion:)`, and `handleForceIdle(for:completion:)`. The two handler bodies are byte-for-byte ports of the logic previously duplicated in `MenubarApp`'s `ownWindowFactory` and `minimalistWindowFactory` closures (both `onAttentionDismissed` and `onForceIdle`), including the doc comments explaining the session/combined/plain-origin asymmetries.

In `MenubarApp.swift`, a single `WindowActionRouter` is constructed just before `FloatingPetWindowPool` is built (its closures reference `self?.floatingPetWindowPool` weakly since the pool doesn't exist yet at router-construction time), stored as `self.windowActionRouter`, and both the own-window and minimalist-window factory closures were rewritten so `panel.onAttentionDismissed` / `panel.onForceIdle` now call `self?.windowActionRouter?.handleAttentionDismissed(for: origin)` / `handleForceIdle(for: origin)` followed by the panel-local badge/bubble cleanup (unchanged, not targeting policy). Roughly 45 lines of duplicated comment+logic were deleted from each of the two factories.

`WindowActionRouterTests.swift` is new: 9 tests covering `resolveWindowOrigins` for all three `WindowKey` shapes, and `handleAttentionDismissed`/`handleForceIdle` for `.origin`, `.session`, and `.combined`, asserting against real temp-directory `state.d/` file writes (mirroring `StateJsonWriterTests`' pattern) plus recorded closure-call order via an injected `Box<[WindowKey]>` reference type.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
