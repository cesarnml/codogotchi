You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**P8.06 Maew two-sheet assets + 8-frame renderer loader**

Outcome:
- Maew's two real sheets are bundled (`apps/menubar/Fixtures/maew/`) with an updated `pet.json`, replacing the single placeholder `codogotchi-spritesheet.webp`.
- The renderer loads **both** sheets: `codogotchi-lite-spritesheet.webp` (11 rows / 8 frames) for the 9 hook/lite states + idle escalation, and `codogotchi-soa-spritesheet.webp` (10 rows / 8 frames) for the SoA gate states.
- Each ActivityState maps to (sheet, row); the renderer picks the SoA sheet for gate states and the lite sheet for hook states. The Phase 07 temporary placeholder rows are removed.
- Animation plays 8 frames/row at 1.5s/loop, looping continuously.
- Unknown/artless states still fall through to hook animation (Phase 07 contract preserved) — no gray pet, no crash.

Rationale notes:
- Row order source: `notes/private/codogotchi-8frame-lite-soa-sheet-prompts.md` — prompt order defines exact row indices for both sheets (11 rows lite, 10 rows SoA).
- Resolution order change: CodogotchiPet is checked BEFORE CodexPet in both MenubarRenderer and FloatingPetScene. The lite sheet now self-contains all 9 hook states.
- GateJsonReader: switched from rowMap to soaRowMap for gate-renderability predicate. All 10 SoA gate states now have art.
- Idle escalation (idle-impatient row 1, idle-frustrated row 2) deferred — constants declared, not yet wired to elapsed-idle timers.

### Files touched

Implementation:
apps/menubar/Sources/CodogotchiPet.swift
apps/menubar/Sources/PetStoreSeeder.swift
apps/menubar/Sources/GateJsonReader.swift
apps/menubar/Sources/MenubarRenderer.swift
apps/menubar/Sources/FloatingPetScene.swift
apps/menubar/Fixtures/maew/pet.json
apps/menubar/Fixtures/maew/codogotchi-lite-spritesheet.webp (new)
apps/menubar/Fixtures/maew/codogotchi-soa-spritesheet.webp (new)

Tests:
apps/menubar/Tests/MenubarTests/CodogotchiPetTests.swift
apps/menubar/Tests/MenubarTests/BundledMaewTests.swift
apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift
apps/menubar/Tests/MenubarTests/GateJsonReaderTests.swift
apps/menubar/Tests/MenubarTests/MenubarRendererTests.swift

### Invariants to hold

1. Every ActivityState.allCases member must produce non-empty frames from `CodogotchiPet.frames(for:)` when both sheets are present (exact 1:1 coverage — no state silently falls through to Codex when sheets are loaded).
2. `CodogotchiPet.frames(for:)` must dispatch SoA gate states exclusively to the SoA sheet and hook/lite states exclusively to the lite sheet — no state resolves to a wrong sheet.
3. When both sheets are absent (soft-degrade path), `frames(for:)` returns empty for all states and the renderers correctly fall through to the Codex idle fallback without crash.

### Attack surfaces to probe

1. **`CodogotchiPet.init` grid validation for both sheets** — the hard-fail guard checks `width % gridColumns == 0` and `height % rowCount == 0`. Does the lite sheet use the hardcoded `liteRows = 11` and the SoA sheet use `soaRows = 10`? If either constant is wrong, the validation is testing the wrong constraint and a mismatched sheet would silently load or incorrectly throw.
2. **`sliceFrames` row-offset arithmetic** — the `y` origin is `spec.rowIndex * frameHeight`. If `frameHeight` is computed from the wrong sheet's row count (e.g., lite height used for SoA slicing), rows beyond the sheet boundary return nil from `cgSheet.cropping(to:)` which triggers an assertionFailure rather than a throw.
3. **`GateJsonReader.resolveActivityState` — soaRowMap predicate completeness** — the old `rowMap` had 8 entries; `soaRowMap` has 10. Was the set of states the gate consumer recognizes faithfully expanded? Probe whether `.pollReview` and `.advance` (newly mapped) can now be activated via gate.json and are not accidentally blocked by any upstream filter.
4. **`PetStoreSeeder.isCanonicalStoreComplete` — store completeness after requiredAssets update** — the old required set included `codogotchi-spritesheet.webp`; the new set includes the two new sheets. Is the store correctly considered incomplete until both new sheets are present? Probe whether an old-format store (with only `codogotchi-spritesheet.webp`) that hasn't been re-seeded passes the completeness check or not.
5. **Resolution order inversion in `MenubarRenderer` and `FloatingPetScene`** — CodogotchiPet is now checked BEFORE Codex. Probe whether the idle-fallback path still fires correctly when CodogotchiPet returns empty (both sheets missing). Also probe whether any previously-Codex-only state (e.g. `.idle` from Codex row 0) is now shadowed by the lite sheet in a way that breaks existing tests or observable behavior.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `pet.json` changed `description` only; no schema version field exists. The `CodogotchiManifest` struct reads only `id` and `displayName` — unchanged. No persisted artifacts changed shape.
2. **CLI flag/arg symmetry** — [N/A — this is a Swift renderer change; no CLI flags or argument parsers are touched in this diff]
3. **Error-class breadth in `catch` blocks** — `init(petDirectory:)` catches Data load errors for `pet.json` and rethrows `petJsonNotFound` / `petJsonMalformed`; sheet loading uses guard-let patterns that throw `spritesheetUnreadable` or `spritesheetIncompatibleGrid`. Probe whether any error path from `NSImage(contentsOfFile:)` or `cgImage(forProposedRect:)` is swallowed without re-throw.
4. **Defensive layering at module boundaries** — `sliceFrames` accepts `fw` and `fh` externally from the calling site's sheet-specific values. If the caller passes the wrong sheet's frame dimensions, the crop rect is wrong. Probe whether the call sites in `frames(for:)` and `floatingFrames(for:)` correctly route to the matching dimensions.
5. **Cross-file atomicity windows** — [N/A — this is a pure in-memory loader change; no multi-step writes or state files are modified at runtime]
6. **Test-contract strength** — new tests assert `liteRowMap.count == 9`, `soaRowMap.count == 10`, and exact row indices for all 19 states. Probe whether the row-index assertions match the actual loaded sheet row order (as documented in the prompts file). Also check whether the `testIncompatibleLiteSheetGridThrows` test correctly fires on a lite-only corrupt sheet without an SoA sheet present.
7. **Doc-vs-code drift in the ticket Rationale** — the ticket specifies "1536×2288 lite, 1536×2080 soa" and "reject if width ÷ 8 isn't integral." Check whether the validation guards in `init` use these exact dimensions or only structural constraints.

### Diff context

CodogotchiPet completely rewritten: single `spritesheet` field + `rowMap` (24-col, 8-entry) replaced with dual fields (`liteSheet`, `soaSheet`) + dual maps (`liteRowMap` 9 entries, `soaRowMap` 10 entries). `gridColumns` 24→8, `frameInterval` 167ms→187.5ms. `init` now loads two sheets sequentially, each with separate dimension guards. `frames(for:)` dispatches: soaRowMap check → liteRowMap check → empty.

GateJsonReader: one-line change — `CodogotchiPet.rowMap[gateState] != nil` → `CodogotchiPet.soaRowMap[gateState] != nil`.

MenubarRenderer and FloatingPetScene: resolution order swapped — CodogotchiPet first, Codex fallback. Comment updated to reflect new order.

PetStoreSeeder.requiredAssets: `codogotchi-spritesheet.webp` removed, `codogotchi-lite-spritesheet.webp` and `codogotchi-soa-spritesheet.webp` added.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
