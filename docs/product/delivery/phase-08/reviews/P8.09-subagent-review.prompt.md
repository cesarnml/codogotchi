You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations**.
Your job is a targeted attack on the behavior this ticket is supposed to protect.

### Ticket scope

**P8.09 Public CLI read-only trim**

Outcome:
- `codogotchi --help` no longer lists `setup`, `hooks install`, `hooks uninstall`.
- `status`, `hooks status`, `rpg` remain visible.
- All three hidden commands remain fully callable (dispatch unchanged).
- A note in help text directs users to Settings → General for hook management.

### Files touched

Implementation:
packages/cli/src/router.ts

Tests:
packages/cli/src/router-help-trim.test.ts (new)

### Invariants to hold

1. The exported `USAGE` string must not match `/^\s{2}setup\s/m`, `/^\s{2}hooks install\b/m`, or `/^\s{2}hooks uninstall\b/m` — they must not appear as listed commands.
2. `dispatch(["setup", "--help"])`, `dispatch(["hooks", "install", "--help"])`, and `dispatch(["hooks", "uninstall", "--help"])` must each exit 0 and not write "Unknown command" to stderr.
3. `dispatch(["--help"])` must exit 0 and its stdout must list `status`, `hooks status`, and `rpg` as visible commands.

### Attack surfaces to probe

1. **`dispatch(["setup"])` without --help flag** — confirm setup still runs (not removed). Since it tries to write `~/.codogotchi/config.json`, test the error path via a pre-existing config (exits 2 with ConfigExistsError) — that still proves dispatch reached the command handler, not "Unknown command".
2. **`dispatch(["hooks", "install"])` without flags** — confirm it still dispatches to the `hooks install` handler and does not fall through to "Unknown command". It will attempt actual installation if no --help is passed; verify the "missing config" path fires.
3. **`USAGE` string — the note mentions "setup, hooks install, and hooks uninstall"** — the test uses `/^\s{2}setup\s/m` to avoid matching the note. Probe whether any other part of the USAGE string could cause unintended re-visibility (e.g. the "Flags (rpg)" section used to say "Flags (setup, rpg)" — confirm the Flags section no longer mentions setup).
4. **`dispatch(["setup", "--help"])` still printing USAGE** — after the change, `setup --help` prints the updated USAGE (which no longer lists setup as a command). Probe whether that creates a confusing UX (user asks for setup help, sees help that doesn't mention setup). This is expected behavior per the ticket but worth confirming the test covers it.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — USAGE is a static string; no schema-version dependency]
2. **CLI flag/arg symmetry** — the "Flags (setup, rpg)" section was changed to "Flags (rpg)". Probe whether the `--force` flag for setup still appears in the flags section or was accidentally removed.
3. **Error-class breadth in catch blocks** — [N/A — no catch blocks changed]
4. **Defensive layering at module boundaries** — [N/A — dispatch logic unchanged]
5. **Cross-file atomicity windows** — [N/A — only USAGE string changed]
6. **Test-contract strength** — tests use `/^\s{2}setup\s/m` regex. Probe whether a two-space indent is reliable (it matches the two leading spaces in the Commands section) and whether it would also match a sub-command like `setup` appearing under another command.
7. **Doc-vs-code drift in the ticket Rationale** — Rationale says "Flags section updated (removed 'setup' from 'Flags (setup, rpg)')". Verify the flags section no longer says "Flags (setup, rpg)" and the note correctly says "(rpg)" or equivalent.

### Diff context

`packages/cli/src/router.ts` — `USAGE` rewritten: removed `setup` line and both `hooks install` / `hooks uninstall` lines from the Commands section; replaced them with a one-line note "(setup, hooks install, hooks uninstall are app-managed — use Settings → General)"; updated "Flags (setup, rpg)" to "Flags (rpg)". Dispatch function unchanged.

`packages/cli/src/router-help-trim.test.ts` — new test file: USAGE string assertions, `--help` dispatch assertions, hidden-command dispatch assertions.

---

### Your directives

**Scope:** Adversarial review of the diff. **Advisory-only — no file writes.** Coverage mandate. No fabrication pressure.

---

### Required output format

**Invariant results**
For each: `[held | broken | untested]` — one line.

**Surface results**
For each: `[probed | N/A — <reason> | blocked — missing-input]`, one to three sentences.

**Actionable findings**
File, what is wrong, discipline clause, fix. If none: "None."

**Advisory Observations**
One bullet per observation. If none: "None."

**Runner termination**
`runnerStatus`: `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one sentence.
