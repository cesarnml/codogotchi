Invariant results

1. `[held]` I installed with and without `execPath`, and with a real sibling `codogotchi-hook`; `installHooks` wrote the absolute sibling path when present and the bare `codogotchi-hook` string when absent or omitted.
2. `[held]` I probed bare→absolute, absolute→absolute by inspection, and absolute-form uninstall/status by execution; the installer converges to one codogotchi-owned entry per Claude/Codex event and removes stale absolute entries on uninstall.
3. `[held]` I inspected `HookStatusClient.resolveRunnerLaunch` and its tests; bundled-present returns `resourceURL/<head>` plus `argv.dropFirst()`, while nil resource URL, absent file, and empty `argv` all fall back to `/usr/bin/env`.

Surface results

1. `[probed]` `resolveHookCommand` uses `join(dirname(execPath), "codogotchi-hook")` exactly once and checks that sibling with `stat()`. A malformed bare `execPath` would degrade to a relative `codogotchi-hook` lookup (`dirname("codogotchi") === "."`), but the real entrypoints always pass absolute `process.execPath`; non-`ENOENT` stat failures rethrow rather than silently masking a broken bundle.
2. `[probed]` `withCodogotchiMatcher` now removes any matcher whose hook command merely `includes("codogotchi-hook")`. I reproduced that this deletes unrelated user hooks such as `/x/codogotchi-hook-wrapper` from both Claude and Codex configs.
3. `[probed]` The Codex TOML writer uses `command = ${JSON.stringify(hookCommand)}`. I verified with `python3` `tomllib` that JSON-quoted strings remain valid TOML basic strings for spaces, quotes, and backslashes, and the dev-path assertion still holds.
4. `[probed]` `codexHookCommand(ctx, command)` appends the absolute path unquoted after the env assignments. I reproduced `/bin/sh -c "CODOGOTCHI_HOME='…' CODOGOTCHI_ORIGIN=codex /Users/me/Applications/My Apps/codogotchi-hook"` failing with `/Users/me/Applications/My: No such file or directory`, so bundle paths with spaces are word-split and do not execute.
5. `[probed]` `resolveRunnerLaunch` consumes `argv.first` only on the bundled branch and otherwise preserves the full argv under `/usr/bin/env`; the empty-argv guard is present, and `resourceURL.appendingPathComponent("codogotchi")` resolves the intended Resources sibling. In unbundled `swift test`, `Bundle.main.resourceURL` may exist, but absence of a bundled `codogotchi` correctly triggers PATH fallback.
6. `[probed]` `runSetup` forwards `deps.execPath` and `router.ts` passes `process.execPath` for `setup` and default `hooks install`; `--platform cursor` intentionally stays on the bare-name path, matching the ticket scope. I did not find another Claude/Codex install entrypoint hardcoding the bare name.
7. `[probed]` `hooksStatus` and `uninstallHooks` both still recognize absolute forms because they share `isCodogotchiCommand`; I executed an absolute-path install and confirmed status reports installed and uninstall removes the absolute entry. The same shared detector is also the source of the over-match bug above.
8. `[probed]` No new CLI flags were added. `execPath` is threaded only as an internal context field from `router.ts`/`setup.ts`, not exposed as user input.
9. `[probed]` `fileExists` returns `false` only for `ENOENT` and rethrows other errors. That means an unreadable/unstatable sibling path aborts install instead of silently falling back to PATH; I did not classify this as a ticket bug because it fails closed rather than writing a likely-broken config.
10. `[probed]` `installHooks` trusts `ctx.execPath`, but normal callers supply absolute `process.execPath` and `resolveHookCommand` still requires a sibling on disk before writing an absolute path. Only malformed injected test/dependency input can produce odd relative behavior.
11. `[probed]` The ticket did not add a new partial-write window: `hookCommand` is resolved once at the top of `installHooks` and reused across Claude, Codex TOML, and Codex JSON writes. There is no per-writer re-stat race.
12. `[probed]` New tests cover bundled-present, sibling-absent dev fallback, bare→absolute convergence, and Swift bundled-present/file-absent/nil-resource cases. They do not cover space-containing absolute bundle paths or preservation of unrelated hooks whose command merely contains `codogotchi-hook`, which is why both real regressions escaped.
13. `[probed]` I read the Phase 08 ticket rationale and did not find doc/code drift on the declared scope: Cursor remains unchanged, `process.execPath` is threaded from router/setup, and the Swift runner is bundle-first with PATH fallback. The rationale’s “substring detection keeps working” claim is true for absolute-form detection but omits the over-match risk.

Actionable findings

1. `[packages/cli/src/hooks.ts:245-253]` The Codex hook command string concatenates the resolved absolute binary path without shell quoting. This is a correctness-gap finding under clause 2: a bundled install in a path with spaces, such as `/Applications/My Apps/Codogotchi.app/...`, writes a command that Codex will execute as multiple shell words, so hooks stop firing even though install succeeds. Fix recommendation: shell-quote the resolved `command` before joining it into the Codex shell string, or switch this surface to an argv-safe representation if Codex supports one.

2. `[packages/cli/src/hooks.ts:237-238, 266-267, 281-286, 528-545]` `isCodogotchiCommand` uses a raw substring match, so install/uninstall/status all treat unrelated commands like `/x/codogotchi-hook-wrapper` as Codogotchi-owned. I reproduced `installHooks` deleting such pre-existing Claude and Codex hooks. This is a correctness-gap finding under clause 2: re-running install can silently remove user hooks and `hooks status` can report false positives. Fix recommendation: replace the substring test with exact executable matching of the command token or basename `codogotchi-hook` after stripping any env-prefix/shell quoting, so only the real hook binary is deduped.

Advisory Observations

- The new tests in [packages/cli/src/hooks.test.ts](/Users/cesar/code/codogotchi_p8_02/packages/cli/src/hooks.test.ts:475) prove the bundle-vs-dev resolution logic, but they never exercise a bundle path containing spaces or an unrelated hook command containing `codogotchi-hook`. Adding those regressions would directly cover both bugs above.
- I did not find any scope drift against [docs/product/delivery/phase-08/ticket-02-bundle-first-resolution-absolute-hook-path.md](/Users/cesar/code/codogotchi_p8_02/docs/product/delivery/phase-08/ticket-02-bundle-first-resolution-absolute-hook-path.md:1); the implementation keeps Cursor out of scope exactly as the rationale says.

**Runner termination**
runnerStatus: completed
terminatedReason: Completed the targeted adversarial review of the touched code, related consumers, and ticket rationale without requiring file edits.
