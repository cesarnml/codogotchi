**Invariant results**  
`[held]` Invariant 1 — [apps/menubar/Sources/SettingsTabModel.swift](/Users/cesar/code/codogotchi_p8_03/apps/menubar/Sources/SettingsTabModel.swift:25) hardwires `tabs` to `SettingsTab.allCases`, defaults `selected` to `.general`, and `select(_:)` guards re-selection before mutating/notifying; [SettingsTabModelTests.swift](/Users/cesar/code/codogotchi_p8_03/apps/menubar/Tests/MenubarTests/SettingsTabModelTests.swift:7) and full `xcodebuild` passed.

`[held]` Invariant 2 — [apps/menubar/Sources/AboutViewModel.swift](/Users/cesar/code/codogotchi_p8_03/apps/menubar/Sources/AboutViewModel.swift:35) invokes `runner(["codogotchi", "--version"])`, trims stdout, and returns `"unknown"` on non-zero or empty-trimmed output; [AboutViewModelTests.swift](/Users/cesar/code/codogotchi_p8_03/apps/menubar/Tests/MenubarTests/AboutViewModelTests.swift:23) covers argv, success, failure, and whitespace-only stdout.

`[held]` Invariant 3 — [packages/cli/src/router.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/router.ts:184) handles `--version`, `-v`, and `version` in one early branch before other commands/fallthrough, writes only to stdout, and returns exit code 0; [router-version.test.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/router-version.test.ts:30) passed under `bun test`.

**Surface results**  
`[probed]` `SettingsTabModel.select(_:)` — I checked the implementation and tests. The no-op guard runs before both mutation and callback, so re-select does not notify, and the callback argument is the same `tab` assigned into `selected`.

`[probed]` `SettingsWindowController.tabView(_:didSelect:)` identifier round-trip — [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_03/apps/menubar/Sources/SettingsWindowController.swift:69) stores `tab.rawValue` in `NSTabViewItem.identifier` and decodes via `as? Int` plus `SettingsTab(rawValue:)`; invalid/nil identifiers safely return. I did not find a teardown crash path because the delegate method is guarded and does not dereference tab subviews.

`[probed]` `AboutViewModel.bundledHookVersion` output parsing — the code trims surrounding whitespace/newlines and maps non-zero or empty-trimmed stdout to `"unknown"`. Multi-line stdout would be returned trimmed rather than normalized, which matches the stated “trimmed stdout” contract.

`[probed]` `router.ts` `--version` placement — the version branch sits immediately after help handling and before `setup`/`rpg`/other command dispatch, so none of the three spellings collide with unknown-command fallthrough. The dedicated Bun test passed for all three spellings.

`[probed]` `SettingsWindowController` lifecycle — the window promotion keeps `isReleasedWhenClosed = false`, `windowWillClose` nils the stored tab views, and later `updateHookStatus` becomes a nil-safe no-op through `generalTab?.updateHookStatus(...)`. I found no post-close crash path in the timer-driven caller model the ticket describes.

`[probed]` Behaviour preservation of moved Hooks/Pet content — I compared the current per-tab views to the pre-rewrite `SettingsContentView` in commit `3a997a9`. The install/uninstall/import handlers, success/error setters, hook status rendering, and the empty-pets popup guard are materially unchanged; only the container/chrome moved.

`[probed]` Output stability across schema-version drift — the CLI now emits exactly `CLI_VERSION + "\n"` from [packages/cli/src/router.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/router.ts:197), and the app trims that to a single token. There is no richer structured contract here, so the consumer is stable as long as the CLI keeps emitting one version line.

`[probed]` CLI flag/arg symmetry — all three spellings share one branch, so runtime behavior is symmetric. `USAGE` in [router.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/router.ts:55) was not updated to mention `version`/`--version`, so the new surface is implemented but not discoverable from help.

`[probed]` Error-class breadth in catch blocks — `handleImportPet` still catches all errors and surfaces `String(describing: error)`, matching pre-rewrite behavior. `AboutTabView.openLink` has no `catch`, but it also ignores the `Bool` return from `NSWorkspace.shared.open(url)`, so link-open failure is silent.

`[probed]` Defensive layering at module boundaries — `AboutViewModel` fails closed to `"unknown"` for bad subprocess outcomes, and `SettingsWindowController` fails closed by returning early on bad tab identifiers. I did not find a ticket-relevant degenerate input that breaks those boundaries.

`[N/A — no multi-step persisted write was added in this diff]` Cross-file atomicity windows — this ticket adds in-memory UI state and stdout emission, but no new multi-step persistent mutation sequence.

`[probed]` Test-contract strength — the Swift tests cover tab order/default/observability/reselect guard and the runner-argv/failure/empty-output About cases; the TS tests cover exit code and semver-shaped stdout for all three spellings, with explicit empty-stderr assertion only for `--version`. Full `xcodebuild ... test` also passed: 260 tests, 0 failures.

`[probed]` Doc-vs-code drift in the ticket Rationale — the rationale says P8.05 will build on the “binary-reported version/generation token,” but [packages/cli/src/version.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/version.ts:1) currently hardcodes `CLI_VERSION = "0.0.0"` mirrored from package metadata, and [packages/cli/package.json](/Users/cesar/code/codogotchi_p8_03/packages/cli/package.json:1) is also `0.0.0`. That does not break P8.03, but it is weaker than the future lockstep rationale assumes.

**Actionable findings**  
None.

**Advisory Observations**  
[packages/cli/src/router.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/router.ts:55): the new version surface is absent from `USAGE`, so `codogotchi --help` does not advertise `--version`/`-v`/`version` even though dispatch supports them. That is a discoverability gap, not an invariant break.

[docs/product/delivery/phase-08/implementation-plan.md](/Users/cesar/code/codogotchi_p8_03/docs/product/delivery/phase-08/implementation-plan.md:1), [docs/product/delivery/phase-08/ticket-03-settings-four-tab-shell.md](/Users/cesar/code/codogotchi_p8_03/docs/product/delivery/phase-08/ticket-03-settings-four-tab-shell.md:1), and [packages/cli/src/version.ts](/Users/cesar/code/codogotchi_p8_03/packages/cli/src/version.ts:1): the docs talk about a binary-reported build/generation token that future lockstep logic can compare across releases, but the implementation is a fixed `0.0.0` constant mirrored from package metadata. P8.03 still satisfies its own contract because About is sourcing from the binary rather than Swift, but the doc narrative is ahead of the actual version-bumping discipline.

[apps/menubar/Sources/SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_03/apps/menubar/Sources/SettingsWindowController.swift:531): `AboutTabView.openLink` silently ignores `NSWorkspace.shared.open(url)` failure. That is outside the ticket invariants, but if product links matter operationally, there is currently no user-visible failure path.

**Runner termination**  
runnerStatus: completed  
terminatedReason: Review finished with code inspection plus passing targeted Bun tests and full `xcodebuild` test suite.
