Invariant results

1. `[held]` I ran `xcodebuild -project apps/menubar/Codogotchi.xcodeproj -scheme Codogotchi build`, then verified `Codogotchi.app/Contents/Resources/codogotchi` and `.../codogotchi-hook` exist, are executable arm64 Mach-O files, and each exits `0` under `env -i PATH=` from both the app bundle and a non-repo working directory.
2. `[held]` I inspected [scripts/build-binaries.sh](/Users/cesar/code/codogotchi_p8_01/scripts/build-binaries.sh:1): it has explicit `bun`-missing `exit 1`, `set -euo pipefail`, and no swallowed compile failures after discovery; I also confirmed the `command -v bun || true` form is safe under `set -euo`.
3. `[held]` I verified [`.gitignore`](/Users/cesar/code/codogotchi_p8_01/.gitignore:214) ignores both `apps/menubar/Generated/` and `*.bun-build`, then confirmed real Bun temp files in repo root and `apps/menubar/` show as ignored via `git status --ignored`.

Surface results

1. `[probed]` `build-binaries.sh` bun discovery is loud on the no-bun branch by inspection: if `command -v bun` and all fallback paths fail, the script prints an error and `exit 1`; I also validated the `$(command -v bun || true)` pattern does not trip `set -euo pipefail`.
2. `[probed]` I ran `bash scripts/build-binaries.sh '/tmp/codogotchi build with spaces'`; both binaries compiled successfully into the space-bearing directory, so the quoting on `OUT_DIR`, `REPO_ROOT`, and `--outfile` is sound.
3. `[probed]` I checked the generated [project.pbxproj](/Users/cesar/code/codogotchi_p8_01/apps/menubar/Codogotchi.xcodeproj/project.pbxproj:241) and the real `xcodebuild` log: the embed script runs before Xcode’s `CodeSign` steps, not after. The finished app still passes `codesign --verify`, so there is no current signing break here.
4. `[probed]` `ENABLE_USER_SCRIPT_SANDBOXING: NO` is set in project-level settings and therefore applies to `CodogotchiTests` too; that is broader than the single app target needs, but I found no ticket-scope behavior break from it.
5. `[probed]` `*.bun-build` does cover both observed emission locations: repo root and `apps/menubar/`; I confirmed actual Bun temp files were created in both places and are ignored, and `apps/menubar/Generated/` matches the default artifacts directory.
6. `[probed]` Repeated rebuilds only produced complete final Mach-O outputs plus temporary `*.bun-build` files in the working directory; I did not observe a partially written target executable, and the script’s straight-line `set -e` flow means a second compile failure would still fail the build loudly even if one binary had already been written.

Diff-derived classes

1. `[N/A — reason]` No persisted schema or on-disk data format changed here; the new binaries are build artifacts only.
2. `[probed]` `package.json` invokes `bash scripts/build-binaries.sh` with no arg, and the script’s `OUT_DIR="${1:-...}"` default correctly resolves to `apps/menubar/Generated/binaries`; the optional Xcode arg path also worked in practice.
3. `[probed]` The shell error model is `set -euo pipefail`; the only intentional suppression is `command -v bun || true` during discovery, and I found no later `|| true` or fallback that would hide a compile failure.
4. `[probed]` The Xcode-to-script boundary quotes the output dir end to end, and the script uses quoted `mkdir -p` and `--outfile`; there is no path-splitting risk on the received env-expanded resources path.
5. `[probed]` The two compiles are sequential; if the second `bun build` returns non-zero, `set -e` aborts the script and therefore the Xcode phase, so a half-embedded state is not silently shipped.
6. `[probed]` There are no automated tests for this ticket, and the current verification story is honestly the empty-`PATH` smoke plus `mac:test`; I confirmed the smoke behavior directly and found no hidden automated assertion pretending to cover it.
7. `[probed]` I read [the ticket rationale](/Users/cesar/code/codogotchi_p8_01/docs/product/delivery/phase-08/ticket-01-compile-bundle-binaries.md:1) and compared it to the implementation: the `--version` claim in the Outcome section does not match current CLI behavior, and the rationale’s “embedded in postBuildScripts after the ad-hoc sign step” claim does not match the actual Xcode build order.

Actionable findings

None.

Advisory Observations

- The ticket doc has two real doc/code drifts. First, the Outcome section still says the smoke check is `.../codogotchi --version` and `.../codogotchi-hook --version`, but the current CLI returns exit `1` for `--version`; the actual passing smoke is `codogotchi help` and `codogotchi-hook </dev/null`. Second, the rationale says the binaries are embedded after the ad-hoc sign step, but the observed `xcodebuild` order is `PhaseScriptExecution Embed codogotchi binaries` before `CodeSign`, and the finished app verifies successfully.
- `ENABLE_USER_SCRIPT_SANDBOXING: NO` is applied at project scope, so it disables script sandboxing for `CodogotchiTests` as well as the app target. I did not find a ticket-scope break from that, but it is broader than the build phase itself requires.

Runner termination

`runnerStatus`: completed  
`terminatedReason`: Completed the targeted adversarial review, including direct build and runtime probes, without finding a ticket-scope correctness break.
