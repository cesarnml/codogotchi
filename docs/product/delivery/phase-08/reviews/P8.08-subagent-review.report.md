**Invariant results**
`[held]` Invariant 1.
`[held]` Invariant 2.
`[held]` Invariant 3.

**Surface results**
`[probed]` `readLastNTransitions` malformed NDJSON line handling. The loop `continue`s only for the malformed line and still parses later lines, so a bad line in the middle does not suppress valid entries that follow. The downside is line-local data loss only: malformed entries disappear silently.

`[probed]` `schemaVersionMismatch` when `state.json` is absent. `stateSchemaVersion` returns `0` on read failure and `schemaVersionMismatch` therefore returns `false`, so the UI shows `state.json: v?` without a mismatch warning. That matches the implementation and does not crash, but I did not find a test that pins this absent-file branch.

`[probed]` `gateJsonPretty` when path is configured but file is absent. The VM returns a non-`nil` absence string when a path exists but the file is unreadable, and the UI renders a `gate.json` section containing that string. When the path itself is `nil`, the section is omitted entirely, so the UI does distinguish “configured but absent” from “not configured.”

`[probed]` `hooksPresentSummary` platform filter. `antigravity` is excluded from the summary because the implementation only enumerates `codex`, `claude_code`, `cursor`, and `vscode`, and also filters `installableInPhase`. The test fixture sets `antigravity.installableInPhase = false`, but the test does not assert its exclusion.

`[probed]` `lastSeenSourceOrigin` searches `last5Transitions` only. This is narrower than the ticket rationale: the code only inspects the already tail-bounded last five non-heartbeat entries, so an older most-recent-with-source entry is invisible once five newer source-less transitions arrive. That can incorrectly suppress the Cursor-bridge explainer.

`[probed]` Output stability across schema-version drift. If `state.json` contains `"schema_version": "4"` or another non-integer value, [`stateSchemaVersion`](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/DeveloperTabViewModel.swift:77) falls back to `0` and [`schemaVersionMismatch`](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/DeveloperTabViewModel.swift:86) returns `false`. That hides a malformed or drifted schema instead of flagging it.

`[N/A — no CLI code changed]` CLI flag/arg symmetry.

`[probed]` Error-class breadth in catch blocks. For `stateJsonPretty` and `gateJsonPretty`, the chained `try?`s degrade to the absence string rather than throwing. I do not see a realistic `JSONSerialization.data(withJSONObject:)` crash path here because the object being re-serialized came from `JSONSerialization.jsonObject`.

`[probed]` Defensive layering at module boundaries. The log reader requires the entire file to decode as UTF-8, so a mixed-encoding or partially corrupt file returns `[]` and drops all otherwise-parseable entries. That is lossy, but it is consistent with the current best-effort “no throw” behavior.

`[N/A — purely reads; no multi-step writes]` Cross-file atomicity windows.

`[probed]` Test-contract strength. The hook-summary assertion in [`DeveloperTabViewModelTests`](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Tests/MenubarTests/DeveloperTabViewModelTests.swift:225) is weak because `summary.contains("codex")` would still pass if the installed indicator regressed. The test exercises the path, but it does not robustly pin the exact rendered contract.

`[probed]` Doc-vs-code drift in the ticket rationale. [`DeveloperTabView`](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/SettingsWindowController.swift:739) has no polling timer or observer; the Refresh button only calls [`renderContent()`](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/SettingsWindowController.swift:806). That matches the stated “button refresh only for v1” behavior and preserves read-only semantics.

**Actionable findings**
- [apps/menubar/Sources/DeveloperTabViewModel.swift:77](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/DeveloperTabViewModel.swift:77), [apps/menubar/Sources/DeveloperTabViewModel.swift:86](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/DeveloperTabViewModel.swift:86): `schemaVersionMismatch` treats a non-integer or otherwise invalid `schema_version` as `0`, which suppresses the mismatch warning instead of surfacing a bad schema. Discipline clause: `(2) demonstrable correctness gap`. Fix: parse `schema_version` with the same strictness as [`StateJsonReader`](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/StateJsonReader.swift:46) or add an explicit “invalid/unknown schema” state that still warns in the Developer tab.

- [apps/menubar/Sources/DeveloperTabViewModel.swift:94](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/DeveloperTabViewModel.swift:94), [apps/menubar/Sources/DeveloperTabViewModel.swift:99](file:///Users/cesar/code/codogotchi_p8_08/apps/menubar/Sources/DeveloperTabViewModel.swift:99): `lastSeenSourceOrigin` and `lastSeenSourceName` only scan `last5Transitions`, but the ticket rationale says they should come from the last non-heartbeat entry with non-`nil` source metadata. Discipline clause: `(2) demonstrable correctness gap`. Fix: scan the full log for the most recent non-heartbeat entry carrying those fields, or add a dedicated reader that tails until it finds the latest sourced transition instead of reusing the display-bounded last-five array.

**Advisory Observations**
The targeted test run could not be executed with `xcodebuild test -only-testing:MenubarTests/DeveloperTabViewModelTests` because the `Codogotchi` scheme/test plan does not include `MenubarTests`; that leaves this review dependent on static inspection. Separately, the new tests miss two important contract edges: invalid `schema_version` input and the “latest source metadata older than the last five displayed transitions” case.

`runnerStatus`: `completed`
`terminatedReason`: Static adversarial review completed; dynamic test execution was attempted but blocked by the current Xcode scheme/test-plan configuration.
