**Invariant results**
[held] 1. `USAGE` no longer lists `setup`, `hooks install`, or `hooks uninstall` as command entries; the note is present instead at [packages/cli/src/router.ts](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router.ts:82).

[held] 2. `dispatch(["setup","--help"])`, `dispatch(["hooks","install","--help"])`, and `dispatch(["hooks","uninstall","--help"])` all exit `0` and do not write `Unknown command` to stderr.

[held] 3. `dispatch(["--help"])` exits `0` and stdout still lists `status`, `hooks status`, and `rpg`.

**Surface results**
[probed] 1. `dispatch(["setup"])` still reaches the setup handler. With a pre-existing config, it exits `2` with the `ConfigExistsError` message rather than `Unknown command`, which confirms dispatch is intact.

[probed] 2. `dispatch(["hooks","install"])` still reaches the install handler. With no config present, it exits `2` on the expected missing-config path and does not fall through to `Unknown command`.

[probed] 3. The `USAGE` note does not accidentally re-list hidden commands as command entries, and the flags header is now `Flags (rpg)` at [router.ts](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router.ts:86). I found no remaining `Flags (setup, rpg)` text.

[probed] 4. `dispatch(["setup","--help"])` prints the global `USAGE` string unchanged, so setup help intentionally omits a visible `setup` command entry. That matches the ticket’s expected UX.

[probed] 5. The `--force` flag still appears in the flags section at [router.ts](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router.ts:87), so trimming setup from help did not remove the setup/rpg shared flag documentation.

[N/A — static string] 6. No schema-version dependency exists in this diff.

[N/A — no catch changes] 7. Error-class breadth was not modified here.

[N/A — dispatch structure unchanged] 8. No module-boundary defensive layering changed in this diff.

[N/A — single static help string change] 9. No cross-file atomicity window exists here.

[probed] 10. The new test file’s regexes are not uniformly the exact invariant regexes. The `--help` assertion uses the safer `^\s{2}` form, but the direct `USAGE` assertions use looser `^\s*` patterns at [router-help-trim.test.ts](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router-help-trim.test.ts:32), [37](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router-help-trim.test.ts:37), and [41](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router-help-trim.test.ts:41).

[probed] 11. The rationale-to-code update is consistent: the flags section says `Flags (rpg)`, and the help note points users to `Codogotchi.app → Settings → General` for hook management.

**Actionable findings**
[packages/cli/src/router-help-trim.test.ts](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router-help-trim.test.ts:76) Hidden-command coverage is materially weaker than the ticket’s protected behavior. The new tests only exercise `--help` branches for `setup` and `hooks install`, so they would still pass if a future regression kept help stubs alive but broke real dispatch for bare `setup` or `hooks install`; discipline clause: coverage mandate. Fix: add no-flag dispatch tests that assert `setup` hits the `ConfigExistsError` path and `hooks install` hits the missing-config path, while explicitly asserting stderr does not contain `Unknown command`.

**Advisory Observations**
- Existing coverage in [packages/cli/src/router-hooks.test.ts](/Users/cesar/code/codogotchi_p8_09/packages/cli/src/router-hooks.test.ts:65) already proves `hooks install` and `hooks uninstall` work on non-help paths in a configured environment; the new file duplicates weaker help-path checks instead of extending coverage where the ticket asked.
- `setup --help` intentionally shows the global trimmed help, so users asking for setup help no longer see setup listed as a command. That is ticket-consistent, but it is a deliberate UX tradeoff rather than command-specific help.
- The comments in the new `setup still dispatches` test mention `--force` even though the test does not pass `--force`, which is harmless but misleading.

**Runner termination**
`runnerStatus`: completed

`terminatedReason`: Reviewed the diff, executed the targeted dispatch probes under Bun, and found one actionable test-coverage gap but no implementation regression.
