**Invariant results**
`[held]` Invariant 1: `PetTabViewModel.allPetIds()` inserts `DEFAULT_PET_NAME` before scanning either root, and the loop `continue`s per-root on read failure, so a missing `~/.codex/pets/` still leaves Maew present and still scans the canonical root.

`[broken]` Invariant 2: `selectPet(id:)` updates `activePetId`, swallows any `PetConfig.write` failure with `try?`, and still fires `onActivePetChanged`, so persistence is not guaranteed and the no-op-on-same-id path can mask a failed first write on retry.

`[held]` Invariant 3: `MenubarApp.reloadActivePet()` rebuilds loaders and fans the new pet into `renderer.replacePets`, `floatingPetPanelController?.replacePets`, and `livePollingDriver?.replaceCodogotchiPet`; `CodexPet()` failure is caught and logged rather than crashing.

**Surface results**
`[probed]` `PetTabViewModel.allPetIds()` with Codex root absent: the `try? contentsOfDirectory` failure only skips that root. Maew is still inserted unconditionally, and the canonical root is still scanned on the next iteration.

`[probed]` `PetConfig.write` atomicity: `Data.write(..., .atomic)` is synchronous and prevents partial-file writes, so I did not find a “half-written JSON / missing pet key” outcome from this helper alone. The ticket-level gap is one layer up: `selectPet` ignores write failure and proceeds as if persistence succeeded.

`[N/A — MenubarApp lifetime makes the described deallocation race implausible]` The closure is attached from `MenubarApp` to `SettingsWindowController`, but `MenubarApp` is the `NSApplication` delegate and is retained for app lifetime; after a real restart, launch re-reads `config.json` anyway. I did not find a stale-after-restart path from this weak capture alone.

`[probed]` `importPet` delegation root symmetry: `PetTabViewModel.allPetIds()` enumerates `self.codexPetsRoot` and `self.canonicalPetsRoot`, and `importPet(id:)` passes those exact stored URLs into a fresh `PetImportHelper`. I did not find a mismatched-root path here.

`[probed]` `FloatingPetPanelController.replacePets` when `scene == nil`: the optional call is harmless because the controller updates its own `codexPet`/`codogotchiPet` fields first. The next `show(frame:)` constructs `FloatingPetScene` from those updated properties.

`[probed]` Output stability across schema-version drift: `PetConfig.write` writes only `{ "pet": ... }`, and `resolvedPetName()` reads only `pet`, so the reader/writer contract is stable for this ticket.

`[N/A — no CLI code changed]` CLI flag/arg symmetry does not apply.

`[probed]` Error-class breadth in catch blocks: `reloadActivePet()` catches thrown `CodexPet()` failures and logs; `CodogotchiPet()` is intentionally soft-degraded via `try?`, and the resulting `nil` is propagated to renderer/panel/driver. I did not find an unhandled `CodogotchiPet` failure class in this path.

`[probed]` Defensive layering at module boundaries: `allPetIds()` itself filters to directory entries, so plain files do not enter the list. But the Pet tab’s `isCanonical(_:)` check only uses `fileExists`, so an invalid canonical-side file or symlink at `~/.codogotchi/pets/<id>` can suppress the Import action and incorrectly present Select for a pet that is not actually loadable.

`[probed]` Cross-file atomicity windows: the write happens synchronously before the callback, so a successful write should be visible to the immediate `reloadActivePet()` read. The real failure path is again swallowed write error: callback fires, `reloadActivePet()` re-reads the old config, and the old pet stays active.

`[probed]` Test-contract strength: `testImportPetAddsToCanonicalAndRefreshesEntries` manually creates the canonical directory before calling `vm.importPet(id:)`, so it never actually verifies the integration of `PetImportHelper.importPet` with post-import enumeration. That is a real coverage gap, but I did not find a second shipped correctness break from it beyond the issues below.

`[probed]` Doc-vs-code drift in the ticket rationale: the rationale says multiple bundled pets are deferred post-launch, and the code always inserts only `DEFAULT_PET_NAME`. That is consistent.

**Actionable findings**
1. `apps/menubar/Sources/PetTabViewModel.swift:61`  
What is wrong: `selectPet(id:)` mutates `activePetId`, ignores any `PetConfig.write` error, and still calls `onActivePetChanged`. If the config write fails, `MenubarApp` immediately reloads from the old config and keeps the old pet active; worse, the in-memory `activePetId` is already changed, so pressing Select again becomes a no-op and the user cannot retry in-session.  
Discipline clause: 1 and 2.  
Concrete fix: make `selectPet` throw or return success/failure, only update `activePetId` and fire `onActivePetChanged` after a successful `PetConfig.write`, and have `SettingsWindowController` surface the error instead of reloading.

2. `apps/menubar/Sources/SettingsWindowController.swift:613` and `apps/menubar/Sources/SettingsWindowController.swift:644`  
What is wrong: the Import-vs-Select decision treats any existing path at `canonicalPetsRoot/<id>` as “already canonical” via `fileExists`, without verifying that it is a directory or a loadable pet. A stray file/symlink at that path can hide Import, let the user Select an unusable pet, and then drive `reloadActivePet()` into a logged load failure while the renderer stays on the old pet.  
Discipline clause: 2 and 3.  
Concrete fix: replace `isCanonical(_:)` with a stricter validation check, at minimum `isDirectory == true`, and preferably require a valid canonical pet marker such as a readable `pet.json` before presenting Select.

**Advisory Observations**
`testImportPetAddsToCanonicalAndRefreshesEntries` is too weak for the ticket’s import path. It pre-populates the canonical destination before invoking `vm.importPet(id:)`, so it does not verify that a real import changes enumeration state.

`allPetIds()` will accept symlinked directories as pets because it trusts `isDirectoryKey`; that broadens the trust boundary to assets outside the rooted pet stores. I did not count this as a ticket-scoped finding because the ticket is about enumerate/select/import behavior, not path-hardening, but it is a real boundary expansion.

`runnerStatus`: `completed`  
`terminatedReason`: Completed the scoped adversarial review and found two ticket-relevant correctness gaps.
