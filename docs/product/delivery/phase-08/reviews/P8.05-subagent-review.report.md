**Invariant results**
- `[held]` Invariant 1: I checked [AppState.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/AppState.swift:85) and the v1/v2 tests in [AppStateTests.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Tests/MenubarTests/AppStateTests.swift:167); v1 payloads with no `installed_hook_version` decode with prior fields preserved and `installedHookVersion == nil`, while v3 still falls back.
- `[held]` Invariant 2: [LockstepPolicy.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/LockstepPolicy.swift:15) hard-guards `hooksInstalled == false` and `bundledVersion == "unknown"`, and [LockstepPolicyTests.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Tests/MenubarTests/LockstepPolicyTests.swift:27) covers both branches including `hooksInstalled == true`, `bundledVersion == "unknown"`, `installedVersion == nil`.
- `[broken]` Invariant 3: [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:199) does write `installedHookVersion` after settings install/update success, but that value is not durable because later app-state writers in [MenubarApp.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/MenubarApp.swift:358) and [FloatingPetController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/FloatingPetController.swift:73) rebuild `FloatingAppState` without the new field and erase it.

**Surface results**
- `[probed]` `AppStateStore.load` schema gate: [AppState.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/AppState.swift:99) accepts schema 1 and 2, so v1 still passes after the bump; [AppStateTests.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Tests/MenubarTests/AppStateTests.swift:51) covers v3 fallback.
- `[probed]` `AppStatePayload` optional decode: `installedHookVersion` is optional in [AppState.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/AppState.swift:148), and the v1 tests in [AppStateTests.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Tests/MenubarTests/AppStateTests.swift:167) show missing-key decode lands on `nil`, not fallback.
- `[probed]` `recordInstalledHookVersion` background write / rapid double tap: the specific “two rapid taps” path is largely suppressed because `handleInstallHooks`/`handleUpdateHooks` disable all hook buttons on the main thread before dispatching work in [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:153). The broader lost-update problem is real, though: unrelated app-state writers later clobber `installedHookVersion` entirely.
- `[probed]` `GeneralTabViewModel.needsBannerUpdate` rows dependency: the exact pre-`applySnapshot` bug does not apply because `openWindow()` loads `installedHookVersion` and then either `applySnapshot(savedState.hooksStatus)` or `refresh()` before the view is created in [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:97). If refresh fails and no cached snapshot exists, `rows` stay empty and the banner stays hidden, but that is a fetch-failure path rather than an initialization-order bug.
- `[probed]` `UpdateBannerView.onUpdate` wiring: [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:422) calls `onUpdateHooks()`, and the update-success path reaches `recordInstalledHookVersion` at lines 176-211, so the settings-banner button is wired to the persistence path.
- `[probed]` `SettingsWindowController.openWindow` re-open behavior: [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:97) re-reads `installedHookVersion` on each open, so a persisted mismatch reappears after close/reopen. The reopen logic itself is sound; the real break is that other writers erase the persisted value before the next open.
- `[probed]` Output stability across schema-version drift: new code reads both v1 and v2 correctly, and no extra field transformation is needed beyond optional-`nil` migration. A pre-bump binary would still reject a v2 file because its schema gate was `<= 1`, so downgrade compatibility is fallback-to-default, not round-trip readback.
- `[N/A — this ticket adds no CLI flags or arguments]` CLI flag/arg symmetry.
- `[probed]` Error-class breadth in `catch`/`try?`: [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:211) swallows save failures, so an update can succeed, the current window can hide the banner in memory, and the next reopen can re-show it from stale disk state. That is a real false-positive loop if `app-state.json` is unwritable.
- `[probed]` Defensive layering at module boundaries: `hookVersion` cannot be `nil`, and the production source in [AboutViewModel.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/AboutViewModel.swift:34) normalizes empty output and non-zero exit to `"unknown"`. Only direct test injection could supply `""`.
- `[probed]` Cross-file atomicity windows: if the settings window closes before `applyViewModel`, the disk write from `recordInstalledHookVersion` should still survive and the next open should see it. The bigger atomicity break is cross-file-state reconstruction: launch refresh and floating-pet saves write a new `FloatingAppState` without `installedHookVersion`.
- `[probed]` Test-contract strength: [LockstepPolicyTests.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Tests/MenubarTests/LockstepPolicyTests.swift:67) covers `hooksInstalled == true`, `bundledVersion == "unknown"`, `installedVersion == nil`, and [AppStateTests.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Tests/MenubarTests/AppStateTests.swift:186) covers v2 round-trip/load. Missing are regression tests for preserving `installedHookVersion` through unrelated app-state saves and for the onboarding install path.
- `[probed]` Doc-vs-code drift in ticket rationale: the `"unknown"` guard described in the rationale matches [LockstepPolicy.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/LockstepPolicy.swift:15) and [SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:200). The outcome’s broader “On Install / Update hooks, the app records the bundled hook version” is not met for onboarding install, which is an implementation gap rather than rationale drift.
- `[probed]` Added surface — legacy `FloatingAppState` writers: [MenubarApp.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/MenubarApp.swift:358) and [FloatingPetController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/FloatingPetController.swift:73) still construct `FloatingAppState` without `installedHookVersion`, so launch refresh, visibility toggles, frame drags, and display reclamps erase the new field.
- `[probed]` Added surface — onboarding install path: [OnboardingWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/OnboardingWindowController.swift:108) persists onboarding success without `installedHookVersion`, so a fresh install performed from onboarding looks “never recorded” to lockstep logic.

**Actionable findings**
- [apps/menubar/Sources/MenubarApp.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/MenubarApp.swift:358), [apps/menubar/Sources/FloatingPetController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/FloatingPetController.swift:73): existing app-state writers erase `installedHookVersion` by rebuilding `FloatingAppState` without the new field. This breaks invariant 3 and finding-discipline clause 1: after a successful install/update records the bundled version, the next launch refresh, frame drag, visibility toggle, or display re-clamp can drop it and make the banner reappear falsely. Fix by threading `installedHookVersion` through every `FloatingAppState(...)` reconstruction, or by introducing a preserving copy/update helper so additive schema fields cannot be dropped silently; add regression tests that save a state with `installedHookVersion`, then exercise launch-refresh and floating-pet save paths.
- [apps/menubar/Sources/OnboardingWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/OnboardingWindowController.swift:108): successful onboarding install never records the bundled hook version. This breaks the ticket outcome and invariant 3: a user who installs hooks from onboarding is left with `installedHookVersion == nil`, so once hook status shows installed the lockstep banner will treat that fresh install as stale. Fix by using the same bundled-version source and `"unknown"` guard as settings install/update and persisting `installedHookVersion` in the onboarding success write; add a focused test for onboarding install success.
- [apps/menubar/Sources/SettingsWindowController.swift](/Users/cesar/code/codogotchi_p8_05/apps/menubar/Sources/SettingsWindowController.swift:199): `recordInstalledHookVersion` mutates the in-memory view model before `try? appStateSaver(updated)` and swallows all save errors. This is a finding-discipline clause 3 bug: the spec permits best-effort persistence, but in production an unwritable `app-state.json` yields a silent false-positive banner loop where the current window says “Updated.” and hides the banner, but the next reopen/launch re-shows it from stale disk state. Fix by making the save result observable, only clearing the banner after persistence succeeds, and surfacing a save failure as a user-visible or at least logged error.

**Advisory Observations**
- Pre-bump downgrade behavior is fallback, not compatibility: because schema version was bumped to 2, an older build would ignore the additive field at the `Codable` layer but still reject the file at the schema gate and load defaults on read. That is not a new regression in this ticket, but it is worth remembering if downgrade workflows matter.
- The reviewed tests are strong on pure decode/predicate coverage, but there is no controller-level coverage for “successful install/update/onboarding persists `installedHookVersion` and it survives later unrelated app-state writes.” That gap is why the field-erasure regressions slipped through.

**Runner termination**
runnerStatus: completed
terminatedReason: Finished the targeted adversarial review and documented all listed surfaces plus the directly related failure paths found.
