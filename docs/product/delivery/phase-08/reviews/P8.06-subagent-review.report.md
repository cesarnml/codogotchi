**Invariant results**
Invariant 1: `[held]` — I verified `ActivityState.allCases` is 19 states, `liteRowMap ∪ soaRowMap` covers all 19 with no overlap, and `xcodebuild test` passed `CodogotchiPetTests.testAllLiteStatesReturn8MenubarFrames` plus `testAllSoaStatesReturn8MenubarFrames`, so every state produces non-empty frames when both sheets are present.

Invariant 2: `[held]` — I traced `CodogotchiPet.frames(for:)` and `floatingFrames(for:)` in [apps/menubar/Sources/CodogotchiPet.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/CodogotchiPet.swift:192) and confirmed the dispatch is exclusive: SoA states use `cgSoaSheet`/SoA dimensions only, lite states use `cgLiteSheet`/lite dimensions only, with disjoint maps.

Invariant 3: `[held]` — `CodogotchiPetTests.testMissingSheetSoftDegrades` proves both-sheets-absent returns `[]` for every state, and the renderer code in [MenubarRenderer.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/MenubarRenderer.swift:118) and [FloatingPetScene.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/FloatingPetScene.swift:257) falls through to Codex frames, then Codex idle, without crashing.

**Surface results**
1. `CodogotchiPet.init` grid validation for both sheets: `[probed]` — The lite branch hardcodes `liteRows = 11` and the SoA branch hardcodes `soaRows = 10` in [CodogotchiPet.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/CodogotchiPet.swift:131) and [CodogotchiPet.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/CodogotchiPet.swift:163). I also checked the bundled assets with `sips`: lite is `1536x2288`, SoA is `1536x2080`, so the validation constants match the shipped sheets.

2. `sliceFrames` row-offset arithmetic: `[probed]` — The crop rect uses `spec.rowIndex * frameHeight`, but the only call sites pass sheet-matched dimensions: SoA branches pass `soaFrameWidth/soaFrameHeight`, lite branches pass `liteFrameWidth/liteFrameHeight`. I did not find a wrong-sheet-dimensions call path.

3. `GateJsonReader.resolveActivityState` — `soaRowMap` predicate completeness: `[probed]` — The resolver now checks `CodogotchiPet.soaRowMap[gateState] != nil` in [GateJsonReader.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/GateJsonReader.swift:69). Upstream contract mapping in [packages/contracts/src/soa-events.ts](/Users/cesar/code/codogotchi_p8_06/packages/contracts/src/soa-events.ts:41) includes `pr_review_window_opened -> poll_review` and `stage_advanced -> advance`, so both new states can be activated; tests explicitly cover `advance`, and the code path for `poll_review` is complete.

4. `PetStoreSeeder.isCanonicalStoreComplete` after `requiredAssets` update: `[probed]` — `requiredAssets` in [PetStoreSeeder.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/PetStoreSeeder.swift:11) requires `spritesheet.webp` plus both new Maew sheets and does not include the old `codogotchi-spritesheet.webp`. An old-format store with only the placeholder sheet fails completeness and triggers reseeding, which is the intended direction.

5. Resolution order inversion in `MenubarRenderer` and `FloatingPetScene`: `[probed]` — Both renderers now try Codogotchi first, then Codex, then idle fallback; the relevant code is [MenubarRenderer.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/MenubarRenderer.swift:118) and [FloatingPetScene.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/FloatingPetScene.swift:257). Full `xcodebuild test` passed, including the missing-sheets floating fallback path, so the inversion itself is sound.

6. Diff-derived `Output stability across schema-version drift`: `[probed]` — `pet.json` still only changes `description`; `CodogotchiManifest` continues to decode only `id` and `displayName`, so no persisted-shape drift is introduced.

7. Diff-derived `CLI flag/arg symmetry`: `[N/A — no CLI/parser code changed]`

8. Diff-derived `Error-class breadth in catch blocks`: `[probed]` — `NSImage(contentsOfFile:)` and `cgImage(forProposedRect:)` both fail into `.spritesheetUnreadable`, and incompatible grids throw `.spritesheetIncompatibleGrid`; I did not find a decode/read error swallowed inside `CodogotchiPet.init`. The loader’s soft-degrade behavior only applies to absent sheet files, not unreadable present files.

9. Diff-derived `Defensive layering at module boundaries`: `[probed]` — `sliceFrames` is dimension-agnostic, but the only callers route the matching dimensions for the chosen sheet. I did not find a mixed-sheet `fw`/`fh` path.

10. Diff-derived `Cross-file atomicity windows`: `[N/A — this change is in-memory asset loading/render resolution only]`

11. Diff-derived `Test-contract strength`: `[probed]` — The shipped assets, row maps, and grid constants are internally consistent, and `testIncompatibleLiteSheetGridThrows` does hit the lite-only corrupt-sheet case. One test is stale and weak: [FloatingPetSceneTests.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift:56) still claims `.idle` resolves from Codex and would not catch a regression in the new Codogotchi-first idle path.

12. Diff-derived `Doc-vs-code drift in the ticket Rationale`: `[probed]` — The ticket cites `notes/private/codogotchi-8frame-lite-soa-sheet-prompts.md`, but that path is absent in this repo. The closest available doc I found, [notes/public/codogotchi-lite-and-soa-spritesheet-contact-sheets.md](/Users/cesar/code/codogotchi_p8_06/notes/public/codogotchi-lite-and-soa-spritesheet-contact-sheets.md:86), still describes 24-frame rows and a different SoA row order than the current code/assets.

**Actionable findings**
[apps/menubar/Sources/GateJsonReader.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/GateJsonReader.swift:69), [apps/menubar/Sources/CodogotchiPet.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/CodogotchiPet.swift:154), [apps/menubar/Sources/MenubarRenderer.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/MenubarRenderer.swift:125), [apps/menubar/Sources/FloatingPetScene.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/FloatingPetScene.swift:262): `resolveActivityState` treats any gate present in `soaRowMap` as renderable even when the SoA sheet is absent at runtime, because the predicate is map-based rather than asset-based. In that state, `CodogotchiPet.frames(for:)` returns `[]`, Codex has no gate rows, and both renderers fall to Codex idle instead of preserving the hook animation; this is a demonstrated correctness gap against the “unknown/artless states fall through to hook animation” contract. Fix by making gate elevation depend on loaded SoA art availability, not just `soaRowMap` membership: either pass a `hasLoadedSoaSheet` predicate into `resolveActivityState`, or add a renderer-level fallback that restores the current hook state when a gate state resolves but Codogotchi returns no SoA frames.

**Advisory Observations**
[apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift:56) is stale after the resolution-order inversion. Its name says `.idle` resolves from Codex, but the assertion only checks for non-empty frames with count `<= 8`, so it would pass whether `.idle` came from lite or Codex and no longer protects the P8.06 behavior.

[notes/public/codogotchi-lite-and-soa-spritesheet-contact-sheets.md](/Users/cesar/code/codogotchi_p8_06/notes/public/codogotchi-lite-and-soa-spritesheet-contact-sheets.md:86) has drifted from the current implementation. It still documents 24-frame rows and lists SoA row 6 as `record_review` and row 7 as `review_clean`, while the shipped implementation is 8 frames/row and maps row 6 to `reviewClean` and row 7 to `recordReview`.

[apps/menubar/Sources/MenubarApp.swift](/Users/cesar/code/codogotchi_p8_06/apps/menubar/Sources/MenubarApp.swift:123) has an outdated comment saying “the nine SoA-owned states fall back to idle.” The current split is 10 SoA gate states and 9 lite hook states; the comment no longer matches the code shape.

**Runner termination**
`runnerStatus`: `completed`
`terminatedReason`: Reviewed the targeted surfaces, inspected the related code/docs/assets, and ran the full `xcodebuild test` suite successfully.
