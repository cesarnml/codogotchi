You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks.

### Ticket scope

Outcome:
- Swift `EXPECTED_STATE_SCHEMA_VERSION` is **10**; `SlicePayload` / `StateSnapshot` carry the four optional stamp fields when present on disk.
- `PromptTimerTracker` prefers `prompt_started_at` for running start; freezes on `turn_ended_at` when present; for `errored`, freezes at `errored_since + 60s` (existing grace) without writing the slice; falls back to today’s `updated_at` heuristics only when stamps are absent.
- Force Idle (and any other menubar idle rewrite of a slice) clears turn clocks (`prompt_started_at`, `errored_since`, `turn_ended_at`) — `session_started_at` is preserved.
- After simulated pool tracker loss / relaunch inputs, tests show matching elapsed for in-flight, standby-frozen, and long-errored cases when stamps are present.

### Files touched

Implementation:
- apps/menubar/Sources/State/ActivityState.swift
- apps/menubar/Sources/State/StateJsonReader.swift
- apps/menubar/Sources/State/StateJsonWriter.swift
- apps/menubar/Sources/Windows/PromptTimer.swift
- apps/menubar/Sources/Pool/Derive/PoolDerive.swift
- apps/menubar/Sources/App/LivePollingDriver.swift

Tests:
- apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift
- apps/menubar/Tests/MenubarTests/PromptTimerTests.swift
- apps/menubar/Tests/MenubarTests/StateJsonWriterTests.swift
- docs/product/delivery/phase-20/ticket-02-swift-prompt-timer-force-idle.md

### Invariants to hold

1. When `prompt_started_at` is present, running elapsed uses that stamp even if `updated_at` advances on mid-turn tool ticks; when absent, existing `updated_at` heuristics still work.
2. Standby freeze uses `turn_ended_at` when present; errored freeze uses `errored_since + 60s` in-memory only — menubar never writes `turn_ended_at` on the 60s threshold.
3. Force Idle / dismissAttention idle rewrites remove `prompt_started_at`, `errored_since`, `turn_ended_at` from on-disk JSON while preserving `session_started_at`.

### Attack surfaces to probe

1. `PromptTimerTracker.observe` stamp priority vs idle/reset race — can a stamped idle slice or Force Idle race leave an immortal chip?
2. `StateJsonWriter.clearTurnStamps` coverage — are all idle rewrite helpers covered, or can one path leave stamps?
3. `LivePollingDriver` gate-merge rebuild — do stamps survive every snapshot reconstruction path, or only directory readers?
4. `PoolDerive` both observe call sites — per-entry and combined winner both pass stamps?
5. Decode of invalid/malformed stamp strings — fail open to nil or crash?
6. Single-writer rule — any path writing `turn_ended_at` from menubar?

#### Diff-derived attack surfaces

1. Output stability across schema-version drift — EXPECTED 10 vs older slices without stamps
2. CLI flag/arg symmetry — [N/A if no CLI changes]
3. Error-class breadth in catch blocks
4. Defensive layering at module boundaries (SlicePayload → StateSnapshot → observe)
5. Cross-file atomicity windows on Force Idle rewrite
6. Test-contract strength for stamp hydrate/freeze/clear
7. Doc-vs-code drift in ticket Rationale

### Diff context

Green commit wires v10 expected schema, optional stamp decode onto StateSnapshot, PromptTimer hydrate preferring stamps with updated_at fallback, Force Idle/dismiss clearTurnStamps helper, and LivePollingDriver stamp passthrough on gate-merge rebuild. PoolDerive threads stamps into both observe call sites.

---

### Your directives

**Scope:** Adversarial review of the implementation diff and named attack surfaces only.
**Advisory-only — no file writes.** Prose findings only.
**Finding discipline:** Report only invariant breaks, demonstrable correctness gaps, or spec-permits-real-bug cases. Else Advisory Observations.
**No fabrication pressure:** Clean report is valid.

### Required output format

Exactly these five headings in order: `Invariant results`, `Surface results`, `Actionable findings`, `Advisory Observations`, `Runner termination`.
Do not use `---` horizontal rules anywhere in the report.
Under Actionable findings write `None.` if clean.
Under Runner termination state review completed without modifying files.
