You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations**.
Your job is a targeted attack on the behavior this ticket is supposed to protect.

### Ticket scope

Outcome:
- Settings > Sessions rows in Active, Live, and Archived show relative Started when `session_started_at` is present (e.g. `Started · 2h ago`).
- When Idle age is already shown, Started and Idle combine into one subtitle line.
- When `session_started_at` is missing, Started is omitted — never fabricated from `updated_at`.
- View-model / formatting tests cover present, absent, and combine-with-idle cases.

### Files touched

Implementation:
- apps/menubar/Sources/State/StateJsonReader.swift
- apps/menubar/Sources/Settings/SessionsTabViewModel.swift
- apps/menubar/Sources/Settings/SessionRowView.swift
- apps/menubar/Codogotchi.xcodeproj/project.pbxproj

Tests:
- apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift
- apps/menubar/Tests/MenubarTests/SessionsTabViewModelTests.swift
- docs/product/delivery/phase-20/ticket-03-sessions-started-subtitle.md

### Invariants to hold

1. All three tiers (Active/Live/Archived) apply the same Started rule when `session_started_at` is present.
2. Missing/unparseable stamp → omit Started; never use `updated_at` as a substitute.
3. When Idle/Quiet age exists, Started combines into one subtitle line without dropping the existing status fragment.

### Attack surfaces to probe

1. `readSessionStartedAt(atPath:)` — partial decode failures, wrong key, invalid datetime
2. `SessionsTabViewModel.refresh()` — Active tier path vs Live/Archived; any tier skipping the stamp read
3. `SessionRowView.subtitleText` — combine ordering, empty strings, nil stamp
4. Fabrication risk — any fallback to `updatedAt` / mtime
5. Timezone/relativeAge edge cases for Started fragment

#### Diff-derived attack surfaces

1. Output stability across schema-version drift (pre-v10 slices)
2. CLI flag/arg symmetry — N/A if unchanged
3. Error-class breadth in catch blocks for partial decode
4. Defensive layering at readSessionStartedAt boundary
5. Cross-file atomicity — N/A for UI read path
6. Test-contract strength across three tiers
7. Doc-vs-code drift in ticket Rationale

### Diff context

Adds lightweight per-slice `session_started_at` read into Sessions refresh, threads into SessionRow, renders combined subtitle via SessionRowView.subtitleText. Omits when missing.

---

### Your directives

Advisory-only — no file writes. Finding discipline as usual. Clean report is valid.

### Required output format

Exactly: `Invariant results`, `Surface results`, `Actionable findings`, `Advisory Observations`, `Runner termination`.
No `---` horizontal rules. Actionable findings = `None.` if clean.
