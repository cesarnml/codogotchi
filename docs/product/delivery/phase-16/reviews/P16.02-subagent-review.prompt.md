You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P16.02 Split FloatingPetPanel.swift — mechanical, pure extraction. Outcome:

- `apps/menubar/Sources/Windows/FloatingPetPanel.swift` (5,144 lines, ~30 top-level types) no longer exists.
- Every type it contained lives in its own file under `Windows/`, named after the type (type-per-file), with two sanctioned bundling exceptions (below).
- No signature changes, with one sanctioned deviation: `private`/`fileprivate` (file-scoped) types/members widened to `internal` where extraction forces it — and no new call sites appear for any widened symbol.
- Full existing suite green, unmodified.

Rationale notes recorded after implementation (verbatim from the ticket doc's `## Rationale`):

- Verified with a full-body diff (imports stripped) between the original file and the concatenation of all 26 new files — exactly one hunk, the sanctioned `extension FloatingPetHidePrompt` relocation next to its enum; no other line altered, reordered, or dropped.
- Tiny-leaf bundling beyond the ticket's explicit `MinimalistPanelSizePill (+ FirstMouseSlider, MinimalistPanelSizePillPanel)` grouping: `FloatingInteractionHitTarget` (4-line enum, sole consumer `FloatingInteractionPolicy`) bundled into `FloatingInteractionPolicy.swift` rather than given its own file.
- `private`/`fileprivate` → `internal` widenings (type or member, each compiler-forced): `MinimalistBadgeView` (class + `hPad` + `beginExternalDrag`/`continueExternalDrag`/`endExternalDrag`/`presentHidePrompt`), `GateBadgeTokenView` (class), `TimerIconChipView` (class), `FloatingPetInteractionView` (class + `beginExternalDrag`/`continueExternalDrag`/`endExternalDrag`/`presentHidePrompt`), `FloatingPetOverlayView` (class), `FloatingPetHidePromptPanel` (class), `FloatingPetHidePromptView` (class), `FloatingPetHidePromptTintView` (class), `MinimalistPanelSizePillPanel` (class).
- `FirstMouseSlider` stayed `private` (no cross-file use).
- Two new files omit `import SpriteKit` despite prose mentions of SpriteKit (`FloatingPetPanelController.swift`, `FloatingPetOverlayView.swift`) — neither uses an actual `SK*` type; `FloatingPetInteractionView.swift` kept `import SpriteKit` (uses `SKView`/`SKScene`).
- `bun run ci:quiet`: PASS, 1015 tests / 0 failures, matching the recorded CI baseline exactly.

### Files touched

Implementation (26 new files, 1 deleted, all under `apps/menubar/Sources/Windows/`):
AnimationBadgeChrome.swift
AnimationBadgeLayout.swift
AnimationBadgePanel.swift
AnimationBadgeView.swift
AnimationLabelPillView.swift
FloatingInteractionPolicy.swift
FloatingPetHidePrompt.swift
FloatingPetHidePromptPanel.swift
FloatingPetHidePromptTintView.swift
FloatingPetHidePromptView.swift
FloatingPetInteractionView.swift
FloatingPetOverlayView.swift
FloatingPetPanelController.swift
FloatingPetPromptCoordinator.swift
FloatingPetPromptItem.swift
GateBadgeLayout.swift
GateBadgePanel.swift
GateBadgeTokenView.swift
GateBadgeView.swift
MinimalistBadgeView.swift
MinimalistPanelController.swift
MinimalistPanelSizePill.swift
PlatformChipView.swift
PlatformSessionBadge.swift
PromptTimerChipView.swift
TimerIconChipView.swift
FloatingPetPanel.swift (deleted)

Also touched: `apps/menubar/Codogotchi.xcodeproj/project.pbxproj` (xcodegen-regenerated file list), `docs/product/delivery/phase-16/ticket-02-split-floating-pet-panel.md` (Rationale only).

Tests:
None changed. `Red: skip` — this is a mechanical, behavior-preserving extraction; the existing suite passing unmodified is the gate, not a new test.

### Invariants to hold

1. Every line of code, doc comment, and `// MARK:` marker present in the deleted `FloatingPetPanel.swift` must be present in exactly one of the 26 new files — no content dropped, duplicated, or silently altered, except for the one sanctioned `extension FloatingPetHidePrompt` relocation and per-file `import` line additions.
2. Every `private`/`fileprivate` → `internal` access widening must be strictly necessary for the split to compile — i.e., removing the widening (reverting to `private`/`fileprivate`) must break compilation of the specific cross-file reference named in the Rationale, and the widening must not make the symbol reachable from any file outside `apps/menubar/Sources/Windows/` that could not reach it before.
3. No type's public API (initializers, method signatures, protocol conformances) changed — only file location and the sanctioned access-level widenings changed.

### Attack surfaces to probe

1. `FloatingPetPanel.swift` deletion vs. the 26 new files — full-content diff. The primary agent self-reported a "one hunk" verbatim diff; independently re-derive this (e.g. `git show <base>:apps/menubar/Sources/Windows/FloatingPetPanel.swift` concatenated/reordered against the new files, or reconstruct via `git diff --stat` + manual read) rather than trusting the self-report.
2. Each of the 9 listed access-widenings (`MinimalistBadgeView`, `GateBadgeTokenView`, `TimerIconChipView`, `FloatingPetInteractionView`, `FloatingPetOverlayView`, `FloatingPetHidePromptPanel`, `FloatingPetHidePromptView`, `FloatingPetHidePromptTintView`, `MinimalistPanelSizePillPanel`, plus the several widened members) — for each, grep the full `apps/menubar/Sources/` tree (not just `Windows/`) for any usage from outside the `Windows/` directory that the widening could now expose.
3. The `FloatingInteractionHitTarget` + `FloatingInteractionPolicy` bundling and the `MinimalistPanelSizePill` + `FirstMouseSlider` + `MinimalistPanelSizePillPanel` bundling — confirm each bundled tiny type genuinely has no consumer outside its bundle-mate's file (i.e. the "tiny leaf, sole owner" justification actually holds, not just asserted).
4. The relocated `extension FloatingPetHidePrompt` (originally after `FloatingPetOverlayView` at old line 4742, now merged into `FloatingPetHidePrompt.swift`) — confirm the extension's members and any stored/computed properties still resolve correctly against the enum now that intervening code (which may have referenced or been referenced by the extension) has moved to different files.
5. `import` pruning — for each of the 26 new files, confirm the imports actually present (`AppKit`, and `SpriteKit` only in `FloatingPetInteractionView.swift`) are sufficient; specifically verify `FloatingPetPanelController.swift` and `FloatingPetOverlayView.swift` truly have no `SK*`-type usage despite prose mentioning SpriteKit, since a missed `SKView`/`SKScene` reference there would only surface as a build break the primary agent might have "fixed" by some other means.
6. `apps/menubar/Codogotchi.xcodeproj/project.pbxproj` regeneration — confirm the regenerated project file correctly lists all 26 new files and no longer references the deleted `FloatingPetPanel.swift`, and that no unrelated pbxproj drift (build settings, other targets) crept in via the `xcodegen generate` step.

#### Diff-derived attack surfaces

Probe each of the seven classes below and report coverage using `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

1. **Output stability across schema-version drift** — N/A expected (no persisted artifact/CLI/JSON format in this diff), but confirm nothing in the touched files serializes state in a way a consumer could break on.
2. **CLI flag/arg symmetry** — N/A expected (no CLI surface touched); confirm.
3. **Error-class breadth in `catch` blocks** — probe whether any of the moved/widened code has `catch` blocks whose behavior could differ now that the type is `internal` instead of `private` (e.g. broader construction/testing surface reaching a catch that previously only saw file-local calls).
4. **Defensive layering at module boundaries** — the widenings are exactly new module-internal (but cross-file) boundary crossings within the app target. For each widened symbol, check whether the newly-possible cross-file caller does anything the original file-local caller didn't already guarantee (e.g. did the original caller call methods only after certain view-lifecycle guarantees that a hypothetical new caller elsewhere in the target could violate — even though no new caller was added, confirm the widening doesn't silently loosen an implicit precondition).
5. **Cross-file atomicity windows** — N/A expected (pure file-move, no multi-step state writes), but if any two widened files reference shared mutable state (e.g. singletons, delegates) confirm ordering/lifecycle assumptions weren't disturbed by the split.
6. **Test-contract strength** — N/A: no tests changed. Confirm the existing suite genuinely exercises the moved types (i.e. their behavior isn't merely compiled but untested), and flag in Advisory Observations if a type appears to have zero test coverage before and after — that's a pre-existing gap, not introduced by this ticket, but worth naming.
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-16/ticket-02-split-floating-pet-panel.md` end to end and confirm every claim in its `## Rationale` (widening list, bundling exceptions, verbatim-diff claim, CI result) matches what the diff actually shows. Report any mismatch as an Advisory Observation, not a patch.

### Diff context

This is a pure structural extraction: one 5,144-line file (`FloatingPetPanel.swift`, ~30 top-level types spanning a minimalist-mode panel system, a main floating-pet panel system, a gate-badge system, an animation-badge system, and a hide-prompt system) is deleted, and its content is redistributed verbatim into 26 new sibling files under the same `Windows/` directory, one file per type (with two sanctioned tiny-leaf bundlings). The only non-verbatim changes are: (a) per-file `import` lines, (b) 9 symbols widened from `private`/`fileprivate` to `internal` where the compiler required it after the split, and (c) one `extension FloatingPetHidePrompt` block relocated to sit next to its enum. No logic, control flow, signatures, or renames changed. `apps/menubar/Codogotchi.xcodeproj/project.pbxproj` was regenerated via `xcodegen generate` to pick up the new file list.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
