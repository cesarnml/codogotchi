You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Ticket: P16.04 WindowKey enum (repo: codogotchi, worktree: /Users/cesar/code/codogotchi_p16_04, branch: agents/p16-04-windowkey-enum, base: agents/p16-03-split-settingswindowcontroller-swift)

Outcome (from ticket-04-windowkey-enum.md):
- `WindowKey` exists in `Pool/`: `.origin(String)`, `.session(origin:id:)`, `.combined`, with `init?(rawValue:)` + `rawValue` as the **only** parse/serialize path in the target.
- `FloatingPetWindowPool` and `RenderKeyResolver` APIs take/return `WindowKey`; every consumer converted in the same PR (compiler-driven sweep, no string-based compatibility shims).
- Kill list satisfied: `grep -rn '"combined"' apps/menubar/Sources` hits only `WindowKey`'s own parse/serialize, persistence encode/decode boundaries (`app-state.json`, slice filenames), and test fixtures. Zero policy-site string checks.
- The colon-split sites are gone: `FloatingPetWindowPool` lines ~241/253 (`origin(forKey:)`, `sessionID(forKey:)`), `RenderKeyResolver:110`, `GateJsonReader:210` — all replaced by `WindowKey` matching or its raw-value boundary.
- Full existing suite green; only new tests added are `WindowKey`'s own.

Rationale notes (appended during implementation, from ticket doc):
- `GateJsonReader.swift`'s filename parser (renamed `sessionWindowKey(of:suffix:)`) still colon-splits a `<origin>:<session_id>.gate.json`/`.context.json` FILENAME (not a bare rawValue) but now returns `WindowKey?` directly instead of a raw tuple — kept as a boundary parse per the ticket's own guidance.
- `StateJsonReader.parseSliceFilename` (a second, independent filename parser, out of this ticket's named scope) was left returning a raw `(origin, sessionId)` tuple; its callers convert to `WindowKey` at their own call sites.
- The sweep additionally touched `SessionSelectionPolicy.swift`, `ConflictBubbleTargetSelector.swift`, `PerPlatformSnapshot.swift`, `SessionsTabViewModel.swift`, and `AppState.swift` (the `app-state.json` `floating_pet_positions`/`floating_pet_hidden` boundary) beyond the ticket's named call-site list — required by the compiler once the pool's internal dictionaries and `PerPlatformSnapshot`'s fields became `WindowKey`-keyed.
- Added `WindowKey: ExpressibleByStringLiteral` (delegates to `init?(rawValue:)`, degrading a malformed literal to `.origin(value)`) for test-fixture ergonomics only.
- `SessionLabelStore`/`RetrievedSessionTitleStore`/`SessionPruner` were left `String`-keyed (same persistence-boundary category as `app-state.json`, outside `FloatingPetWindowPool`/`RenderKeyResolver`'s public surface); the pool's closures convert via `.rawValue` at the call boundary.
- `AssignmentsJsonReader.resolve(origin:)`'s `"combined"` and `PlatformAttribution.init?(origin:)`'s `"combined"` remain `String` comparisons by design — a different "combined" concept (pet-assignment slot name; `source_event.origin` wire value) that merely shares the literal string with `WindowKey.combined`.

### Files touched

Implementation:
apps/menubar/Sources/Pool/WindowKey.swift (new)
apps/menubar/Sources/Pool/FloatingPetWindowPool.swift
apps/menubar/Sources/Pool/RenderKeyResolver.swift
apps/menubar/Sources/Pool/SessionSelectionPolicy.swift
apps/menubar/Sources/Windows/ConflictBubbleTargetSelector.swift
apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/State/AppState.swift
apps/menubar/Sources/State/GateJsonReader.swift
apps/menubar/Sources/State/PerPlatformSnapshot.swift
apps/menubar/Sources/Settings/SessionsTabViewModel.swift
apps/menubar/Sources/App/LivePollingDriver.swift
apps/menubar/Sources/App/MenubarApp.swift
apps/menubar/Sources/App/MenubarMenu.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj (WindowKey.swift + WindowKeyTests.swift build-file registration)

Tests:
apps/menubar/Tests/MenubarTests/WindowKeyTests.swift (new)
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift
apps/menubar/Tests/MenubarTests/SessionSelectionPolicyTests.swift
apps/menubar/Tests/MenubarTests/ConflictBubbleTargetSelectorTests.swift
apps/menubar/Tests/MenubarTests/MenuItemsTests.swift
apps/menubar/Tests/MenubarTests/PerSessionReaderTests.swift

### Invariants to hold

1. `WindowKey.rawValue` must byte-match the pre-existing string convention exactly for all three cases (`.origin(x)` → `x`, `.session(origin:id:)` → `"origin:id"`, `.combined` → `"combined"`) — any drift silently corrupts `app-state.json` and gate/slice filenames on upgrade from a pre-P16.04 build.
2. Every window-key POLICY decision (mode dispatch, TTL/last-active/hidden/pending checks, combined-vs-direct branching, session-cap partitioning) must be a `switch`/`if case` match on `WindowKey`, never a string comparison against a re-derived `.rawValue` — outside the three sanctioned boundaries (`app-state.json` encode/decode, slice/gate filename construction, test fixtures).
3. No change to key-resolution order or fallback semantics: TTL dismissal, last-active immunity, session-cap selection, and combined-window winner election must behave identically to the pre-refactor string-keyed implementation for the same inputs.

### Attack surfaces to probe

1. `WindowKey.init?(rawValue:)` — session ids or origins that themselves contain a colon (e.g. a session id like `"a:b"`), or a raw string equal to the literal `"combined"` colliding with a plain-origin key literally named `"combined"`: does parsing silently misroute origin/session-boundary detection, and was this ambiguity already present pre-refactor (i.e. not a regression) or newly introduced?
2. `FloatingPetWindowPool`'s internal bookkeeping dictionaries (`windows`, `lastSeenAt`, `firstSeenAt`, `lastUpdatedAt`, `slotOccupants`, `promptTimers`, `windowSpawnedModes`, `userHiddenWindowKeys`, `evictedSessionFrames`, `activeConflictBubbleTargets`) — did ALL of them consistently convert to `WindowKey`-keyed, or does any one remain `String`-keyed (or keyed by `.rawValue` at a call site that should have used `WindowKey` directly), which would silently desync bookkeeping across two representations of the same key?
3. `AppState.swift`'s `app-state.json` boundary (`floating_pet_positions`/`floating_pet_hidden`) — does encode/decode go through `WindowKey.rawValue`/`init?(rawValue:)` symmetrically, and does a malformed or legacy key in an existing on-disk `app-state.json` (pre-P16.04 file) fail closed (dropped/ignored) rather than crashing on load?
4. `GateJsonReader.sessionWindowKey(of:suffix:)` — filename parsing for `<origin>:<session_id>.gate.json`/`.context.json`: does an origin or session id containing whitespace, an extra colon, or empty half after trimming produce a `nil` (skip) rather than a malformed `WindowKey`, and does this match the pre-refactor tuple-returning behavior exactly (no new silent-drop or newly-accepted malformed case)?
5. `RenderKeyResolver.resolveRenderKeys`/`makeSessionKey` — with the render-key type now `WindowKey`, is the equal-`updated_at` tie-break (documented as "lexicographically smallest per-session key, sorted iteration + strict `>`") still deterministic and unchanged now that dictionary keys are `WindowKey` (Hashable, not Comparable) rather than `String`? Where does the sort/iteration order now come from, and could `WindowKey`'s `Hashable` conformance (no custom `Comparable`) have silently made this order-dependent again?
6. `WindowKey: ExpressibleByStringLiteral` — this degrades a malformed literal to `.origin(value)` rather than trapping. Are there any PRODUCTION (non-test) call sites — even indirectly, via a default parameter, generic inference, or an API that accepts a literal — that could rely on this implicit conversion and silently swallow what should have been a `nil`/rejected key?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `app-state.json` and `session-labels.json`/slice `.gate.json`/`.context.json` filenames are the persisted formats crossing this refactor. Probe: does a real pre-P16.04 `app-state.json` fixture (containing plain-origin keys, `origin:session_id` keys, and the literal `"combined"` key) round-trip through the new `WindowKey`-based load/save path with byte-identical persisted key strings on the next write?
2. **CLI flag/arg symmetry** — N/A, no CLI flags changed in this diff (menubar app, not the delivery CLI). Confirm and mark N/A.
3. **Error-class breadth in `catch` blocks** — probe whether any new `guard`/`init?` failure path added by this diff (e.g. `WindowKey.init?(rawValue:)` returning `nil`) is now silently swallowed somewhere that used to at least log or assert, versus the pre-refactor string-based code.
4. **Defensive layering at module boundaries** — at each new cross-module `WindowKey` boundary crossing (`AppState`, `GateJsonReader`, `RenderKeyResolver` → `FloatingPetWindowPool`), does the receiving side still validate/guard against a malformed or unexpected key rather than assuming the sender always hands a well-formed one?
5. **Cross-file atomicity windows** — N/A expected (no new multi-step persisted write introduced by this ticket), but confirm `AppState.swift`'s save path for `floating_pet_positions`/`floating_pet_hidden` wasn't restructured into a new partial-write window as a side effect of the key-type change.
6. **Test-contract strength** — do `WindowKeyTests` assert the exact persisted raw-string forms (byte-for-byte), not just "it parses"? Do the updated `FloatingPetWindowPoolTests`/`SessionSelectionPolicyTests`/etc. still assert original BEHAVIOR (TTL, cap, election outcomes) rather than having been loosened to merely compile against the new type?
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-16/ticket-04-windowkey-enum.md`'s `## Rationale` section (appended post-sweep) against the actual diff: does every claim in the Rationale (the `GateJsonReader` boundary decision, the files touched beyond the named list, the `ExpressibleByStringLiteral` addition, the `AssignmentsJsonReader`/`PlatformAttribution` "different combined concept" claims) hold up under your own independent trace of those call sites?

### Diff context

This is a compiler-driven mechanical type-introduction refactor: a new `WindowKey` enum (`.origin(String)`, `.session(origin:id:)`, `.combined`) replaces raw `String` window/render keys throughout the menubar pool layer. `FloatingPetWindowPool`'s internal state dictionaries and public API, `RenderKeyResolver.resolveRenderKeys`'s output maps, and `PerPlatformSnapshot`'s render-keyed fields all switch from `String` to `WindowKey`. Two colon-splitting helpers (`origin(forWindowKey:)`, `sessionIdentity(forWindowKey:)` in the pool; `splitSessionKey` in the resolver) are deleted and replaced by `WindowKey`'s own `origin`/`sessionIdentity` properties and `init?(rawValue:)`. `GateJsonReader`'s filename-boundary parser now returns `WindowKey?` directly instead of a raw tuple. `AppState.swift`'s `app-state.json` persistence layer converts window/hidden-key sets through `WindowKey.rawValue`/`init?(rawValue:)` at encode/decode. No behavioral change to TTL, last-active election, session-cap selection, or combined-window folding logic was intended — only the key's representation changed. Run `git diff agents/p16-03-split-settingswindowcontroller-swift..HEAD -- apps/menubar/Sources apps/menubar/Tests` in the worktree for the full hunks (21 files changed, 642 insertions, 413 deletions).

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
