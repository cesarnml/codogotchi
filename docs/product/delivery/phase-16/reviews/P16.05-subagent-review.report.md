**Invariant results**

1. [held] `SessionLifecycle.classify` reads only its arguments and contains no time source, I/O, or shared-state access.
2. [held] Exhaustive comparison against the pre-refactor branch chain found identical pruning, tier, and `isShown` results for every combination of rendered, hidden, pending-show, and TTL-dismissed state, including both TTL boundaries.
3. [held] `pendingShowKeys.remove(key)` remains confined to the rendered and hidden-by-user branches; overlapping flags preserve the original rendered > hidden > pending-show > TTL-dismissed precedence.

**Surface results**

- Classifier boundary precedence: [probed]. `guard age < archiveTTL else { return .pruned }` executes before the rendered check. The `isRendered: true, age == archiveTTL` test asserts `.pruned`.
- Concealment collapse: [probed]. Exhaustive branch reasoning found the boolean collapse affects only lifecycle tier selection. The inner `.active` branch preserves the original flag precedence, `isShown`, and removal behavior for all overlaps.
- Concealed `.live`/`.archived` paths: [probed]. Concealed entries reach `.archived` when `age >= liveTTL` and remain `isShown: false`, matching the old fallthrough. Concealed entries cannot reach `.live`, because any concealed entry below `liveTTL` is `.active`.
- Unreachable `.pruned` switch case: [probed]. The immediately preceding guard provably excludes `.pruned`; the switch case is dead but harmless and does not mask any current path.
- Menubar lifecycle tiering: [probed]. `buildPetSection()` refreshes and consumes the shared view model’s `activeRows` and `liveRows` without clock comparisons. `fallbackActiveRows()` is used only when no view model is supplied and reconstructs pool state without lifecycle-clock judgment.
- Consumer-wiring test coverage: [probed]. Existing menu tests exercise rendered/hidden tiering, fresh unrendered rows, stale hidden rows, and TTL-dismissed rows. No test directly exercises `pendingShowKeys` or overlapping concealment flags, but exhaustive comparison of the old and new branches found no behavior difference.
- Output stability across schema-version drift: [N/A — no persisted artifact, stdout contract, or JSON schema changed].
- CLI flag/arg symmetry: [N/A — no CLI flags or arguments changed].
- Error-class breadth in catch blocks: [N/A — no catch block was added or modified].
- Defensive layering at module boundaries: [probed]. The internal classifier has one controlled call site using an mtime-derived age and persisted TTL settings. Preserving the caller’s pre-existing handling of future mtimes or unusual TTL values is appropriate for this behavior-neutral extraction.
- Cross-file atomicity windows: [N/A — classification is pure and no multi-step write was introduced].
- Test-contract strength: [probed]. Tests use `XCTAssertEqual` against enum cases and cover all five classifier outcomes/branches, strict live and archive boundaries, and prune precedence over both rendered and concealed state.
- Doc-vs-code drift in the ticket Rationale: [probed]. The appended Rationale accurately explains that menubar tiering already delegated to `SessionsTabViewModel`, so no `MenubarMenu` edit was required. The earlier Outcome/Green wording still literally says both consumers are “rewired,” but the implementation has one lifecycle-judgment site and no behavioral disagreement.

**Actionable findings**

None.

**Advisory Observations**

- `apps/menubar/Tests/MenubarTests/SessionLifecycleTests.swift`: `testConcealedAtLiveTTLBoundaryIsLive` asserts `.archived`, and its comment calls the exact-boundary input “the still-under-TTL side.” The assertion correctly pins behavior, but the test name and comment contradict it.

- `apps/menubar/Tests/MenubarTests/SessionLifecycleTests.swift`: existing consumer coverage does not directly pin `pendingShowKeys` during the one-tick respawn gap or simultaneous hidden/pending/TTL-dismissed flags. Manual exhaustive comparison protects the current refactor, but a focused view-model test would make invariant 3 machine-checkable.

- `docs/product/delivery/phase-16/ticket-05-session-lifecycle.md`: Outcome and Green say menubar lifecycle tiering is “rewired,” while the diff leaves `MenubarMenu` unchanged because it already delegates to the shared view model. The appended Rationale resolves the behavioral intent, but the earlier wording remains literally imprecise.

- `apps/menubar/Sources/Pool/SessionLifecycle.swift`: its file-level comment says the classifier replaces inline clock comparisons that the view model and menubar “each used to carry separately.” Repository history shows the menubar already delegated and did not contain a second set of clock comparisons.

**Runner termination**

runnerStatus: completed  
terminatedReason: review completed against every specified invariant and attack surface without modifying repository files.
