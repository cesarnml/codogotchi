```
You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P5.05 — App bootstrap + app-state + `hooks status`.

Outcome (from ticket):
- Before onboarding: if `~/.codogotchi/config.json` missing, app writes minimal Lite
  config (new `profile_id`, `pet: "maew"`, `rpg_enabled: false`) — coordinates with P5.04
  pet seed order so first frame can show Maew.
- `app-state.json` schema extended (version field bumped if needed) with:
  `onboarding_completed_at` (optional ISO timestamp), `last_hook_activity_at`, optional
  cached `hooks_status` snapshot for UI.
- App runs `codogotchi hooks status --json` via `Process`, parses JSON, surfaces errors
  in logs/onboarding later.
- `Hooks not active` predicate: hooks not installed OR no recent hook-driven activity per
  status contract.
- `CODOGOTCHI_HOME` respected for config, app-state, and pet paths in Swift.

Rationale highlights:
- Bootstrap is a single-purpose helper (`ConfigBootstrap`); subprocess/parse split via
  `HookStatusClient.runner` for hermetic tests.
- App-state `schema_version` intentionally NOT bumped (still 1) — three new fields are
  additive optionals; older readers ignore them and pre-existing files load fine.
- "Hooks not active" ≡ no platform with `installed && firing_recently`. The CLI's
  `firing_recently` boolean is the source of truth; `lastHookActivityAt` is metadata for
  later UI freshness display only.

### Files touched

Implementation:
apps/menubar/Sources/AppState.swift
apps/menubar/Sources/ConfigBootstrap.swift
apps/menubar/Sources/FloatingPetController.swift
apps/menubar/Sources/HookStatusClient.swift
apps/menubar/Sources/HooksStatusSnapshot.swift
apps/menubar/Sources/MenubarApp.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj

Tests:
apps/menubar/Tests/MenubarTests/AppBootstrapTests.swift
apps/menubar/Tests/MenubarTests/AppStateTests.swift

### Invariants to hold

1. `ConfigBootstrap.ensureLiteConfig()` must NEVER overwrite an existing
   `config.json` — outcome `.alreadyExists` is the only allowed result when the file
   exists at `PetConfig.configURL()`, and file bytes must be byte-identical after the
   call regardless of file contents.
2. `AppStateStore.load` / `AppStateStore.save` must round-trip the three new optional
   fields (`onboardingCompletedAt`, `lastHookActivityAt`, `hooksStatus`) AND must still
   accept pre-existing payloads that omit those keys (returning `nil` for each) without
   downgrading or rewriting the file.
3. `HookStatusClient.fetch` must throw `Failure.commandFailed` on any non-zero runner
   exit and `Failure.parseFailed` on any non-decodable stdout — it must never return a
   partially populated `HooksStatusSnapshot` or a default-initialized one when the CLI
   misbehaves.

### Attack surfaces to probe

1. `ConfigBootstrap.ensureLiteConfig` — overwrite-guard: race or symlink between
   `FileManager.default.fileExists` check and `data.write(to:options:.atomic)`. Probe
   whether `.atomic` write here would ever clobber an existing file the existence check
   missed (e.g. symlink target written between check and write).
2. `ConfigBootstrap.ensureLiteConfig` — schema shape: does the JSON payload match the
   `CodogotchiConfig` shape used by the TS CLI (`profile_id`, `pet`, `features.rpg_enabled`)?
   Any extra/missing keys would make `codogotchi hooks install` fail on first launch.
3. `MenubarApp.refreshHookStatusCache` — read-modify-write race against
   `FloatingPetController`: both the controller and this refresh path call
   `AppStateStore.load` / `AppStateStore.save` on the same `app-state.json`. Probe whether
   a frame-change save during refresh can drop the newly-cached `hooksStatus`, or vice
   versa drop a pending visibility change.
4. `HookStatusClient.defaultRunner` — `/usr/bin/env codogotchi` PATH resolution: when the
   menubar app launches from `/Applications` (no shell), is the user's PATH set such that
   `codogotchi` resolves? If not, this returns exitCode 127 with no JSON — does the
   failure path stay benign (logged-only) and not corrupt cached state?
5. `HooksStatusSnapshot.isHooksNotActive` — predicate honesty: the predicate iterates
   ALL five platforms including `cursor`/`vscode`/`antigravity` which are
   `installable_in_phase: false` for Phase 05. If any of those ever set
   `installed && firing_recently` in a malformed snapshot, would the CTA flip to
   "active" when no real Codex/Claude integration is firing?
6. `FloatingPetController.setFloatingPetVisible` and `saveClampedFrame` — field
   preservation: now that these construct `FloatingAppState` with the new optional
   fields read from `state`, probe whether any callsite still constructs
   `FloatingAppState` via the old positional/labeled init and silently zeroes the new
   fields.

#### Diff-derived attack surfaces

In addition to the ticket-spec-derived surfaces above, probe each of the seven
diff-derived classes below. These are the finding classes external code review
(e.g. CodeRabbit) catches consistently and the prior template did not name. The
primary agent must enumerate concrete surfaces drawn from this diff against each
class. The subagent must report coverage for every class using the form
`[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

1. **Output stability across schema-version drift.** `app-state.json` schema_version
   stays at `1` despite three new fields. Probe: a Phase 04 build reading a Phase 05
   `app-state.json` — does the older `AppStatePayload` Codable conformance fail to
   decode (rejecting unknown keys) or silently ignore the new fields? Phase 04
   `AppStateStore.load` is the relevant reader; check whether its fallback path on
   decode failure would silently wipe the new fields by overwriting with a fresh
   default. Concrete surface: `private struct AppStatePayload: Codable` in
   `AppState.swift` — Swift's synthesized `Codable` ignores unknown keys by default,
   but the file is also re-saved by any controller action, which would drop the
   un-decoded keys.
2. **CLI flag/arg symmetry.** `HookStatusClient` calls `codogotchi hooks status --json`.
   Probe: does the CLI's `hooks status` command actually accept `--json` (router.ts
   shows it does), and does its JSON shape match `HooksStatusSnapshot` field for field
   including the `source_origin` optional? Mismatch → `parseFailed` on every fetch.
3. **Error-class breadth in `catch` blocks.** New `catch` sites:
   `MenubarApp.applicationDidFinishLaunching` for ConfigBootstrap,
   `MenubarApp.refreshHookStatusCache` for fetch + save,
   `HookStatusClient.defaultRunner` for `process.run()`,
   `HookStatusClient.fetch` for JSON decode. Probe: do any of these mask a real
   developer-attention-worthy failure mode (e.g. permission-denied on
   `~/.codogotchi/config.json` vs. file-already-exists) behind a generic NSLog?
4. **Defensive layering at module boundaries.** `MenubarApp.refreshHookStatusCache`
   trusts the `HooksStatusSnapshot` returned by `HookStatusClient.fetch` and writes
   it straight into app-state. Probe: if a malicious or buggy `codogotchi hooks status
   --json` produced a snapshot with bogus `last_event_at` strings, would they
   propagate into `lastHookActivityAt` as-is (no ISO validation) and later confuse a
   UI consumer's `max()` comparison via string ordering?
5. **Cross-file atomicity windows.** None added in this diff (no multi-file writes).
   `ConfigBootstrap.ensureLiteConfig` writes a single file atomically;
   `AppStateStore.save` is also atomic; `refreshHookStatusCache` does load → mutate →
   save on one file. Probe: is there any caller pair (FloatingPetController and
   refreshHookStatusCache running concurrently on app launch) that could create an
   observable partial state in `app-state.json`?
6. **Test-contract strength.** `AppBootstrapTests` asserts `HookStatusClient.Failure`
   enum cases explicitly. Probe: do all three failure tests assert the stable enum
   case identity (commandFailed/parseFailed) before any prose, and is the
   omitted-runner default path covered (or is it acceptably untested because the
   default uses real subprocess that can't be hermetic-tested)?
7. **Doc-vs-code drift in the ticket Rationale.** Ticket Rationale claims
   `schema_version` is intentionally NOT bumped — diff matches (still `1`). Rationale
   claims `Hooks not active` ≡ "no platform with `installed && firing_recently`" —
   `isHooksNotActive` matches. Rationale claims `CODOGOTCHI_HOME` is honored for
   config, app-state, pet paths — verify each is preserved end-to-end.

### Diff context

The diff adds three new Swift files and edits `AppState.swift`,
`FloatingPetController.swift`, and `MenubarApp.swift`. `ConfigBootstrap` writes a
minimal Lite config when `PetConfig.configURL()` is missing. `HooksStatusSnapshot` is a
Codable mirror of the CLI's JSON shape with a `Platform` row struct and an
`isHooksNotActive()` predicate. `HookStatusClient` wraps a `runner` closure
(test-injectable) around `codogotchi hooks status --json` and decodes its stdout into
`HooksStatusSnapshot`, throwing typed `Failure` cases on non-zero exit or parse error.
`FloatingAppState` gains three optional fields (`onboardingCompletedAt`,
`lastHookActivityAt`, `hooksStatus`) wired through the payload struct and the
`AppStateStore.load`/`save` round-trip with `schemaVersion` unchanged.
`FloatingPetController` is patched to carry forward the new fields when it rebuilds
`FloatingAppState` for a visibility or frame change. `MenubarApp` adds a launch-time
`ConfigBootstrap.ensureLiteConfig()` call (best-effort, NSLog on failure) and a
`refreshHookStatusCache()` call after `LivePollingDriver` starts that fetches via
`HookStatusClient()`, computes the max `last_event_at` across the five platforms, and
writes the snapshot into app-state.
```

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable
outcome. Keep any added surfaces tied to the ticket behavior; do not turn this into
broad style, cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production. Name which spec
   clause permitted the unsafe behavior.

Do not report style, preference, or hypothetical future requirements as blocking
findings. If you notice something worth flagging but it is outside these three clauses,
put it in **Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound,
your correct output is a clean report. Do not invent findings to justify the review
step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file
edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]` — one short note.

**Actionable findings**
Numbered list. For each: file:line, the broken invariant or demonstrable bug, and the
shortest patch concept. Empty list is OK if nothing fires the three finding-discipline
clauses.

**Advisory Observations**
Non-blocking off-scope-but-real notes. Empty list is OK.
