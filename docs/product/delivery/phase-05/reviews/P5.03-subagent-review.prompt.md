You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**P5.03 CLI setup / rpg split**

Outcome:
- `codogotchi setup`: Lite only — if no config, write minimal Lite config (`profile_id`, `pet: "maew"`, `features.rpg_enabled: false`); then `hooks install`. No handle, Convex, GitHub, or Wakatime prompts. `--force` overwrites config.
- `codogotchi rpg`: interactive Alive enrollment (current `setup` flow): handle, Convex URL, optional GitHub/Wakatime, first sync; sets `features.rpg_enabled: true` and required RPG fields.
- `USAGE` and `--help` reflect the split.
- `setup` does not enroll in Convex; `rpg` does not silently reinstall hooks unless documented (default: assume hooks already installed; if missing, error points to `hooks install` or app onboarding).
- Tests cover: greenfield `setup` produces Lite config + calls install; `rpg` on Lite config upgrades shape; `rpg` refuses or prompts when already RPG as appropriate.

Review focus from ticket:
- No regression path that runs full enrollment on plain `setup`.
- `InstallHooksContext` no longer requires `convex_http_url` for hook install.

Rationale notes:
- `runRpg` does NOT call `installHooks` — assumes hooks already installed by `setup`.
- `InstallHooksContext` drops `convex_http_url` since it was never used in the hook wiring (only `ctx.home` was used by `codexHookCommand`).
- Lite→RPG upgrade allowed in `runRpg`; RPG→RPG requires `--force`.
- Config is written BEFORE `installHooks` in `runSetup` because `installHooks` checks for config.json existence.

### Files touched

Implementation:
- packages/cli/src/setup.ts
- packages/cli/src/router.ts
- packages/cli/src/hooks.ts (type import only — no logic changes)

Tests:
- packages/cli/src/setup.test.ts (updated for Lite behavior)
- packages/cli/src/rpg.test.ts (new: Lite + RPG enrollment tests)
- packages/cli/src/hooks.test.ts (removed convex_http_url from installHooks calls)
- packages/cli/src/router-hooks.test.ts (removed convex_http_url from config fixture)

### Invariants to hold

1. `runSetup` must never access `deps.prompter` or `deps.fetch` — the `LiteSetupDeps` type does not include them, and any path that calls a prompter or makes a network call in Lite setup is a regression.
2. `runRpg` must throw `ConfigExistsError` when `configExists(home)` is true AND existing config has `features.rpg_enabled: true` AND `opts.force` is false; it must NOT throw when existing config has `rpg_enabled: false` (Lite→RPG upgrade path).
3. `installHooks` must never be called with a `convex_http_url` field — after this change, `InstallHooksContext` only has `home: string`; any call site passing `convex_http_url` is a type error that indicates the old contract leaked back in.

### Attack surfaces to probe

1. **`runSetup` in `setup.ts`** — does the Lite path truly have no way to call `prompter.ask()` or `fetch()`? Check that `LiteSetupDeps` type excludes these fields and that no helper function called from `runSetup` imports or references the prompter/fetch machinery.

2. **`runRpg` in `setup.ts` — Lite→RPG gate** — the guard is `existing?.features.rpg_enabled === true && !opts.force`. Probe edge cases: what if `readConfig` returns null (config exists on disk but fails schema parse)? What if `configExists` returns true but `readConfig` returns null (ENOENT race or corrupt file)?

3. **`router.ts` `rpg` handler** — does the `rpg` command pass `prompter` and `fetch` to `runRpg`, while `setup` does NOT? Verify that the `setup` command handler passes only `{ home, randomUUID, installHooks }` and does not silently access `terminalPrompter()` or `fetch` in any branch.

4. **`hooks install` router handler** — the old code passed `convex_http_url: "convex_http_url" in config ? config.convex_http_url : ""`. The new code passes `{ home: getCodogotchiHome() }`. Probe whether any caller of `installHooks` in router.ts still references `convex_http_url` (runtime or type-level regression).

5. **`runRpg` atomicity** — when Convex `/sync` fails, is `configPath(home)` guaranteed NOT to exist on disk? The current order is: prompt → register with Convex → writeConfig. If Convex fails BEFORE writeConfig, is the config reliably absent? If Convex fails AFTER a prior Lite config existed, is the Lite config preserved or clobbered?

6. **`installHooks` contract in `runSetup`** — config is written BEFORE installHooks. If `writeConfig` succeeds but `installHooks` throws, the config file remains on disk. Probe: does the caller (router `setup` handler) surface this correctly, or does it treat the error as a clean failure that hides the partial state?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `InstallHooksContext` drops `convex_http_url`. Does any persisted artifact (TOML, hooks.json, Claude settings.json) include a Convex URL that would now be missing and break downstream readers?

2. **CLI flag/arg symmetry** — `rpg` reuses `parseSetupFlags(rest)` for `--force` and `--help`. Does this parse all valid flags for `rpg`? Are any `rpg`-specific flags missing? Does `--help` on `rpg` print the correct USAGE?

3. **Error-class breadth in `catch` blocks** — the `rpg` handler catches `ConfigExistsError` and `ConfigReadError`; unknown errors are rethrown. Does any other recoverable error class escape without user-friendly messaging (e.g., network errors from `doFetch`)?

4. **Defensive layering at module boundaries** — `runRpg` calls `readConfig(home)` which validates via Zod. If the file on disk is valid JSON but fails the union schema (neither Lite nor RPG), `readConfig` throws `ConfigReadError`. Does `runRpg` surface that error correctly, or does it propagate as an uncaught exception?

5. **Cross-file atomicity windows** — in `runSetup`, `writeConfig` then `installHooks`. In `runRpg`, register with Convex then `writeConfig`. Are there observable windows where the user has a config without hooks, or has registered with Convex without a config file?

6. **Test-contract strength** — do new tests in `rpg.test.ts` assert the Lite→RPG upgrade preserves a fresh `profile_id` (not the Lite one), and that `rpg_enabled` is typed as `true` (not just truthy)? Do setup tests verify that `"handle" in result.config === false` (not just that `result.config.handle` is undefined)?

7. **Doc-vs-code drift in the ticket Rationale** — does the ticket's Rationale accurately state that config is written BEFORE hooks in Lite setup? Does the Rationale correctly state that `rpg` does NOT call installHooks? Read `docs/product/delivery/phase-05/ticket-03-cli-setup-rpg-split.md` and surface any drift.

### Diff context

Key changes:

1. `setup.ts`: `InstallHooksContext` drops `convex_http_url`. `SetupDeps` renamed `LiteSetupDeps` (drops `prompter`/`fetch`/`installHooks` from old shape → adds just `home`, `randomUUID`, `installHooks`). New `runSetup` is non-interactive: generates `profile_id`, writes Lite config, calls `installHooks({ home })`. New `runRpg` is the old `runSetup` flow with a modified guard: only throws `ConfigExistsError` when existing config has `rpg_enabled: true` (not for Lite). `runRpg` does NOT call `installHooks`.

2. `router.ts`: `setup` handler now passes `{ home, randomUUID, installHooks }` (no prompter/fetch). New `rpg` handler passes `{ prompter, fetch, home, randomUUID }`. `hooks install` handler drops `convex_http_url` from the `installHooks` call.

3. `hooks.ts`: unchanged logic; type import of `InstallHooksContext` now narrower.

4. Tests: `setup.test.ts` updated to test Lite behavior (no fetch calls, no prompts, Lite config shape). `rpg.test.ts` adds full coverage for Lite setup + RPG enrollment + Lite→RPG upgrade + RPG→RPG guard.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
