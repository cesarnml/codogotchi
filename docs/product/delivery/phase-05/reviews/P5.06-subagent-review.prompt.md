```
You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome (P5.06 — First-run onboarding sheet):**

- Menubar-attached sheet (or equivalent modal) on first launch when `onboardingCompletedAt` is absent.
- Copy explains: animation driven by agent hooks; Codex + Claude installable in this phase; Cursor/VS Code/Antigravity detected and labeled deferred or bridge-only per product plan.
- Single primary action: **Approve & install hooks** — runs `codogotchi hooks install` subprocess; shows stderr on failure.
- **No** skip, "Not now", or dismiss-without-install that marks onboarding complete.
- On success, set `onboardingCompletedAt`; until hook activity observed, show persistent **Hooks not active** with next action (retry install / open Settings).
- Pet remains **idle** (no user demo); mouse interactions on floating pet unchanged.
- Sheet does not block app launch indefinitely on subprocess failure — CTA remains until healthy.

**Rationale:** Onboarding is consent to touch other apps' configs, not optional hooks; persistent CTA until install works.

### Files touched

Implementation:
apps/menubar/Sources/OnboardingController.swift
apps/menubar/Sources/OnboardingWindowController.swift
apps/menubar/Sources/MenubarApp.swift
apps/menubar/Sources/MenubarMenu.swift

Tests:
apps/menubar/Tests/MenubarTests/OnboardingControllerTests.swift
apps/menubar/Tests/MenubarTests/MenuItemsTests.swift

### Invariants to hold

1. `OnboardingWindowController.windowShouldClose` must return `false` until `installSucceeded` is true — there must be no code path that sets `installSucceeded = true` without a confirmed zero-exit from the subprocess runner.
2. `AppStateStore.save` with a non-nil `onboardingCompletedAt` must be called if and only if `runHooksInstall()` returns nil (subprocess exit 0); it must never be called on subprocess failure.
3. `OnboardingWindowController.showIfNeeded` must be a no-op when `appState.onboardingCompletedAt` is already set — repeat launches must not re-show the panel.

### Attack surfaces to probe

1. `OnboardingWindowController.runInstall` / `handleInstallSuccess` — can a race condition between `DispatchQueue.global` and `DispatchQueue.main.async` cause `handleInstallSuccess` to fire twice, setting `onboardingCompletedAt` twice and allowing a second `close()` after the panel is already deallocated?
2. `windowShouldClose` gate — `NSWindowDelegate.windowShouldClose` is only called when the close button or `performClose` is invoked. Does calling `panel?.close()` from `handleInstallSuccess` (programmatic close) bypass `windowShouldClose`? If so, is there a path where the user can close the panel before install by any other mechanism?
3. `AppStateLoader` closure in `handleInstallSuccess` — the closure calls `AppStateStore.load(visibleFrame:)` at install-success time, which reads the on-disk state. If a concurrent `refreshHookStatusCache` has already written an updated state (e.g., with a new `hooksStatus`), and `handleInstallSuccess` reads THEN writes, does it clobber the more recent `hooksStatus` snapshot from the concurrent write?
4. `OnboardingWindowController.showIfNeeded` guard `panel == nil` — if the panel is dismissed by a programmatic `close()` inside `handleInstallSuccess` but the `panel` ivar is not cleared (no `self.panel = nil` after close), a second call to `showIfNeeded` will no-op even if `onboardingCompletedAt` is still nil (e.g., save failed). Is this correct behavior or a silent failure to re-show?
5. `MenubarMenu.refreshHooksNotActive` / `hooksNotActiveItem` — `hooksNotActiveItem` is stored as `weak var`. If the menu is deallocated between `build()` and the first `refreshHooksNotActive` call, the item silently does nothing. Is there a lifecycle path where the `NSMenu` outlives the `MenubarMenu` but the item reference goes nil?
6. `retryHooksInstall` closure in `MenubarApp` captures `onboardingController` weakly — `retryHooksInstall` calls `onboardingController?.showIfNeeded()`. After onboarding completes, `showIfNeeded` will no-op because `panel != nil` (post-install panel still in memory), not because `onboardingCompletedAt` is set. If the user retries from the menu item before the panel is fully dismissed, does a second panel window appear?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `AppStatePayload.onboardingCompletedAt` was added in P5.05. P5.06 writes it with `ISO8601DateFormatter`. Does the existing round-trip test cover the ISO8601 format written by `OnboardingWindowController`? Does any consumer that reads `onboardingCompletedAt` depend on a specific format (e.g., date parsing for comparison)?

2. **CLI flag/arg symmetry** — P5.06 invokes `["codogotchi", "hooks", "install"]` via `OnboardingController.runHooksInstall`. Probe whether the CLI `hooks install` command exists in `packages/cli` and whether its exit-code contract matches what the controller assumes (0 = success, non-zero = failure with stderr).

3. **Error-class breadth in `catch` blocks** — `OnboardingWindowController.handleInstallSuccess` calls `try? appStateSaver(next)` (silent failure). If the save fails (disk full, permissions), `installSucceeded` is set to `true` and the panel closes, but `onboardingCompletedAt` is never written. On next launch the sheet re-appears (correct behavior), but the user has no feedback that the save failed. Probe whether this silent path is acceptable per the ticket contract.

4. **Defensive layering at module boundaries** — `OnboardingWindowController.appStateLoader` defaults to calling `AppStateStore.load(visibleFrame:)` with `NSScreen.main?.visibleFrame ?? .zero`. In a test or headless context where `NSScreen.main` is nil, the fallback is `.zero`. Does `FloatingFramePolicy.defaultFrame(in:)` handle a zero visible frame gracefully (no division by zero or negative size)?

5. **Cross-file atomicity windows** — `handleInstallSuccess` sets `installSucceeded = true`, then calls `appStateLoader()` then `appStateSaver(next)`. Between the load and the save, the disk state may differ. A concurrent `refreshHookStatusCache` (on MainActor) could interleave: read-old-state → load-in-handleInstall → save-by-refreshHookStatus → save-by-handleInstall (clobbering `hooksStatus`). Probe this ordering.

6. **Test-contract strength** — `OnboardingControllerTests` tests `runHooksInstall` return values and `needsOnboarding` gating. Probe whether the tests cover: (a) the case where `onboardingCompletedAt` is an empty string `""` (is that treated as present or absent?); (b) the `isHooksActive` test for `vscode` and `antigravity` platforms (only `codex`, `claudeCode`, `cursor` are covered in the diff).

7. **Doc-vs-code drift in the ticket Rationale** — the ticket says "until hook activity observed, show persistent **Hooks not active** with next action (retry install / open Settings)." The diff shows: (a) "Open Settings" is not wired (P5.07 deferred — acceptable); (b) after install success the `hooksNotActive` panel state is shown by `setHooksNotActive()`, but the panel can still be closed by `windowShouldClose` returning `true` (installSucceeded = true). Probe whether the "persistent" requirement is met after install: can the user close the panel before hooks become active?

### Diff context

**OnboardingController.swift (new):** Struct with injectable `InstallRunner` closure (same pattern as `HookStatusClient`). Three methods: `needsOnboarding(appState:)` checks `onboardingCompletedAt == nil`; `runHooksInstall()` calls runner with `["codogotchi", "hooks", "install"]` and returns nil on exit 0 or stderr string on failure; `isHooksActive(_:)` delegates to `HooksStatusSnapshot.isHooksNotActive()`.

**OnboardingWindowController.swift (new):** `@MainActor final class` implementing `NSWindowDelegate`. `showIfNeeded()` guards on `needsOnboarding` and `panel == nil`. Shows `NSPanel` with `.titled, .closable` style. `windowShouldClose` returns `installSucceeded`. `runInstall()` dispatches subprocess to `DispatchQueue.global`, then marshals result to main via `DispatchQueue.main.async`. On success calls `handleInstallSuccess()` which sets `installSucceeded = true`, writes `onboardingCompletedAt` via `try? appStateSaver(next)`, calls `contentView?.setHooksNotActive()`. `updateHookStatus(_:)` calls `panel?.close()` once hooks are active.

**MenubarApp.swift (modified):** Adds `onboardingWindowController` ivar. Passes `retryHooksInstall` closure to `MenubarMenu` init. Calls `onboardingController.showIfNeeded()` after `refreshHookStatusCache()`. Adds `@MainActor` annotation to `refreshHookStatusCache`. After hook status save, checks `next.onboardingCompletedAt != nil` before calling `menuBuilder?.refreshHooksNotActive(isActive:)` and calls `onboardingWindowController?.updateHookStatus(snapshot)`.

**MenubarMenu.swift (modified):** Adds `hooksNotActiveTitle` constant, `retryHooksInstall` closure, `hooksNotActiveItem` weak NSMenuItem ref. `build()` adds the item hidden at tail of menu. `refreshHooksNotActive(isActive:)` toggles `hooksNotActiveItem?.isHidden`. `retryHooksInstallAction` calls the injected closure.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
```
