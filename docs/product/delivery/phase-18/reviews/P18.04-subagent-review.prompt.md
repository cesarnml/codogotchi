You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Outcome (docs/product/delivery/phase-18/ticket-04-diff-apply-comparator.md):
- `diff(desired:current:)` is a mechanical set comparison: spawn / dismiss / update sets keyed by `WindowKey`, with frame-inheritance directives passed through as data — no policy branches (any needed decision is evidence of a P18.03 gap, fixed there).
- `apply` executes a diff against the Phase 17 converged factories: spawn (with directive-driven frame adoption read from the live donor window at execution time), teardown, and per-window pushes straight from `DesiredWindow` fields; performs title resolution for `titleResolutionRequests` (disk cache read-through + write-through) and feeds results back for the next tick's memory. Zero policy decisions.
- A recording `FloatingPetWindowControlling` proxy exists: forwards every call to a wrapped controller while logging pushes per tick; also runnable against a stub (no real window) whose `currentFrame` reads through to a live window by key — both shadow directions covered.
- A field-level comparator diffs recorded old-pipeline behavior + extracted decision sets against a `(DesiredWindows, PoolMemory)` pair: asserts under tests/debug, emits structured divergence records (tick-input fingerprint, field path, both values) for log-only use. Frame directives compared structurally, never CGRect values. The title-seam delay is a built-in exemption.
- Still unwired into the live tick; full existing suite green; purity gate green (diff is pure and lives in `Pool/Derive/`; apply/proxy/comparator live outside it and may import AppKit).

Rationale highlights (full text in the ticket doc's `## Rationale` section):
- `PoolApply.apply`'s push completeness deliberately excludes `isMinimalist` (spawn-time factory choice made by the `spawn` closure's caller) and `petId`/`replacePets` (needs a pet-catalog lookup outside `DesiredWindow`'s data — untested, deferred).
- Title resolution is a separate `PoolApply.resolveTitles(...)` function, NOT folded into the 3-arg `apply(diff:controllers:spawn:)` the red tests exercise. No live caller wires its `[RenderKeyIdentity: String]` result into `PoolMemory` yet — nothing to fold into until P18.05.
- `PoolShadowComparator` only covers the per-window push-payload half of the contract (`[WindowKey: DesiredWindow]` vs. the recorded old-pipeline equivalent); the decision-set half (membership, spawned modes, blocked/pending/TTL sets, session numbers, hidden keys) has no comparable "old" shape yet and is explicitly deferred.
- `DesiredWindow.promptTimerStatus` is a `PromptTimerPresentation` (already-rendered), but `FloatingPetWindowControlling.applyPromptTimerStatus` took the raw `PromptTimerStatus`. Resolved by adding a new protocol requirement `applyPromptTimerPresentation(_ presentation: PromptTimerPresentation?)` (default no-op via protocol extension) rather than fabricating a fake raw status. Real conformers forward directly to their existing renderer calls, never re-deriving via `.presentation()`. This new path does NOT participate in either panel's local heartbeat `Timer` (`syncPromptTimerHeartbeat`), which still ticks the raw-status path the still-live old pipeline drives today — flagged as a plausible-looking gap that resolves once P18.05 wires per-tick redraws from `PoolDerive` instead of a local timer.
- `RenderKeyIdentity` gained `Hashable` conformance (was `Equatable`-only) so `PoolApply.resolveTitles` could key a dictionary by it.

### Files touched

Implementation:
apps/menubar/Sources/Pool/Derive/PoolDiff.swift
apps/menubar/Sources/Pool/PoolApply.swift
apps/menubar/Sources/Pool/RecordingFloatingPetWindowControllingProxy.swift
apps/menubar/Sources/Pool/PoolShadowComparator.swift
apps/menubar/Sources/Pool/RenderKeyResolver.swift
apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/MinimalistBadgeView.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj (xcodegen-regenerated, mechanical)

Tests:
apps/menubar/Tests/MenubarTests/Derive/PoolDiffTests.swift
apps/menubar/Tests/MenubarTests/PoolApplyTests.swift
apps/menubar/Tests/MenubarTests/RecordingFloatingPetWindowControllingProxyTests.swift
apps/menubar/Tests/MenubarTests/PoolShadowComparatorTests.swift

### Invariants to hold

1. `PoolDiff.diff` must partition purely by `WindowKey` membership: every key in `desired.windows` not in `current` lands in `toSpawn`, every key in `current` not in `desired.windows` lands in `toDismiss`, every key present in both lands in `toUpdate` — regardless of whether the `DesiredWindow` value actually changed. `diff` must contain zero conditional logic that depends on `DesiredWindow` field *values* (only key presence/absence).
2. `PoolApply.apply` must read every `toSpawn` entry's `inheritedFrameFrom` donor's live `currentFrame` before that donor is torn down in the same call, so a same-tick evict-and-hand-off never reads a stale/missing frame.
3. `PoolApply`'s per-window `push` must forward every `DesiredWindow` push-payload field to the controller except the two explicitly-documented exclusions (`isMinimalist`, `petId`/`replacePets`) — any other silently-dropped field is a push-completeness bug the comparator can't see from the new side.
4. `PoolShadowComparator`'s only exemption must be the title-seam delay, gated strictly by `DesiredWindows.titleResolutionRequests` containing a matching `RenderKeyIdentity` for that key — not a loose pattern match on `sessionLabel` divergence in general.

### Attack surfaces to probe

1. `PoolDiff.diff` — does any code path (e.g. equality check on the `DesiredWindow` value before assigning to `toUpdate`) accidentally reintroduce value-based filtering, dropping keys whose value is unchanged from `toUpdate` and silently skipping their pushes?
2. `PoolApply.apply`'s `adoptedFrames` pre-read loop — if two spawns in the same tick share the same `inheritedFrameFrom` donor (unlikely per P18.02's eviction contract, but not structurally prevented here), does the second spawn read a correct frame, or does order-dependent mutation of `controllers` between the two spawn iterations change what `donor.currentFrame` returns?
3. `PoolApply.apply`'s dismiss-then-spawn-then-update ordering — does a key that is both in `toDismiss` this tick (via `current`) and in `toSpawn` this tick (a same-key teardown+respawn) ever exist, and if so does the loop order (dismiss first, spawn second) still leave `controllers[key]` in the correct final state, or could `toUpdate`'s guard-let on a stale `controllers[key]` silently no-op a push that should have landed on the freshly spawned controller?
4. `PoolApply.push`'s field list vs. `DesiredWindow`'s actual field set — walk `DesiredWindows.swift`'s `DesiredWindow` struct field-by-field against `push`'s call list; is every field either pushed or one of the two named exclusions, or is there a field neither pushed nor documented as excluded (e.g. `conflictBubble`, `sessionTooltip`, `attentionSourceEvent` — confirm each is actually wired, not just present in the doc comment)?
5. `applyPromptTimerPresentation`'s default no-op protocol-extension member — does any real (non-test-double) conformer silently inherit the no-op instead of the intended forwarding implementation because of a protocol-witness resolution subtlety (e.g. the conforming type implements it in an extension that doesn't satisfy the protocol requirement, or a subclass/wrapper shadows it incorrectly)?
6. `PoolShadowComparator`'s exemption check — is the `RenderKeyIdentity` → `WindowKey` mapping used to test membership in `titleResolutionRequests` exact (origin + session id), or could a same-origin-different-session identity collision cause a real divergence to be wrongly exempted?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `[N/A — reason]` or probe: does `DivergenceRecord`'s shape (tick-input fingerprint, field path, both values) get persisted anywhere with a version marker, or is it purely in-memory/log-only per the ticket? If log-only, confirm no consumer parses it as a stable schema yet.
2. **CLI flag/arg symmetry** — likely N/A (no CLI surface in this diff); confirm no delivery-tool CLI flags were touched.
3. **Error-class breadth in `catch` blocks** — `PoolApply.resolveTitles`'s `resolveTitle` closure returns `String?` rather than throwing; probe whether a resolver failure (vs. a genuine "no title available") is distinguishable, or whether both collapse to the same silent `continue`.
4. **Defensive layering at module boundaries** — `PoolApply.apply` receives `controllers: inout [WindowKey: FloatingPetWindowControlling]` and a `spawn` closure from an unspecified caller (P18.05 territory). Does `apply` assume the incoming `controllers` dictionary is already consistent with the previous tick's `DesiredWindows` (i.e., no defensive re-validation that `toUpdate` keys actually exist in `controllers` beyond the `guard let`)? Confirm the guard-let silently skipping a missing controller doesn't mask a real bug upstream.
5. **Cross-file atomicity windows** — `resolveTitles`'s cache read → resolve → write-through sequence: if the process is interrupted between `resolveTitle` and `writeCachedTitle`, is the disk cache left in a consistent state (worst case: a repeated subprocess resolve next tick, not corruption)? Confirm no partial-write risk.
6. **Test-contract strength** — do `PoolApplyTests`/`PoolDiffTests` assert structural/value equality (not just "doesn't crash"), and do they cover both the omitted-frame (`inheritedFrameFrom == nil`) and supplied-frame paths for spawn?
7. **Doc-vs-code drift in the ticket Rationale** — read the ticket's `## Rationale` section in full against the actual diff; specifically verify the promptTimerPresentation heartbeat-timer gap description matches what `syncPromptTimerHeartbeat` actually does today, and that the "decision-set half deferred" comparator claim matches `PoolShadowComparator.swift`'s actual scope (no partial/hidden decision-set comparison exists in the code that the Rationale doesn't mention).

### Diff context

Four new files implement the ticket's four pieces (see summaries in Ticket scope above); five existing files were extended:
- `FloatingPetController.swift`: widened `FloatingPetWindowControlling`/`PanelManaging` protocols with `applyPromptTimerPresentation(_:)`, default no-op in both extensions.
- `FloatingPetPanelController.swift`, `MinimalistBadgeView.swift`, `MinimalistPanelController.swift`: real forwarding implementations of the new method, calling existing renderer entry points directly.
- `RenderKeyResolver.swift`: `RenderKeyIdentity` widened `Equatable` → `Hashable`.

`PoolApply.apply`'s core sequence: pre-read donor frames for all spawns → run all dismissals → run all spawns (adopt frame if donor existed, then push) → run all updates (push only, guarded on controller still existing). `PoolApply.push` is a flat sequence of `controller.apply*` calls, one per `DesiredWindow` field, with two documented omissions. `PoolShadowComparator.compare` walks paired `DesiredWindow` values field-by-field, emitting a `DivergenceRecord` per mismatched field except when the field is `sessionLabel` and the key has an in-flight `titleResolutionRequests` entry (the sole `ShadowCompareExemption.titleResolutionDelay` case).

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
