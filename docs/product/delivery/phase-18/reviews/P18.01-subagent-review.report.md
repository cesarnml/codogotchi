**Invariant results**

1. [held] `PoolMemory` is a value-type struct containing value-type dictionaries and keys; `derive` shadows the immutable parameter with a local mutable copy and returns that copy. No reference-backed mutable state is shared across calls.
2. [held] `isTTLExpired` checks last-active immunity first, returns `true` when a non-active key has no `lastSeenAt`, and uses strict `> ttlSeconds` for stale entries, matching the live source.
3. [held] `eligibleKeys` unconditionally includes every currently visible key and unions absent keys whose own TTL window remains open. All three clock dictionaries are then filtered against exactly that set.

**Surface results**

1. [probed] Off-mode filtering resolves `customization.platformModes[key.origin]`. `WindowKey.origin` returns the platform origin for `.session`, so an off-mode platform excludes all its session keys as intended.
2. [probed] A stale, non-eligible HUD holder produces `holderStillInFlight == false`; when another eligible candidate exists it is re-elected. If no candidate exists, the old optional remains stored, matching `FloatingPetWindowPool.update()` and not blocking a later election once candidates return.
3. [probed] Both derive and live implementations filter the same dictionary and call the identical `max(by: { $0.value < $1.value })` expression. Equal timestamps remain nondeterministic, but the transcription introduces no distinct tie-breaker that would systematically cause shadow-compare divergence.
4. [probed] The set union always retains keys present in `visibleEntries`, even when their prior `lastSeenAt` is beyond TTL. Such a key therefore survives all three bounding filters.
5. [probed] Placeholder values could look meaningful if a populated `DesiredWindow` were constructed prematurely, but P18.01 always returns an empty `windows` dictionary and documents that contract in both relevant types. This is not a current correctness failure.
6. [probed] `assignments` and `idleEscalationEnvironment` have no reads in `derive`; there are no environment accesses or defaulted computation hooks in the core. They are inert inputs reserved for P18.03.
7. [N/A — output stability across schema-version drift] The new types have no persistence, serialization, CLI output, or callers outside tests.
8. [N/A — CLI flag/arg symmetry] No CLI surface was added or changed.
9. [N/A — error-class breadth] The four new implementation files contain no `catch` blocks or other error-handling branches.
10. [probed] Defensive boundary behavior matches the live source: both parse `updatedAt` through `StateJsonReader.parseISO8601Date` and fall back to the tick’s current time on failure. No new decoding or validation boundary was introduced.
11. [N/A — cross-file atomicity windows] The derive core performs no writes; the project-file change is generated membership metadata rather than a multi-step runtime write.
12. [probed] The behavior tests assert concrete clock, election, pruning, immunity, and empty-output values. The purity test is not structurally strong enough: it recognizes only whitespace-trimmed lines exactly equal to `import AppKit`, missing other valid AppKit import forms.
13. [probed] The Rationale gives a ticket-scoped reason for deferring the named P18.02/P18.03 policy fields and the non-`Equatable` helpers. However, the ticket Outcome’s combined prompt-timer exemption is not implemented in this ticket, and the claimed deferred-field accounting is not exhaustive; details are recorded under Advisory Observations.

**Actionable findings**

- `apps/menubar/Tests/MenubarTests/Derive/PoolDerivePurityGateTests.swift`: `testNoAppKitImportUnderPoolDerive` permits valid AppKit imports such as `import  AppKit`, `@_exported import AppKit`, and declaration-specific imports such as `import class AppKit.NSWindow`. This demonstrably violates the ticket outcome that the CI check assert no file under `Pool/Derive/` imports AppKit, satisfying finding-discipline clause 2. Parse normalized import declarations or use a regex that accepts attributes, arbitrary whitespace, and declaration-specific import syntax, and add adversarial fixtures for those forms.

**Advisory Observations**

- `docs/product/delivery/phase-18/ticket-01-derive-skeleton.md` says the eligibility bounding includes the `.combined` prompt-timer exemption, while `PoolDerive.swift` bounds only `firstSeenAt`, `lastSeenAt`, and `lastUpdatedAt`; `PoolMemory` contains no prompt-timer state. The Rationale defers `promptTimers` to P18.03, but does not explicitly acknowledge this Outcome-level behavior deferral.

- The same ticket’s Review Focus asks for every cross-tick mutable policy field to have a home or documented shell-owned reason. The Rationale covers the specifically named selection fields and several push fields, but is not a complete field-by-field disposition: for example, the live pool’s in-memory `resolvedSessionTitles` and `lastAppliedIdleEscalationConfig` are not explicitly addressed. This does not break P18.01 runtime behavior because derive is unwired, but the documentation overstates the completeness of its deferred-field list.

**Runner termination**

runnerStatus: completed  
terminatedReason: Completed the read-only adversarial review of every stated invariant and attack surface.
