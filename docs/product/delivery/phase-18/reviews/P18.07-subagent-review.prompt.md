You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P18.07 "Old-pipeline deletion and closeout" — final ticket of Phase 18 (Pool Pipeline
Split). Outcome:

- The old imperative pipeline is deleted from `FloatingPetWindowPool` — every superseded
  step, helper, and stored property; the class is the thin shell (input assembly, stored
  `PoolMemory`, `derive`→`diff`→`apply` composition, public surface, user-action two-limb
  methods).
- The shadow machinery (comparator wiring in the tick, divergence logging, stub
  read-through path) and the `CODOGOTCHI_POOL_ENGINE` rollback flag are deleted. The
  recording proxy and comparator may survive only where tests use them; nothing
  shadow-related runs in production.
- The purity gate remains in CI permanently.
- A dogfood DMG is packaged, installed, and running as the daily driver. No public
  release, no download-page update.

Rationale (from the ticket doc, appended during implementation): deletion done as a single
coherent commit (not the dependency-ordered multi-commit split originally suggested)
because `LegacyPoolEngine` itself references `NoOpStubWindowController`, so splitting
would create a non-compiling intermediate state. **Binding contract note:** the phase's
P18.06→P18.07 stop condition (reversed-shadow rare-branch soak with zero unexplained
divergences before deletion starts) was explicitly waived by the developer rather than
independently verified — recorded honestly in `docs/product/delivery/phase-18/exit-audit.md`
§5, not claimed as a false PASS. This is a known, disclosed gap — do not re-flag the waiver
itself as a finding; instead focus on whether the *code* is correct on its own merits.

### Files touched

Implementation:
apps/menubar/Sources/App/MenubarApp.swift
apps/menubar/Sources/Pool/FloatingPetWindowPool.swift
apps/menubar/Sources/Pool/PoolApply.swift
apps/menubar/Sources/Pool/RecordingFloatingPetWindowControllingProxy.swift
apps/menubar/Sources/Pool/ShadowDivergenceLogger.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj

Deleted:
apps/menubar/Sources/Pool/Derive/ShadowTickFingerprint.swift
apps/menubar/Sources/Pool/NoOpStubWindowController.swift
apps/menubar/Sources/Pool/RecordedPushDesiredWindowReconstruction.swift

Tests:
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift (modified)
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolShadowTickTests.swift (deleted)

### Invariants to hold

1. `FloatingPetWindowPool`'s public surface — `setVisible`, `pruneSession`,
   `hideAllOtherWindows`, `resetPromptTimer`, `sessionNumber`, `sessionDisplayLabel`,
   `controller(for:)`, `activeOrigins`, `blockedOrigins`, `pendingSessionKeys`,
   `ttlDismissedWindowKeys`, `replacePet`, `clearAttentionBubbles`,
   `pruneHiddenKeysWithoutBackingSlice` — must exist with identical signatures and
   observable behavior after this deletion; `MenubarApp` and menu wiring must not need any
   change beyond removing dead shadow-handler arguments.
2. No production code path may construct, invoke, or reference `LegacyPoolEngine`,
   `PoolEngine`, `CODOGOTCHI_POOL_ENGINE`, `activeEngine`, `poolEngineEnvironment`,
   `shadowDivergenceHandler`, `shadowTickInputPerturbation`, or `NoOpStubWindowController`
   — a grep across `apps/menubar/` for these identifiers must return zero hits.
3. The `apps/menubar/Sources/Pool/Derive/` purity gate (no AppKit imports) must still exist
   and still pass unmodified by this ticket.

### Attack surfaces to probe

1. `FloatingPetWindowPool.init` — before this ticket, `hiddenKeysLoader`/`hiddenKeysSaver`
   behavior branched on `engine == .new` (load-once-and-share vs pass-through). Now that
   there is only one path, does the surviving code still correctly load hidden keys exactly
   once and never write through from a shadow context — or did the deletion silently leave
   a dead branch, a double-load, or drop the "shared loaded set" fix from the P18.06
   subagent-review finding?
2. `MenubarApp.swift`'s `startHUDDemo()` — its `RecordingFloatingPetWindowControllingProxy`
   unwrap was "simplified since production controllers are no longer proxy-wrapped." Does
   any remaining demo/debug/test call path still expect a proxy-wrapped controller here and
   now silently no-op, force-unwrap-crash, or degrade the HUD demo instead of failing
   loudly?
3. `ShadowDivergenceLogger.swift`, `RecordingFloatingPetWindowControllingProxy.swift`,
   `PoolShadowComparator.swift` are kept as "directly-tested utilities" with production
   wiring removed. Do any of them retain an implicit production entry point — a default
   parameter that still constructs one, a shared/singleton instance, a still-registered
   notification observer, or a reachable call site outside test targets — that would let
   shadow logging/comparison still fire in production despite the wiring removal?
4. `FloatingPetWindowPoolTests.swift`'s modified title-resolution tests: call-count
   assertions changed from 2 to 1 based on the claim that "2, not 1" was a temporary
   P18.06→P18.07 soak-window artifact. Verify against the actual (post-deletion) code path
   that title resolution now genuinely calls the resolver/cache exactly once per need —
   not that the test was merely relaxed to match a caching regression (e.g. a stale
   on-disk-cached title no longer refreshing when it should).
5. `apps/menubar/Codogotchi.xcodeproj/project.pbxproj` was regenerated via `xcodegen
   generate` to drop deleted files. Confirm no orphaned build-file/reference remains for the
   three deleted Swift files, and no test-target reference remains for the deleted
   `FloatingPetWindowPoolShadowTickTests.swift`.
6. Literal grep for each named public method/property in invariant 1 inside the new
   `FloatingPetWindowPool.swift` to confirm none was accidentally renamed, dropped, or
   changed from a stored property to something with different access semantics during the
   ~1490-line deletion.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — N/A unless you find a persisted
   artifact (e.g. hidden-keys JSON, session-title cache) whose shape changed. Probe the
   `hiddenKeysLoader`/`hiddenKeysSaver` and `retrievedSessionTitleReader`/Writer contracts
   for any shape change.
2. **CLI flag/arg symmetry** — the `CODOGOTCHI_POOL_ENGINE` env var is deleted outright.
   Confirm every read site AND every doc/script reference to it is gone (not just the
   `init` read site) — check scripts, CI config, and any lingering doc mentioning the
   rollback flag as still-available.
3. **Error-class breadth in `catch` blocks** — probe any `catch` touched or newly exposed
   by the deletion (e.g. around DMG packaging script invocation is out of scope; focus on
   Swift call sites in the touched files).
4. **Defensive layering at module boundaries** — `FloatingPetWindowPool` is now the sole
   caller into `PoolDerive`/`PoolDiff`/`PoolApply`. Does it still validate/normalize inputs
   at that boundary the same way, or did any legacy-path input massaging silently disappear
   along with `LegacyPoolEngine`?
5. **Cross-file atomicity windows** — the DMG packaging + install (already done outside
   this diff) is not in scope; focus on whether the single deletion commit itself could
   leave a partially-broken intermediate state if `git bisect`/`git revert` were run against
   it (i.e., does the commit boundary genuinely compile and pass tests standalone, matching
   the ticket's own claim).
6. **Test-contract strength** — do the 2 removed engine-selection tests
   (`testDefaultEngineSelectionIsNewEngineDrivesOldShadows`,
   `testLegacyEnvVarSelectsOldEngineAsAuthoritative`) have any residual assertion value that
   should have been preserved in a different form (e.g. "the pool always drives the new
   engine" as a standing invariant test), or is their removal fully justified because the
   `CODOGOTCHI_POOL_ENGINE` selection concept no longer exists at all?
7. **Doc-vs-code drift in the ticket Rationale** — read
   `docs/product/delivery/phase-18/ticket-07-deletion-closeout.md`'s Rationale and
   `docs/product/delivery/phase-18/exit-audit.md` against the actual diff. Confirm the
   exit-audit's condition-by-condition claims (especially conditions 1, 2, and 6, which
   claim PASS) are accurate against what the diff and current repo state actually show —
   and confirm condition 5's "developer-waived, not verified" framing is not silently
   overstated or understated elsewhere in the docs.

### Diff context

`FloatingPetWindowPool.swift` shrank from 2002 to ~500 lines: `final class LegacyPoolEngine`
(~1490 lines, the entire pre-P18.06 imperative pipeline) and `enum PoolEngine` are deleted
outright. The surviving `FloatingPetWindowPool` class had, before this ticket, an
`activeEngine: PoolEngine` property, a `private let legacy: LegacyPoolEngine`, and an
`update(snapshot:)` that opened with `guard activeEngine == .new else { legacy.update(...); return }`
before running derive→diff→apply. All of that branching, the `poolEngineEnvironment` init
parameter, and the `shadowDivergenceHandler`/`shadowTickInputPerturbation` pass-throughs to
`legacy` are removed; `update(snapshot:)` now runs the derive→diff→apply path
unconditionally. `MenubarApp.swift` lost its `shadowDivergenceHandler: { ShadowDivergenceLogger.log($0) }`
init argument (the last production shadow wiring) and had its HUD-demo proxy unwrap
simplified. Three shadow-only support files were deleted outright (`NoOpStubWindowController.swift`,
`RecordedPushDesiredWindowReconstruction.swift`, `Derive/ShadowTickFingerprint.swift`), along
with the shadow-tick test file whose whole premise (the two removed init params) no longer
exists. `PoolApply.swift`, `RecordingFloatingPetWindowControllingProxy.swift`, and
`ShadowDivergenceLogger.swift` got doc-comment-only cleanup with no behavior change — they
remain as standalone, still-tested utilities per the ticket's explicit allowance.
`FloatingPetWindowPoolTests.swift` lost 2 dead engine-selection tests and had 2
title-resolution tests' call-count assertions corrected from 2 to 1.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
