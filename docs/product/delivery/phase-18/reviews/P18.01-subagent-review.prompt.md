You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P18.01 Derive skeleton — PoolMemory, DesiredWindows, tick core (`docs/product/delivery/phase-18/ticket-01-derive-skeleton.md`)

Outcome:
- `apps/menubar/Sources/Pool/Derive/` exists holding `PoolMemory`, `PoolTickInput`, `DesiredWindows`/`DesiredWindow`, and `PoolDerive.derive(input:memory:) -> (DesiredWindows, PoolMemory)` — all value types, `PoolMemory` and `DesiredWindows` `Equatable`.
- The derive core reproduces `update()` Steps 1–5b and the eligibility bounding as pure logic: off-mode filtering, the idle-frozen TTL clock rule (advance only while not idle; seed on first sight), first-seen (set once), last-updated tracking, last-active election restricted to eligible keys, the sticky HUD-bearer election (re-elect only when the holder leaves in-flight or eligibility), TTL expiry with last-active immunity, and memory bounding to the eligibility window.
- `DesiredWindow` fields not yet computed by this ticket (selection, pushes) exist in the type with explicit placeholder semantics documented, so P18.02/P18.03 extend without reshaping.
- Nothing is wired: the live pipeline (`FloatingPetWindowPool.swift`) is byte-for-byte untouched; the new code has no callers outside tests.
- The purity gate runs in CI: a check asserting no file under `Pool/Derive/` imports AppKit.
- Full existing suite green.

Rationale (as recorded on the ticket doc):
- `PoolMemory` deliberately carries only the fields Steps 1–5b and eligibility-bounding need (`lastSeenAt`, `firstSeenAt`, `lastUpdatedAt`, `lastActiveRenderKey`, `hudBearingRenderKey`). Selection state (P18.02) and push-spec state (P18.03) are explicitly deferred — see the ticket's `## Rationale` section for the full deferred-field list and why `SessionNumberAllocator`/`ConflictBubbleRateLimiter` were not folded in yet (they are reference/non-`Equatable` types today).
- `derive` always returns an empty `DesiredWindows` this ticket.

### Files touched

Implementation:
apps/menubar/Sources/Pool/Derive/PoolMemory.swift
apps/menubar/Sources/Pool/Derive/PoolTickInput.swift
apps/menubar/Sources/Pool/Derive/DesiredWindows.swift
apps/menubar/Sources/Pool/Derive/PoolDerive.swift

Tests:
apps/menubar/Tests/MenubarTests/Derive/PoolDeriveTests.swift
apps/menubar/Tests/MenubarTests/Derive/PoolDerivePurityGateTests.swift

Docs:
docs/product/delivery/phase-18/ticket-01-derive-skeleton.md (Rationale only)

Project file (regenerated by `xcodegen generate`, not hand-edited):
apps/menubar/Codogotchi.xcodeproj/project.pbxproj

### Invariants to hold

1. `PoolDerive.derive` must never mutate its `memory` parameter in place and return a stale/aliased copy — the returned `PoolMemory` must reflect exactly the Step 1–5b transcription for the given `PoolTickInput`, with no accidental sharing of mutable state across calls (value semantics only).
2. `PoolDerive.isTTLExpired` must return `false` for `memory.lastActiveRenderKey` regardless of how stale its `lastSeenAt` entry is, and must return `true` for any other key whose `lastSeenAt` entry is absent or older than `ttlSeconds`.
3. The three bounding filters (`firstSeenAt`, `lastSeenAt`, `lastUpdatedAt`) must retain every key in `eligibleKeys` (visible this tick OR still within its own TTL window) and drop every key outside it — a key with an open "virtual" TTL window must survive even when absent from `visibleEntries`.

### Attack surfaces to probe

1. `PoolDerive.derive`'s off-mode filter (`mode(forWindowKey:)` closure) — does it correctly resolve mode via `key.origin` for `.session` keys, or could a session-keyed origin in `.off` mode leak through `visibleEntries` because the closure reads `customization.platformModes[key.origin]` rather than something keyed by the full render key?
4. Eligibility-window union logic — `Set(visibleEntries.keys).union(memory.lastSeenAt.keys.filter { ... })`: does a key present in `visibleEntries` but ALSO past its `lastSeenAt` TTL window (e.g. a stale `lastSeenAt` entry surviving from a mode flip) get incorrectly excluded, or does the union correctly always include currently-visible keys regardless of clock state?
2. Sticky HUD-bearer re-election guard (`holderStillInFlight`) — when `memory.hudBearingRenderKey` is set but the key has since disappeared from `eligibleKeys` entirely (TTL-expired and gone), does the ternary correctly fall through to re-election, or could a stale non-eligible holder block re-election forever because `eligibleForElection` is also filtered by `eligibleKeys` and could be empty?
3. `lastActiveRenderKey`/`hudBearingRenderKey` election tie-breaking — `max(by: { $0.value < $1.value })` on a `Dictionary` is iteration-order-dependent when two keys share the exact same `lastUpdatedAt` timestamp. Does this diverge from `FloatingPetWindowPool.update()`'s existing (also order-dependent) behavior in a way that would make shadow-compare (P18.05) flag a false divergence, or is the nondeterminism identical to the source?
5. `DesiredWindow`/`DesiredWindows` placeholder fields — do any of the default values (e.g. `activityState: .idle`, `hudEnabled: false`) risk being mistaken for meaningful "computed" output by a later ticket that forgets this ticket never populates `windows`, given `DesiredWindows()` is indistinguishable from "no pets currently exist"?
6. `PoolTickInput.idleEscalationEnvironment` and `assignments` fields are accepted but never read by `PoolDerive.derive` in this ticket — confirm this is genuinely inert (no silent partial computation using them) and not a source of the "purity" gate accidentally passing while a later hidden environment-read sneaks in via a default parameter.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — this ticket adds no persisted artifact, CLI output, or on-disk JSON format; `PoolMemory`/`DesiredWindows` are in-memory-only value types with no callers yet.]
2. **CLI flag/arg symmetry** — [N/A — no CLI surface touched.]
3. **Error-class breadth in `catch` blocks** — [N/A — `PoolDerive.derive` contains no `catch` blocks; it is pure computation over already-decoded value types.] Confirm this is actually true by reading the diff — there is no error handling anywhere in the new files.
4. **Defensive layering at module boundaries** — `PoolDerive.derive` trusts `PoolTickInput.snapshot`/`customization` as already-validated (mirroring how `FloatingPetWindowPool.update()` trusts its own `customizationReader()`/`assignmentsReader()` results). Probe whether any new boundary crossing (e.g. `StateJsonReader.parseISO8601Date` returning `nil` and falling back to `currentTime`) silently masks a malformed `updatedAt` string in a way that diverges from the source `update()`'s identical fallback.
5. **Cross-file atomicity windows** — [N/A — no multi-step write; the only file-adjacent effect is the `xcodegen generate` project-file regeneration, which is a single deterministic regen from the `Sources`/`Tests` folder trees, not a partial write.]
6. **Test-contract strength** — do `PoolDeriveTests` assert the actual `PoolMemory` field values (not just "no crash"), and does `PoolDerivePurityGateTests` assert the stable/structural condition (directory scan for a literal `import AppKit` line) rather than a loose substring match that could false-negative on `import  AppKit` (double space) or `@_exported import AppKit`?
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-18/ticket-01-derive-skeleton.md` in full (Outcome, Red, Green, Review Focus, Rationale) against the actual diff. In particular: the ticket's "Review Focus" section says `PoolMemory` should hold every cross-tick mutable "field-by-field against the pool's stored properties... there should be none for policy state" — does the Rationale's deferral list adequately justify why fields like `slotOccupants`, `windowSpawnedModes`, `userHiddenWindowKeys`, `prunedOrigins`, `evictedSessionFrames`, `combinedWindowIsMinimalist` are absent from `PoolMemory` in this ticket, or does this look like the ticket's own Review Focus contract not landing as claimed?

### Diff context

Four new files under `apps/menubar/Sources/Pool/Derive/`:

- `PoolMemory.swift`: an `Equatable` struct with `lastSeenAt`/`firstSeenAt`/`lastUpdatedAt: [WindowKey: Date]` and `lastActiveRenderKey`/`hudBearingRenderKey: WindowKey?`, all defaulted to empty/nil.
- `PoolTickInput.swift`: a plain (non-`Equatable`) struct wrapping `snapshot: PerPlatformSnapshot`, `customization: CustomizationSnapshot`, `assignments: AssignmentsSnapshot`, `currentTime: Date`, `idleEscalationEnvironment: [String: String]`.
- `DesiredWindows.swift`: `DesiredWindow` (keyed by `WindowKey`, ~14 placeholder fields covering the eventual push-spec surface) and `DesiredWindows` (a `windows: [WindowKey: DesiredWindow]` dict plus pool-level placeholder fields: `blockedOrigins`, `pendingSessionKeys`, `ttlDismissedWindowKeys`, `hiddenWindowKeysToPersist`, `monochromeChanged`, `idleEscalationConfig`, `titleResolutionRequests`), all `Equatable`.
- `PoolDerive.swift`: `enum PoolDerive` with `static func derive(input:memory:) -> (DesiredWindows, PoolMemory)` transcribing `FloatingPetWindowPool.update()` Steps 1 (off-mode filter), 2 (clock tracking), 3 (last-active election), 3b (HUD-bearer election), and the eligibility-bounding block that follows — plus a standalone `static func isTTLExpired(windowKey:memory:ttlSeconds:currentTime:)` mirroring the source's private `isTTLExpired` predicate. `derive` always returns `(DesiredWindows(), memory)` — no membership computation yet.

Two new test files under `apps/menubar/Tests/MenubarTests/Derive/`: `PoolDeriveTests.swift` (11 table-driven tests covering first-seen seeding, idle-frozen TTL clock, election eligibility/clock-skew exclusion, sticky HUD-bearer stickiness and re-election, eligibility bounding, the two `isTTLExpired` branches, and one multi-tick fold) and `PoolDerivePurityGateTests.swift` (scans `Sources/Pool/Derive/*.swift` for a literal `import AppKit` line via `#file`-relative path navigation).

The original `FloatingPetWindowPool.swift` (1294 lines) is byte-for-byte unchanged; `PoolDerive` transcribes but does not call into it.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
