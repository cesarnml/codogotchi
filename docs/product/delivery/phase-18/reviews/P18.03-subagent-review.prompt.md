You are conducting an adversarial review of a code change. You may add extra ticket-relevant attack surfaces. Findings outside the finding discipline belong in Advisory Observations. Do not modify files.

### Ticket scope

P18.03 completes the pure fat DesiredWindows push spec while leaving the live pipeline unwired. It transcribes Steps 7-9: combined freshest-updated election and teardown semantics, renderer/pet/platform/labels/tooltips, gate and attention payloads, prompt timers observed before guards, sticky HUD projection, RPG snapshot, conflict bubble targeting/re-home/rate limit, monochrome change, idle escalation, and the pure title-resolution request seam. Rationale: disk/process/config reads are shell-provided inputs; diff/apply and live comparison remain later tickets.

### Files touched

Implementation:
apps/menubar/Sources/Pool/Derive/DesiredWindows.swift
apps/menubar/Sources/Pool/Derive/PoolDerive.swift
apps/menubar/Sources/Pool/Derive/PoolMemory.swift
apps/menubar/Sources/Pool/Derive/PoolTickInput.swift
apps/menubar/Sources/State/RpgStateReader.swift
docs/product/delivery/phase-18/ticket-03-derive-pushes.md

Tests:
apps/menubar/Tests/MenubarTests/Derive/PoolDerivePushTests.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj

### Invariants to hold

1. For every desired direct or combined window, derive emits all controller payload data deterministically from PoolTickInput plus PoolMemory, with no I/O or AppKit dependency.
2. Combined winner election uses the freshest updated_at with deterministic ties; idle combined uses platform chip combined and label Combined, while active combined attributes the driving source.
3. Session title precedence is rename then known title then Session N then platform name; missing rendered session titles emit deterministic resolution requests, and HUD/conflict state stays sticky/re-homes without incorrectly advancing rate limits.

### Attack surfaces to probe

1. PoolDerive winnerEntry construction: combined/uncombined folded groups, timestamp parse failures, deterministic ties, and source attribution.
2. Session-number/title lifecycle around freshKeys and torndownKeys: stale values, hidden/capped windows, duplicate title requests, and precedence.
3. Prompt timer and HUD projection: combined tracker key, observe-before-guards, folded bearer identity, and background updates.
4. Conflict-bubble block after window construction: longest-lived target, hidden/evicted host re-home, resolved episodes, and one-hour boundary semantics.
5. DesiredWindow completeness: compare every legacy Step 7-9 controller call to data fields, including RPG snapshot, attention/source, gate, minimalist/direct differences, and title seam.

#### Diff-derived attack surfaces

1. Output stability across schema-version drift: RpgSnapshot gains Equatable only and DesiredWindows is internal/unpersisted; verify no persisted schema consumer changes.
2. CLI flag/arg symmetry: N/A unless the diff unexpectedly changes CLI behavior.
3. Error-class breadth in catch blocks: N/A unless the diff adds/modifies catch behavior.
4. Defensive layering at module boundaries: PoolTickInput values, identity lookup, date parsing, and optional maps entering derive.
5. Cross-file atomicity windows: N/A for pure in-memory fold; verify title requests and memory updates cannot expose internally inconsistent output.
6. Test-contract strength: verify tables cover missing/supplied title paths, combined election/defaults, HUD stickiness, monochrome/idleness; identify ticket-required branches left untested.
7. Doc-vs-code drift: compare ticket Outcome/Rationale and PoolDerive step map against actual implementation; report drift only as Advisory Observations.

### Diff context

The diff adds pure label/title/summary/HUD inputs, push-memory for numbering/titles/monochrome/conflict state, and fills DesiredWindow payloads after selection. Combined windows elect a freshest folded entry; direct windows resolve their own or folded entry. Conflict state retains or re-homes an active target and rate-limits only a new episode. DesiredWindows now emits title requests, idle config, monochrome changes, and each window carries the shared RPG snapshot. Tests lock combined defaults, title precedence/request behavior, sticky HUD, and pure config outputs.

### Your directives

Read the full ticket, diff, directly related legacy FloatingPetWindowPool Steps 7-9, and report exactly five top-level sections in this order: **Invariant results**, **Surface results**, **Actionable findings**, **Advisory Observations**, **Runner termination**. Probe every listed surface and all seven diff-derived classes. Findings must demonstrate a broken invariant, correctness gap, or spec-permits-real-bug. No style findings. Do not write files. In Runner termination use runnerStatus: completed and a short terminatedReason only if fully completed.
