**Invariant results**

1. **Fail** — payload derivation remains pure, but not every desired window receives the correct legacy-equivalent payload. Combined prompt timers are never observed, and source-less active combined windows lose their fallback label.
2. **Fail** — combined freshest election is deterministic, including malformed-date ties, but transient-gap retention is not implemented. Non-combined folded election also uses raw strings and nondeterministic ties.
3. **Pass with gaps** — direct title precedence is rename → cached/known title → Session N → platform name, and requests are deterministically ordered. Teardown clears number/title state. HUD and conflict re-homing are sticky, but the combined timer and transient-gap defects violate the broader lifecycle contract.

**Surface results**

- Winner construction: combined parsing and tie-breaking are deterministic; direct folded groups are not.
- Title lifecycle: rendered session requests are sorted; hidden/capped windows do not request titles until rendered; teardown clears cached titles and numbering. Repeated unresolved requests match the legacy retry behavior.
- Prompt/HUD: render keys observe before membership guards and HUD bearer folding is sticky. The shared `.combined` tracker is never observed or explicitly cleared on mode-switch-away.
- Conflict bubble: longest-lived selection, deterministic ties, re-home without rate-limit advancement, resolution, and the strict `> 3600` boundary match the legacy contract.
- DesiredWindow completeness: RPG, attention/source, winner gate, renderer choice, pet assignment, numbering, labels/tooltips, monochrome, idle escalation, HUD, and conflict data exist. Combined timer and source-less fallback semantics are incomplete.
- Schema drift: `RpgSnapshot` only gains `Equatable`; `DesiredWindows` remains internal and unpersisted. No persisted schema consumer changed.
- CLI symmetry and catch breadth: no CLI or catch behavior changed.
- Defensive boundaries: input seams are value-based and derive remains free of I/O/AppKit. Date handling is inconsistent between combined and direct folded election.
- Atomicity: output and next memory are returned together; no partial external state is exposed. Title requests and cached results are internally coherent.
- Test strength: current tests cover basic combined winner/defaults, one title path, HUD stickiness, monochrome, and idle config. They omit transient gaps, shared timer behavior, malformed/tied dates, conflict lifecycle, gate/attention/RPG payloads, and source-less active combined fallback.
- Documentation comparison: addressed under Advisory Observations.

**Actionable findings**

1. **High — transient combined polling gaps tear down the shared window despite last-active immunity.**  
   `finalKeys` is built solely from this tick’s folded groups, so an empty snapshot produces no `.combined` window and records an immediate teardown at [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:162) and [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:286). There is no equivalent of legacy Step 8’s transient-gap branch, which retains an existing combined window when `.combined` remains last-active. A one-tick polling gap therefore flashes the combined pet off, directly violating the ticket’s required teardown semantics.

2. **High — the shared combined prompt timer is never observed.**  
   Step 2 updates `memory.promptTimers[renderKey]` only for underlying visible render keys at [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:74). Payload construction later reads `memory.promptTimers[.combined]` at [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:378), but no code feeds the winner into that tracker as legacy Step 8 does. Consequently combined windows always emit a nil timer, including across hidden/TTL guards. The obsolete combined tracker is also not cleared when all origins leave combined mode.

3. **Medium — non-combined folded winner election is neither date-correct nor deterministic on ties.**  
   Plain-origin folds use `.max { $0.state.updatedAt < $1.state.updatedAt }` at [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:365). This compares raw timestamp strings instead of parsed instants and supplies no key tie-break. Equivalent instants expressed with different offsets or fractional formats can elect the wrong source; equal/malformed values can depend on the folded array’s dictionary-derived order. The emitted activity, attention, gate, timer, and platform payload can therefore vary for identical logical input.

4. **Medium — an active combined winner without `sourceEvent.origin` loses the legacy “Combined” label.**  
   Legacy Step 8 defaults `combinedDefaultOrigin` to `"combined"` when an active winner has no source origin, then applies label `Combined`. The new derivation leaves `platformChip` nil and derives a nil fallback at [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:380) and [PoolDerive.swift](/Users/cesar/code/codogotchi_p18_03/apps/menubar/Sources/Pool/Derive/PoolDerive.swift:393). Thus the fat desired state omits a controller payload the legacy pipeline always supplied.

**Advisory Observations**

- The ticket and `PoolDerive` step map claim the three combined Step 8 branches and every Steps 7–9 controller policy were transcribed, but the transient-gap branch and shared-timer observation are absent. This is documentation drift; the behavioral consequences are already reported above.
- The ticket explicitly calls for failing tables covering conflict rate-limit/re-home, timer observation while hidden/capped/TTL’d, tie hazards, and combined teardown. The added test file does not contain those rows, allowing the principal defects to pass.
- The test named for title precedence verifies rename over a simultaneously supplied known title, but does not independently test cached known-title precedence or platform fallback.

**Runner termination**

runnerStatus: completed  
terminatedReason: Full adversarial review completed without modifying files.
