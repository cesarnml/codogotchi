**Invariant results**

1. [held] — `PoolDiff.diff` partitions solely by key membership; unchanged shared values still enter `toUpdate`, and focused tests confirm this.
2. [held] — all donor frames are read into `adoptedFrames` before any dismissal or spawn mutation; shared donors are therefore read consistently.
3. [broken] — `PoolApply.push` calls every required method, but the minimalist real conformer immediately overwrites `promptTimerStatus` with stale raw-status state, so that field does not reach the rendered window.
4. [held] — the sole exemption is `sessionLabel` gated by exact `WindowKey.session(origin:id:)` equality against `titleResolutionRequests`.

**Surface results**

1. [probed] `PoolDiff.diff` value filtering — shared keys enter `toUpdate` without comparing `DesiredWindow` values. The unchanged-value test passed.
2. [probed] shared frame donor — every spawn reads its donor before either spawn iteration mutates `controllers`; two spawns sharing a donor receive the same live frame.
3. [probed] dismiss/spawn/update overlap — `PoolDiff` cannot produce the same key in multiple partitions. A hand-crafted inconsistent `WindowDiff` can, but that is outside the mechanical diff output contract.
4. [probed] push field completeness — all `DesiredWindow` fields are pushed or documented exclusions except structural `key`/`inheritedFrameFrom`; however, the minimalist prompt-presentation forwarding path discards the pushed value as described below.
5. [probed] protocol-witness resolution — both real controllers and both real panel implementations explicitly implement `applyPromptTimerPresentation`; none inherits the default no-op. The minimalist implementation has a separate overwrite bug.
6. [probed] title exemption identity mapping — comparison reconstructs `WindowKey.session` from both origin and session ID, preventing same-origin/different-session collisions.
7. [probed] output stability/schema drift — `DivergenceRecord` is referenced only by the comparator and tests; no persistence or parser currently treats it as a stable schema.
8. [N/A — no CLI files, flags, arguments, or delivery-tool command surfaces changed in this diff.]
9. [probed] resolver failure breadth — `resolveTitle` returns `String?`, so resolver failure and “no title” both produce no result. This matches the best-effort seam but provides no diagnostic distinction.
10. [probed] defensive module boundary — missing `toUpdate` controllers are silently skipped. Valid `PoolDiff` plus a consistent controller table prevents this; malformed/upstream-inconsistent input would be masked.
11. [probed] cache atomicity — `RetrievedSessionTitleStore` uses atomic `Data.write`; interruption before write causes only a repeated resolve, while interruption during write does not expose a partial JSON file.
12. [probed] test-contract strength — the 20 focused tests passed and assert structural/value equality, including spawn paths with and without frame inheritance. Push completeness does not exercise `promptTimerStatus`, and proxy tests omit the old raw timer call.
13. [probed] doc-vs-code drift — heartbeat behavior matches the Rationale’s description, and the comparator contains no hidden decision-set comparison. The unchanged Outcome still promises decision-set comparison, debug assertions/log emission, and title-result feedback that the Rationale defers.

**Actionable findings**

1. `apps/menubar/Sources/Windows/MinimalistPanelController.swift:250-252` and `apps/menubar/Sources/Windows/MinimalistBadgeView.swift:96-104` — `applyPromptTimerPresentation` first installs the derived presentation, then calls `applyBadge()`. That invokes `configureBadge`, which immediately replaces it with `promptTimerStatus?.presentation()`. Because the new path deliberately does not update raw `promptTimerStatus`, the pushed presentation is commonly replaced by `nil` or stale old-pipeline data. This breaks invariant 3 and is a demonstrated correctness gap. Store the latest presentation separately and have `configureBadge` render it, or restructure the method so layout refresh does not overwrite the supplied presentation. Add a real minimalist-renderer test proving a non-nil presentation survives the complete call.

2. `apps/menubar/Sources/Pool/RecordingFloatingPetWindowControllingProxy.swift:68-70` — the pre-cutover old pipeline calls `applyPromptTimerStatus`, but the proxy forwards that call without recording it, and `RecordedPush` has no raw-timer case. Consequently the proxy cannot capture the old pipeline’s prompt-timer push for shadow comparison, contrary to its “every push” contract. This is a correctness gap that creates a silent comparator blind spot. Add a recorded raw-status case or record its presentation-equivalent, and extend the forwarding/logging test to exercise this method. Consider explicitly classifying `replacePets` similarly if it is intentionally excluded.

**Advisory Observations**

- `docs/product/delivery/phase-18/ticket-04-diff-apply-comparator.md` retains Outcome promises that the implementation does not satisfy: feeding resolved titles into next-tick memory, comparing extracted decision sets against `PoolMemory`, asserting under debug, and emitting log records. The Rationale explicitly defers these behaviors, so the document currently contains conflicting contract statements.

- `PoolApply.apply` silently skips a missing update controller. Valid output from `PoolDiff` should prevent this when caller state is consistent, but a debug assertion would make upstream controller-table drift visible instead of suppressing a whole window’s pushes.

- `PoolApply.resolveTitles` intentionally cannot distinguish resolver failure from a genuine absent title because its resolver returns `String?`. This is acceptable for best-effort behavior but leaves no failure telemetry.

- The focused test command completed successfully: 20 tests, zero failures. Its push-completeness test omits `promptTimerStatus`, allowing the minimalist rendering defect to pass.

**Runner termination**

runnerStatus: completed  
terminatedReason: Completed every mandated invariant and attack-surface probe without modifying repository files.
