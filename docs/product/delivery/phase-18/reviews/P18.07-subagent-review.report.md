Invariant results

- [held] Public methods/properties remain present with the same signatures; direct source inspection found no dropped or renamed surface.
- [held] Production grep found zero references to the deleted engine, rollback flag, shadow wiring, or stub identifiers under `apps/menubar/`.
- [held] The `Pool/Derive` purity gate remains present and unchanged; no AppKit import was found there.

Surface results

- [probed] `init` loads hidden keys once into `PoolMemory`; the old shared-shadow loader branch is gone, and persistence remains explicit through `MenubarApp`.
- [probed] `startHUDDemo()` now casts the directly stored controller to `FloatingPetController`; the production factory returns that controller directly, so no proxy-dependent path remains.
- [probed] Shadow utilities have no production call sites, defaults, observers, or singleton wiring. Remaining references are definitions and tests.
- [probed] Title resolution invokes the platform resolver and write-through once per request; the changed assertions match the surviving path. The cache reader is consulted in both input assembly and resolution on a cache miss, but this is pre-existing behavior and not a correctness failure.
- [probed] The Xcode project contains no references to the three deleted Swift files or the deleted shadow-tick test.
- [probed] All named public members were located in `FloatingPetWindowPool.swift` with unchanged access/signature shape.
- [probed] No persisted artifact schema changed; hidden-key and retrieved-title reader/writer contracts remain the same.
- [probed] Runtime source contains no `CODOGOTCHI_POOL_ENGINE` reads. Historical delivery documents still mention the former flag.
- [probed] No newly exposed Swift `catch` path was introduced by this deletion.
- [probed] The sole update path still assembles `PoolTickInput`, runs `derive` → `diff` → `apply`, and preserves boundary filtering for missing minimalist factories.
- [probed] The deletion is coherent in the implementation commit: removed files and references are deleted together, with no partial project state visible.
- [probed] Removing the two engine-selection tests is justified because the engine-selection concept and flag no longer exist; remaining pool behavior tests cover the active path.
- [probed] Exit-audit conditions 1, 2, and 6 agree with the current source/project/artifact state. The documented condition-5 waiver is explicitly presented as unverified and is not overstated.

Actionable findings

None.

Advisory Observations

- Historical documents such as `docs/product/delivery/phase-18/ticket-06-cutover.md`, `implementation-plan.md`, and prior review artifacts retain literal references to `CODOGOTCHI_POOL_ENGINE` and `LegacyPoolEngine`. They describe prior-phase behavior or explicitly say the flag is deleted, so this is documentation history rather than a production-path violation.

Runner termination

runnerStatus: completed
terminatedReason: Completed the targeted read-only review across all required invariants and attack surfaces.

