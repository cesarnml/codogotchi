**Invariant results**

1. PASS — `FloatingPetController.swift` contains no `func applyPromptTimerStatus(`.
2. PASS — the Minimalist regression calls `applyLocalPromptTimerStatus`, then `configureBadge`, and verifies the stale presentation override is replaced by the raw status presentation.
3. PASS — both local one-second heartbeat timers and reset paths remain unchanged. `PoolApply.push` calls only `applyPromptTimerPresentation`.

**Surface results**

1. PASS — no residual protocol requirement, default implementation, controller forward, or production reference to `applyPromptTimerStatus`.
2. PASS — production and test conformers compile without the removed method; `PoolApply.MockController` now implements presentation push only.
3. PASS — override-clear test is correctly retargeted and exercises the subsequent `configureBadge` path.
4. PASS — heartbeat timers remain at a 1.0-second cadence in both Own and Minimalist implementations; reset paths still invalidate and clear them.
5. PASS — presentation-push persistence through `configureBadge` remains explicitly tested.
6. Output stability — N/A; no output-producing behavior changed.
7. CLI symmetry — N/A.
8. Error-class catch — N/A.
9. Defensive layering — PASS; all protocol conformers compile after surface narrowing.
10. Cross-file atomicity — PASS; protocols, forwards, concrete helpers, mocks, project membership, and tests are consistent.
11. Test-contract strength — PASS; both source-surface and override-clear regressions are present.
12. Doc-vs-code drift — PASS; the ticket Rationale accurately describes the implementation and preserved cadence.

Verification: selected macOS tests passed—152 executed, 3 pre-existing skips, 0 failures.

**Actionable findings**

None.

**Advisory Observations**

The new surface test is intentionally a narrow source scan of `FloatingPetController.swift` for the exact removed declaration spelling. Compilation of the broader conformer suites provides the complementary protection against protocol-conformance gaps.

**Runner termination**

runnerStatus: completed  
terminatedReason: All ticket invariants and attack surfaces were inspected with no actionable correctness gap found.
