You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks.

### Ticket scope

Outcome:
- Production protocols used by PoolApply expose presentation push only — applyPromptTimerStatus not on FloatingPetWindowControlling / PanelManaging (or defaults).
- PoolApply push continues presentation-only.
- Own panel + Minimalist badge retain internal helper for heartbeat / override-clear; retarget MinimalistBadgeViewTests override-clear regression.
- No intentional timer label / wipe / chip visibility change.

### Files touched

apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/MinimalistBadgeView.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift
apps/menubar/Tests/MenubarTests/MinimalistBadgeViewTests.swift
apps/menubar/Tests/MenubarTests/PoolApplyTests.swift
apps/menubar/Tests/MenubarTests/PromptTimerProtocolSurfaceTests.swift
docs/product/delivery/phase-21/ticket-03-prompt-timer-protocol-surface.md

### Invariants to hold

1. FloatingPetController.swift contains no `func applyPromptTimerStatus(` (pool-facing surface is presentation-only).
2. Override-clear regression still proves local status helper clears a stale presentation override on MinimalistBadgeView.
3. Local Timer heartbeat / resetPromptTimer paths remain (not deleted); PoolApply still calls presentation only.

### Attack surfaces to probe

1. Residual protocol/default/forward requiring status on pool-facing surface.
2. Window-controller stubs / PoolApply MockController still requiring removed status method (compile gap).
3. Override-clear test retarget correctness vs applyLocalPromptTimerStatus.
4. Accidental heartbeat Timer deletion or cadence change.
5. Presentation override still survives configureBadge after presentation push.

#### Diff-derived attack surfaces

1. Output stability — N/A expected.
2. CLI symmetry — N/A expected.
3. Error-class catch — N/A expected.
4. Defensive layering — protocol conformance for all FloatPetWindowControlling implementers after status removal.
5. Cross-file atomicity — protocols + forwards + helpers + tests consistent.
6. Test-contract strength — surface scan + override-clear both present.
7. Doc-vs-code drift in Rationale.

### Diff context

Removed applyPromptTimerStatus from FloatingPetWindowControlling and PanelManaging and window-controller forwards. Renamed concrete Own/Minimalist status methods to applyLocalPromptTimerStatus (internal helper). Retargeted override-clear test. Added source-scan invariant test. PoolApply unchanged (already presentation-only). Local Timer heartbeat kept.

---

### Your directives

**Scope:** Adversarial review of implementation diff and related paths in attack surfaces. Do not expand beyond ticket outcome.
**Advisory-only — no file writes.** Entire deliverable is findings prose.
**Read boundary for delivery docs:** Read ticket Rationale for drift; do not write under docs/product/delivery/**.
**Coverage mandate:** Probe each surface or explain N/A. Clean results are valid.
**Finding discipline:** Only report when (1) invariant broken, (2) demonstrable correctness gap, or (3) spec-permits-real-bug. Else Advisory Observations only.
**No fabrication pressure.** Clean report is correct when surfaces hold.

### Required output format

Exactly these headings in order — no `---` rules:
**Invariant results**
**Surface results**
**Actionable findings**
**Advisory Observations**
**Runner termination**

runnerStatus: completed | rate_limit | sandbox_denied | runner_unavailable
terminatedReason: one short sentence
