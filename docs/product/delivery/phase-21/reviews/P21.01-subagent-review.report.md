**Invariant results**

1. [held] Grep under `apps/menubar` found zero references to `PoolShadowComparator`, `RecordingFloatingPetWindowControllingProxy`, or `ShadowDivergenceLogger`; no empty test stubs remain.
2. [held] The live derive → diff → `PoolApply` path compiles and passes the full suite: 1,198 tests executed, 3 skipped, 0 failures.
3. [held] The five scrubbed comments accurately describe current title-resolution and prompt-timer presentation wiring; all Swift changes outside deleted utilities are comment-only.

**Surface results**

- Residual type/symbol references: [probed]. Current `Sources`, `Tests`, and generated pbxproj contain zero deleted-symbol hits. The base commit’s hits were confined to the deleted files, their dedicated tests, and project entries.
- Production spawn/wrap sites: [probed]. `FloatingPetWindowPool` factories return concrete Own or Minimalist controllers directly; no recording proxy construction or wrapping remains.
- Comment scrub accuracy: [probed]. `PoolDerive` populates `titleResolutionRequests` and timer presentations, `FloatingPetWindowPool` resolves titles, and `PoolApply` pushes presentations to both controller implementations.
- Accidental behavior edits: [probed]. Diff inspection confirmed the five retained Swift files changed comments only; no update, prune, apply, or timer statements changed.
- Husk claim: [probed]. `Menubar.xcodeproj` is absent both currently and in base commit `4215d9af`; the Rationale honestly records that there was nothing to delete.
- Output stability across schema-version drift: [N/A — reason: the implementation diff changes no persisted artifact, state, gate, or sidecar schema].
- CLI flag/arg symmetry: [N/A — reason: no CLI implementation, flags, arguments, or command wiring changed].
- Error-class breadth in `catch` blocks: [probed]. Deleting `ShadowDivergenceLogger` removes its entire isolated disk-log path; no remaining shadow logger call, catch, or half-wired shadow persistence path exists.
- Defensive layering at module boundaries: [probed]. `FloatingPetWindowControlling`, its concrete conformers, `PoolApply`, and remaining test doubles compile without any proxy-only API dependency.
- Cross-file atomicity windows: [probed]. The three source deletions and three test deletions match removal of all six pbxproj file/build references. The regenerated project builds and tests successfully.
- Test-contract strength: [probed]. Remaining `PoolDerivePushTests`, `PoolApplyTests`, `MinimalistBadgeViewTests`, controller tests, and `FloatingPetWindowPoolTests` cover live title/timer derive and presentation push behavior. Full suite result: 1,198 executed, 3 skipped, 0 failures.
- Doc-vs-code drift in the ticket Rationale: [probed]. The Rationale matches the diff: three unused utilities and their dedicated tests were deleted, five source comments changed, the project was regenerated, and the already-absent husk produced no deletion.

**Actionable findings**

None.

**Advisory Observations**

- `apps/menubar/Tests/MenubarTests/PoolApplyTests.swift:14` still says “`PoolApply` does not exist yet.” This is stale P18 red-phase archaeology in Tests, outside the ticket’s `Sources/` comment-scrub contract, and does not affect behavior or the stated invariants.

**Runner termination**

runnerStatus: completed
terminatedReason: Finished every required invariant and attack-surface probe, including a successful full Xcode test run.
