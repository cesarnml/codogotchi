**Invariant results**

1. [held] `rg` under `apps/menubar/Sources` found zero production references to `foldedSessionDisplay`, `applyFoldedSessionDisplay`, or `pruneMenuTitle`.
2. [held] The shared prompt builder and both Own and Minimalist confirmation alerts use `FloatingPetHidePrompt.pruneTitle` directly.
3. [held] The diff leaves `hasActiveSession` derivation and prompt-builder gating unchanged; prune remains offered only inside the existing active-session gate.

**Surface results**

- Residual field/push/protocol references: [probed]. Searched production and test code and inspected the complete ticket diff. The field, derive computation, PoolApply push, protocol requirements/defaults, controller forwarding, view storage, and helper are removed atomically.
- Own and Minimalist prune titles: [probed]. `FloatingPetInteractionView`, `MinimalistBadgeView`, and `FloatingPetPromptBuilder` all use bare `FloatingPetHidePrompt.pruneTitle`.
- Derive computation: [probed]. The fold-display construction block was deleted from `PoolDerive`; no alternate computation or storage remains.
- PoolApply removed-method calls: [probed]. `PoolApply` no longer invokes `applyFoldedSessionDisplay`, and the focused test build compiled successfully.
- Test doubles and mocks: [probed]. No mock requires the removed method or capability field. All `FloatingPetPromptCapabilities` initializers compile without it.
- Output stability across schema-version drift: [N/A — no persisted schema or serialized shape changed].
- CLI flag/arg symmetry: [N/A — this ticket changes no CLI surface].
- Error-class breadth in catch blocks: [N/A — no error-handling or catch path changed; the existing prune confirmation flow is otherwise unchanged].
- Defensive layering at module boundaries: [probed]. Inspected every production and test memberwise initializer for `FloatingPetPromptCapabilities`; the focused build confirms call-site symmetry after field removal.
- Cross-file atomicity windows: [probed]. DesiredWindow, derive, PoolApply, both protocols and defaults, controller forwarding, both skins, prompt capabilities, and title helper were updated together with no residual production symbols.
- Test-contract strength: [probed]. Mirror tests assert field absence from `DesiredWindow` and `FloatingPetPromptCapabilities`; the PoolApply update-path test exercises surviving pushes. Eight focused tests passed with zero failures.
- Doc-vs-code drift in the ticket Rationale: [probed]. The Rationale’s prune-only audit and full E2E deletion claims match the implementation diff.
- Additional ticket-relevant compile surface: [probed]. Ran `xcodebuild` for `FloatingPetPromptBuilderTests` and `PoolApplyTests`; all eight selected tests passed. The worktree remained clean.

**Actionable findings**

None.

**Advisory Observations**

- The test named `testPoolApplyDoesNotPushFoldedSessionDisplay` cannot independently detect a stale default `applyFoldedSessionDisplay` protocol method because it only reflects `DesiredWindow` and verifies surviving pushes. The current production grep proves the invariant holds, but a future regression that restores only the protocol method could evade this test.

**Runner termination**

runnerStatus: completed  
terminatedReason: Completed every required invariant and attack-surface probe without modifying the repository.
