You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations**.
Your job is a targeted attack on the behavior this ticket is supposed to protect.

### Ticket scope
- SessionPruner.pruneSession no longer takes allocator; disk-only (slice + label + retrieved-title).
- FloatingPetWindowPool.pruneSession does not construct SessionNumberAllocator().
- Class SessionNumberAllocator deleted from Sources.
- Tests migrated to SessionNumberAllocatorState; assign/reuse/unlimited covered.
- Behavior freeze: no double-release; numbering unchanged.

### Files touched
apps/menubar/Sources/State/SessionPruner.swift
apps/menubar/Sources/State/SessionNumberAllocator.swift (deleted)
apps/menubar/Sources/Pool/FloatingPetWindowPool.swift
apps/menubar/Sources/Pool/Derive/SessionNumberAllocatorState.swift
apps/menubar/Tests/MenubarTests/SessionPrunerTests.swift
apps/menubar/Tests/MenubarTests/SessionNumberAllocatorTests.swift
apps/menubar/Tests/MenubarTests/SessionPrunerAllocatorSurfaceTests.swift
apps/menubar/Codogotchi.xcodeproj/project.pbxproj
docs/product/delivery/phase-21/ticket-04-pruner-allocator-unify.md

### Invariants
1. No `final class SessionNumberAllocator` and no `SessionNumberAllocator()` under apps/menubar/Sources.
2. SessionPruner source has no allocator param/release; number release only via PoolMemory.pruning / SessionNumberAllocatorState.
3. Migrated allocator tests prove sequential assign, lowest-free reuse, no live renumber, unlimited monotonic, per-origin independence.

### Attack surfaces
1. Residual class construction or Pruner release path.
2. Double-release if Pruner still released while memory.pruning also releases.
3. Migrated tests not covering unlimited/reuse parity with deleted class.
4. Pool prune call-site still passing removed param (compile gap).
5. pbxproj still referencing deleted SessionNumberAllocator.swift.

#### Diff-derived
1 Output stability N/A 2 CLI N/A 3 catch N/A 4 module boundaries on prune call sites 5 Sources+pbxproj deletion atomicity 6 test-contract strength of surface scans + migrated suite 7 Rationale drift.

### Diff context
Removed allocator from SessionPruner; removed throwaway construction from FloatingPetWindowPool.pruneSession; deleted class SessionNumberAllocator.swift; migrated SessionNumberAllocatorTests to SessionNumberAllocatorState; SessionPrunerTests disk-only.

### Directives
Advisory-only no file writes. Read ticket Rationale for drift. Coverage mandate on every surface. Finding discipline: broken invariant / demonstrable gap / spec-permits-real-bug only. No fabrication.

### Required output format
**Invariant results**
**Surface results**
**Actionable findings**
**Advisory Observations**
**Runner termination**
runnerStatus / terminatedReason — no `---` rules.
