You are conducting an adversarial review of a code change. You may add extra ticket-relevant attack surfaces. Findings outside the finding-discipline clauses belong in Advisory Observations. This is a targeted attack on P19.02, not a general code review.

### Ticket scope

Prune must resolve the real `(origin, sessionId)` from the current window's P19.01 `resolvedIdentity`, work for session-keyed, folded origin, default-sentinel origin, and Combined windows, and remain a no-op when no resolved identity exists. The right-click Sync Label and Prune actions must be offered whenever the window resolves to a real session. Menu copy and the mode badge remain deferred.

Rationale: click-time targeting reads `lastDesired`; a bare origin represents the `default` sentinel; menu capability is separate from session numbering. Production snapshots are render-key-collapsed, so `renderKeyIdentities` reconstructs the real winner before label and prune use.

### Files touched

Implementation:
apps/menubar/Sources/Pool/Derive/DesiredWindows.swift
apps/menubar/Sources/Pool/Derive/PoolDerive.swift
apps/menubar/Sources/Pool/FloatingPetWindowPool.swift
apps/menubar/Sources/Pool/PoolApply.swift
apps/menubar/Sources/Pool/RecordingFloatingPetWindowControllingProxy.swift
apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/MinimalistBadgeView.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift
docs/product/delivery/phase-19/ticket-02-prune-every-window-kind.md

Tests:
apps/menubar/Tests/MenubarTests/FloatingPetWindowPoolTests.swift

### Invariants to hold

1. Pruning any live rendered window deletes exactly its currently resolved backing slice, including folded-origin, bare-origin default sentinel, and Combined winners, without deleting a sibling.
2. A prune with no current desired/resolved session is a no-op, while every live window backed by a real session receives `hasActiveSession == true` independently of session numbering.
3. A production render-key-collapsed snapshot uses `renderKeyIdentities` for both live label lookup and click-time prune identity without changing `WindowKey.rawValue` persistence semantics.

### Attack surfaces to probe

1. `FloatingPetWindowPool.pruneSession` — stale/missing `lastDesired`, default-sentinel filename translation, sibling deletion, and teardown bookkeeping keyed by display key versus resolved key.
2. `PoolDerive.derive` winner identity reconstruction — Combined/folded/default winners, transient Combined retention, missing identity-map fallback, and whether `hasActiveSession` can become true without a real slice.
3. `FloatingPetWindowPool.update` input-key expansion — whether labels/titles for resolved identities are actually read in production and whether default-sentinel or duplicate identities regress existing lookups.
4. `PoolApply` plus Own/Minimalist controller forwarding — whether active-session capability reaches both prompt builders and stays independent of session-number badge layout.
5. Tests — whether assertions prove exact target deletion, sibling survival, default physical filename convention, menu capability delivery, and no-identity no-op behavior.

#### Diff-derived attack surfaces

1. Output stability across schema-version drift: probe `WindowKey.rawValue`, persisted sidecar keys, and state.d filenames; no schema should change.
2. CLI flag/arg symmetry: N/A unless the diff unexpectedly changes a CLI surface.
3. Error-class breadth in catch blocks: probe the existing best-effort deletion boundary; no new catch was intended.
4. Defensive layering at module boundaries: probe `RenderKeyIdentity` to `WindowKey`, DesiredWindow to controller, and controller to prompt capability normalization.
5. Cross-file atomicity windows: probe teardown plus slice/label/title deletion ordering and whether the widened target creates a new partial-state hazard.
6. Test-contract strength: probe stable structural behavior and both resolved and missing-identity paths, not prose-only assertions.
7. Doc-vs-code drift in the ticket Rationale: compare the rationale, default-sentinel convention, capability claim, and exact implementation.

### Diff context

The diff adds `DesiredWindow.hasActiveSession`, reconstructs a real resolved identity from the snapshot's parallel identity map, and pushes the capability through PoolApply to Own and Minimalist prompt builders. Pool input lookup now includes resolved identity keys so folded windows read the winning session's live label. `pruneSession` reads the current desired identity and sends that resolved raw key plus origin/session id to `SessionPruner`, mapping a bare origin to the default sentinel. Tests cover folded origin, bare default slice, Combined winner, sibling survival, and capability pushes; full CI passed with 1,154 tests, 3 skipped, 0 failures.

### Directives

Advisory-only: do not create, modify, or delete repository files. Read the diff, ticket, and directly related code. Report every surface as probed, N/A with reason, or blocked. Findings must be invariant breaks, demonstrated correctness gaps, or spec-permits-real-bug cases; put other real notes in Advisory Observations. Do not fabricate findings.

Use exactly these five headings in order and no horizontal rules:

**Invariant results**
For each invariant: `[held | broken | untested]` and what you tried.

**Surface results**
For every ticket-derived and diff-derived surface: `[probed | N/A — reason | blocked — missing-input]` and findings.

**Actionable findings**
Path, defect, applicable invariant/clause, and concrete fix; or `None.`

**Advisory Observations**
One observation per bullet/paragraph; or `None.`

**Runner termination**
runnerStatus: completed | rate_limit | sandbox_denied | runner_unavailable
terminatedReason: one short sentence.
