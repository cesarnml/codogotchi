**Invariant results**

1. [held] `DesiredWindow.resolvedIdentity` defaults to its own key and is replaced by the elected winner for folded `.origin`/`.combined` windows.
2. [held] Labels, title cache writes, and title-resolution requests use `resolvedIdentity`; raw per-slice input population was confirmed in `FloatingPetWindowPool.update()`.
3. [held] Empty combined folds retain the previous window while resetting identity to `.combined`, preserving the P18.03 transient-gap fallback.

**Surface results**

- Origin/session identity derivation: [probed] Session, origin-fold, solo-origin, and combined winner paths were inspected and targeted tests passed.
- Live label and title-cache reuse: [probed] Folded labels read winner-keyed `sessionLabels` and `knownSessionTitles`; no new fold-key cache write remains.
- Title-resolution seam: [probed] Requests now use the winning session identity and its `renderKeyIdentities` entry.
- Combined transient fallback: [probed] Existing transient-gap test passed; fallback resolves to `.combined`.
- Raw per-slice input construction: [probed] `keysNeedingInput` is initialized from every `snapshot.perPlatform` key and includes `.combined`.
- Output stability across schema-version drift: [N/A — reason: no persisted schema or serialized output shape changed].
- CLI flag/arg symmetry: [N/A — reason: no CLI flags or arguments changed].
- Error-class breadth in `catch` blocks: [N/A — reason: no catch blocks changed].
- Defensive layering at module boundaries: [N/A — reason: no new untrusted module boundary was introduced].
- Cross-file atomicity windows: [N/A — reason: no multi-file runtime write sequence changed].
- Test-contract strength: [probed] Targeted tests passed, but the new tests do not cover the explicitly required `.session` identity assertion, tick-to-tick winner rotation, known-title cache reuse, or folded title-resolution requests.
- Doc-vs-code drift in the ticket Rationale: [probed] The Rationale matches the implementation; no drift found.

**Actionable findings**

- `apps/menubar/Tests/MenubarTests/Derive/PoolDerivePushTests.swift` — the required Red contract is incompletely represented: there is no explicit `.session` `resolvedIdentity == key` test, no tick-to-tick winner rotation test, and no assertion that `knownSessionTitles`/`titleResolutionRequests` follow the real folded winner. This is a ticket-specific test-contract gap. Add table-driven coverage for those cases, including winner rotation and cache/request behavior.

**Advisory Observations**

- The targeted `xcodebuild` run passed all 9 `PoolDerivePushTests` with 0 failures. Xcode emitted unrelated environment warnings about `com.apple.linkd.autoShortcut` and absent optional pet sprite sheets.

**Runner termination**

runnerStatus: completed  
terminatedReason: Review completed with source inspection and targeted xcodebuild verification.
