You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

## Outcome

- The Prune context-menu item and its destructive-action confirmation alert (`FloatingPetInteractionView.presentPruneConfirmation`) include the resolved session's identity (platform + label) in their displayed text for a fold window (`.origin` folding multiple sessions, or `.combined`) — e.g. "Prune Session (Claude Code · refactor the diff module)" — instead of a bare "Prune Session" that gives no indication of what's about to be removed.
- For a genuinely solo `.session`-keyed or solo `"default"`-sentinel window, text is unchanged from today — the window is already unambiguous, no extra copy needed.
- No change to Prune's targeting logic (P19.02) or the mode badge (P19.04) — this ticket is copy/UI-text only, reusing data both prior tickets already resolved.

## Rationale (as recorded)

Why this path: `DesiredWindow` already carries `resolvedIdentity` and a per-tick-resolved `sessionLabel` (P19.01/P19.02) keyed off it, but no view ever learns whether a window is actually folding another identity (`key != resolvedIdentity`) — only `currentSessionLabel`, which is populated the same way for a genuinely solo window too, so it can't disambiguate "no fold" by itself. The fix computes one new formatted field, `DesiredWindow.foldedSessionDisplay` ("`<platform> · <label>`", `nil` when not folding), in `PoolDerive` right after `sessionLabel` is resolved, and threads it through the existing `applySessionLabel`-shaped push pipeline (`PoolApply` → `FloatingPetWindowControlling`/`PanelManaging` protocols → `FloatingPetPanelController`/`MinimalistPanelController` → view) to both Own and Minimalist. `FloatingPetHidePrompt.pruneMenuTitle(foldedSessionDisplay:)` is one pure formatter shared by the menu item (`FloatingPetPromptBuilder`) and both `presentPruneConfirmation()` alert bodies.
Alternative considered: a plain `Bool` "isFold" flag on the view was rejected — it can't carry the platform name needed for the ticket's example text without duplicating the platform-name-vs-label dedup logic in two views.
Deferred: the mode-indicator badge (P19.04); any change to confirmation-alert mechanics (skip-confirmation preference, button structure).

### Files touched

Implementation:
apps/menubar/Sources/Pool/Derive/DesiredWindows.swift
apps/menubar/Sources/Pool/Derive/PoolDerive.swift
apps/menubar/Sources/Pool/PoolApply.swift
apps/menubar/Sources/Pool/RecordingFloatingPetWindowControllingProxy.swift
apps/menubar/Sources/Windows/FloatingPetController.swift
apps/menubar/Sources/Windows/FloatingPetHidePrompt.swift
apps/menubar/Sources/Windows/FloatingPetInteractionView.swift
apps/menubar/Sources/Windows/FloatingPetPanelController.swift
apps/menubar/Sources/Windows/FloatingPetPromptBuilder.swift
apps/menubar/Sources/Windows/MinimalistBadgeView.swift
apps/menubar/Sources/Windows/MinimalistPanelController.swift

Tests:
apps/menubar/Tests/MenubarTests/FloatingPetPromptBuilderTests.swift

### Invariants to hold

1. For any `DesiredWindow` where `key != resolvedIdentity` (a fold window) and a resolved `sessionLabel` exists, `window.foldedSessionDisplay` must be non-nil and the Prune menu item / confirmation alert title must equal `"Prune Session (<foldedSessionDisplay>)"`.
2. For any `DesiredWindow` where `key == resolvedIdentity` (a genuinely solo window — solo `.session`-keyed, or a solo `"default"`-sentinel `.origin` window per P19.01's mapping), `window.foldedSessionDisplay` must be `nil` and both Prune surfaces must show exactly the bare `"Prune Session"` string, byte-identical to pre-ticket behavior.
3. `FloatingPetHidePrompt.pruneMenuTitle(foldedSessionDisplay:)` must be the single formatter used by both the menu item title (`FloatingPetPromptBuilder.items`) and both `presentPruneConfirmation()` alert `messageText` assignments (Own and Minimalist) — no surface may compute its own ad hoc string.

### Attack surfaces to probe

1. `PoolDerive.swift` fold-display computation (the new block right after `window.sessionLabel = input.sessionLabels[resolvedIdentity] ?? fallback`): does `key != resolvedIdentity` correctly evaluate to `false` for a solo `"default"`-sentinel `.origin` window (where P19.01's mapping sets `resolvedIdentity` back to `.origin(identity.origin)` when `identity.sessionId == "default"`), and to `true` for every fold case (multi-session origin-fold, and `.combined`)? Check especially the `.combined` case, which never has `key == resolvedIdentity` by construction — does the platform-name lookup (`PlatformAttribution(origin: resolvedIdentity.origin)?.displayName`) ever `nil`-out for an origin `.combined` legitimately resolves to, producing a `nil`-platform "just the label" fallback when a real platform name should have been shown?
2. Same block — the `platformName != label` de-dup branch: could `sessionLabel`'s own fallback chain (`resolvedSessionTitles` → `"Session N"` → platform display name) produce a value that's a *different string but same concept* as `platformName` in a way that isn't caught by strict `!=` (e.g. `sessionLabel` fallback already equals `platformName` verbatim vs. a `"Session 3"` label that's clearly distinct) — verify no case produces a nonsensical duplicate like `"Claude Code · Claude Code"` slipping through, or an awkward `"Codex · Session 2"` where `"Session 2"` isn't really an identity-differentiator worth pairing with the platform name.
3. `FloatingPetPanelController.applyFoldedSessionDisplay` / `MinimalistPanelController.applyFoldedSessionDisplay`: unlike `applySessionLabel`/`applySessionNumber`, this new method has no `guard currentX != newValue else { return }` dedup guard before forwarding to the view — does the always-forward behavior cause any observable issue (e.g. redundant `repositionAndShowAnimationBadge()`-style side effect, or a stale-write race) compared to the guarded siblings it sits next to, or is the omission safe because the view-side property write is a plain assignment with no side effect of its own?
4. `PoolApply.push`: `controller.applyFoldedSessionDisplay(window.foldedSessionDisplay)` is now called unconditionally for every window key including `.combined` — verify this doesn't hit the same `key != .combined` guard `applySessionNumber` needs (comment: "The combined window is never session-keyed — its push site never calls applySessionNumber at all"). Is skipping that guard for `foldedSessionDisplay` correct given `.combined` should always carry a folded display per the ticket's own "`.combined` always shows it" rule, or does an unguarded push risk pushing a stale/wrong value on a tick where `winnerEntry` was nil and `window.resolvedIdentity` defaulted to `window.key` (i.e. the `if let winnerEntry` block at the top of the tick body never ran)?
5. `RecordingFloatingPetWindowControllingProxy.applyFoldedSessionDisplay`: confirm the new `RecordedPush.foldedSessionDisplay` case is actually asserted somewhere (or intentionally not, since this proxy is described as "no production code wraps this anymore") — is silently recording-but-never-asserting a signal of dead test infrastructure growing unchecked, or is that expected given the ticket's doc comment on the proxy file?
6. `FloatingPetInteractionView.foldedSessionDisplay` / `MinimalistBadgeView.foldedSessionDisplay`: both are plain stored `var`/`private var` with no didSet triggering a UI refresh (unlike `currentSessionLabel`'s sibling `applySessionLabel` control-flow, which calls `repositionAndShowAnimationBadge()`). Since the Prune menu/alert text is only read lazily at right-click time (not rendered eagerly), is a missing refresh trigger actually a non-issue here, or could a stale value be read if the view is captured/cached across ticks in some code path this review should check?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — no persisted artifact, CLI stdout shape, or on-disk JSON format changes in this diff; `foldedSessionDisplay` is purely in-memory tick state].
2. **CLI flag/arg symmetry** — [N/A — no CLI flags added or changed].
3. **Error-class breadth in `catch` blocks** — [N/A — no new or modified `catch` blocks in this diff].
4. **Defensive layering at module boundaries** — probe whether `FloatingPetHidePrompt.pruneMenuTitle(foldedSessionDisplay:)` (a new cross-module-callable pure function) needs to defend against a malformed/empty-string `foldedSessionDisplay` (e.g. `""` rather than `nil`) reaching it from `PoolDerive`'s computation, and whether the computation itself could ever produce an empty string instead of `nil`.
5. **Cross-file atomicity windows** — [N/A — this diff has no multi-step write (state+artifact, commit+push, file-A+file-B); it's a single in-memory per-tick field computed and consumed within the same pool tick].
6. **Test-contract strength** — probe whether `FloatingPetPromptBuilderTests` covers both the omitted (`foldedSessionDisplay: nil`, default-shaped call) and supplied (`foldedSessionDisplay: "..."`) paths for every surface that reads it (menu builder is covered; the two `presentPruneConfirmation()` alert call sites — Own and Minimalist — are NOT covered by this new test file since they're view-internal AppKit code with no existing test harness pattern in the diff). Is that gap acceptable given the shared-formatter design (invariant 3), or does it leave a real risk that one of the two alert call sites silently reverts to a raw string during a future edit?
7. **Doc-vs-code drift in the ticket Rationale** — read `docs/product/delivery/phase-19/ticket-03-prune-menu-names-real-session.md`'s Rationale (just written) against the actual diff and confirm every claim (which file computes what, which alternative was rejected and why, what was deferred) matches what the code actually does. Also cross-check the Rationale's "no change to Prune's targeting logic (P19.02) or the mode badge (P19.04)" outcome claim against the diff — is that claim accurate (no `pruneSession` targeting logic or badge-visibility code touched)?

### Diff context

Key logic change 1 — `PoolDerive.swift`, immediately after `window.sessionLabel = input.sessionLabels[resolvedIdentity] ?? fallback`:
```swift
if key != resolvedIdentity, let label = window.sessionLabel {
    let platformName = PlatformAttribution(origin: resolvedIdentity.origin)?.displayName
    window.foldedSessionDisplay =
        if let platformName, platformName != label {
            "\(platformName) · \(label)"
        } else {
            label
        }
}
```

Key logic change 2 — `FloatingPetHidePrompt.swift`, new pure formatter:
```swift
static func pruneMenuTitle(foldedSessionDisplay: String?) -> String {
    guard let foldedSessionDisplay else { return pruneTitle }
    return "\(pruneTitle) (\(foldedSessionDisplay))"
}
```

Key logic change 3 — `FloatingPetPromptBuilder.swift`, menu item construction now reads `capabilities.foldedSessionDisplay`:
```swift
items.append(
    FloatingPetPromptItem(
        title: FloatingPetHidePrompt.pruneMenuTitle(foldedSessionDisplay: capabilities.foldedSessionDisplay),
        onActivate: handlers.prune))
```

Key logic change 4 — both `presentPruneConfirmation()` implementations (`FloatingPetInteractionView.swift`, `MinimalistBadgeView.swift`) now build `alert.messageText` from the same formatter instead of a literal `"Prune Session"` string:
```swift
alert.messageText = FloatingPetHidePrompt.pruneMenuTitle(foldedSessionDisplay: foldedSessionDisplay)
```

Push threading — `PoolApply.push` gained one line right after `controller.applySessionLabel(window.sessionLabel)`:
```swift
controller.applyFoldedSessionDisplay(window.foldedSessionDisplay)
```
which flows through new `applyFoldedSessionDisplay(_:)` members added (with default no-op extensions) to `FloatingPetWindowControlling` and `PanelManaging`, implemented as pass-through in `FloatingPetController`/`MinimalistWindowController` and as store-and-forward-to-view in `FloatingPetPanelController`/`MinimalistPanelController`.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
