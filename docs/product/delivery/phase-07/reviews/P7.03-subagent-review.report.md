I have all the information needed. Here is the adversarial review.

---

**Invariant results**

1. `classifyEvent({ hook_event_name: "StopFailure", error: "<any>" })` → `errored` — **held**. The branch at `hook-binary.ts:203` fires on `rawEventName === "stopfailure"` unconditionally, before any `error` field check. Any error value (including values not representable as strings) returns `errored`.

2. `postToolUseFailure` `is_interrupt` discrimination — **held**. `is_interrupt !== true` fires for both `false` and `undefined`; `is_interrupt: true` falls through. Tests at lines 869 and 880 cover both arms.

3. `installHooks` writes `StopFailure` slot; `claudeInstalled` detects its absence — **held**. `CODOGOTCHI_EVENTS` at `hooks.ts:82` includes `"StopFailure"`. The installer loop at line 358–364 writes that slot. `claudeInstalled` at line 317 iterates `CODOGOTCHI_EVENTS.every(...)` which now requires `StopFailure`. The P7.03 test at `hooks.test.ts:586` confirms `claudeInstalled` returns false when `StopFailure` is absent.

---

**Surface results**

1. **StopFailure always-errored — no `error` field required** — [probed] Passed `{ hook_event_name: "StopFailure" }` (no `error` field) through the code path. `rawEventName` becomes `"stopfailure"`, the branch at line 203 returns `errored` immediately. The `error` field is never read. Invariant holds for absent `error`. No test covers the absent-`error` case directly (both P7.03 tests supply `error: "rate_limit"` and `error: "server_error"`), but the code is unconditional so the gap is in test coverage only, not in production behavior.

2. **`postToolUseFailure` `is_interrupt` default** — [probed] `undefined !== true` evaluates to `true`, so the condition fires and returns `errored`. This matches the spec ("with `is_interrupt` absent"). The test at line 869 covers `is_interrupt: false` explicitly; `undefined` is not directly tested but the code is correct.

3. **`rawHookKind` for `StopFailure`** — [probed] `rawHookKind` maps `"stopfailure"` → `"session_end"` at line 126. `sourceEventKindSchema` at `contracts/src/state-json.ts` includes `"session_end"` in its enum. `normalize` builds `candidate` with `kind: "session_end"` and `origin: "claude_code"`, both valid. `sourceEventSchema.safeParse` succeeds; `normalize` returns a non-null result. `classifyEvent` then hits the `rawEventName === "stopfailure"` branch before using the normalized result for anything else, but the `sourceEvent` passed back in the result has `kind: "session_end"` and `origin: "claude_code"` — both valid contract values.

4. **`rawHookOrigin` for `StopFailure`** — [probed] `"StopFailure"[0]` is `"S"`, which equals `"S".toUpperCase()`, so the PascalCase branch at line 107 returns `"claude_code"`. `sourceEvent.origin` in the `StopFailure` result is `"claude_code"`. Correct.

5. **Cursor `stop` with `status:"success"` still produces `standby`** — [probed] `rawEventName === "stop"` reaches the Stop branch at line 211. `input.status === "error"` is false for `"success"`. `input.is_error` and `input.stop_reason` are also falsy. The branch returns `{ state: "standby", ... }`. No regression. The existing test at line 419 (`classifies Stop event as standby`) covers the no-status case; the new test at line 891 also covers it.

6. **`claudeInstalled` detection — CODOGOTCHI_EVENTS loop** — [probed] `claudeInstalled` at line 317 uses `CODOGOTCHI_EVENTS.every((event) => { const slot = hooks[event]; ... })`. In TypeScript, `hooks[event]` on a `Record<string, ...>` is bracket notation, which for the string `"StopFailure"` is identical to `hooks.StopFailure`. No distinction is possible at runtime; both access the same property. The key stored in settings.json is the string `"StopFailure"` (written by the installer loop), and `claudeInstalled` reads `hooks["StopFailure"]`. These match. No issue.

7. **Defensive layering — malformed `StopFailure` payload and normalize order** — [probed] `classifyEvent` calls `normalize(input)` first at line 187, which may return null. When it returns null, the fallback `{ origin: "claude_code", kind: "session_start", name: "unknown", command: undefined }` is used. The `rawEventName` check at line 201 is derived from `input.hook_event_name` directly, independently of normalize's output. So even if normalize returned null (it won't for `StopFailure` — see surface 3), the `rawEventName === "stopfailure"` check still fires and returns `errored`. There is no path where a malformed `StopFailure` payload produces a non-errored state.

8. **Test-contract strength — revert-detects-regression** — [probed] If the `rawEventName === "stopfailure"` branch were removed, the `StopFailure` event would fall through to `rawEventName === "stop"` (no match, different string), then `input.is_error` (undefined → false), then the `kind === "tool_use"` block (kind is `"session_end"` → no match), and finally `return { state: "idle", ... }`. The P7.03 tests at lines 842 and 850 assert `state === "errored"`; they would fail on revert. If `"StopFailure"` were removed from `CODOGOTCHI_EVENTS`, the installer test at line 574 (`raw.toContain("StopFailure")`) and the `hooksStatus` test at line 586 (`installed === false`) would both fail. The test contract is mutation-detecting.

9. **Doc-vs-code drift in ticket Rationale** — [probed] The comment above `CODOGOTCHI_EVENTS` at `hooks.ts:77–81` reads: `StopFailure fires instead of Stop on API errors (rate-limit, authentication, billing, server errors)`. The array at line 82 is `["PreToolUse", "Stop", "StopFailure"]`. The ticket Rationale states "`CODOGOTCHI_EVENTS` extended to `["PreToolUse", "Stop", "StopFailure"]`." These match exactly. No drift.

---

**Actionable findings**

None.

---

**Advisory Observations**

- The `StopFailure`-without-`error` case (surface 1) has no dedicated test. The spec says "any `error` value including absent/undefined." The code is unconditionally correct, but a test passing `{ hook_event_name: "StopFailure" }` with no `error` field would make the invariant verifiable by inspection and protect against a future accidental narrowing of the branch to `rawEventName === "stopfailure" && input.error !== undefined`. Low priority; no production risk today.

- `postToolUseFailure` with `is_interrupt` absent (undefined) is not directly tested. The code handles it correctly (`undefined !== true` → `errored`), but the spec explicitly calls out the "absent" case as a covered path. A test with `{ hook_event_name: "postToolUseFailure" }` (no `is_interrupt` field) would explicitly document that the absent case is covered, as the spec intends.

- `uninstallHooks` at `hooks.ts:439–450` iterates `Object.entries(claudeHooks)` and strips codogotchi matchers from every event slot it finds, but it does not explicitly delete `StopFailure` from the resulting object if the cleaned array is empty. It does `delete claudeHooks[event]` when `cleaned.length === 0`. This is correct for `StopFailure` — after a fresh install, `StopFailure` has exactly one matcher (the codogotchi one), so after stripping it the slot is empty and gets deleted. The existing `uninstall removes codogotchi hook entries` test at line 389 does not verify that `StopFailure` is absent after uninstall. Not a bug in the current implementation, but the gap means a future refactor of `withCodogotchiMatcher` that adds a second matcher entry would silently leave a `StopFailure` ghost slot after uninstall.

---

**Runner termination**
`runnerStatus`: completed
`terminatedReason`: All invariants held; all attack surfaces probed to conclusion with no actionable findings.
