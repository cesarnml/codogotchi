You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P7.01 Schema v4 vocabulary — 19-state enum, drop work_mode, row-map rename + placeholders (TS + Swift)

Outcome:
- `ACTIVITY_STATES` in `packages/contracts/src/animation-state.ts` is the 19-state schema-v4 closed enum: `idle`, `standby`, `implementing`, `testing`, `thinking`, `reading`, `cramming`, `errored`, `waiting_for_input` (hook/lite/Codex) + `ticket_started`, `red_tdd`, `green_tdd`, `adversarial_review`, `open_pr`, `poll_review`, `record_review`, `advance`, `ticket_completed`, `review_clean` (SoA gates). The 12 deleted states are gone.
- `STATE_JSON_SCHEMA_VERSION` is `4`; `state-json.ts` drops the `work_mode` field; no `gate_badge` field is added.
- Swift `ActivityState` has 19 cases with correct `rawValue` strings; `EXPECTED_STATE_SCHEMA_VERSION = 4`.
- `CodogotchiPet.rowMap` keys are renamed to surviving gate states with temporary placeholder rows: `green_tdd`→2, `red_tdd`→3, `open_pr`→4, `record_review`→8. Comment marks placeholders as temporary.
- All TS and Swift fixtures referencing old state names are updated to v4 names; the full suite (TS `bun test` + Swift `mac:test`) is green.
- `RELIABLE_ACTIVITY_STATES` / `HEURISTIC_ACTIVITY_STATES` groupings are updated to the v4 vocabulary.

Rationale notes:
- `running-tests`→`testing`, `reviewing`→`thinking`, `pushing`→`implementing` for vocabulary rename (no new classification logic — that is P7.02).
- SoA gate-to-state mapping updated in both `soa-events.ts` (SOA_EVENT_TO_ACTIVITY_STATE) and `hook-binary.ts` (SOA_GATE_TO_STATE) to use v4 states.
- CodexPet row 7 now maps to both `.implementing` and `.testing` (shared running loop — visual compromise until lite sheet ships).
- DemoCycleDriver cycle grew from 15 to 19 states.

### Files touched

Implementation:
packages/contracts/src/animation-state.ts
packages/contracts/src/state-json.ts
packages/contracts/src/soa-events.ts
packages/cli/src/hook-binary.ts
apps/menubar/Sources/ActivityState.swift
apps/menubar/Sources/StateJsonReader.swift
apps/menubar/Sources/CodogotchiPet.swift
apps/menubar/Sources/CodexPet.swift
apps/menubar/Sources/DemoCycleDriver.swift

Tests:
packages/contracts/src/state-json.test.ts
packages/cli/src/hook-binary.test.ts
apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift
apps/menubar/Tests/MenubarTests/CodogotchiPetTests.swift
apps/menubar/Tests/MenubarTests/CodexPetTests.swift
apps/menubar/Tests/MenubarTests/DemoModeTests.swift
apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift
apps/menubar/Tests/MenubarTests/MenubarRendererTests.swift
apps/menubar/Tests/MenubarTests/PetStateFanoutTests.swift
apps/menubar/Tests/MenubarTests/TransitionLogTests.swift
apps/menubar/Tests/MenubarTests/LivePollingTests.swift

Fixtures (new v4):
apps/menubar/Fixtures/state-json/testing.json
apps/menubar/Fixtures/state-json/thinking.json
apps/menubar/Fixtures/state-json/ticket_started.json
apps/menubar/Fixtures/state-json/ticket_completed.json
apps/menubar/Fixtures/state-json/review_clean.json
... (and 10 more new v4 fixture files)

### Invariants to hold

1. `ACTIVITY_STATES` in `animation-state.ts` must contain exactly 19 entries — no more, no fewer — and every entry must have a corresponding Swift `ActivityState` case with a matching `rawValue` string.
2. `parseStateJson` must accept `schema_version` 1–4 and reject `schema_version` 5+; `work_mode` must be silently stripped (not cause a parse error) and must not appear as a typed field on the parsed output.
3. The Swift `StateJsonReader` must accept `schema_version` 1–4 payloads and return `.schemaNewer(got: N, expected: 4)` for any payload with `schema_version` > 4; `ActivityState(rawValue:)` must return nil for all 12 removed states.

### Attack surfaces to probe

1. **`ACTIVITY_STATES` enum completeness cross-check (TS ↔ Swift)** — does every TS string literal in `ACTIVITY_STATES` have a matching Swift `rawValue`? Probe by reading both `animation-state.ts` and `ActivityState.swift` and doing a set comparison.

2. **`work_mode` stripping behavior** — Zod's default is to strip unknown keys. Probe whether `work_mode` is truly stripped (not rejected as an invalid field) by checking the Zod schema mode. If the schema uses `.strict()` or `.passthrough()` instead of the default `.strip()`, work_mode would either throw or be preserved — both wrong.

3. **SOA_GATE_TO_STATE / SOA_EVENT_TO_ACTIVITY_STATE consistency** — the hook-binary has a local `SOA_GATE_TO_STATE` and the contracts package has `SOA_EVENT_TO_ACTIVITY_STATE`. Both were updated. Probe whether they map the same SoA event names to the same v4 activity states (they should be consistent).

4. **`CodogotchiPet.rowMap` key coverage** — the rowMap now has 8 entries (down from 9). Probe whether `.reviewClean` and `.ticketCompleted` both map to row 0 correctly (they share a row — valid but unusual). Also verify no old removed states (`.celebrating`, `.panicking`, etc.) remain as keys.

5. **Forward-compat boundary exactness** — `EXPECTED_STATE_SCHEMA_VERSION = 4` in Swift and `max(STATE_JSON_SCHEMA_VERSION)` in TS Zod schema. Probe the edge case: schema_version 4 must succeed, schema_version 5 must fail with `.schemaNewer(got: 5, expected: 4)`. Check both the Zod `.max()` call in `state-json.ts` and the Swift version guard in `StateJsonReader.swift`.

6. **Removed states decode to `.idle` in Swift** — `ActivityState(rawValue: "running-tests")` and `ActivityState(rawValue: "hyped")` should return nil, and `init(from decoder:)` should fall back to `.idle` when decoding a removed state. Probe the `init(from decoder:)` fallback in `ActivityState.swift`.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — old v1/v2/v3 fixtures (`idle.json`, `standby.json`, `errored.json`) still use their original schema_versions (1, 3, 2 respectively). New v4 fixtures use schema_version 4. Probe whether old fixtures still parse successfully under the new TS and Swift schemas.

2. **CLI flag/arg symmetry** — no CLI flags were added or changed in this ticket. `[N/A — no CLI surface changes]`

3. **Error-class breadth in `catch` blocks** — no new catch blocks introduced. `[N/A — no new error handling]`

4. **Defensive layering at module boundaries** — the `SOA_GATE_TO_STATE` map in `hook-binary.ts` and `SOA_EVENT_TO_ACTIVITY_STATE` in `soa-events.ts` are the key module boundaries. Probe whether unmapped SoA gate names still fall through to idle correctly (the else branch in the gate classifier).

5. **Cross-file atomicity windows** — vocabulary change touches multiple files atomically. No multi-step writes to disk artifacts were introduced. `[N/A — pure vocabulary/enum change]`

6. **Test-contract strength** — new tests assert `STATE_JSON_SCHEMA_VERSION === 4`, enum membership (contains/not-contains), and parsing round-trips. Probe whether the Red tests would re-fail if the Green changes were reverted (i.e., are they truly regression-catching?).

7. **Doc-vs-code drift in the ticket Rationale** — the Rationale says "no behavioral classifier changes" but the diff changes what states are emitted by `classifyEvent` (e.g., `running-tests`→`testing` for Bash test-runner commands). Read the ticket spec and rationale against the actual diff to check if this vocabulary change is within scope or is being mislabeled.

### Diff context

Key changes:

**`packages/contracts/src/animation-state.ts`**: `ACTIVITY_STATES` rewritten from 15 old states to 19 v4 states. `RELIABLE_ACTIVITY_STATES` updated to include idle/standby/errored/waiting_for_input + 10 SoA gate states. `HEURISTIC_ACTIVITY_STATES` updated to implementing/testing/thinking/reading/cramming.

**`packages/contracts/src/state-json.ts`**: `STATE_JSON_SCHEMA_VERSION` bumped from 3 to 4. `work_mode` field removed from `stateJsonV1Schema`. The Zod schema still uses `.max(STATE_JSON_SCHEMA_VERSION)` for forward-compat refusal.

**`packages/contracts/src/soa-events.ts`**: `SOA_EVENT_TO_ACTIVITY_STATE` mapping updated from old states (hyped, focused, nervous, etc.) to v4 states (ticket_started, errored, poll_review, ticket_completed, review_clean, advance, adversarial_review).

**`packages/cli/src/hook-binary.ts`**: `SOA_GATE_TO_STATE` updated to v4 states. In `classifyEvent`: `"pushing"` → `"implementing"`, `"running-tests"` → `"testing"`, `"reviewing"` → `"thinking"` (for Bash explore bucket), Read ×3+ → `"thinking"`.

**`apps/menubar/Sources/ActivityState.swift`**: Enum replaced from 15 cases to 19 v4 cases. `init(from decoder:)` fallback unchanged (`?? .idle`).

**`apps/menubar/Sources/StateJsonReader.swift`**: `EXPECTED_STATE_SCHEMA_VERSION` changed from 3 to 4. No other logic changes.

**`apps/menubar/Sources/CodogotchiPet.swift`**: `rowMap` entirely rewritten with v4 gate states. `reviewClean`/`ticketCompleted`→row 0 (shared), `ticketStarted`→1, `greenTdd`→2 (placeholder), `redTdd`→3 (placeholder), `openPr`→4 (placeholder), `adversarialReview`→5, `recordReview`→8 (placeholder). Row count changed from 9 to 8 entries.

**`apps/menubar/Sources/CodexPet.swift`**: `rowMap` updated: `runningTests`→`.testing` (row 7), `waiting` removed, `thinking` added at row 8. `.implementing` and `.testing` now both map to row 7 (shared running-loop — visual compromise flagged in doc comment).

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
