You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P7.03 Terminal failure parity — StopFailure + Cursor error classification

Outcome:
- Claude Code `StopFailure` hook event (any `error` value) → `errored`.
- Cursor `stop` with `status: "error"` → `errored`.
- Cursor `postToolUseFailure` with `is_interrupt: false` (or absent) → `errored`.
- Cursor `postToolUseFailure` with `is_interrupt: true` → NOT `errored` (user interrupt).
- `CODOGOTCHI_EVENTS` includes `StopFailure`; installer writes it and detection checks for it.
- Normal `Stop` success path still yields `standby` (no regression).
- Codex: no `StopFailure` equivalent registered; gap documented in runbook.

### Files touched

Implementation:
packages/cli/src/hooks.ts
packages/cli/src/hook-binary.ts

Tests:
packages/cli/src/hooks.test.ts
packages/cli/src/hook-binary.test.ts

### Invariants to hold

1. `classifyEvent({ hook_event_name: "StopFailure", error: "<any>" })` must return `errored` regardless of what the `error` field contains (including absent/undefined).
2. `classifyEvent({ hook_event_name: "postToolUseFailure", is_interrupt: false })` → `errored`; `{ is_interrupt: true }` → NOT `errored`. The `is_interrupt` field is the sole discriminant.
3. After `installHooks`, the Claude `settings.json` must contain a `StopFailure` slot with the codogotchi-hook matcher; `claudeInstalled` must return false when `StopFailure` is absent from the settings.

### Attack surfaces to probe

1. **`StopFailure` always-errored — no `error` field required** — probe that `classifyEvent({ hook_event_name: "StopFailure" })` (no `error` field) still returns `errored`. The invariant says "any `error` value" which should include absent. Read `classifyEvent` line `if (rawEventName === "stopfailure")`.

2. **`postToolUseFailure` `is_interrupt` default** — probe the behavior when `is_interrupt` is absent (undefined). The check is `input.is_interrupt !== true`. `undefined !== true` → true → the condition fires → `errored`. Confirm this matches the spec ("with `!is_interrupt`").

3. **`rawHookKind` for `StopFailure`** — the `sourceEvent.kind` recorded for a `StopFailure` event. Read `rawHookKind`: it maps `"stopfailure"` → `session_end`. Check that the `sourceEventSchema` in `@codogotchi/contracts` accepts `session_end` as a valid kind, and that `normalize` does not return null for a `StopFailure` event.

4. **`rawHookOrigin` for `StopFailure`** — `StopFailure` is PascalCase, so `rawHookOrigin` returns `"claude_code"`. Probe that the `sourceEvent.origin` is `"claude_code"` for a `StopFailure` event.

5. **Cursor `stop` with `status:"success"` still produces `standby`** — the added `input.status === "error"` check must not affect the success path. Probe that `classifyEvent({ hook_event_name: "stop", status: "success" })` yields `standby`.

6. **`claudeInstalled` detection — CODOGOTCHI_EVENTS loop** — `claudeInstalled` uses `CODOGOTCHI_EVENTS.every(event => ...)` which now includes `"StopFailure"`. Probe that the loop accesses `hooks["StopFailure"]` (string key) not `hooks.StopFailure` (property access) — both are equivalent in TS but verify the implementation uses the correct key.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — no schema change in this ticket. `[N/A — vocabulary/hook-config change only]`

2. **CLI flag/arg symmetry** — `HookInput` gained three optional fields (`error`, `status`, `is_interrupt`). These are input fields not output; no CLI surface changes. `[N/A — input type extension only]`

3. **Error-class breadth in `catch` blocks** — no new catch blocks. `[N/A]`

4. **Defensive layering at module boundaries** — the new `StopFailure` check fires before `normalize(input)` is called on the result. Actually, `normalize` is called first at line 183, then the `rawEventName` checks follow. Read the order carefully: is there any path where a malformed `StopFailure` payload could produce a non-errored state?

5. **Cross-file atomicity windows** — `installHooks` writes `settings.json` atomically (per existing implementation). The `StopFailure` slot is written in the same loop as `PreToolUse` and `Stop`. `[N/A — no new atomicity concern]`

6. **Test-contract strength** — the new tests assert `StopFailure` → `errored`, Cursor `stop` `status:"error"` → `errored`, `postToolUseFailure` `is_interrupt:false` → `errored`, and `is_interrupt:true` → NOT `errored`. Probe whether reverting the `rawEventName === "stopfailure"` branch would cause the `StopFailure` test to fail. Also probe whether the hooks.test.ts `StopFailure` installer test would fail if `StopFailure` were removed from `CODOGOTCHI_EVENTS`.

7. **Doc-vs-code drift in ticket Rationale** — the Rationale says "`CODOGOTCHI_EVENTS` extended to `["PreToolUse", "Stop", "StopFailure"]`." Read `hooks.ts` to confirm the array matches. Also check that the comment above `CODOGOTCHI_EVENTS` was updated to mention `StopFailure`.

### Diff context

`hooks.ts`: `CODOGOTCHI_EVENTS = ["PreToolUse", "Stop", "StopFailure"]`. Comment updated to mention StopFailure fires "instead of Stop on API errors."

`hook-binary.ts` HookInput new fields: `error?: string`, `status?: string`, `is_interrupt?: boolean`.

`hook-binary.ts` `rawHookKind`: `"stopfailure"` mapped to `"session_end"`; `"posttoolusefailure"` mapped to `"tool_use"`.

`hook-binary.ts` `classifyEvent` new branches (before Stop branch):
```typescript
if (rawEventName === "stopfailure") {
  return { state: "errored", sourceEvent, readRun: 0 };
}
if (rawEventName === "posttoolusefailure" && input.is_interrupt !== true) {
  return { state: "errored", sourceEvent, readRun: 0 };
}
```

`hook-binary.ts` Stop branch extended: `input.status === "error"` added to the failure condition.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the behavior, but that behavior is nevertheless unsafe in production.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your correct output is a clean report.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.**
- **`Runner termination` must be the section heading**, then `runnerStatus:` and `terminatedReason:` as plain-text lines below.
- Inside `Advisory Observations`, write **one observation per bullet or one observation per paragraph**.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface: `[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
If none: "None."

**Advisory Observations**
If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence.
