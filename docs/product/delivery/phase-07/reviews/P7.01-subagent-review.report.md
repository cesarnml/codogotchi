**Invariant results**

**Invariant 1 — ACTIVITY_STATES count and TS ↔ Swift cross-match:** Held. Counted 19 entries in `ACTIVITY_STATES` (lines 4–26, `animation-state.ts`) and 19 cases in `ActivityState.swift` (lines 15–35). Set comparison: every TS literal maps to a Swift `rawValue` with identical snake_case string. No gaps, no extras.

**Invariant 2 — parseStateJson version range + work_mode strip:** Held. `schemaVersionField` uses `.min(1).max(STATE_JSON_SCHEMA_VERSION)` where `STATE_JSON_SCHEMA_VERSION = 4` (`state-json.ts` lines 9–13). The `stateJsonV1Schema` is a plain `z.object(...)` with no `.strict()` or `.passthrough()` call, so Zod's default strip mode applies: `work_mode` is silently discarded, not rejected. The `work_mode` test at line 109–119 of `state-json.test.ts` confirms this round-trip.

**Invariant 3 — Swift forward-compat boundary and removed-state fallback:** Held. `EXPECTED_STATE_SCHEMA_VERSION = 4` in `StateJsonReader.swift` line 8; the guard at line 63 returns `.schemaNewer(got: N, expected: 4)` for any `schemaVersion > 4`. `ActivityState.init(from decoder:)` falls back to `.idle` via `?? .idle` at line 39; raw values for all 12 removed states (e.g. `"running-tests"`, `"hyped"`) produce `nil` from `ActivityState(rawValue:)` because they have no matching case.

**Surface results**

**Surface 1 — ACTIVITY_STATES enum completeness cross-check (TS ↔ Swift):** Probed. Full set comparison performed manually. All 19 TS string literals appear as Swift `rawValue` strings. No mismatches found.

**Surface 2 — work_mode stripping behavior:** Probed. `stateJsonV1Schema` at line 42 of `state-json.ts` uses `z.object(...)` with no mode modifier. Zod strips unknown keys by default. `work_mode` does not appear as a declared field. Payloads carrying `work_mode` parse without error; the key is absent from the returned type. Correct behavior.

**Surface 3 — SOA_GATE_TO_STATE / SOA_EVENT_TO_ACTIVITY_STATE consistency:** Probed. Both maps contain the same 9 event-name keys with identical v4 target states. The hook-binary's `SOA_GATE_TO_STATE` (lines 49–59 of `hook-binary.ts`) and the contracts `SOA_EVENT_TO_ACTIVITY_STATE` (lines 45–56 of `soa-events.ts`) are fully congruent.

**Surface 4 — CodogotchiPet.rowMap key coverage:** Probed. The map has 8 entries: `.reviewClean` and `.ticketCompleted` both point to row 0 (valid shared row per spec). `.ticketStarted`→1, `.greenTdd`→2, `.redTdd`→3, `.openPr`→4, `.adversarialReview`→5, `.recordReview`→8. No old v3 state names (`.celebrating`, `.panicking`, `.hyped`, etc.) remain as keys. `.pollReview` and `.advance` are absent from the map (they render as idle/empty via the empty-array fallback), which is consistent with the ticket scope.

**Surface 5 — Forward-compat boundary exactness:** Probed. TS: `z.number().int().min(1).max(4)` — schema_version 4 passes, 5 throws. Swift: `schemaVersion > 4` guard — schema_version 4 passes, 5 returns `.schemaNewer(got: 5, expected: 4)`. Both edges exact.

**Surface 6 — Removed states decode to .idle in Swift:** Probed. `ActivityState` has no `rawValue` matching any of the 12 removed state strings. `ActivityState(rawValue: "running-tests")` returns nil. `init(from decoder:)` falls back `?? .idle`. Correct.

**Diff-derived surface 1 — Old v1/v2/v3 fixtures parse under new schema:** Probed. The fixture activity_state values (`idle`, `standby`, `errored`) are all present in v4 `ACTIVITY_STATES`. Their schema_versions (1, 2, 3) are all ≤4. Both the TS Zod schema and the Swift guard accept them. The backward-compat tests at lines 121–134 of `state-json.test.ts` confirm v1 and v2 round-trips.

**Diff-derived surface 2 — CLI flag/arg symmetry:** N/A — no CLI surface changes in this ticket.

**Diff-derived surface 3 — Error-class breadth in catch blocks:** N/A — no new catch blocks introduced.

**Diff-derived surface 4 — Unmapped SoA gate names fall through to idle:** Probed. In `classifyEvent`, when `origin === "soa" && kind === "gate"` and the name is absent from `SOA_GATE_TO_STATE`, the code returns `{ state: "idle", sourceEvent, readRun: prior.readRun }` (line 205). Correct fallback.

**Diff-derived surface 5 — Cross-file atomicity windows:** N/A — pure vocabulary change; no multi-step disk-artifact writes introduced.

**Diff-derived surface 6 — Test-contract strength:** Probed. Tests assert version is 4, each v4 state is present, each of 11 deleted states is absent, schema_version 5 is rejected, `work_mode` is stripped, old schema versions still parse. Reverting the Green changes (restoring old state names or bumping `STATE_JSON_SCHEMA_VERSION` back to 3) would re-fail multiple assertions. Tests are genuinely regression-catching.

**Diff-derived surface 7 — Doc-vs-code drift in the Rationale:** Probed. The Rationale says "no new classification logic — that is P7.02." The diff renames the emitted state strings (`running-tests`→`testing`, `reviewing`→`thinking`, `pushing`→`implementing`) in `classifyEvent` but does not alter which command patterns trigger which branch. The behavioral structure is unchanged; only the ActivityState value returned from each existing branch changed. This is vocabulary rename, not classifier logic change. No drift from the spec's stated boundary.

**Actionable findings**

None.

**Advisory Observations**

Four v4 SoA gate states — `red_tdd`, `green_tdd`, `open_pr`, and `record_review` — appear in `ACTIVITY_STATES`, `RELIABLE_ACTIVITY_STATES`, and `CodogotchiPet.rowMap` but are not mapped in either `SOA_EVENT_TO_ACTIVITY_STATE` or `SOA_GATE_TO_STATE`. No current hook event produces them. The placeholder comment in `CodogotchiPet.rowMap` and the absence of corresponding `SOA_EVENT_NAMES` entries suggest this is intentional (art and event wiring deferred to a later phase), but these states are classified as `RELIABLE` while being unreachable in production. If a future phase adds the gate event names and forgets to update both maps, the states will silently fall through to `idle`. This is not a bug in P7.01 scope, but it is a latent wiring risk worth noting in the P7.02 ticket context.

The `waiting_for_input` state is in `RELIABLE_ACTIVITY_STATES` but `classifyEvent` in `hook-binary.ts` has no branch that emits it. It appears to have no production pathway in the current TS hook. If Codex or Cursor hooks are expected to emit this state, that pathway is not visible in this file. Out of scope for P7.01, but worth verifying during P7.02 classifier work.

**Runner termination**
runnerStatus: completed
terminatedReason: All invariants and surfaces were probed from direct source reads; no early stops or tool denials occurred.
