You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P7.02 Hook-binary pure classifier — drop events.ndjson reader, full §7 read/explore buckets

Outcome:
- `packages/cli/src/hook-binary.ts` no longer reads `.soa/events.ndjson`: the `SOA_GATE_TO_STATE` map, `last_gate`/`soa_tail` counters, `parseSoaTail`, `pickLatestMappedSoaEvent`, and the `resolveSoaRoot`/`readSoaEventsSince` calls are removed.
- `packages/engine/src/sources/soa.ts` (the events.ndjson reader) and its test are deleted; no `mapSoaEventToActivityState`/`SOA_EVENT_TO_ACTIVITY_STATE` references remain in the hook path.
- The classifier emits the v4 hook vocabulary per research §6/§7:
  - `Edit`/`Write`/`MultiEdit` and Bash write/mutate commands → `implementing`
  - Bash test/lint/format runners → `testing`
  - Bash read/search (`grep`/`rg`/`find`/`ls`/`cat`/`tail`/`head`/`wc`/`awk`/`git log`/`git diff` + `jq`) → `thinking`
  - `Read` ×1–2 (streak) → `reading`; `Read` ×3+ → `cramming`
  - `Stop` success → `standby`; no-signal/TTL floor → `idle`
- No deleted state name is ever emitted.

Rationale notes:
- `CRAMMING_THRESHOLD = 3`: Read ×1 → reading, Read ×2 → reading, Read ×3+ → cramming.
- `THINKING_BASH_PREFIXES`: grep, find, rg, ls, cat, head, tail, wc, awk, jq, "git log", "git diff". `sed` removed from the list (not in §7 spec, `sed -i` is a write command).
- All other Bash/Shell commands fall through to `implementing` (not `idle`).
- `RunHookOptions` stripped of `env`/`cwd` (only needed for SoA root resolution).
- `Counters` reduced to `{ read_run: number }` (removed `soa_tail` and `last_gate`).
- Engine `soa.ts` deleted; `contracts/soa-events.ts` retained but no longer imported by hook.

### Files touched

Implementation:
packages/cli/src/hook-binary.ts
packages/engine/src/index.ts
packages/engine/src/sources/soa.ts (deleted)

Tests:
packages/cli/src/hook-binary.test.ts
packages/engine/src/sources/soa.test.ts (deleted)

### Invariants to hold

1. `classifyEvent` must never emit a deleted v3 state (`running-tests`, `reviewing`, `pushing`, `hyped`, `celebrating`, `calling_for_backup`, `waiting`, `focused`, `nervous`, `panicking`, `ascended`) — the classifier returns only states in the v4 `ACTIVITY_STATES` enum.
2. `Read ×1` and `Read ×2` must emit `reading`; `Read ×3+` must emit `cramming`. The `readRun` counter must increment on each Read and reset to 0 on any write-tool (`Edit`/`Write`/`MultiEdit`) invocation.
3. `runHook` must never consult `resolveSoaRoot`, `readSoaEventsSince`, or any `.soa/events.ndjson` path — the resulting `state.json` must reflect only the direct tool-use event, regardless of whether a `.soa` directory exists.

### Attack surfaces to probe

1. **`CRAMMING_THRESHOLD` boundary** — probe that the boundary is at ×3 not ×2 or ×4: `readRun=2` input produces `reading`, `readRun=2+1=3` produces `cramming`. Read the `classifyEvent` Read branch: `nextRun >= CRAMMING_THRESHOLD ? "cramming" : "reading"` where `CRAMMING_THRESHOLD = 3`.

2. **`readRun` reset completeness** — which events reset `readRun` to 0? Edit/Write/MultiEdit reset it. But what about Bash/Shell (thinking or implementing bucket), Stop events, unknown events, and SoA gate events? If a Bash thinking command does NOT reset readRun, a mixed Edit+Bash+Read sequence could accumulate incorrectly.

3. **`THINKING_BASH_PREFIXES` word-boundary check for multi-word prefixes** — `"git log"` and `"git diff"` are multi-word prefixes. The `matchesThinkingCommand` function checks `startsWith(prefix)` followed by a boundary character check. Probe that `"git log --oneline"` matches (boundary = space after "git log") and `"git logfile.txt"` does NOT match.

4. **`git push` falls through correctly** — `git push` previously had an explicit `startsWith("git push")` guard. After removal, probe that `git push origin main` produces `implementing` via the catch-all (since it does not match any test-runner prefix and does not match any thinking prefix).

5. **`sed` removal** — `sed` was in the old thinking bucket. After removal, any `sed` command (including read-only `sed`) must produce `implementing`. Probe `sed 's/foo/bar/'` (no `-i`) also falls to implementing.

6. **Engine barrel re-export** — `packages/engine/src/index.ts` now lacks `export * from "./sources/soa"`. Any package that imported `SoaTailState`, `readSoaEventsSince`, or `resolveSoaRoot` from `@codogotchi/engine` would fail to compile. Probe that no other file in the monorepo imports these from the engine.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — the `counters` file format changed: `soa_tail` and `last_gate` fields are no longer written. An existing `.hook-counters.json` with those fields will be silently ignored on next read (parsed as `Partial<Counters>`, only `read_run` is extracted). Probe whether old counters files cause silent state corruption.

2. **CLI flag/arg symmetry** — `RunHookOptions.env` and `RunHookOptions.cwd` removed. Any external caller that passes these fields will get a TypeScript compile error. Probe the CLI entrypoint (`packages/cli/src/main.ts` or equivalent) to confirm it does not pass these fields.

3. **Error-class breadth in `catch` blocks** — `readCounters` has a `catch` that returns `{ read_run: 0 }` for ENOENT and all other errors. The old implementation also silently recovered. No regression here, but probe whether non-ENOENT errors (corrupted JSON, permissions) are still handled identically to the old behavior.

4. **Defensive layering at module boundaries** — `writeCounters` writes only `{ read_run: N }`. Old counters with extra fields are silently dropped on next write. Probe whether this could cause any cross-process issue (e.g., if another tool reads `soa_tail` from counters after P7.02).

5. **Cross-file atomicity windows** — `writeCounters` uses atomic rename via a temp file. `writeStateAtomic` also uses atomic rename. These run sequentially inside `withHomeLock`. Probe whether the lock is still correctly held across both writes.

6. **Test-contract strength** — probe whether the new `P7.02 §7 pure classifier` tests would re-fail if any of the Green changes were reverted: specifically, if `CRAMMING_THRESHOLD` went back to 3 but Read ×1 was mapped to `idle` again.

7. **Doc-vs-code drift in the ticket Rationale** — the Rationale claims "`sed` removed (not in §7 spec)." The §7 spec in research notes says the classifier list is "TBD" with a `…` after the listed commands. Read the ticket spec and rationale to verify whether removing `sed` is within the ticket's stated contract or a scope drift.

### Diff context

Key changes in `hook-binary.ts`:

1. **Imports**: removed `mapSoaEventToActivityState`, `SoaEventLine`, `SoaTailState`, `readSoaEventsSince`, `resolveSoaRoot`. Only `@codogotchi/contracts` remains; `@codogotchi/engine` import entirely removed.

2. **`SOA_GATE_TO_STATE` / `GATE_STATES`**: deleted.

3. **`REVIEWING_BASH_PREFIXES` → `THINKING_BASH_PREFIXES`**: `sed` removed; `"git log"` and `"git diff"` added.

4. **`classifyEvent` Read branch**: `nextRun >= CRAMMING_THRESHOLD ? "cramming" : "reading"` where `CRAMMING_THRESHOLD = 3`. Previously: `nextRun >= READ_RUN_THRESHOLD ? "thinking" : "idle"` where `READ_RUN_THRESHOLD = 3`.

5. **`classifyEvent` Bash branch**: removed the explicit `startsWith("git push")` → implementing guard. `matchesReviewingCommand` renamed to `matchesThinkingCommand`. Unknown Bash → `implementing` (unchanged).

6. **`classifyEvent` SoA gate branch**: removed entirely. SoA gate events (`origin === "soa" && kind === "gate"`) now fall through to `return { state: "idle", ... }`.

7. **`Counters` type**: reduced from `{ read_run, soa_tail, last_gate }` to `{ read_run }`.

8. **`readCounters` / `writeCounters`**: no longer parse/write `soa_tail` or `last_gate`.

9. **`runHook`**: removed `resolveSoaRoot`/`readSoaEventsSince` calls, removed sticky gate logic, removed `env`/`cwd` from `RunHookOptions`.

Key changes in `engine/index.ts`: removed `export * from "./sources/soa"`.

Deleted files: `packages/engine/src/sources/soa.ts`, `packages/engine/src/sources/soa.test.ts`.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
