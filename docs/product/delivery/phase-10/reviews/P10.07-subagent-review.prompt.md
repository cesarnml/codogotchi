You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome (P10.07 Swift — floating HUD render + flashes):**
- The floating pet renders the HUD: 3 hearts (from `half_hearts`, including a dimmed/half
  state), an XP ring whose fill = `level_fraction` with the level number inside.
- HUD is hidden by default, shown on hover, auto-hides on leave (reuses existing
  floating-pet interaction).
- Heart flash on every ±½-heart change: injury flash on decrease, heal/potion flash on
  increase.
- Ring flash on level-up; reusable sparkle/confetti burst at milestones (10/25/50/75/100),
  persisting to next hover if unseen.
- HUD is fully hidden when the `rpg_hud_enabled` flag is `false` (opt-out), regardless of
  hover.
- No bespoke per-event character animations — only the reusable flash/particle effects.

**Rationale key points:**
- Delta-driven flashes: `previousHalfHearts`/`previousLevel` nil-guard prevents first-render
  flash. `onFlash` is never called when there is no prior state.
- `rpg_hud_enabled` defaults `true` when key absent in config.json.
- `Outcome.==` custom implementation added to `LivePollingDriver.Outcome` because tuple
  fields can't use synthesized `Equatable`.
- Deferred: unseen-burst persistence across sessions, hover auto-hide timer, bespoke
  character animations.

### Files touched

Implementation:
- apps/menubar/Sources/RPGHUDViewModel.swift (new — view-model logic)
- apps/menubar/Sources/RPGHUDPanel.swift (new — AppKit HUD panel with hearts + ring)
- apps/menubar/Sources/FloatingPetPanel.swift (modified — hover callback + HUD wiring)
- apps/menubar/Sources/FloatingPetController.swift (modified — protocol + forwarding)
- apps/menubar/Sources/LivePollingDriver.swift (modified — RPG state emission)
- apps/menubar/Sources/PetConfig.swift (modified — resolvedRPGHUDEnabled())
- apps/menubar/Sources/MenubarApp.swift (modified — wire applyRPGState)

Tests:
- apps/menubar/Tests/MenubarTests/RPGHUDViewModelTests.swift (new — 23 tests)

### Invariants to hold

1. `RPGHUDViewModel.hearts(from: n)` must always return exactly 3 `HeartState` elements for
   any `n` in 0...6; and `onFlash` must NOT be called on the first `update()` call
   (when `previousHalfHearts` and `previousLevel` are both nil).
2. When `hudEnabled = false` is passed to `RPGHUDViewModel.update()`, `isHUDEnabled` must
   be `false` after the call regardless of other field values; `repositionAndShowHUD` must
   then call `rpgHUDPanel?.orderOut(nil)` and not show the panel.
3. Flash events must only fire on state delta: `heartInjured` only when
   `halfHearts < previousHalfHearts`, `heartHealed` only when
   `halfHearts > previousHalfHearts`, `levelUp` only when `level > previousLevel`,
   `milestoneBurst` only when `level > previousLevel AND level ∈ RPG_MILESTONE_LEVELS`.

### Attack surfaces to probe

1. `RPGHUDViewModel.update()` first-call guard — could `onFlash?` be called before
   `previousHalfHearts`/`previousLevel` are set? The guard executes `guard prevHalfHearts != nil, prevLevel != nil else { return }` after the assignments — but the assignments happen unconditionally before the guard. Verify there is no path where the guard fires `true` when it should fire `false`.
2. `FloatingPetInteractionView.handlePointerEvent` hover state edge — does `onHoverChange?(false)` fire on every path where `pointerInsideFrame` transitions `true → false`? Specifically: `mouseExited` sets `pointerInsideFrame = false` then calls `handlePointerEvent`; inside `handlePointerEvent` the `wasInBounds/inBounds` delta check should fire. But `mouseExited` also sets `pointerInsideFrame = false` BEFORE calling `handlePointerEvent`, so `wasInBounds` reads the already-updated value. Check if the delta fires correctly.
3. `repositionAndShowHUD()` opt-out path — when `rpgHUDViewModel.isHUDEnabled` is `false` after an `applyRPGState(hudEnabled: false)` call with the panel previously visible, does `orderOut(nil)` definitely get called on the existing `rpgHUDPanel`? The guard is `guard isPanelShown, isHoveringPet, rpgHUDViewModel.isHUDEnabled else { rpgHUDPanel?.orderOut(nil); return }`. Verify all three conditions gating the hide path.
4. `LivePollingDriver.emit()` RPG state cache suppression — does the `lastRPGState` cache correctly suppress repeat ticks with identical values? The cache uses `!=` on `halfHearts`, `levelFraction`, and `level` individually — but `levelFraction` is a `Double` and floating-point equality could cause spurious suppression or spurious emission on identical computed values. Probe the edge.
5. `LivePollingDriver.Outcome` custom `==` completeness — the custom `==` compares
   `rpgState?.halfHearts`, `rpgState?.levelFraction`, and `rpgState?.level` separately. But
   `Outcome` is `private struct`; does the custom `==` break any test that previously relied
   on synthesized `Equatable` for the struct (e.g. `LivePollingTests`)?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `LivePollingDriver.Outcome` is a
   private struct not persisted to disk or passed over any protocol boundary. No JSON
   artifacts changed. `StateSnapshot` fields `halfHearts`, `levelFraction`, `level` already
   existed from P10.06. Probe whether the new `rpgState` field in `Outcome` breaks the
   `Equatable` contract for existing test fixtures.
2. **CLI flag/arg symmetry** — no new CLI flags added. N/A for Swift AppKit change.
3. **Error-class breadth in catch blocks** — `PetConfig.resolvedRPGHUDEnabled()` uses `guard
   ... else { return true }` (no throws). No new error handling paths. Probe whether config
   read failures are truly silent (they should be — falls back to `true`).
4. **Defensive layering at module boundaries** — `LivePollingDriver.applyRPGState` callback
   receives `(Int, Double, Int)` without validation. `RPGHUDViewModel.update()` accepts
   unclamped `halfHearts` and `level` values. Probe whether out-of-range inputs
   (`halfHearts: 99`, `level: 0`) cause crashes or silent mis-render vs. graceful fallback.
5. **Cross-file atomicity windows** — no multi-step disk writes introduced. `isHoveringPet`
   and `rpgHUDViewModel.isHUDEnabled` are read in `repositionAndShowHUD` on the main actor.
   Probe whether there is a race between `applyRPGState` (sets `isHUDEnabled`) and `onHoverChange` (reads it).
6. **Test-contract strength** — 23 tests in `RPGHUDViewModelTests`. Do the tests cover the
   edge case where `halfHearts` is out of range (e.g., 7 or -1)? Do they cover the case
   where `level` decreases (should not fire levelUp)? Probe assertion coverage for boundary
   inputs and the negative-delta level case.
7. **Doc-vs-code drift in the ticket Rationale** — the Rationale says "milestone burst
   persists to next hover if unseen" is deferred. The diff does not implement persistence.
   Does the Rationale accurately describe what was deferred vs. what was implemented?
   Also probe whether "hover/auto-hide on leave" is correctly described — the implementation
   uses `onHoverChange` callback, not an auto-hide timer.

### Diff context

Key logic changes:

**RPGHUDViewModel.update()** (new file):
- Saves `previousHalfHearts` and `previousLevel` from fields before updating
- Updates all fields unconditionally (`hearts`, `ringFraction`, `level`, `isHUDEnabled`)
- Sets `previousHalfHearts = halfHearts`, `previousLevel = level`
- Guard: `guard prevHalfHearts != nil, prevLevel != nil else { return }`
- Then fires `onFlash?(.heartInjured)` / `onFlash?(.heartHealed)` / `onFlash?(.levelUp)` / `onFlash?(.milestoneBurst)` based on deltas

**FloatingPetInteractionView.handlePointerEvent** (modified):
- Added `let wasInBounds = pointerInsideFrame` before `pointerInsideFrame = inBounds`
- Added `if inBounds != wasInBounds { onHoverChange?(inBounds) }`
- BUT: `mouseExited` sets `pointerInsideFrame = false` before calling `handlePointerEvent`

**LivePollingDriver.Outcome** (modified):
- Added `rpgState: (halfHearts: Int, levelFraction: Double, level: Int)?` field
- Added custom `static func ==` because tuples cannot use synthesized Equatable
- Custom `==` compares each rpgState subfield individually with `?.halfHearts == ?.halfHearts` etc.

**repositionAndShowHUD()** (new method):
- Guard: `guard isPanelShown, isHoveringPet, rpgHUDViewModel.isHUDEnabled else { rpgHUDPanel?.orderOut(nil); return }`
- Creates panel lazily on first show
- Calls `hud.reposition(...)` and `hud.orderFrontRegardless()`

**PetConfig.resolvedRPGHUDEnabled()** (new method):
- Reads `features.rpg_hud_enabled` from config.json
- Returns `true` on any failure (missing file, wrong type, absent key)

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
