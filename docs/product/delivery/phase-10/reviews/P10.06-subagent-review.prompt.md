You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**P10.06 Swift — consume v5 + local decay timer**

Outcome:
- `StateJsonReader`/`AppState` decode the v5 fields (`level`, `level_fraction`, `half_hearts`, `last_activity_at`) and tolerate their absence (fallback to safe defaults) for resilience.
- A local decay engine recomputes displayed `half_hearts` between writes: `displayed = max(0, written_half_hearts − floor((now − last_activity_at) / 8h))`, using the shared decay constant from contracts.
- The writer is authoritative on **heal**; Swift only ever **decays** below the written value (never invents heals).
- Sleep/wake handled: decay reflects true elapsed wall-clock on resume, floored at 0.
- Fresh/`null` `last_activity_at` ⇒ no decay (stays at written value).

Rationale notes:
- `StatePayload` uses `String??` to distinguish absent key (v4) from JSON null (v5 explicit nil).
- `HALF_HEART_DECAY_SECONDS = 8 * 3600` and `MAX_HALF_HEARTS = 6` match contracts/decay-constants.ts exactly.
- Timer wiring into HUD render deferred to P10.07.
- `EXPECTED_STATE_SCHEMA_VERSION` bumped 4 → 5; forward-compat: any version ≤5 accepted.

### Files touched

Implementation:
- `apps/menubar/Sources/ActivityState.swift` — added 4 v5 fields to `StateSnapshot` with safe defaults
- `apps/menubar/Sources/StateJsonReader.swift` — bumped schema version to 5; extended `StatePayload` with optional v5 fields; passes through with fallbacks
- `apps/menubar/Sources/HalfHeartDecayEngine.swift` (new) — `HALF_HEART_DECAY_SECONDS`, `MAX_HALF_HEARTS`, `HalfHeartDecayEngine.displayed(written:lastActivityAt:now:)`

Tests:
- `apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift` — added v5 decode/schema tests; updated hardcoded v4 assertions
- `apps/menubar/Tests/MenubarTests/LivePollingTests.swift` — updated hardcoded "v4" tooltip string to "v5"
- `apps/menubar/Tests/MenubarTests/HalfHeartDecayTests.swift` (new) — 8 decay math tests

### Invariants to hold

1. `StateJsonReader.read(at:)` must accept any payload with `schema_version ≤ 5` and must reject `schema_version > 5` with `.schemaNewer(got:expected:)`.
2. For a v4 or earlier payload missing v5 fields, `snapshot.level == 1`, `snapshot.halfHearts == 6`, `snapshot.levelFraction == 0.0`, `snapshot.lastActivityAt == nil` (safe defaults, never crashes).
3. `HalfHeartDecayEngine.displayed(written:lastActivityAt:now:)` must return `max(0, written − floor(elapsed / 8h))` when `lastActivityAt` is non-nil, and must return `written` unchanged when `lastActivityAt` is nil.

### Attack surfaces to probe

1. `StatePayload.lastActivityAt: String??` — double-optional decode path: does a JSON `null` correctly decode as `.some(.none)` vs an absent key decoding as `.none`? Probe that the `?? nil` flattening in `StateJsonReader.read` maps both absent and explicit-null to `nil` in `StateSnapshot`.
2. `halfHearts` default fallback — `payload.halfHearts ?? MAX_HALF_HEARTS`: if a v5 payload is sent without `half_hearts` (which the contracts superRefine requires but Swift doesn't enforce), does the fallback silently produce 6 when 0 might be the correct value? Probe the fallback path against a partial v5 payload.
3. `HalfHeartDecayEngine.displayed` with negative elapsed — `now.timeIntervalSince(last)` can be negative if system clock is adjusted backward or the writer uses future timestamps. The `guard elapsed > 0` guard handles zero but not negative. Probe the negative-elapsed path.
4. `EXPECTED_STATE_SCHEMA_VERSION = 5` consistency — probe that the schema-newer check (`schemaVersion > EXPECTED_STATE_SCHEMA_VERSION`) still rejects v6+, and that the `StateJsonReader` no longer rejects a valid v5 payload.
5. `StateSnapshot.Equatable` conformance — `level`, `levelFraction`, `halfHearts`, `lastActivityAt` are new fields on an `Equatable` struct. Probe that tests comparing `StateSnapshot` values still compile and that the Equatable synthesis includes the new fields.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `EXPECTED_STATE_SCHEMA_VERSION` bumped 4 → 5; v5 payloads that were previously rejected are now accepted. Probe: do any fixtures hardcode `schema_version: 5` expecting rejection that now silently succeed?
2. **CLI flag/arg symmetry** — N/A; this diff is Swift-only with no CLI changes.
3. **Error-class breadth in `catch` blocks** — `StateJsonReader.read` catches all errors from `JSONDecoder` and maps to `.malformed`; the `String??` double-optional path could produce unexpected decode behavior. Probe whether a malformed `last_activity_at` (non-null, non-string) silently becomes `nil` vs correctly triggering `.malformed`.
4. **Defensive layering at module boundaries** — `HalfHeartDecayEngine.displayed` takes unchecked `written: Int`; negative `written` would produce valid-looking output. Probe whether the engine clamps or passes through negative inputs.
5. **Cross-file atomicity windows** — N/A; this diff is pure read logic with no write paths.
6. **Test-contract strength** — new tests cover the happy paths and edge cases. Probe whether the `testOlderPayloadRPGFieldsFallBackToSafeDefaults` test correctly validates all four field defaults and not just some of them.
7. **Doc-vs-code drift in the ticket Rationale** — the Rationale says "Timer-driven recompute wiring into HUD render path deferred to P10.07." Probe that the diff does not accidentally wire a timer without the P10.07 HUD render path, and that the engine is correctly positioned as a pure callable.

### Diff context

Key changes:
1. `ActivityState.swift`: `StateSnapshot` gains four new stored properties (`level: Int = 1`, `levelFraction: Double = 0.0`, `halfHearts: Int = 6`, `lastActivityAt: String? = nil`). All existing call sites use default parameters; no breaking change.
2. `StateJsonReader.swift`: `EXPECTED_STATE_SCHEMA_VERSION` 4 → 5. `StatePayload` adds `level: Int?`, `levelFraction: Double?`, `halfHearts: Int?`, `lastActivityAt: String??`. Reader passes through with `?? defaults`; double-optional `lastActivityAt` is flattened via `?? nil`.
3. `HalfHeartDecayEngine.swift` (new): two module-level constants (`HALF_HEART_DECAY_SECONDS: Double = 8 * 3600`, `MAX_HALF_HEARTS: Int = 6`) and a pure `enum HalfHeartDecayEngine` with one static method.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
