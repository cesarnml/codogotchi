I have everything I need. Here is the adversarial review report:

---

**Invariant results**

**Invariant 1** — `[held]` — `EXPECTED_STATE_SCHEMA_VERSION = 5`; the guard at `StateJsonReader.swift:63` is `schemaVersion > 5`, so v5 passes and v6 fails with `.schemaNewer(got:expected:)`. Verified against `testSchemaVersion6FailsWithSchemaNewer` (got: 6, expected: 5) and the production schema-newer fixture (version 99).

**Invariant 2** — `[held]` — For a v4 payload missing all four RPG fields, `payload.level` is `nil` → `?? 1`; `payload.halfHearts` is `nil` → `?? MAX_HALF_HEARTS` (6); `payload.levelFraction` is `nil` → `?? 0.0`; `payload.lastActivityAt` is `Optional<Optional<String>>.none` → `?? nil` → `nil`. All four defaults are independently asserted in `testOlderPayloadRPGFieldsFallBackToSafeDefaults`.

**Invariant 3** — `[held]` — For non-nil `lastActivityAt` with `elapsed > 0`, `HalfHeartDecayEngine.displayed` returns `max(0, written - Int(floor(elapsed / HALF_HEART_DECAY_SECONDS)))`. For nil `lastActivityAt`, the guard at line 19 returns `written` unchanged. Both paths verified across all eight tests in `HalfHeartDecayTests.swift`.

---

**Surface results**

**Surface 1 — `String??` double-optional decode path** — `[probed — clean]` — `String??` = `Optional<Optional<String>>`. Swift's `JSONDecoder` synthesized decoder uses `decodeIfPresent` for the outer Optional, correctly producing `.none` for an absent key and `.some(.none)` for JSON `null`. The expression `payload.lastActivityAt ?? nil` is the `??` operator with `T = String?`: it unwraps the outer optional, mapping both absent (`.none`) and JSON-null (`.some(.none)`) to `String?.none` = nil, and a string value to the string. Both cases correctly propagate nil into `StateSnapshot.lastActivityAt`. `testV5PayloadNullLastActivityAtDecodesAsNil` confirms the explicit-null path.

**Surface 2 — `halfHearts` fallback on partial v5 payload** — `[probed — clean]` — A v5 payload with `half_hearts` absent decodes `payload.halfHearts` as `Int?.none` → `?? MAX_HALF_HEARTS` = 6. The contracts' TypeScript `superRefine` makes this case unreachable in production, and the ticket spec designates 6 as the safe default for missing data. A payload with `half_hearts: 0` explicit decodes as `Int?.some(0)` → `0 ?? 6` = 0, correctly distinguished. No finding under the stated spec.

**Surface 3 — negative elapsed time** — `[probed — clean]` — The guard at `HalfHeartDecayEngine.swift:21` is `guard elapsed > 0 else { return written }`. For negative elapsed (clock skew or future timestamp), `elapsed > 0` is false, so the else-branch executes and returns `written` unchanged — identical behavior to zero elapsed. The attack surface description noted that the guard "handles zero but not negative," but the implementation actually handles both correctly through the same guard path. No decay is applied in either case.

**Surface 4 — `EXPECTED_STATE_SCHEMA_VERSION = 5` consistency** — `[probed — clean]` — `testSchemaVersion6FailsWithSchemaNewer` confirms v6 is rejected with `schemaNewer(got: 6, expected: 5)`. `testSchemaVersion5ParsesSuccessfullyAfterV5Bump` confirms v5 is accepted. `testSchemaNewerRendersIdleDesaturatedWithInterpolatedSchemaNewerTooltip` in `LivePollingTests` confirms the tooltip string now reads "this app supports v5."

**Surface 5 — `StateSnapshot.Equatable` synthesis with new fields** — `[probed — clean]` — `StateSnapshot` is a struct; Swift synthesizes `==` over all stored properties including the four new ones (`level: Int`, `levelFraction: Double`, `halfHearts: Int`, `lastActivityAt: String?`). All four types are `Equatable`. `testRepeatedTicksWithUnchangedStateDoNotEmitDuplicateRenders` indirectly exercises the synthesized equality (the driver suppresses duplicate renders by comparing snapshots). The new fields are included in that comparison. `Double` equality on `levelFraction` has the usual NaN caveat but JSON decoding cannot produce NaN from a valid number literal.

**Diff-derived Surface 1 — Fixture stability across schema-version drift** — `[probed — clean]` — The only fixture with a numeric schema version expected to fail is `schema-newer.json` (version 99). No fixture uses `schema_version: 5` expecting rejection. All old v4 and lower fixtures continue to parse under the widened `≤ 5` gate.

**Diff-derived Surface 2 — CLI flag/arg symmetry** — `[N/A — diff is Swift-only with no CLI changes]`

**Diff-derived Surface 3 — Error-class breadth for malformed `last_activity_at`** — `[probed — clean]` — A JSON payload with `"last_activity_at": 123` (integer instead of string or null) causes `JSONDecoder` to throw `DecodingError.typeMismatch` when decoding `String??` for a non-null, non-string value. This error is caught by the catch-all at `StateJsonReader.swift:99` and mapped to `.failure(.malformed)`. It does not silently produce nil.

**Diff-derived Surface 4 — Negative `written` input** — `[probed — clean under spec]` — `max(0, written - decayed)` with `written = -N` (N > 0) gives 0 for any non-negative `decayed`. The engine is spec-compliant: it returns `max(0, ...)` as stated. Negative `written` is not a reachable value from the reader's fallback chain (minimum is 0 from an explicit payload value; the default is 6).

**Diff-derived Surface 5 — Cross-file atomicity windows** — `[N/A — pure read logic with no write paths]`

**Diff-derived Surface 6 — Test-contract strength for `testOlderPayloadRPGFieldsFallBackToSafeDefaults`** — `[probed — clean]` — The test at `StateJsonReaderTests.swift:417–435` asserts all four field defaults independently: `level == 1`, `halfHearts == 6`, `levelFraction == 0.0` (with accuracy), `lastActivityAt == nil`. No field is skipped.

**Diff-derived Surface 7 — Doc-vs-code drift: timer wiring** — `[probed — clean]` — `HalfHeartDecayEngine` has no `Timer`, no `DispatchQueue`, and no `@Published` properties. No caller in `Sources/` invokes `HalfHeartDecayEngine.displayed` at runtime; the only references are the `HalfHeartDecayTests.swift` test file and a comment in `StateJsonReader.swift`. The engine is correctly positioned as a pure callable with no HUD render wiring.

---

**Actionable findings**

None.

---

**Advisory Observations**

**A1 — Stale test name `testExpectedSchemaVersionIs4`** (`StateJsonReaderTests.swift:110`): The test body asserts `EXPECTED_STATE_SCHEMA_VERSION == 5`, but the function name says "Is4." This is a copy-paste artifact from the v4 bump and will appear as "Is4" in CI output while asserting 5. There are now three separate functions asserting the same constant (`testExpectedSchemaVersionIs4`, `testExpectedSchemaVersionIsV4` at line 294, and `testExpectedSchemaVersionIsV5` at line 355). Not a correctness bug, but the stale names add confusion.

**A2 — Contract unit-name mismatch between TypeScript and Swift** (`packages/contracts/src/decay-constants.ts` vs. `apps/menubar/Sources/HalfHeartDecayEngine.swift`): The TypeScript contract exports `HALF_HEART_DECAY_HOURS = 8`; the Swift constant is `HALF_HEART_DECAY_SECONDS = 8 * 3600`. The values are consistent but the unit conversion is implicit. The ticket Rationale says the constants "match contracts/decay-constants.ts exactly," which is true in value but not in name or unit. If the TS constant were changed to a non-multiple of 3600, the Swift constant would drift silently. An inline comment referencing the TS source would surface this dependency.

**A3 — `payload.lastActivityAt ?? nil` is a necessary type coercion, not obvious guard** (`StateJsonReader.swift:83`): The `?? nil` is required to coerce `String??` → `String?` for the `StateSnapshot` initializer; without it the code would not compile. A future refactor that makes the initializer parameter `String??` directly could silently remove the distinction between absent key and explicit null. A one-line comment explaining that `?? nil` is a type coercion from double-optional would prevent inadvertent removal.

---

**Runner termination**

runnerStatus: completed
terminatedReason: All invariants probed and held; all attack surfaces (ticket-spec-derived and seven diff-derived) fully covered with no actionable findings.
