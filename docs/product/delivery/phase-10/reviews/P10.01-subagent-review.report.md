I have all the information I need. Here is the adversarial review.

---

**Invariant results**

**Invariant 1** — held. `levelForXp(0)`: `safeXp(0)` returns 0 (since `0 > 0` is false); the `xp >= LEVEL_T` guard is false; the binary search starts with lo=0, hi=99. `LEVEL_THRESHOLDS[50]` ≈ 17.4e9 > 0, so hi collapses toward 0; when mid=1, `LEVEL_THRESHOLDS[1]` ≈ 697,000 > 0, so hi=0; loop ends, lo=0, returns 1. `levelForXp(LEVEL_T)`: `safeXp(LEVEL_T) = LEVEL_T`; the guard `xp >= LEVEL_T` fires, returns `LEVEL_COUNT = 100`. XP > LEVEL_T: same guard. Negative and NaN: `Number.isFinite` or `n > 0` clamp both to 0, return 1.

**Invariant 2** — held. `LEVEL_THRESHOLDS[0]` hardcoded to 0. `LEVEL_THRESHOLDS[99]` hardcoded to `LEVEL_T`. Strict monotonicity: minimum increment is at i=1: `Math.round(68e9 × (1/99)^2.5) ≈ 697,272` — well above 1; no rounding collision is possible anywhere in the 100-entry range (see Surface 5 below).

**Invariant 3** — held. For interior levels (< 100): binary search guarantees `LEVEL_THRESHOLDS[level-1] <= xp < LEVEL_THRESHOLDS[level]`, so `into = xp - LEVEL_THRESHOLDS[level-1] >= 0` and `into < span`; `fraction = into/span` in [0,1). For level 100: `fraction` is hardcoded to 1. `span = LEVEL_THRESHOLDS[99] - LEVEL_THRESHOLDS[98] > 0` because the threshold array is strictly monotone.

---

**Surface results**

**Surface 1 — `levelForXp` binary-search boundary at index 0** — probed. Traced the loop step-by-step: `LEVEL_THRESHOLDS[1] ≈ 697,272`, which is > 0, so on the final iteration hi collapses to 0. Loop exits with lo=0, returns `0 + 1 = 1`. No off-by-one.

**Surface 2 — `safeXp` with `0`, `-0`, `Infinity`, very large values** — probed. `-0`: `Number.isFinite(-0)` is true but `(-0) > 0` is false → returns 0 → level 1. `Infinity`: `Number.isFinite(Infinity)` is false → returns 0 → level 1. `-Infinity`: same path. `Number.MAX_SAFE_INTEGER` (≈ 9e15 >> LEVEL_T): passes `xp >= LEVEL_T` guard → returns 100. The binary-search `mid` computation uses `(lo + hi + 1) >> 1` on values capped at 99; no integer overflow risk.

**Surface 3 — `levelProgress` `into` when `xp === LEVEL_T`** — probed. `lo = LEVEL_THRESHOLDS[99] = LEVEL_T`; `xp - lo = 0`; the ternary `xp - lo > 0 ? xp - lo : 0` correctly yields 0. `into` cannot be negative here because `safeXp` enforces non-negative input and `xp >= LEVEL_T = lo` by construction in the level-100 branch.

**Surface 4 — `levelProgress` `span` at level 100** — probed. `lo = LEVEL_THRESHOLDS[99] = LEVEL_T`; `prev = LEVEL_THRESHOLDS[98] = Math.round(LEVEL_T × (98/99)^2.5)`. Since `(98/99)^2.5 < 1`, `prev < LEVEL_T`, so `span = LEVEL_T - prev > 0`. Consumers reading `span` will not get zero or a negative value.

**Surface 5 — Rounding collision in `LEVEL_THRESHOLDS`** — probed analytically. The minimum gap between consecutive entries occurs near the bottom of the curve. At i=1: `68e9 × (1/99)^2.5 ≈ 697,272`. At i=2: `≈ 3,944,000`. The derivative of `C(L) = T × ((L-1)/99)^2.5` at L=2 is `T × 2.5 × (1)^1.5 / 99^2.5 ≈ 1,743,000`; even the smallest inter-entry gap far exceeds 1. `Math.round` cannot produce a collision. The test that sweeps all 100 entries with `toBeGreaterThan` confirms this at runtime.

**Surface 6 — `export *` name collision with existing exports** — probed. `level.ts` exports: `LEVEL_T`, `LEVEL_EXPONENT`, `LEVEL_COUNT`, `LEVEL_THRESHOLDS`, `LevelProgress`, `levelForXp`, `levelProgress`. Checked all other modules in `index.ts` — `xp.ts`, `health.ts`, `loot.ts`, `sources/github.ts`, `sources/jsonl-parser.ts`, `sources/wakatime.ts`. None exports any of those names. No collision.

**Diff-derived Surface 1 — Output stability across schema-version drift** — N/A. `level.ts` is pure computation with no serialized artifacts.

**Diff-derived Surface 2 — CLI flag/arg symmetry** — N/A. No CLI flags or parsers added.

**Diff-derived Surface 3 — Error-class breadth in `catch` blocks** — N/A. No try/catch blocks introduced.

**Diff-derived Surface 4 — Defensive layering at module boundaries** — probed. `safeXp` rejects `Infinity`, `-Infinity`, and `NaN` via `Number.isFinite`; rejects negative numbers and `-0` via `n > 0`; all malformed inputs produce level 1. Runtime non-number types (`undefined`, strings) are also caught by `Number.isFinite`. The guard is complete for all practical input classes at this boundary.

**Diff-derived Surface 5 — Cross-file atomicity windows** — N/A. Pure computation module, no multi-step writes.

**Diff-derived Surface 6 — Test-contract strength** — probed. The `LEVEL_THRESHOLDS` monotonicity test is exhaustive (all 100 entries). The `levelForXp` boundary test at index 50 is correct: `LEVEL_THRESHOLDS[50]` is the threshold for level 51; `threshold - 1` returns 50, `threshold` returns 51. The 500-sample monotonicity sweep for `levelForXp` is coarser than the threshold sweep, but because `levelForXp` is a deterministic lookup on a fully-validated monotone array, the sampling is sufficient for a sanity check. One minor test gap: the `at T XP` test for `levelProgress` asserts `fraction` and `level` but not `into` (should be 0) or `span` (should be positive).

**Diff-derived Surface 7 — Doc-vs-code drift in the ticket Rationale** — probed. Ticket Outcome says `LEVEL_THRESHOLDS[99] === T (or the rounded constant)`. Implementation hardcodes `if (i === LEVEL_COUNT - 1) return LEVEL_T;` — no rounding, exact value. The parenthetical "or the rounded constant" is permissive language; the implementation satisfies the primary claim. The `@deprecated` JSDoc on `stageForXp` matches the spec requirement. Constants `LEVEL_T`, `LEVEL_EXPONENT`, `LEVEL_COUNT` match their rationale values exactly. No meaningful drift found.

---

**Actionable findings**

None.

---

**Advisory Observations**

- **`into` semantics at level 100 with xp > LEVEL_T:** When `xp > LEVEL_T`, the level-100 branch sets `into = xp - LEVEL_T` (a positive excess) while `span = LEVEL_T - LEVEL_THRESHOLDS[98]` (the width of the last level interval). This means `into` can exceed `span` at level 100, which is inconsistent with interior-level behavior where `into < span` is guaranteed. Invariant 3 explicitly exempts level 100 from the `into ≤ span` rule, so this is spec-permitted. Callers who consume the raw `into` field (rather than `fraction`) at level 100 may encounter surprising values if xp grows past `LEVEL_T`. Consider documenting the level-100 semantics of `into` in the JSDoc, or clamping it to `Math.min(into, span)` in a future pass.

- **`levelProgress(T)` test does not assert `into` or `span`:** The test `at T XP: level 100, fraction is 1` checks `level` and `fraction` but leaves `into` and `span` unexercised. Both are correct (`into = 0`, `span = LEVEL_T - LEVEL_THRESHOLDS[98] > 0`) but unasserted. A future edit to the level-100 branch could silently break `into` without failing any test.

---

**Runner termination**

runnerStatus: completed
terminatedReason: All invariants held, all attack surfaces probed; two advisory observations filed, no actionable findings.
