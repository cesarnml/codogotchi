**Invariant results**

1. [held] `resolveHalfHearts({ lastActivityAt: null, ... }, now)` returns `MAX_HALF_HEARTS` via the unconditional early return in [packages/engine/src/hearts.ts](/Users/cesar/code/codogotchi_p10_02/packages/engine/src/hearts.ts:28); the test file covers representative `currentHalfHearts` and `activeMinutes` variations.
2. [broken] Invariant `[0, 6]` does not hold for hostile runtime inputs: `lastActivityAt: "not-a-date"`, `activeMinutes: NaN`, or `currentHalfHearts: NaN` all return `NaN` when exercised against the exported function, which is outside the required integer range.
3. [held] `health.ts` / `health.test.ts` are unchanged in the diff, and the new barrel export in [packages/engine/src/index.ts](/Users/cesar/code/codogotchi_p10_02/packages/engine/src/index.ts:1) does not alter the dormant `tickHealth` API surface.

**Surface results**

- `resolveHalfHearts` — NaN/Infinity/invalid ISO inputs: [probed] I executed the exported function with `lastActivityAt: "not-a-date"`, `activeMinutes: NaN`, `currentHalfHearts: NaN`, `Infinity`, and `-Infinity` variants. Invalid ISO and NaN numeric inputs produce `NaN`; positive `Infinity` happens to clamp to `6`, so the break is specifically the unguarded NaN path.
- `resolveHalfHearts` — `currentHalfHearts` out-of-range input: [probed] Negative and oversize finite values do stay bounded because decay is floored first and the final result is capped. `currentHalfHearts: -5` plus huge healing resolved to `6`, so I found no finite out-of-range escape path.
- `resolveHalfHearts` — simultaneous idle + active clamping order: [probed] The implementation uses decay-floor-first in [packages/engine/src/hearts.ts](/Users/cesar/code/codogotchi_p10_02/packages/engine/src/hearts.ts:38). Runtime probe `48h idle + 60 active minutes + current 0` returned `1`, confirming it does not use the incorrect combined clamp formula.
- `index.ts` — export collision: [probed] I enumerated existing engine exports and then imported the barrel; the new heart symbols load cleanly and do not collide with `health`, `level`, `loot`, `xp`, or source exports.
- `health.ts` — dormant model untouched: [probed] `git diff --name-only -- packages/engine/src/health.ts packages/engine/src/health.test.ts` returned no changes, so this surface is clean.

- Output stability across schema-version drift: [probed] There are no persisted artifact changes in this diff, but I still validated that the barrel re-export compiles/imports and exposes the new heart symbols without consumer-visible export breakage.
- CLI flag/arg symmetry: [N/A — no CLI surface in this diff]
- Error-class breadth in `catch` blocks: [N/A — no catch blocks introduced]
- Defensive layering at module boundaries: [probed] The public function trusts runtime inputs despite being exported from the engine barrel. A downstream caller can pass malformed ISO or NaN numerics and receive `NaN` silently, so the boundary is not defensively safe.
- Cross-file atomicity windows: [N/A — pure computation, no multi-step writes]
- Test-contract strength: [probed] The 18 tests cover the happy-path constants, decay/heal math, future timestamps, and the decay-first ghost recovery case, but they do not cover malformed ISO or NaN/non-finite numerics. That omission is why the invariant-2 failure ships undetected.
- Doc-vs-code drift in the ticket Rationale: [probed] The clamping-order rationale matches the code. I did find non-blocking drift around the promised token-floor documentation and where the canonical decay constants are said to live; I’ve put that under Advisory Observations.

**Actionable findings**

- [packages/engine/src/hearts.ts](/Users/cesar/code/codogotchi_p10_02/packages/engine/src/hearts.ts:32) — `Date.parse(lastActivityAt)` and the numeric inputs are used without `Number.isFinite` guards, so malformed ISO or NaN numerics propagate to `idleDecay`, `healGain`, and the final return value as `NaN`. This breaks invariant 2 and is a demonstrated correctness gap. Fix by sanitizing all runtime inputs before arithmetic: treat invalid `lastActivityAt` as a non-decaying timestamp (or another explicitly chosen safe fallback), coerce non-finite `activeMinutes`/`currentHalfHearts` to safe finite integers, and add tests that assert the final result is always an integer in `[0, 6]` for malformed ISO and NaN/non-finite inputs.

**Advisory Observations**

- [docs/product/delivery/phase-10/ticket-02-engine-heart-model.md](/Users/cesar/code/codogotchi_p10_02/docs/product/delivery/phase-10/ticket-02-engine-heart-model.md:11) says `ACTIVE_HOUR_TOKEN_FLOOR` or an event floor is documented, but this diff adds only `MAX_HALF_HEARTS`, `HALF_HEART_DECAY_HOURS`, `ACTIVE_MINUTES_PER_HALF_HEART`, and the resolver. I did not find any new token-floor constant or documentation in the touched code/tests.
- [docs/product/delivery/phase-10/implementation-plan.md](/Users/cesar/code/codogotchi_p10_02/docs/product/delivery/phase-10/implementation-plan.md:17) says decay constants live in `@codogotchi/contracts`, while [docs/product/delivery/phase-10/ticket-02-engine-heart-model.md](/Users/cesar/code/codogotchi_p10_02/docs/product/delivery/phase-10/ticket-02-engine-heart-model.md:14) says this engine ticket exports them so contracts can reference one definition. The diff currently implements the ticket-local version only: constants exist in engine, and contracts are not yet updated in this changeset.

**Runner termination**

runnerStatus: completed  
terminatedReason: review finished against the requested invariants and attack surfaces with direct code and runtime probes.
