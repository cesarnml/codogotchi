**Invariant results**
1. `held` — I probed `parseStateJson` with v5 payloads and confirmed `schema_version >= 5` rejects an absent required field while accepting `last_activity_at: null`; the `superRefine` check is keyed on `=== undefined`, not null.
2. `held` — I checked the v4 path in code and tests; the required-field gate only runs for `schema_version >= 5`, so valid v1-v4 payloads without the four new fields still parse.
3. `held` — `resolveConfigPath("features.rpg_hud_enabled")` returns `{ kind: "features", key: "rpg_hud_enabled" }`, and `features.rpg_enabled` still resolves unchanged.

**Surface results**
1. `probed` — I ran a direct Zod check for `.nullable().optional()` plus the new `superRefine`. `{ last_activity_at: null }` passes, `{}` fails with the custom required-field issue, so null vs absent is distinguished correctly.
2. `probed` — I exercised `parseStateJson` with `schema_version: 5` and `schema_version: 6`. Version 5 is accepted; version 6 is rejected with a `schema_version` `too_big` error before any missing-field complaint, which is the intended boundary behavior.
3. `probed` — The schema now accepts half-configured/local-only RPG configs, but existing CLI sync code still assumes cloud fields exist. A config containing only `{ profile_id, features: { rpg_enabled: true } }` passes validation, and `dispatch(["sync"])` returns `sync ok`/exit `0` even though `runSync()` reads `config.handle`, `config.health`, and `config.convex_http_url`; that is a real correctness gap.
4. `probed` — `packages/engine/src/hearts.ts` imports the constants into module scope and re-exports the same bindings. `resolveHalfHearts()` references the imported names directly, so there is no runtime “not defined” failure path here.
5. `probed` — The `superRefine` field list exactly matches the declared keys: `level`, `level_fraction`, `half_hearts`, `last_activity_at`. I did not find a typo-driven silent pass.
6. `probed` — `rpg_hud_enabled` is only `optional()`, with no `.default(true)` and no read-side normalization. No current repo consumer reads the flag yet, so I did not find a present-day false-disable path, but the “default true” contract is not enforced centrally.

7. `probed` — The pinned writer still emits `schema_version: 4`, and those files continue to parse because the v5 required-field check is skipped for `< 5`.
8. `N/A — reason` — No CLI flags or argv shapes were changed in this diff.
9. `N/A — reason` — No new catch blocks were introduced in the touched code; the schema change adds Zod issues, not thrown non-Zod errors.
10. `probed` — `packages/contracts/src/decay-constants.ts` exports `6`, `8`, and `60`, matching the previous inlined values exactly. `packages/engine/src/hearts.test.ts` still asserts the same literals.
11. `N/A — reason` — No new multi-step write window was introduced; the new module is read-only.
12. `probed` — The new bounds tests fail for the correct field validators: a direct `parseStateJson({ half_hearts: 7 })` probe reports a `half_hearts` `too_big` issue, and `schema_version: 6` reports a `schema_version` `too_big` issue. The one weakness is coverage breadth: the missing-required-field test only omits `level`, so the other three required keys are verified by code inspection rather than individual tests.
13. `probed` — The schema bounds match the ticket exactly: `level` is `.int().min(1).max(100)`, `half_hearts` is `.int().min(0).max(6)`, and `level_fraction` is `.min(0).max(1)`. I also verified that `.datetime({ offset: true })` accepts the `Z` timestamps used by the tests.

**Actionable findings**
1. `packages/cli/src/config-command.ts` — The new HUD flag is not actually part of the working settable config surface. `resolveConfigPath("features.rpg_hud_enabled")` resolves in contracts, but `configSet({ path: "features.rpg_hud_enabled" })` throws `Unknown features key: rpg_hud_enabled`, and `configGet` throws `Unknown config key: features.rpg_hud_enabled`. This breaks the ticket outcome that the flag be added to the settable config paths. Fix: extend `getDottedValue()` and `applyFeaturesValue()` to support `rpg_hud_enabled`, preserve `rpg_enabled` when writing the flag, and add CLI tests for get/set round-trip.
2. `packages/contracts/src/config.ts`, `packages/cli/src/router.ts`, `packages/cli/src/sync.ts` — The new local-RPG schema permits configs with `features.rpg_enabled: true` and no cloud fields, but the `sync` command still runs against those configs and reports success. I demonstrated `dispatch(["sync"])` on a local-only RPG config returning `sync ok` with exit `0` while `runSync()` still reads `config.handle`, `config.health`, and posts to `${config.convex_http_url}/sync`. This is a demonstrated correctness gap, and it is also a spec-permits-real-bug under the rationale’s deferred “all-or-none” cloud validation. Fix: either block `sync` for local-only configs with a clear nonzero error, or add a sync-specific refinement/guard that requires the cloud tuple before `runSync()` executes.

**Advisory Observations**
The ticket doc at `docs/product/delivery/phase-10/ticket-03-contracts-state-v5-config.md:43` says the cloud-field relaxation is acceptable because “sync is fully disabled in Phase 10 and no code reads those cloud fields.” The current code does not match that claim: `packages/cli/src/router.ts:269-290` still exposes `sync`, and `packages/cli/src/sync.ts:142-158` reads `handle`, `health`, and `convex_http_url`.

The same ticket doc says `stateJsonV5Schema` adds the new fields, but the implementation kept a single `stateJsonV1Schema` with conditional `superRefine`. That drift is harmless behaviorally, but the doc names a schema that does not exist.

**Runner termination**
runnerStatus: completed  
terminatedReason: Review completed with targeted probes of the touched implementation and directly related runtime paths.
