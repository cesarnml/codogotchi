**Invariant results**
1. `[broken]` I ran the existing `runHook v5 local RPG fields` tests and they pass for the fixture timestamp `< now`, but a direct `computeAndPersistV5Fields` probe with a transcript event timestamp exactly equal to the stored cursor re-counted the same 15 tokens on the second run, so the no-double-count invariant does not hold at the equality boundary.
2. `[held]` I verified `computeAndPersistV5Fields` only scans JSONL when `origin` is `claude_code` or `codex`, and the cursor-origin integration test passes; the same `isTokenSource` gate excludes `vscode` and `antigravity`, so non-token origins preserve level/ring.
3. `[held]` I ran `runHook` once with no config and once with `features.rpg_enabled: false`; both wrote parseable `schema_version: 4` state with no v5 fields present.

**Surface results**
1. `[probed]` Exact-boundary cursor overlap is real. `computeAndPersistV5Fields` stores `last_read_at_* = now`, while `readJsonlSignals` only skips `event.timestamp < sinceIso`; with an event at exactly `sinceIso`, my probe showed `cumulative_claude_tokens` go `15 -> 30` on an unchanged transcript.
2. `[probed]` The `prevLastActivityAt` snapshot is inside the lock scope. `runHook` enters `withHomeLock` before the config/v5 block, and `computeAndPersistV5Fields` is called inside that closure, so the concurrent-hook race described here does not apply.
3. `[probed]` `readConfig` errors are swallowed. With an invalid config payload `{ "profile_id": "p" }`, `readConfig` throws, the outer `catch {}` in `runHook` suppresses it, and `state.json` is silently written as v4.
4. `[probed]` The `active_minutes % 60` logic is arithmetically sound. `resolveHalfHearts` uses `floor(activeMinutes / 60)` and the cache stores the remainder; even if `active_minutes` somehow reached `120`, the code would consistently award two heals and persist `0`.
5. `[probed]` `computeAndPersistV5Fields` definitely runs inside `withHomeLock`. The lock covers both cache read/write and state write, so same-home concurrent hook runs cannot race this path.
6. `[probed]` JSONL read failure currently advances the cursor and can permanently skip tokens. I simulated a missing Claude root on the first run, which wrote `last_read_at_claude = 2026-06-03T00:01:00.000Z`; after creating a transcript event at `2026-06-03T00:00:30.000Z`, the second run still left cumulative tokens at `0`.
7. `[probed]` Output stability across schema-version drift is broken for the current menubar consumer. `apps/menubar/Sources/StateJsonReader.swift` still hard-rejects any `schema_version > 4`, and its tests explicitly assert that v5 fails with `.schemaNewer`.
8. `[N/A — no new CLI flags or args were added]`
9. `[probed]` The broad `catch` blocks hide real degradations. I confirmed two concrete cases: invalid config silently downgrades to v4, and JSONL read failure silently advances the cursor.
10. `[probed]` Malformed cache values are type-guarded and do not crash the hook path. Invalid fields are defaulted (`number` fields to `0`/`MAX_HALF_HEARTS`, string cursors to `null`), so this boundary is defensive, though it can silently reset progression if the cache is externally corrupted.
11. `[probed]` There is an observable cross-file atomicity window. `writeLocalXpCache` happens before `writeStateAtomic`, so if the cache write succeeds and the state write fails, the next successful run will see advanced cursors and cannot replay the skipped tokens from JSONL.
12. `[probed]` The no-double-count test is strong enough to catch `undefined === undefined`, but the cursor-origin test is weaker: it never asserts that the preexisting Claude state actually has the expected level, so a silent fallback on the setup run could still make that test pass. There is also no test for non-zero `active_minutes` carry-forward.
13. `[probed]` The ticket doc drifts internally and from the code. The Outcome says hook events include “a coarse periodic refresh if one already exists,” while the Rationale defers JSONL read rate-limiting/refresh to follow-up, and the implementation only computes on hook events.

**Actionable findings**
1. `packages/cli/src/local-xp-writer.ts:147-156` and `packages/engine/src/sources/jsonl-parser.ts:153,183` — exact-boundary events are double-counted because the cache stores `last_read_at_* = now` and the parser includes `timestamp === since`. This breaks invariant 1 and is a demonstrated correctness gap. Fix by making the lower bound exclusive in one place: either advance the stored cursor past the newest consumed event (for example from `result.lastEventAt` plus a minimal increment / sequence marker) or change the parser/filter contract so events at the saved cursor are excluded.
2. `packages/cli/src/local-xp-writer.ts:160-166` — the error path advances `last_read_at_claude` / `last_read_at_codex` even when `readJsonlSignals` fails, which permanently drops unread tokens once the source becomes readable again. This is a demonstrated correctness gap. Fix by leaving the cursor unchanged on read failure and, if needed, recording the failure separately so the next successful run can retry the same window.
3. `apps/menubar/Sources/StateJsonReader.swift:8,63-66` and `apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift:336-354` — the current renderer refuses `schema_version: 5`, while `runHook` now writes v5 for `rpg_enabled: true`. That breaks the consumer for the very profiles this ticket upgrades. Fix by teaching the renderer to accept v5 before shipping this CLI change, or gating the CLI so it does not emit v5 until the renderer bump lands.

**Advisory Observations**
- `packages/cli/src/hook-binary.ts:922-933` silently swallows `ConfigReadError` and all v5 compute errors, so a malformed config leaves the user on v4 forever with no signal. I demonstrated this with `{ "profile_id": "p" }`, which produced a clean v4 `state.json`.
- `docs/product/delivery/phase-10/ticket-05-cli-xp-hp-writer.md:10` claims “a coarse periodic refresh if one already exists,” but neither `packages/cli/src/hook-binary.ts` nor `packages/cli/src/local-xp-writer.ts` implements any periodic refresh path; the code only runs on hook events. The same doc’s Rationale at lines 48-50 instead says that read rate-limiting/refresh is deferred.
- `docs/product/delivery/phase-10/ticket-05-cli-xp-hp-writer.md:40,50` says the hook falls back to v4 when `rpg_enabled` is absent/false, but the implementation also falls back on invalid config and any v5 compute error because of the broad outer catch.
- `packages/cli/src/hook-binary.test.ts:1944-1963` does not assert that the pre-cursor state actually has a concrete level/value before comparing post-cursor equality, so it would not catch a setup-run silent fallback to v4 in the same way the no-double-count test would.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: Completed the targeted adversarial review with code inspection, targeted tests, and direct repro probes for the main failure paths.
