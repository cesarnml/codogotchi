Invariant results

[held] Invariant 1 — I checked every new/changed internal route in the touched content against `web/src/pages/`; `/privacy`, `/terms`, `/gallery`, `/rpg`, `/docs/spritesheet`, `/hatch`, and `/upload` all resolve, and legacy `/pets` still exists as a 301 redirect to `/gallery`.

[broken] Invariant 2 — Most Privacy/Terms data-handling claims match `convex/auth.ts`, `convex/schema.ts`, `convex/actions/uploadPet.ts`, and `convex/http.ts`, but `web/src/pages/privacy.astro` says Codogotchi stores “a generated thumbnail image” even though the implementation stores an optional client-generated thumbnail blob when present and does not generate one server-side.

[held] Invariant 3 — The README npm-surface claim matches `packages/cli/src/npm-router.ts`: the published bare `codogotchi` package exposes only `add`, `status`, and `version`/`--version`, while `setup` and `hooks install` remain outside that surface.

Surface results

[probed] Footer link list — `web/src/components/Footer.astro` points to `/privacy`, `/terms`, `/gallery`, `/rpg`, `/docs/spritesheet`, and `/hatch`; all corresponding pages exist, and `web/src/pages/gallery.astro`/`upload.astro` still use `active="pets"` so the gallery nav highlight keeps working after the `/pets` → `/gallery` shift. `web/src/pages/pets.astro` preserves old consumers with a permanent redirect.

[probed] Privacy data-handling claims — Google/GitHub/password auth, Resend verification, Convex `users`/`pets`/`_storage`, aggregate `downloadCount`, and lack of in-repo analytics all line up with the code. The one mismatch is the thumbnail claim: the code path is client-side thumbnail extraction in `web/src/components/UploadApp.tsx:29-49,81-90`, then optional blob acceptance in `convex/actions/uploadPet.ts:100-120`, not server-side thumbnail generation.

[probed] Terms content-license/operator-rights/community-content claims — “validated, stripped to an allowlist, re-packed” matches the upload action plus validator, and the unlist claim matches the `listed` kill-switch on list/detail/download paths via `convex/pets.ts` and `convex/http.ts`. I did not find a path where an unlisted pet still remains downloadable in the current code.

[probed] README marketplace section — The documented install paths, destination `${CODOGOTCHI_HOME:-~/.codogotchi}/pets/<pet-id>/`, no-overwrite default with `--force`, and the minimal npm surface all match `packages/cli/src/add-command.ts` and `packages/cli/src/npm-router.ts`. The doc’s boundary claim is sound.

[probed] Launch-readiness runbook — The runbook is explicit that it is a public-announcement gate, separate from code completion, and it repeats that separation in both the intro and closeout note. I did not find wording that turns it into a merge gate.

[probed] Retrospective factual anchors — Ticket count, PR span, and branch family generally line up with the local branch/log history, but the opening sentence is internally inconsistent: it says “P11.01 → P11.08 (8/8) landed” while also saying “P11.08 PR pending at closeout.” The section ordering otherwise matches the established retrospective structure used in prior phase retrospectives.

[probed] Output stability across schema-version drift — The only JSON touched here, `docs/product/delivery/phase-11/reviews/P11.07-subagent-review.ledger.json`, is whitespace-only; the SHA-256 of `HEAD^` content and current file content matches exactly.

[N/A — no CLI/parser code was added in this ticket] CLI flag/arg symmetry — I still checked the README surface against `packages/cli/src/npm-router.ts`; it documents existing `add`, `--force`, and `status` behavior rather than inventing new flags.

[N/A — the ticketed files add no new catch blocks] Error-class breadth in `catch` blocks — the touched Astro/docs files are static content only.

[N/A — presentational/static content only] Defensive layering at module boundaries — the new Astro pages use the existing `Base` layout and internal links; no new cross-module behavior was introduced in the ticketed files.

[N/A — doc/content only] Cross-file atomicity windows — I did not find any new multi-write or multi-module state transition introduced by this ticket.

[probed] Test-contract strength — There are no tests for this doc ticket, which is expected, and the Astro pages contain prose plus internal links only. I did not find hidden client logic or behavior changes smuggled into `privacy.astro` or `terms.astro`.

[probed] Doc-vs-code drift in the ticket Rationale — The Rationale correctly explains why `docs/template/overview/start-here.md` was skipped as subtree content, but the ticket Outcome section above it still claims that file was updated. That is doc drift, not an implementation bug.

Actionable findings

1. `web/src/pages/privacy.astro:94-100` — The policy says uploaded pets store “a generated thumbnail image,” but the actual implementation stores an optional client-generated thumbnail blob (`web/src/components/UploadApp.tsx:29-49,81-90`) and merely accepts/rejects it server-side (`convex/actions/uploadPet.ts:100-120`). This breaks Invariant 2 and finding-discipline clause 1. Fix by changing the copy to something like “an optional thumbnail image generated from the uploaded sheet and stored with the pet” or by explicitly saying the thumbnail is client-generated before upload.

2. `docs/product/retrospectives/phase-11-pet-gallery-marketplace-retrospective.md:8` — The retrospective says all 8 tickets “landed” while also stating “P11.08 PR pending at closeout,” which is a factual inconsistency within the ticket’s required retrospective artifact. This fits finding-discipline clause 2. Fix by changing either “8/8 landed” to “8/8 delivered in the stack, with P11.08 pending at closeout” or by removing the pending qualifier if the intent is to describe post-merge state.

Advisory Observations

- The README marketplace block calls the npm package an “add-only surface” at `README.md:131-139`, then immediately says its commands are `add`, `status`, and `--version`. That is not a contract break, but “minimal install/status surface” would be more precise.

- The ticket file `docs/product/delivery/phase-11/ticket-08-legal-docs-retrospective.md` still says the outcome included updating `docs/template/overview/start-here.md`, while the appended Rationale says that change was deliberately skipped because it would create `.son-of-anton` subtree drift, and the actual diff updates `README.md` plus `plugins/hatch-codogotchi/README.md` instead. That is the doc-vs-code drift requested for advisory reporting.

**Runner termination**
runnerStatus: completed
terminatedReason: Completed the targeted adversarial review of the ticket-scoped diff and related implementation paths.
