**Invariant results**

1. [held] I executed `createUser` twice with the same `username` and then queried `getUserByUsername`; the second mutation threw `Username "alice" is already taken`, the lookup returned one row, and the table count stayed at 1.
2. [held] I probed `listPets` with two listed pets and one unlisted pet; the query returned only the listed rows, and the returned order was descending by `createdAt` (`newer` before `older`).
3. [held] I exercised `migrateDropLegacyUsers` against an empty table, against repeated calls, and against a table with two rows; it returned `{ status: "clean" }` on repeat clean runs and threw once the row count exceeded 1.

**Surface results**

- `createOrUpdateUser` uniqueness loop in `auth.ts`: [probed] `attempt` is initialized before the loop and the suffix search terminates once a free `by_username` slot exists; because the callback receives a `GenericMutationCtx`, the read-then-insert path itself is serialized. The real problem is elsewhere: this custom callback replaces the library’s default user-linking logic, so same-person sign-ins across providers can create separate user rows.
- `createUser` in `users.ts` uniqueness check vs. `db.insert` ordering: [probed] The function is an `internalMutation`, and the auth library types its callback as `GenericMutationCtx`, so the check-then-insert path is mutation-serialized in both places. I did not find a race window here.
- `listPets` index usage: [probed] I inserted pets with `createdAt` values `100`, `200`, and an unlisted row at `300`; `.withIndex("by_listed_createdAt", q => q.eq("listed", true)).order("desc")` returned only the `listed=true` partition and ordered it by descending `createdAt`.
- `migrateDropLegacyUsers` safety threshold: [probed] The >1-row path throws correctly, but the message only says `Investigate before proceeding.` It surfaces the anomaly, though it does not tell the operator that the failure likely means the migration ran after multiple auth users already existed.
- `schema.ts` `...authTables` spread followed by `users` override: [probed] The installed auth library explicitly documents inlining/extending `authTables.users`, so the override pattern itself is sound. The code preserves the required index names (`email`, `phone`) and adds `by_username`; using compound `["email", "_creationTime"]` / `["phone", "_creationTime"]` still satisfies the library’s prefix-equality queries.
- `Password` provider `profile()` cast safety: [probed] The installed `Password` provider types `profile()` to return an object with `email: string`, so email is part of the provider contract rather than an optional field. This is not a silent phone-only path in the current library.
- Output stability across schema-version drift: [probed] `sizes: v.any()` is intentionally loose and creates future drift risk, but nothing in this ticket currently reads a stronger shape. More importantly, `username` is now required on `users`, so any auth-managed row inserted without going through this callback would violate schema expectations.
- CLI flag/arg symmetry: [N/A — no CLI changes in this diff]
- Error-class breadth in `catch` blocks: [probed] No new catch-and-swallow path was introduced in the touched Convex modules; the new mutations throw directly and will abort transactionally.
- Defensive layering at module boundaries: [probed] `auth.ts` bypasses `internal.users.createUser` and reimplements user creation inline. That duplication is not just stylistic: it also bypasses the auth library’s default account-linking/update behavior, which is a ticket-relevant correctness gap.
- Cross-file atomicity windows: [probed] The auth callback executes in mutation context, so the uniqueness check and insert are atomic with respect to concurrent mutations. The cross-file risk is the callback contract override, not transactionality.
- Test-contract strength: [probed] The checked-in tests cover duplicate `createUser`, `getPet`, `listPets` filtering, and clean migration idempotency, and they currently pass. They do not cover the auth callback path at all, so the cross-provider unified-identity regression would ship undetected.
- Doc-vs-code drift in the ticket Rationale: [probed] The ticket prompt says the migration verifies the legacy users-table row count is `<=1`, but [convex/migrations/p11_02.ts](/Users/cesar/code/codogotchi_p11_02/convex/migrations/p11_02.ts:17) actually checks the new auth-managed `users` table row count. The implementation comment is honest about that distinction.

**Actionable findings**

- [convex/auth.ts](/Users/cesar/code/codogotchi_p11_02/convex/auth.ts:35) — The custom `createOrUpdateUser` callback breaks the ticket’s unified-identity outcome by replacing Convex Auth’s default linking/update path with “existing account id or insert a new user.” The library source shows that once `createOrUpdateUser` is provided, default verified-email/phone linking and user patching are skipped entirely (`node_modules/@convex-dev/auth/src/server/implementation/users.ts:57-145`, `node_modules/@convex-dev/auth/src/server/types.ts:152-198`). A user who signs up with Password and later signs in with Google/GitHub on the same verified email will get a second `users` row instead of one unified identity, and existing users also skip profile-field updates on sign-in. This is a demonstrated correctness gap under finding-discipline clause 2. Fix: make the custom callback fully implement the library’s default semantics before assigning usernames — when `existingUserId` is null, look up/link a verified-email or verified-phone user before inserting; when a user is found or already exists, patch the auth-managed fields instead of returning immediately; only synthesize a unique username for genuinely new rows.

**Advisory Observations**

- [docs/product/delivery/phase-11/reviews/P11.02-subagent-review.prompt.md](/Users/cesar/code/codogotchi_p11_02/docs/product/delivery/phase-11/reviews/P11.02-subagent-review.prompt.md:17) claims the migration verifies the old legacy `users` table row count before proceeding, but [convex/migrations/p11_02.ts](/Users/cesar/code/codogotchi_p11_02/convex/migrations/p11_02.ts:17) checks the new auth-managed `users` table instead. That is doc-vs-code drift rather than a direct invariant break.
- [convex/users.test.ts](/Users/cesar/code/codogotchi_p11_02/convex/users.test.ts:14) and [convex/pets.test.ts](/Users/cesar/code/codogotchi_p11_02/convex/pets.test.ts:91) still contain “stub/red” comments even though the tests now pass. That does not break ticket behavior, but it will mislead the next person reading failures or coverage intent.
- No test currently exercises the auth callback path in [convex/auth.ts](/Users/cesar/code/codogotchi_p11_02/convex/auth.ts:35), so the unified-identity bug above is not guarded by the current suite.

**Runner termination**
runnerStatus: completed
terminatedReason: review finished against the requested invariants and attack surfaces without file writes.
