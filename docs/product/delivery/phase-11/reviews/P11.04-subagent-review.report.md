**Invariant results**
1. `[broken]` Unlisted pets are filtered out of `listPets` and `getPet`, and the download action initially checks visibility via `getPetForDownload`, but the download flow then mutates by `petId` in a separate transaction without re-checking `listed`. If the operator unlists the pet after line 67 and before line 77 in `convex/http.ts`, the request can still return `200`.
2. `[held]` The HTTP action fetches storage first (`convex/http.ts:72-75`) and only then calls `incrementDownloadCount` (`convex/http.ts:77`), so a missing blob returns `500` without a phantom increment. I also ran `bun test convex/pets.test.ts convex/http.test.ts` and the happy-path count bump passes.
3. `[held]` The response sets `content-disposition: attachment; filename="<petId>.codogotchi-pet.zip"` at `convex/http.ts:83`, which matches the canonical filename requirement.

**Surface results**
1. `[probed]` `getListedPet` is used by `getPet` and `getPetForDownload`, and `listPets` enforces `listed: true` through the `by_listed_createdAt` index. The remaining bypass is the second-step mutation in `incrementDownloadCount`, which can observe an already-unlisted row and still allow the download to complete.
2. `[probed]` `incrementDownloadCount` is ordered after `ctx.storage.get`, so the `blob === null` path short-circuits correctly. I found no path where a storage miss increments the counter.
3. `[probed]` The path parser rejects `/pets/<petId>` and other malformed shapes with `404`, and a trailing slash still normalizes to the expected 3 segments. A `%2F` inside `petId` stays percent-encoded in `URL.pathname`, so it does not create an extra segment here.
4. `[probed]` I checked `convex/http.ts` for other HTTP mounts: only `auth.addHttpRoutes(http)`, `/sync`, and this `/pets/` prefix exist. I found no current `/pets/*` auth route to shadow, though this prefix is broad enough that a future `/pets/...` route would have to be ordered carefully.
5. `[probed]` The schema requires `createdAt: v.number()` on `pets` and the list query uses the `by_listed_createdAt` index with `eq("listed", true)` and `order("desc")`. I found no schema-level path for a listed pet without `createdAt` to bypass the sort/filter.
6. `[N/A — no changed persisted artifact or CLI/stdout schema in this diff]` I did not find any new on-disk JSON, stdout contract, or versioned artifact shape affected by these changes.
7. `[N/A — no CLI flags or parser/help changes in this diff]` The touched files are Convex query/HTTP code and tests only.
8. `[N/A — no new catch broadening in the touched diff]` `convex/pets.ts` adds no `catch`, and the `/pets/.../download` handler has none. I did not find a modified catch block relevant to this ticket behavior.
9. `[probed]` The module boundary from public queries to clients is under-validated: `listPets` and `getPet` return raw pet rows, not the public payload the ticket calls for. In particular, they do not synthesize the public download URL or a thumbnail URL, so the gallery/detail boundary is incomplete.
10. `[probed]` The read-then-mutate download flow spans `convex/http.ts` and `convex/pets.ts` with a visible race window between `getPetForDownload` and `incrementDownloadCount`. That window is observable because `listed` can change between those calls.
11. `[probed]` Test coverage is weak on the public contract: `convex/http.test.ts` checks status and filename substring, but not the JSON error codes/body for 404/500 paths, malformed `/pets/...` shapes, or the URL fields the queries are supposed to return. The current tests all pass, but they would not catch the payload-shape drift I found.
12. `[probed]` The ticket Rationale in `docs/product/delivery/phase-11/ticket-04-download-endpoint-catalog-queries.md` is still an unfilled stub, so there is no concrete rationale claim to contradict. I did compare the Outcome against code and found a code/spec mismatch on query payload shape, which I list below as a finding rather than doc drift.

**Actionable findings**
1. `convex/http.ts:67-77`, `convex/pets.ts:46-58` — The download kill-switch is not atomic. The handler authorizes the download using `getPetForDownload`, then later increments by `petId` without re-checking `listed`; if the pet is unlisted in between, the request still returns `200`, breaking invariant 1. Fix by collapsing the visibility check and count increment into one mutation that verifies `listed === true` at mutation time and returns the storage ID/blob metadata only when still listed, or by re-checking `listed` inside `incrementDownloadCount` and aborting the response path on failure.
2. `convex/pets.ts:18-34` — `listPets` and `getPet` do not return the public URL fields the ticket contract requires. `listPets` paginates raw rows and exposes `thumbnailStorageId` instead of a thumbnail URL, and `getPet` returns the raw document without a `downloadUrl` matching `GET /pets/<petId>/download`. This breaks the stated Outcome for the gallery/detail payloads under finding-discipline clause 2. Fix by shaping both query responses explicitly: include deterministic `downloadUrl: /pets/<petId>/download` for detail payloads, produce a thumbnail URL field the SPA can consume, and extend tests to assert those machine-readable fields.

**Advisory Observations**
- `convex/actions/uploadPet.ts:51-55` now uses `api.pets.getPet` for the duplicate-slug precheck. Because `getPet` hides unlisted rows, an upload reusing an unlisted slug will miss the cheap precheck and fail later in `createPet` instead. The atomic uniqueness guard still prevents corruption, and the canonical zip cleanup path avoids orphaning the new blob, so this is not a blocking bug for this ticket.
- `convex/http.test.ts:113-125` covers unlisted and unknown downloads only at the status-code level. If you care about stable machine-readable error contracts for downstream clients, add assertions on `{"error":"not_found"}` and the storage-failure `{"error":"storage_error"}` body as follow-up hardening.
- `docs/product/delivery/phase-11/ticket-04-download-endpoint-catalog-queries.md:37-41` still contains placeholder Rationale bullets rather than implementation-specific notes. That is doc debt, but it does not by itself contradict the shipped behavior.

**Runner termination**
runnerStatus: completed
terminatedReason: Finished the targeted adversarial review of the touched code, tests, and ticket docs.
