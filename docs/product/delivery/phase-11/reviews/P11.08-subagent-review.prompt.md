You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P11.08 — Legal pages, docs + retrospective (doc/content ticket; `Red: skip`). Outcome:

- **Privacy** and **Terms** pages are published on the site (adapted, not copied, from the
  codex-pets references), reachable from the footer alongside the community-content disclaimer.
- Documentation reflects the shipped marketplace: `README.md` and the user-visible product docs
  updated where behavior/commands changed (the new `codogotchi add` command, `/gallery`,
  `/upload`); the `hatch-codogotchi` README cross-links the gallery as the publish destination.
- A **launch-readiness checklist** is recorded (not code): ~10 seeded pets live in `/gallery`,
  `admin@codogotchi.app` intake confirmed, operator kill-switch verified, Privacy/Terms published —
  explicitly the gate for the *public announcement*, separate from code completion.
- The **phase retrospective** is written.

Review focus from the ticket: Privacy/Terms are adapted (not copy-pasted) and accurately describe
codogotchi's actual data handling (Convex storage, OAuth providers, what's collected); the
launch-readiness checklist is unambiguous about being a go-public gate, not a code definition-of-done;
the retrospective follows the soa-write-retrospective structure.

Rationale note: the ticket's "update `docs/template/overview/start-here.md`" line was deliberately NOT
applied — that file is generic SoA orchestrator workflow doc living under the `.son-of-anton/` git
subtree (zero codogotchi/marketplace content); editing it would create subtree drift. README.md and
plugins/hatch-codogotchi/README.md were updated instead.

### Files touched

Implementation (content/templates):
web/src/pages/privacy.astro
web/src/pages/terms.astro
web/src/components/Footer.astro
README.md
plugins/hatch-codogotchi/README.md
docs/runbooks/phase-11-launch-readiness.md
docs/product/retrospectives/phase-11-pet-gallery-marketplace-retrospective.md
docs/product/delivery/phase-11/ticket-08-legal-docs-retrospective.md (Rationale only)
docs/product/delivery/phase-11/reviews/P11.07-subagent-review.ledger.json (reformat only)

Tests:
None — doc/content ticket, no automated test; the PR is the gate.

### Invariants to hold

1. Every internal link/route introduced or changed in the diff resolves to a page that exists in
   `web/src/pages/` (e.g. footer `/privacy` → `web/src/pages/privacy.astro`, `/terms` →
   `web/src/pages/terms.astro`, `/gallery`, `/upload`); no link points at a 404.
2. The Privacy/Terms copy must not assert a data-handling fact that contradicts the actual
   implementation — auth providers are Google + GitHub + email/password (Convex Auth), storage is
   Convex (DB + file storage), email is Resend for verification only, downloads are counted in
   aggregate (no per-user download tracking), and there is no third-party analytics.
3. The npm-surface claim in README must match P11.05: the published bare `codogotchi` package exposes
   only `add` / `status` / `--version` and cannot run app-owned write commands (`setup`,
   `hooks install`).

### Attack surfaces to probe

1. `web/src/components/Footer.astro` link list — do all `href`s (`/privacy`, `/terms`, `/gallery`,
   `/rpg`, `/docs/spritesheet`, `/hatch`) resolve to real pages? The gallery link changed from `/pets`
   to `/gallery`; confirm `/gallery` exists and the `active` key wiring (`pets`) still highlights
   correctly without breaking the pre-existing `/pets` consumers.
2. `web/src/pages/privacy.astro` data-handling claims — cross-check each concrete claim (providers,
   storage, Resend, aggregate-only downloads, no cookies/analytics) against `convex/auth.ts`,
   `convex/schema.ts` (pets/users tables), and the upload/download implementation. Flag any claim the
   code does not support.
3. `web/src/pages/terms.astro` content-license + operator-rights + community-content claims — does the
   "server validates, strips to allowlist, re-packs" description match P11.01/P11.03, and does the
   "operator can unlist instantly / removes from gallery and download endpoints" claim match the
   `listed` kill-switch + download endpoint behavior?
4. `README.md` "Pet gallery marketplace" section — verify the install paths (npx/curl/zip), the
   install destination `~/.codogotchi/pets/<id>/`, the no-overwrite/`--force` behavior, and the
   minimal-npm-surface boundary against the P11.04/P11.05 implementation and ticket specs.
5. `docs/runbooks/phase-11-launch-readiness.md` — is it unambiguous that it gates the public
   announcement and NOT the code merge? Look for any wording that could be read as a code
   definition-of-done, or any checklist item that contradicts what actually shipped.
6. Retrospective factual anchors — PR numbers (#106–#113, P11.08 pending), branch names
   (`agents/p11-01`…`p11-08`), ticket count (8/8), and section ordering vs the soa-write-retrospective
   required sections. Flag any factual claim that is checkable and wrong.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `[probed]` / `[N/A — reason]`. The only JSON
   touched is the inherited P11.07 ledger (whitespace reformat). Confirm the reformat is whitespace-only
   and does not change any field value or schema version.
2. **CLI flag/arg symmetry** — `[N/A — reason]` expected: this ticket adds no CLI flags. Confirm the
   README's documented `codogotchi add`/`--force`/`status` surface matches the shipped CLI rather than
   inventing flags.
3. **Error-class breadth in `catch` blocks** — `[N/A — reason]` expected: no new `catch` blocks (static
   pages + docs). Confirm.
4. **Defensive layering at module boundaries** — `[N/A — reason]` expected: presentational Astro pages
   with no new cross-module calls. Confirm no new logic was smuggled in.
5. **Cross-file atomicity windows** — `[N/A — reason]` expected: doc/content only. Confirm.
6. **Test-contract strength** — `[N/A — reason]` expected: no tests in a doc-only ticket. Confirm there
   is no untested behavior change hiding in the Astro files (e.g. a script tag with logic).
7. **Doc-vs-code drift in the ticket Rationale** — `[probed]`. Read ticket-08 Rationale and confirm:
   (a) start-here.md was correctly identified as subtree content and legitimately skipped; (b) the
   Rationale's claims about what was adapted/added match the diff. Surface drift in Advisory
   Observations only.

### Diff context

This is a doc/content ticket. Two new static Astro pages (`privacy.astro`, `terms.astro`) reuse the
existing `Base` layout + sidebar/section pattern from `docs/spritesheet.astro`; they contain prose and
internal links only, no client logic. `Footer.astro` gains `/privacy` and `/terms` links, repoints the
gallery link to `/gallery`, and carries the community-content disclaimer + `admin@codogotchi.app`
takedown line site-wide. README gains a Phase-11 status line and a "Pet gallery marketplace" section
documenting `codogotchi add`, the three install paths, and the minimal npm-surface boundary. The hatch
README gains a one-line gallery cross-link. A new launch-readiness runbook and the phase retrospective
are added. The P11.07 ledger.json change is a pure whitespace reformat (biome) to keep the branch's
`biome check` green.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

For a doc/content ticket, the highest-value findings are: a footer/README link that 404s, a
Privacy/Terms statement that misdescribes real data handling (e.g. claims data is NOT collected when it
is, or names a provider that is not used), or a launch-readiness checklist that reads as a code DoD.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
Use exactly these five top-level section headings, in this order:
`Invariant results`, `Surface results`, `Actionable findings`, `Advisory Observations`,
`Runner termination`. Do not use `---` horizontal rules anywhere in the report. Write
`Runner termination` as a bold span on its own line, then `runnerStatus:` and
`terminatedReason:` as plain-text lines below. Inside `Advisory Observations`, write one
observation per bullet or paragraph; do not put a bold label on its own line before the body.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
runnerStatus: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
terminatedReason: one short sentence explaining why this status was reported.
