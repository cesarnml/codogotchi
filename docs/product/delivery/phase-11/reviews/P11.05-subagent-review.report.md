**Invariant results**
1. `[held]` `codogotchi add <pet-id>` fetches `/pets/<petId>/download`, re-validates via `validateAndRepackPet`, and unpacks the canonical zip into `${CODOGOTCHI_HOME}/pets/<pet-id>/` (`[add-command.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/add-command.ts:25)`).
2. `[held]` No-overwrite is the default and `--force` overwrites existing files; the new tests covering both paths pass (`[add-command.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/add-command.ts:76)`, `[add-command.test.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/add-command.test.ts:113)`).
3. `[held]` Invalid downloaded zips are rejected before any file writes, so malformed packages do not leave a partial pet dir on the validation-failure path (`[add-command.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/add-command.ts:58)`).
4. `[broken]` The npm package is not publish/install ready as shipped: the packed manifest still contains `@codogotchi/cli: "workspace:*"`, and a real `npm install` of the tarball fails with `EUNSUPPORTEDPROTOCOL` (`[packages/cli-npm/package.json](/Users/cesar/code/codogotchi_p11_05/packages/cli-npm/package.json:20)`).
5. `[broken]` The npm entry exposes `--version`, but it reports `0.0.0` while the publishable package version is `0.1.0`, so the published surface does not report its own packaged version (`[version.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/version.ts:8)`, `[packages/cli-npm/package.json](/Users/cesar/code/codogotchi_p11_05/packages/cli-npm/package.json:3)`).
6. `[broken]` The baked marketplace base URL does not match the repo’s public Convex HTTP contract, which consistently uses `https://...convex.site`; the npm router hardcodes `https://careful-bat-587.convex.cloud` instead (`[npm-router.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/npm-router.ts:8)`, `[convex-deployment.md](/Users/cesar/code/codogotchi_p11_05/docs/contracts/convex-deployment.md:19)`).

**Surface results**
1. `[probed]` Public command boundary: I built the npm artifact and ran `node packages/cli-npm/dist/codogotchi.js hooks install`; it rejects with `Unknown command: hooks`, so the minimal surface is enforced by routing, not just hidden from help.
2. `[probed]` Node-runnability: `tsup` emits a single CJS file with a `#!/usr/bin/env node` shebang, and `node ... --version` executes successfully (`[tsup.config.ts](/Users/cesar/code/codogotchi_p11_05/packages/cli-npm/tsup.config.ts:4)`).
3. `[probed]` Packaging/publish path: `npm pack` succeeds, but installing the produced tarball fails because the packed manifest preserves the workspace dependency.
4. `[probed]` Cross-boundary download URL: the CLI fetch path suffix matches P11.04 (`/pets/<id>/download`), but the hardcoded origin does not match the documented/public Convex base.
5. `[probed]` No-overwrite/force semantics: covered by passing tests and by code inspection of the `stat(dest)` skip path.
6. `[probed]` Malformed-download cleanup: validation happens before `mkdir`, so invalid zip input cannot create a partial install directory.
7. `[probed]` Published surface versioning: the runtime version constant is decoupled from the npm package version and currently wrong for the publish artifact.
8. `[probed]` Workflow behavior: the publish workflow builds and publishes the package, but it does not contain a check that catches the broken packed manifest before `npm publish` (`[npm-publish.yml](/Users/cesar/code/codogotchi_p11_05/.github/workflows/npm-publish.yml:22)`).
9. `[probed]` Doc-vs-code drift: the ticket claims a published bare `codogotchi` package; the current tarball is not installable by npm consumers because of the workspace protocol leak.
10. `[N/A — no changed persisted JSON/state artifact contract in this diff]` I did not find a new on-disk schema or API response shape added here beyond the install side effects already covered.
11. `[N/A — no new catch broadening in the touched diff]` Error handling is narrow and explicit; the review issues are packaging/boundary defects rather than catch-surface regressions.

**Actionable findings**
1. `[packages/cli-npm/package.json:20](/Users/cesar/code/codogotchi_p11_05/packages/cli-npm/package.json:20)` — The npm package is not actually installable/publishable in consumer npm because the packed manifest still declares `@codogotchi/cli: "workspace:*"`. I verified this with a real install: `npm install /.../packages/cli-npm/codogotchi-0.1.0.tgz` fails with `EUNSUPPORTEDPROTOCOL Unsupported URL Type "workspace:"`. This breaks the ticket’s “published over the claimed bare `codogotchi` package” outcome. Fix by removing the workspace dependency from the published manifest entirely or rewriting it during build/pack so the tarball contains no workspace protocol entries.
2. `[packages/cli/src/npm-router.ts:8](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/npm-router.ts:8)` — The default origin for `codogotchi add` is hardcoded to `https://careful-bat-587.convex.cloud`, but the repo’s Convex HTTP contract and existing CLI enrollment flow use `https://...convex.site` URLs (`[docs/contracts/convex-deployment.md:19](/Users/cesar/code/codogotchi_p11_05/docs/contracts/convex-deployment.md:19)`, `[packages/cli/src/setup.ts:164](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/setup.ts:164)`). That is a cross-boundary contract drift against the review focus item “must match P11.04’s endpoint and the curl one-liner.” If `.cloud` is not the public HTTP base, `npx codogotchi add` will point at the wrong service by default.
3. `[packages/cli/src/version.ts:8](/Users/cesar/code/codogotchi_p11_05/packages/cli/src/version.ts:8)` — The published npm entry’s `--version` output is wrong. The packaged artifact is version `0.1.0`, but `node packages/cli-npm/dist/codogotchi.js --version` prints `0.0.0`. The ticket explicitly includes read-only `--version` in the minimal surface; shipping a mismatched version string makes that surface incorrect and will confuse users/debugging. Fix by sourcing the npm package version into the build or keeping the CLI version constant in lockstep with the publish package version.

**Advisory observations**
- `runAdd` only guarantees cleanup for malformed downloads because validation runs before `mkdir`. If a later filesystem write fails mid-extract, the code can still leave a partially written pet directory. That is outside the exact malformed-download invariant, but worth noting if install atomicity becomes a future requirement.
- The publish workflow unnecessarily runs `bun add -d tsup` in CI even though `tsup` is already declared in `packages/cli-npm/package.json`. That is not a ticket blocker, but it mutates the workspace during the workflow and adds noise to the release path.

**Runner termination**
runnerStatus: completed  
terminatedReason: Finished the adversarial review of the diff, built the npm artifact, ran the targeted CLI tests, and verified the packed install surface with a real `npm install` attempt.
