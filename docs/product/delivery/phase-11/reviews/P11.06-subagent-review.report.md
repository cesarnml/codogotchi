**Invariant results**
[held] Install strings use the Convex HTTP base: `buildInstallStrings()` concatenates the passed `apiBase`, `GalleryApp.API_BASE` is `https://careful-bat-587.convex.site`, and the built client bundle preserves that `.site` URL while using `.cloud` only for `ConvexReactClient` ([web/src/lib/installStrings.ts](/Users/cesar/code/codogotchi_p11_06/web/src/lib/installStrings.ts:15), [web/src/components/GalleryApp.tsx](/Users/cesar/code/codogotchi_p11_06/web/src/components/GalleryApp.tsx:7)).

[held] `listPetsForGallery` preserves the listed kill-switch exactly: it queries `by_listed_createdAt` with `q.eq("listed", true)` before paginating, matching `listPets` ([convex/pets.ts](/Users/cesar/code/codogotchi_p11_06/convex/pets.ts:35)).

[held] The gallery island is emitted as `client:only="react"` and a real `cd web && bun run build` completed as static output with `/gallery/index.html`; the generated HTML contains an `astro-island client="only"` stub and no server-rendered GalleryApp execution path ([web/src/pages/gallery.astro](/Users/cesar/code/codogotchi_p11_06/web/src/pages/gallery.astro:35), [web/dist/gallery/index.html](/Users/cesar/code/codogotchi_p11_06/web/dist/gallery/index.html:1)).

**Surface results**
[probed] `buildInstallStrings` uses `apiBase` directly for both `curl` and `zipUrl`, and `GalleryApp` passes the `.convex.site` constant. I also checked the built JS bundle; it still embeds `.site` for downloads and `.cloud` only for the Convex query client.

[probed] `listPetsForGallery` keeps the `listed: true` gate in the index query exactly as required. No omission or refactor drift was present.

[probed] The `PetDetail` object-URL cleanup does not dereference an already-revoked URL unsafely: both the cancelled branch and the unmount cleanup revoke defensively, and a second revoke on the same blob URL is harmless. I did not find a correctness break from this lifecycle alone.

[probed] Module-scope `new ConvexReactClient(CONVEX_URL)` did not run in Astro’s SSG path in a way that broke the build: `client:only="react"` produced a static island shell and the build completed cleanly. The client constructor remains in the browser bundle, not the generated page HTML.

[probed] `getPet.downloadUrl` is `/pets/<id>/download` while `buildInstallStrings` produces `https://...convex.site/pets/<id>/download`; both describe the same HTTP action path shape from P11.04, but only the absolute `.convex.site` form is directly usable from the static web app. Current `PetDetail` correctly uses `buildInstallStrings`, not `pet.downloadUrl`.

[probed] `pets.astro` redirects to `/gallery`, and a real static build emitted both `/pets/index.html` and `/gallery/index.html`. The redirect target therefore exists in build output.

[probed] `listPetsForGallery` returns `{ ...result, page }`, which preserves the paginated object fields while swapping only the `page` array contents. I did not find a pagination-cursor break in this spread pattern.

[N/A — no CLI surface in this ticket] No CLI flags or argv handling were added here.

[probed] `PetDetail` does not actually catch errors; `fetch`/JSZip failures will reject the async effect and leave the user with fallback UI instead of a surfaced error. That is a UX weakness, but I did not treat it as a blocking finding against the stated invariants.

[probed] `GalleryGrid` casts `results as GalleryPet[]` with no runtime validation. There is no guard at that boundary, but the current query shape matches the render fields, so I did not find an immediate ticket-breaking defect there.

[probed] The `objectUrlRef.current = url; setSheetUrl(url)` ordering creates only a narrow timing window, and cleanup revokes by ref rather than state, which is the safer choice here. I did not find a real leak or crash from that atomicity window.

[probed] The utility tests do not cover an empty `petId` or a trailing-slash `apiBase`. That is a test-gap, not a demonstrated production bug in the current ticket path.

[probed] The ticket rationale says the detail page animates the 8 atlas frames; the implementation animates exactly the first row with `totalCols=8`, which is consistent with that wording. The doc/code drift is elsewhere: the ticket also says “per-tier readout (which tiers + sizes)”, but the UI only shows tier badges plus one generic frame-size line.

**Actionable findings**
[web/src/components/PetDetail.tsx:145](/Users/cesar/code/codogotchi_p11_06/web/src/components/PetDetail.tsx:145), [convex/actions/uploadPet.ts:104](/Users/cesar/code/codogotchi_p11_06/convex/actions/uploadPet.ts:104), [docs/product/delivery/phase-11/ticket-03-upload-action.md:46](/Users/cesar/code/codogotchi_p11_06/docs/product/delivery/phase-11/ticket-03-upload-action.md:46) — The detail page assumes `pet.sizes` is `{ width, height }`, but the upload action stores `{ fileSizes: ... }` for real uploaded pets. That breaks the ticket outcome’s sprite animation and size readout on actual marketplace data, satisfying finding-discipline clause 2 (demonstrable correctness gap). Fix by making the contract consistent on both sides: either store validated frame dimensions in the `pets` row and type/query them explicitly, or change `PetDetail` to derive/read the intended per-tier size metadata shape and render that instead of `sizes.width`/`sizes.height`.

**Advisory Observations**
`getPet.downloadUrl` is a relative `/pets/<id>/download` path, while the static web app needs the absolute Convex HTTP origin. The current detail page avoids this by using `buildInstallStrings(apiBase)`, but the query field itself is misleading and easy to misuse later.

The schema comment for `pets.sizes` says `{ width, height } per-frame dimensions`, but the upload action and ticket docs say the stored payload is `{ fileSizes: Record<string, number> }`. That comment/test-data drift likely helped cause the detail-page bug.

The ticket outcome says “per-tier readout (which tiers + sizes)”; the shipped UI renders tier badges and one generic `Frame size: W × H px` line, not a per-tier size breakdown. That is contract drift adjacent to the blocking `sizes` mismatch above.

`runnerStatus`: completed  
`terminatedReason`: Completed the scoped adversarial review and probed each listed surface without needing repo writes.
